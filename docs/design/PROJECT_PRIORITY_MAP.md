# Project Priority Map

> **Created**: 2026-04-25
> **Purpose**: Single resolver for "what should we work on next?". Consolidates the various roadmap documents (service architecture, agent refactor, SA roadmap, signal detection, RL, rename) into one ordered backlog.
> **How to use**: Before starting a new feature, scope shift, multi-slice line, or branch, check this map. If the work isn't here, either add it (with justification) or argue the active P0/P1 should change. New multi-slice lines must pass this check before their first implementation slice.
> **When to update**: Whenever a P0/P1 item completes, blocks, or a higher-priority item emerges. Insert at the TOP of §10 Decision Log (newest-first ordering — see the note inside that section).
> **Small evidenced debt**: `docs/design/ENGINEERING_ISSUE_REGISTER.md` is the single owner for bounded issues that do not yet require a dedicated workstream. Contract violations, design decisions, protected-boundary changes, and active slices stay in this map rather than being hidden in the register.

---

## 1. TL;DR

> **Project identity (Phase 2 rename EXECUTED 2026-05-31)**: Local repo directory + code/docs references renamed `MindfulRL-Intraday` → `ArkScope`. The only remaining `mindfulrl` (lowercase) references are intentional and must NOT be "fixed": PostgreSQL DB name/password, Native Messaging host id `com.mindfulrl.sa_alpha_picks`, Firefox addon id `sa-alpha-picks@mindfulrl.local`, and historical/archived docs. A further *product-brand* rename may STILL happen if the local-first workbench positioning settles on a different name (AIRA Desk / ARK Desk are draft-only) — until then do NOT preemptively introduce a fourth name, and do NOT block development on rename hygiene. Execution detail: git history + memory `project_rename_arkscope.md` (the executed runbook `ARKSCOPE_RENAME_PHASE2.md` was removed 2026-06-07, recoverable via git).

> **Formal product repositioning 2026-05-02**: This is no longer an "intraday RL trading research repo" or "agent/tool backend for analysis". The project transitions to **local-first financial research agent workbench** — a single-user research environment that combines (a) Hermes-like agent capabilities (persistent memory, skills, tools, scheduler, replay, compression, subagents), (b) this codebase's existing financial data layer (news / SA / macro / prices / fundamentals / signals), and (c) a research GUI that lets the user read, edit, schedule, and migrate research artefacts. **North star**: the agent reads its own accumulated knowledge substrate across sessions and machines; the user sees and edits everything via a research GUI; zipping the profile directory moves a researcher's work between machines. **Five-layer architecture**: Workbench UI → Agent Layer → Data Layer → Profile Layer → Portability Layer (top-to-bottom). **Hermes is capability reference, NOT product reference** — we borrow filesystem-first profile + daemon scheduler + FTS5 cross-session recall + skills/tools flexibility, but our differentiation is the financial data layer + investment research workflow, not general-purpose agent platform. **Phase C PAUSED** — runner refactor is now second-order to product boundary, profile, sync, GUI IA, and agent-data-access API. Active sequence: audit done → spec done (`b0743da`) → **docs governance cleanup** (audit `docs/design/DOCS_GOVERNANCE_AUDIT_2026_05.md` + follow-up archive/delete/rewrite commit) → read-only UI skeleton against existing reads → **two-SQLite split + transient DuckDB** migration first cut (kill criterion: zip-and-go on a second machine). Phase C resumes only after `workbench v1 ship + 2 wks stable + 1 verified migration`. Repo identity renamed to `ArkScope` (Phase 2 / P3.2 executed 2026-05-31; only intentional `mindfulrl` lowercase for DB/host/addon/historical remains). Detail in §10 newest entry.

### Current operating map (post-pivot, corrected 2026-07-03)

The detailed P0/P1/P2/P3 sections (§2–§5) and the pre-pivot dependency graph /
sequencing sections (§8–§9) are retained as historical provenance for already-landed
foundation work. The active resolver for "what next?" is now:

| Priority | Workstream | Status / next action | Why it is here |
|---|---|---|---|
| **P0-A** | **Layer C-1 SA evidence feed** | ✅ **Already implemented** before this TL;DR refresh. `SA_EVIDENCE_FEED_C1_SPEC.md` now records the reconciliation; no new implementation plan is needed. | Direct user-visible workbench value; turns existing SA/news evidence into a daily research surface. |
| **P0-B** | **PG-exit cleanup, small slices** | ✅ **PG market-data physical drop COMPLETE 2026-07-05 (N9 batch-1 + batch-2 + batch-3 all live).** Batch-3 dropped PG `prices` (2,314,293 frozen rows) + dependent `get_recent_prices(...)` after archive/two-stage restore proof and explicit approval (fingerprint `028efacd…`, archive `data/pg_archive/n9_batch3_prices_20260705T010022Z/`). Pre/post PG-unreachable E2E ×2 each all green. **PG public schema = 3 app-record archive tables only — RULED archive-only 2026-07-05 (closeout: `use_local_records` collapsed to local-default, fresh-profile stranding hole closed). PG-EXIT IS CLOSED.** | PG is archive storage, not a runtime dependency; the whole PG-exit line is complete. |
| **P0-C** | **Prices Reconcile + Direct-Local Cutover**（改名 2026-07-03；不再預設 bulk migration） | ✅ **LIVE COMPLETE 2026-07-04; IBKR partial-status Hotfix A + durable body retry LIVE COMPLETE 2026-07-15; entitlement-aware retry LIVE COMPLETE 2026-07-16 through `a73939f`; two-attempt 10172 recalibration MERGED 2026-07-17 through `80dd6e8`, desktop restart/natural-cycle check pending.** Final reconcile fingerprint `61bbf613…`; local rows `2,324,487` / 149 tickers, PG-only unexplained `0`; no bulk copy; scoped HAPN patch only. P0-C.1 moved `ibkr_prices`/`price_backfill` to sanitized subprocess workers, fetches prices outside `market_write_lock`, adds same-tick market-writer backpressure, and classifies lock-busy as retryable skip. **P0-C.2 news burst hardening COMPLETE 2026-07-05:** normalized Polygon/Finnhub/IBKR news fetch outside the market write lock, short-lock SQLite write phases, `skipped_lock_busy` parity for residual contention, and live startup-burst soak green. **ACTIVE TRUTH FOLLOW-UP 2026-07-28:** Coverage v2 exposed one all-zero LCID target while a 150-ticker price run inserted 3,874 sibling rows and persisted success. The reviewed next slice derives price partial truth from per-ticker post-write targets before EIR-002 and root scripts retirement; it does not reopen PG, alter Coverage v2, or authorize repair. **IBKR headline long-catch-up audit remains closed/runbook-only:** normal cadence is below the observed 300/ticker cap, while multi-week downtime cannot be reconstructed. The truthful count UI, bounded SQLite-derived body retry, entitlement correction, and terminal-at-two recalibration are merged: one strict provider observation derives reversible capability blocks so absent subscriptions do not spend retry slots; currently entitled typed-10172 rows receive one delayed hedge and no third request. Fresh headline work remains independent, provider identity stays inside the isolated child, and this does not reopen PG or claim unknown-headline backfill. Authorities: `docs/superpowers/specs/2026-07-14-ibkr-news-partial-retry-design.md` plus its 2026-07-17 retry-threshold supersession. | 核心市場資料域、原最後一個 PG runtime 依賴；先證明差異再動資料，不把更乾淨的本地資料用 PG 覆蓋回去。 |
| **P0-D** | **Provider config authority Phase 2** | ✅ **LIVE COMPLETE 2026-07-05.** Strict-by-default is live for FieldDefs-managed provider config (`provider_env_fallback` unset/false => no `config/.env` runtime fallback); explicit `provider_env_fallback=true` remains the documented rollback lever. The separate SA `use_local_sa` fresh-profile stranding follow-up is also complete: unset/false/env-unset now route SA local by default. | Supports zip-and-go portability and avoids silent `.env` authority in the desktop runtime. |
| **P1** | **IV proof packet Task 1 only** | Optional cleanup: names-only cost/key authorization audit, then stop at sign-off. Paid sampling default = **DEFER**. | IV is PG-exit design insurance, not an active product line; no current consumer justifies paid data or S-D/S-E implementation yet. |
| **P1** | **Publication / backup hygiene** | Push cadence healthy as of 2026-07-05 (master synced to origin); publication review remains its own gated thread. | Operational risk reduction, not product scope. |
| **Gated** | **IV S-D/S-E / paid options data** | Blocked until written hypothesis + OOS plan + kill criteria + cost-vs-value. | Research subscription rule applies; no consumer and no hypothesis today. |
| **Gated** | **Unified Runner Phase C** | Still paused. Resume only after workbench v1 ships, two weeks stable, and one verified cross-machine migration. | Internal infra win; premature before storage/profile/UI shape settles. |
| **Deferred** | RL productionization, Reddit/StockTwits/X paid, MCP integration | No active work. | Prior pause/defer decisions still stand. |

Subscription strategy: see §10. Splits into **operational** (must-keep) vs
**research** (evidence-gated). Paid IV/options data is research subscription work and
therefore defaults to defer until the hypothesis gate exists.

---

## 2. P0 — Immediate

### P0.1 Replay Harness (Major Refactor Phase 0)

- **Why**: Gates every subsequent agent change. Without replay, Phase B (compression) / Phase C (unified runner) are unfalsifiable — we cannot prove a refactor preserved behaviour. Smallest of the P0 items, so do FIRST.
- **Approach**: ship the **minimal spike** first; expand only after the trace schema has survived contact with real captures.
- **Acceptance criteria — minimal spike (do first)**:
  - Trace schema written down (one Python dataclass + JSON example) covering: turn id, input messages, system prompt, tool list, model config, final assistant message, tool calls + results, token usage. Streaming chunk merging is OPTIONAL for v1 — final response is enough.
  - Capture wired into one provider path (Anthropic) end-to-end. OpenAI path can come later.
  - 1-2 captured fixtures saved under `tests/replay_fixtures/` (simple Q&A + one multi-tool turn).
  - `scripts/replay_run.py <session>` reads a fixture, re-runs it against current code, prints a diff of (a) final assistant message and (b) tool call sequence. Diff format can be plain text — no fancy framework yet.
  - User can manually rerun and visually accept/reject diffs.
- **Acceptance criteria — full v1 (after schema stabilises)**:
  - Both Anthropic and OpenAI paths capturing.
  - 5+ fixture set covering attachment query, subagent delegation, compaction-triggered turn.
  - Pre-commit / CI integration: fixtures must replay clean (or be explicitly marked as expected-diff).
  - Streaming chunk merge if needed for token-level analysis.
- **Effort**: 0.5-1 day for minimal spike; +1-2 days for full v1.
- **Status**: ✅ FULL V1 DONE 2026-05-01. Minimal spike landed 2026-04-25 (`5e12d63` schema/validator + 33 tests, `875a6ef` Anthropic capture wiring); full-v1 landed in 3 commits + multiple review-fix passes (`14567db` commit 1 OpenAI capture + canonicalization + shared `server_tools.py` → `8829780`/`682ac49`/`0c63f49`/`c3353ea` High/Medium/Low/AST-guard test → `229d033` commit 2 fixture set + schema fields + attachment classifier → `d0a3f21`/`0a46512` round-1/round-2 review findings → `a8eaf60` round-3 wording cleanup → `4e30e71` commit 3 pytest gate + unified resolver + attachment pair gate → `172b5a1` Low fix availability diff resolver-aware). Spec at `docs/design/P0_1_FULL_V1_SPEC.md` — status "3/3 commits landed". Delivered: `src/agents/shared/replay.py` (validator + classifier + unified resolver), `src/agents/shared/server_tools.py` (Anthropic + OpenAI hosted-tool single source of truth), `src/agents/shared/bridge_tools.py` (bridge-only tool spec table — `delegate_to_subagent`), `tests/test_replay_fixtures.py` (parametrised gate over 7 fixtures), 7 fixtures (`no_tool_turn.json`, `one_tool_turn.json`, `p1_4_l0_overflow.json`, `openai_no_tool_turn.json`, `openai_one_tool_turn.json`, `attachment_turn.json`, `subagent_turn.json`), 98 replay-suite tests + 294 cross-suite regression. Phase C now has the safety net it was designed to consume; CI wiring still TBD (orthogonal cycle, see §2.3 of full-v1 spec).

### P0.2 Service-first S2: jobs persistence + history API

- **Why**: `/jobs/status` and `/jobs/run` exist but state is in-process. DB persistence is foundation for S3 dashboard, OpenClaw orchestration, and scheduler observability. Zero blast radius (no agent path changes).
- **Acceptance criteria**:
  - `job_runs` table (sql/0XX migration): job_id, name, started_at, finished_at, status (running/success/failed), last_error, payload (jsonb), trigger_source.
  - `job_definitions` table (or config-driven): name, schedule, last_success_at, last_error_at, next_run_at, enabled.
  - `/jobs/history?name=&limit=&offset=` paginated.
  - `/jobs/status` reads from DB (`last_success` + `last_error` + `next_run` from latest rows per job).
  - Existing job runners updated to write a row at start and update at finish (transactional so partial failures don't leak).
  - 5 sample queries pass: "last error for SA refresh", "jobs that haven't run in 24h", "all failed jobs in last 7d", etc.
- **Effort**: 2-3 days incl. migration.
- **Status**: ✅ done 2026-04-25 (commit `a86c2ab`). Delivered: `sql/011_add_job_runs.sql` (status enum running/succeeded/failed, trigger_source free-text, payload+result jsonb, message+error split, duration_ms, two indexes for name + status time-series queries); `src/service/job_runs_store.py::JobRunsStore` with `create_run`/`finish_run`/`list_runs` (paginated, clamped) / `latest_runs_by_name` — all best-effort so DB unavailability degrades to process-local state; `src/service/jobs.py::run_job` records start (running) and finish (succeeded/failed) rows with `trigger_source` threaded through; `list_jobs_status` merges DB-latest with process-local fallback; `GET /jobs/history?name=&limit=&offset=` with pydantic `JobRunRow` / `JobsHistoryResponse` models in `src/api/routes/jobs.py`. 27 tests covering store CRUD, availability gating, error swallowing, pagination clamping, DB-merge + fallback, persistence on success/failure, and endpoint round-trip. Smoke-tested against the live DB. Per priority-map decision, v1 keeps `job_definitions` in-code (static catalog merged with DB latest at read time) — separate `job_definitions` table deferred until a config-driven schedule actually shows up as a need. Unblocks P1.5 S3 admin dashboard.

### P0.3 SA Comment Intelligence Stage 1 (rule-based) ✅ COMPLETED 2026-04-25

- **Why**: 21K+ comments already in `sa_article_comments`. Rule-based extraction is near-zero cost and immediately useful for ticker-mention discovery and high-value-comment surfacing. **No LLM in Stage 1** — keep it deterministic and cheap.
- **Delivered** (commits `aa76cb0` Stage 1, `5e72f6f` v1.1 fixes, `ba66fde` version filter pin):
  - `sa_comment_signals` table (sql/012) with `comment_row_id BIGINT FK`, denorm article_id+comment_id, `ticker_mentions[]` + `candidate_mentions[]`, `keyword_buckets jsonb` (matched terms preserved), `high_value_score numeric`, `needs_verification bool`, `rule_set_version text`. GIN on tickers + score DESC + extracted_at + article_id indexes.
  - Detection rules in `src/sa/comment_signals.py`; documented in `docs/design/SA_COMMENT_INTELLIGENCE_PLAN.md` §5.1. Rule-set version `v1.1`.
  - Backfill via `extract_sa_comment_signals` job (`src/sa/comment_signal_backfill.py`), gated on `sa_enabled`, observable through S2 `job_runs`.
  - Tool: `list_high_value_comments(window_days, ticker, min_score, limit)` registered in registry + Anthropic + OpenAI bridges. Pinned to current `RULE_SET_VERSION` so partial re-extracts cannot leak stale signals.
  - 38 unit/integration tests pass; 20-row spot-check on score≥4 gave **~85% precision** on `ticker_mentions` (target ≥80% met).
  - Production backfill: 36,352 comments in ~95 seconds, universe = 102 tickers, 1,508 score≥5 / 481 score≥8 / 10,267 with ticker_mentions / 11,358 with candidate_mentions / 3,508 needs_verification / 189 dot-suffix tickers found.
- **Deferred to Stage 2**: `get_sa_comment_consensus` (needs sentiment direction to mean bullish/bearish), thread/parent grouping, LLM summarization on Stage 1 high-score candidates.

### P0.4 SA extension reliability & Market News recovery

- **Why**: Daily-use feed. Already exists but has freshness gaps, intermittent anti-bot issues, and incomplete article bodies. Smaller patches that interleave with P0.1-P0.3 — **not** a new main thrust.
- **Delivered (observability slice)** 2026-04-25:
  - Three-layer health report (`freshness` / `feed_health` / `detail_health`) with `ok` / `warning` / `critical` severity; thresholds visible in response. `src/service/sa_market_news_health.py` + `GET /sa/market-news/health`. Strict mode upgrades any non-ok severity to HTTP 503. Market-hours rule: zero published items in 24h is critical only during regular US session, warning otherwise. (commit `d923177`)
  - Chrome extension SA syncs (alpha-picks refresh, market-news, articles) record into `job_runs` via new `record_extension_job` native_host action + `JobRunsStore.record_completed_run()`. Slug `sa_extension:<flow>` keeps these distinct from in-process jobs but visible to `/jobs/status` and `/jobs/history`. (commit `78e56d2`)
- **Reliability incident and reviewed response** 2026-07-19 through 2026-07-25:
  - Firefox packaging omitted `article_identity.js`; manual copy/reload restored capture, and the shipped dependency-closure builder now makes the omission a hard pre-install failure.
  - 25 Market News runs recorded `detail_failed=18` while outer `job_runs.status` remained `succeeded`, proving that the existing telemetry can hide degraded detail work.
  - The combined design is written at `docs/superpowers/specs/2026-07-25-sa-extension-reliability-control-clarity-design.md`. It locks packaging closure first, status derived from structured phase/item truth, four-state audited repairs, 24h routine catch-up plus no-age recorded-ID retry and 168h-capped incident recovery, five grouped popup controls with honest bounds, and fresh approval of the historical repair manifest. The prior 27-item observation is not a constant; a 2026-07-25 read-only reconstruction found 118 unique failed IDs, 88 now body-present, and 30 still missing.
- **Still deferred**:
  - Auto-recovery on anti-bot rejection: delayed retry with backoff, alternate endpoint. Holding until health report shows chronic anti-bot failure mode worth automating against.
  - Generic arbitrary-range or lifetime detail backfill. The reviewed incident operation is bounded recovery, not evidence that SA exposes complete history.
  - `/sa/market-news` p95 latency <500ms monitoring (existing query has 3 indexes; add FastAPI middleware only if read latency starts mattering for downstream agents).
- **Status**: ✅ **LIVE COMPLETE 2026-07-26**. The SA extension reliability batch remains live through merged tip `2ef8bb69`: backend `+91/-2`, frontend `+8/-1`, popup contrast gate included, historical repair `88 already_present / 30 repaired / 0 unavailable / 0 retryable`, all `118` bodies present, and installed Chrome/Firefox readback green. Firefox remains the sole auto-sync owner by operating choice; Chrome is manual fallback, and the app-owned collector remains an unadopted P2.6 Part 2 decision. Calibration Anthropic-refusal normalization is live through `8e73dba4`. Coverage v2 is also **LIVE COMPLETE** through merged product tip `3f0fb18f`: backend `+76/-40` to `4749`, focused `230`; frontend `+12/-3` to `96/1072`, focused `118`; resources `714/1814/44`; scanner `36/20/0/20`; typecheck/build/no-PG and production read-only blank-row evidence green. No provider, scheduler, repair, or production-write action occurred during Coverage closeout.

---

## 3. P1 — Next

### P1.1 Multi-factor signals → tool/API exposure ✅ COMPLETED 2026-04-26

- **Why**: `src/signals/` already computed factor signals (sector_momentum, event_chain, sentiment_anomaly, volume_spike). Exposing as a per-factor breakdown + cross-sectional rank converts existing latent value to active value, with explicit "recommendation, not prediction" framing. NOT a price-prediction claim.
- **Delivered** (commit `550c4da`):
  - `get_signal_factors(ticker, days, as_of_date, strategy)` tool — returns per-factor `{factor_type, impact, weight, contribution, details}` rows + a `data_quality` block (`news_count`, `scored_news_count`, `missing_factors`, `errors`) so callers can tell "no data" from "neutral signal". Refactored `synthesize_signal` to share `_compute_signal_context`; existing `TradingSignal` contract preserved.
  - `GET /signals/factor-rank?universe=&factor=&top=&days=&as_of_date=` — universe `watchlist` | `alpha_picks` (alpha_picks deduped). Factor enum maps URL-friendly names to synthesizer factor_type strings — `volume_spike` resolves to `VOLUME_SPIKE` (output factor_type), NOT `volume_anomaly` (synthesizer's input bucket). News df preloaded ONCE per request and shared across per-ticker calls. Single global anchor (caller-supplied or `df.max()`) so the cross-section is reproducible. `missing_data_tickers` carries reasons; no-data tickers never enter the ranked list. `notes[]` surfaces caveats — sector_momentum ranking flags that the factor is sector-shared, composite ranking flags that no validation has been wired yet.
  - 30 new unit/integration tests in `tests/test_signal_factors_p1.py` covering shape, factor mapping (`volume_spike → VOLUME_SPIKE`), universe dedup, news_df preloaded once (call-counter), single global anchor, missing-data routing, top-N truncation, invalid factor/universe → 422, sector caveat. Existing `test_synthesize_signal` contract test still green.
- **Deferred** (deliberately out of P1.1 scope):
  - Signal validation: forward-return decile analysis (top vs bottom 1d/5d/20d returns) sits on top of this exposure; not yet wired.
  - Entry / exit timing logic — that's a strategy/backtest layer, not a ranking exposure.
  - SA comment intelligence as a factor — Stage 1 has no sentiment direction, so it can only mean attention not bullish/bearish. Hold.
- **Status**: ✅ done 2026-04-26 (commit `550c4da`).

### P1.2 Free calendar / macro: FRED + Finnhub calendar

- **Why**: CPI / FOMC / GDP / earnings calendar / IPO calendar. Cheap, broadly useful in analysis & monitor flows. Strictly precedes paid macro subscriptions.
- **Acceptance criteria** (revised vs initial draft after `P1_2_PROVIDER_DISCOVERY.md` smoke results):
  - `data_sources/fred_client.py` — FRED series + ALFRED vintage replay (free API key, smoke §6).
  - `data_sources/finnhub_calendar_client.py` — `/calendar/economic`, `/calendar/earnings`, `/calendar/ipo` with UTC-aware parse (smoke §5.2).
  - `src/macro_calendar/store.py` — canonical/revision upsert with as-of read; `MacroCalendarStore`.
  - `src/macro_calendar/{fred,finnhub}_ingestion.py` — pure ingestion functions (no API/tools coupling).
  - 6 jobs in `/jobs/status` gated on `macro_calendar.enabled`: `fetch_fred_series`, `fetch_fred_release_dates`, `fetch_economic_calendar_recent`, `fetch_economic_calendar_backfill`, `fetch_earnings_calendar`, `fetch_ipo_calendar`.
  - Health/freshness telemetry — `compute_macro_calendar_health` + `/macro/health[?strict]` (jobs cadence + table coverage, market-hours upgrade for `recent`).
  - Read API + agent tool surface — final shape: 4 routes (`/macro/economic-calendar`, `/macro/earnings-calendar`, `/macro/ipo-calendar`, `/macro/series/{series_id}`) + 2 tools (`get_economic_calendar`, `get_macro_value`). All gate on `macro_calendar.enabled`, support `?as_of=` (date inputs are EOD UTC; `from_date` is SOD).
- **Effort**: 1 day → revised to ~5 days incremental over 6 commits (see `P1_2_SPEC.md` §9). Provider smoke + revision-log semantics turned out to be the load-bearing parts.
- **Status**: ✅ done 2026-04-27. All 6 commits + 2 follow-ups landed (`e8fc1db`/`926a81c` schema+DAL → `f03cf86`/`6536652`/`959c589` FRED → `d3ec85a`/`5b376aa`/`35e8d4f` Finnhub+rename → `2edea5e`/`3bde0ec`/`6cca46a` jobs+API → `b8454a2`/`a5b24d3` health → `2478e74` read API+tools → `7eeffb0` review findings → `95a644d` from_date/to_date boundary). Final at `95a644d`.

### P1.3 SA Digest / Reading Workflow v1

- **Why**: SA data already in our DB (Alpha Picks articles, Market News feed, Stage 1 comment signals) but the agent doesn't proactively surface it in decision moments — the bottleneck is **usage**, not **coverage**. Original framing was "SA Latest Articles metadata v1" (more data); reframed after review to "make existing SA data actually enter watchlist daily / earnings prep flows" via a deterministic evidence-pack tool + targeted skill prompt updates. See `P1_3_SPEC.md`.
- **Acceptance criteria** (see `P1_3_SPEC.md` §3, §8 for detail):
  - Tool `get_sa_digest(ticker, days=14, max_articles=5, max_news=5, max_comments=8, min_comment_score=4.0)` — pulls from `sa_articles` + `sa_market_news` + `sa_comment_signals` ↔ `sa_article_comments`; returns a deterministic structured evidence pack with `recent_articles[]`, `high_discussion_news[]`, `high_value_comments.{ticker_mentions[], candidate_mentions[]}`, `data_quality`, `source_notes[]`.
  - Selection rules locked: comments reuse Stage 1 `high_value_score`, ≤3 per article (anti-domination), `needs_verification=True` rows kept (not filtered), ticker vs candidate mention split preserved.
  - **No internal LLM call** in v1 — evidence pack is deterministic; main agent produces Chinese summary in its own context. v1.1 candidate: `summary_mode="llm"` opt-in.
  - **No new SA data sources** — v1 is read-only over existing tables. New scrapers, transcripts, StockTalks, author tracking all deferred.
  - Skill prompt updates: `earnings_prep` (must call, days=30) and `full_analysis` (recommended, days=14). Skills are prompt templates, not executable workflows — the acceptance test asserts the body contains the canonical instruction string + tool name.
  - 18 acceptance tests covering disabled-state, window clamps, ordering + caps, mention split, per-article cap, score cutoff, `needs_verification` passthrough, data_quality always-present, source_notes disclaimer, per-source failure isolation, ticker uppercasing, JSON serializability, excerpt truncation, registry + bridge wiring, skill body content (×2).
- **Out of scope (v1.1+ candidates)**: earnings-call transcripts (on-demand spike first), author-tracking pipeline (needs curated author list), event-gated market-news comment threads (extension change required), `portfolio_scan` skill integration (defer until SA-coverage rate observed), market-wide digest, server-side scrapers.
- **Effort**: 1-2 days, 3 commits (tool + tests → registry + bridges → skill prompts + docs).
- **Status**: ✅ done 2026-04-27. Spec at `docs/design/P1_3_SPEC.md`. Commit chain: `ce5f780` spec → `1108310` 5 review findings → `55c02bd` tool + 18 unit tests → `70065c5` 4 commit-1 follow-ups (clamp, SQL canary, spec sync) → `a19441e` registry + bridges + 12-file count sync → `68a60a5` dispatch regression test + 3 stale docstrings → `fd9fb80` envelope assertion → `1db2789` skill prompts + acceptance tests + this status flip.

### P1.4 Context Compression Phase B

- **Why**: Long agent runs and large tool results will eventually hit the context limit. **Framing**: P1.4 is not a defensive cap — it's what enables more tool calls and (later) multi-round runs to be safe under sustained load. Large tool outputs become summaries in-prompt + raw on disk (retrievable); compaction becomes a normal operating layer rather than a panic mode.
- **Acceptance criteria**: see `docs/design/P1_4_SPEC.md` for the locked-in shape. Spec encodes 5 guarantees (compressor is a library not a runner; subagent path unchanged; Layer 0 net-positive for heavy queries; no fabricated reasoning; per-layer toggles), the 7-layer design (Layers 0-3 deterministic + Layer 4 provider-native optional + Layer 5 LLM full compact + Layer 6 anchor recovery), per-tool reducer policy, and 5-commit decomposition.
- **Borrowed patterns** from `~/PycharmProjects/AI_Agent_Researcher` (see P1.4 spec §2): marker-based summary, recent-boundary algorithm, circuit breaker (3 consecutive failures → off). NOT borrowed: drop-in `ClientCompactionSession` (OpenAI-specific; we're dual-provider) or SDK Session injection (proven painful).
- **Effort**: 4-5 days, 5 commits (overflow_store → deterministic layers → Anthropic integration → observability → LLM full compact).
- **Status**: ✅ done 2026-04-30. Spec at `docs/design/P1_4_SPEC.md` (5/5 commits landed). Commit chain: `99f90c9` spec → `0f23bdc` 5 spec review findings → `d2d83f0` record_id wording + Anthropic model list sync → `05960fc` commit 1/5 (overflow_store + record_id derivation) → `b79472c` 3 commit-1 findings (tamper detection + utf-8 + newlines) → `15f4259` commit 2/5 (Layers 0-3 + reducer registry + ContextCompressor) → `60c92d9` 3 commit-2 findings (wrap-aware reducers + real shapes + L2 idempotency) → `2711c11` commit-2 medium follow-up (L1 envelope-aware) → `08bbbb0` commit 3/5 (wire ContextCompressor via ContextManager) → `0e73969` commit-3 spec staleness → `eaf13c0` commit 4/5 (observability + /overflow CLI) → `b3d44e2` commit-4 findings (/overflow integrity gate + spec sync) → `39fa8fc` gpt-5.4 → gpt-5.5 default → `1ed4d77` commit 5/5 (Layer 5 LLM compact + Layer 6 anchor recovery) → `95ff3cb` commit-5 findings (/compact wiring + L5 noop vs failure). Default ships master `compaction.enabled=true` (always-cheap Layers 0-3) but `compaction_layer_5_enabled=false` — LLM-call layer is OFF until explicitly opted in or armed for one turn via `/compact`. 308 tests passing (compressor + adjacent layers).

### P1.5 S3 admin dashboard — OSS-first survey

- **Why**: Operational visibility (jobs, freshness, SA sync, reports, errors). **Survey OSS before building.** A custom dashboard would consume 1-2 weeks for a thin domain layer; an off-the-shelf admin we extend is better leverage.
- **Acceptance criteria**:
  - 1-day spike: stand up 2-3 OSS candidates against our schema. Candidates to evaluate:
    - **FastAPI Admin** / **sqladmin** (auto-CRUD over SQLAlchemy)
    - **RQ Dashboard** or **Prefect UI** (if jobs migrate to a queue)
    - **Apache Superset** (if analytical views are the priority, not ops)
    - **Custom minimal Next.js** (only if all above fail spec)
  - Decision doc: chosen tool, reasoning, gaps to fill, integration plan.
  - If chosen tool fits: 1-2 days integration. If "build custom": ~4 days minimal jobs/freshness/SA-sync/reports/errors view.
- **Effort**: 1d spike + 2-4d integration.
- **Status**: ✅ SPIKE DONE 2026-05-02. Decision doc at `docs/design/P1_5_S3_OSS_SPIKE_DECISION.md`. Outcome: sqladmin / FastAPI Admin / Superset / RQ-Prefect UI all rejected (SQLAlchemy gap + wrong shape — CRUD/BI vs ops-monitoring). Custom minimal (FastAPI + Jinja2 under `src/api/admin/`, ~300-500 LoC) recommended; passes all four non-negotiables (no ORM, single process, reuses existing JSON read paths, read-only default). HTMX additive in v1.1. Integration is NOT next: P1.5 stops here, Phase C spec begins immediately per the priority map's 2026-05-02 sequence decision. When integration cycle opens later, this doc is the input.

---

## 4. P2 — Later (dependencies pending)

### P2.1 Unified Agent Runner Phase C
- **Status**: ⏸ **PAUSED 2026-05-02** — product pivot to local-first research workbench takes priority. Spec at `docs/design/PHASE_C_UNIFIED_RUNNER_SPEC.md` is preserved as-is (review-pass-1 closures + §8 closures all stand; nothing in the spec is invalidated). The pause is sequencing-only — Phase C remains the right refactor when it is the right time. **Resume gate (all three required)**: (1) local-first workbench v1 ships, (2) ≥ 2 weeks of stable single-user use without storage / scheduler / sync regressions, (3) at least one verified cross-machine migration completes (zip-and-go second-machine smoke). Rationale: Phase C improves provider symmetry and removes ~3,800 LoC of dual-SDK duplication, but those are internal-infra wins; doing them before storage / sync / UI re-architecture risks reworking the runner's edges (compaction surface, subagent dispatch, flag rollout) once the new product shape settles. See `PHASE_C_UNIFIED_RUNNER_SPEC.md` for authoritative design (original framing in retired `MAJOR_REFACTORING_PLAN.md`, available in git history) and §10 decision log 2026-05-02 entry on local-first pivot.

### P2.2 Knowledge Graph Phase A
- **Status**: blocked on SA Comment Intel (P0.3) and Market News (P0.4) being mature data sources.
- See `PHASE_A_KNOWLEDGE_GRAPH_SKETCH.md` (extracted from retired `MAJOR_REFACTORING_PLAN.md` during Group 3 consolidation).
- Should consume mature data, not generate it. Premature build leads to graph of low-quality entities.

### P2.3 OpenClaw integration / external orchestration
- **Status**: blocked on S2/S3 stability (P0.2 + P1.5).
- HTTP API surface must be stable + dashboardable before exposing to external orchestrators.

### P2.4 Key Markets snapshot / Factor Grades
- **Status**: optional supplementary data. Not blocking, not high-value. Can be picked up opportunistically alongside P1.3 SA Digest.

### P2.5 Provider capability display (app Data Sources panel + README)
- **Status**: backlog, filed 2026-07-06 (scripts-consolidation review). Not started.
- **What**: surface per-provider data differences where the user decides about API keys — Settings 資料來源 panel gets a static per-provider capability blurb (content type 摘要 vs 完整內文、有無內建情緒分數、歷史深度、免費層限制), README gets a short "why get API keys" table.
- **Source material**: `docs/data/NEWS_PROVIDER_DATA_DICTIONARY.md` provider-comparison sections (2025-12 measurements — re-verify numbers or rewrite qualitatively before they enter UI; see that file's status header).
- **Absorbs the av/eodhd future-provider item**: `collect_alphavantage_news.py` + `collect_eodhd_news.py` were test-era collectors retired in scripts-consolidation Task 1 (recover via `git show f9d00c7^:scripts/collection/<file>`); user intends to re-add Alpha Vantage / EODHD as proper Data Sources providers later — the same capability-blurb mechanism describes them when they land.

### P2.6 SA Extension Integration Health / Setup Surface
- **Status**: **part 1 ✅ SHIPPED + LIVE 2026-07-06** (see §10 closeout entry; plan `docs/superpowers/plans/2026-07-06-sa-extension-telemetry-health.md` = LIVE COMPLETE). Telemetry lands under both workflows (dev:desktop config-binding + standalone-8420 default); Settings has the 7-segment health panel. **Part 2 (embedded SA browser/session vs external-extension mode) remains an open Desktop App Vision decision.** Original part-1 scope for reference: telemetry config plumbing (shell writes per-run `api_base`/`api_token` into the native-host config; host resolves env > config > default-8420) + read-only per-segment health panel. Promoted after the cutover test exposed the structural gap (see boundary doc incident section). Part 2 (embedded SA browser/session vs external-extension mode) = **explicitly deferred** to a Desktop App Vision decision — it is a new ingestion client (login/2FA/DOM drift/security boundary), not a UI tweak (user ruling 2026-07-06).
- **What**: desktop-app-facing setup + diagnostic view for the SA extension pipeline (manifest/launcher/config checks, app-side simulated host ping, SA DB write/read freshness).
- **Boundary note (authoritative)**: `docs/design/SA_EXTENSION_HEALTH_SETUP_BOUNDARY.md` — App-owned vs Browser-owned vs App-display split, incl. the direction constraint (app cannot initiate the browser→host hop; it verifies preconditions + simulates the host directly).
- **Existing building blocks** (don't redesign): native host → sidecar job telemetry POST (S-H1) → `job_runs`; `sa_market_news_health`; gates-doc Level 2 host smoke; `extensions/sa_alpha_picks/install*.sh` are the CLI precursors the app setup surface will absorb.

### P2.7 Model capability/catalog slice ✅ SHIPPED 2026-07-11
- **Status**: ✅ **SHIPPED — merged FF `7e4f5b7` 2026-07-11** (plan `docs/superpowers/plans/2026-07-10-model-capability-catalog.md`: 7 plan-review rounds + Task 0 live verification + 2 implementation-review rounds; final A/B failure sets identical 30=30, +48 passed = exact 48-test collect diff). Delivered: `src/model_capabilities.py` (22-entry code-reviewed registry = single capability authority; all 7 drift sites below now read registry views) · `src/model_discovery_cache.py` (per provider+auth_mode+credential-fingerprint, ok/seed_only/never_discovered) · `src/model_effective.py` + Settings verified-first picker (advanced toggle, discovery nudge, seed_only badge, collapsed manual override) · Fable-class refusal contract at all three seams (agent loop `model_refusal` event; synthesis/translation `AnthropicRefusalError`; never enters effort-retry). **Live verification 2026-07-11: server-side COMPLETE** (all-7-credential discovery round; cache-DB proof incl. failure-records-nothing; effective-view flips with all three badge polarities via route flip + exact restore; pinned_only zero leak; Luna + Sonnet 5 smoke ok — **Fable 5 smoke NOT run per ruling**; chatgpt_oauth credential needs re-login = S3 state, not P2.7). Remaining user-side: eyeball Settings picker rendering in the desktop app (rendering itself pinned by the frontend suite). Follow-ups live in the 07-10 decision-log entry: context-management strategy audit / CLI removal candidate / seed_only api_key-borrow hint (deferred). Original framing + architecture kept below for the record.
- ~~**Status**: backlog, filed 2026-07-10 (user request); **architecture refined + prioritized ahead of P2.8 per gpt-5.6 Sol review 2026-07-10 (I verified every code claim)**. Not started.~~
- **User's frame**: two new model generations landed within ~2 weeks (Anthropic Fable 5; OpenAI gpt-5.6 series — Sol/Terra/Luna per the Sol review) and the app's lists are stale; wants (a) the lists updated, (b) updatable without a code slice each time, (c) stop listing models the app can't actually use.
- **Adopted shape (Sol refinement over the earlier "DB-backed authority" sketch — DB is NOT the sole authority)**:
  1. **Code-reviewed capability registry** — tools/structured-output support, effort tiers, context/output limits, executable driver per model family. These are engineering contracts, not user data; they stay in reviewed code.
  2. **DB discovery cache** — actually-visible models per provider + auth_mode + credential, with query timestamps (extends the existing per-auth-mode discovery the AI 研究 picker already uses via `researchModels.ts`).
  3. **Effective picker** — intersection: visible to the current active credential AND executable for the task at hand. **Adopted default (Sol Q → yes): picker shows only verified-usable models; legacy/pinned/custom-ID/unverified-seed entries live in an Advanced section with explicit warnings.**
  4. CLI / discord / routing / Settings all read the same effective view.
- **Drift sites to converge (all verified 2026-07-10)**: `src/agents/shared/model_catalog.py` (CLI+discord; newest opus-4.7/gpt-5.5) · `src/model_routing.py` (routing options; has opus-4.8, no Fable 5/gpt-5.6) · `config/user_profile.yaml` (scoring models, gpt-5.4 era) · **`src/agents/anthropic_agent/agent.py:102`** (`_ADAPTIVE_THINKING_MODELS`/`_EFFORT_MODELS` capability sets) · **`src/agents/openai_agent/agent.py:64`** (`_OPENAI_MODEL_MAX_OUTPUT`) · **`src/agents/shared/context_manager.py:58`** (`_MODEL_CONTEXT_LIMITS`) · prompt-side defaults. Sol's warning stands: converge the capability tables too, or the dropdown offers a model whose effort/output/context contract is then wrong at execution time.
- **Source material for slice-time verification (do not trust cached values)**: https://www.anthropic.com/claude/fable · https://developers.openai.com/api/docs/models · https://help.openai.com/en/articles/20001354 (gpt-5.6 availability; actual visibility varies by account/plan/auth channel — which is exactly why discovery is per-credential). openai package already bumped to 2.45.0 on 2026-07-10 (stale-process ImportError incident recorded in the calibration closeout entry).

### P2.8 UI improvement pass (audit-first + user irritation list)
- **Status**: **PHASE-1 AUDIT + CANONICAL DESIGN ADOPTED; written review and
  Claude Design conformance sync COMPLETE** (`docs/superpowers/specs/2026-07-12-p2-8-canonical-shell-interaction-design.md`).
  **Slice 1 UI primitives is MERGED / ALL GATES CLOSED 2026-07-12 through
  `3449e32`** (`docs/superpowers/plans/2026-07-12-p2-8-slice-1-ui-primitives.md`):
  the primitive foundation, Holdings migration, and Investor Profile
  presentation repair are on `master`. The bounded Settings stabilization is
  **LIVE COMPLETE / MERGED 2026-07-13 through `ca3db2f`** at
  `docs/superpowers/plans/2026-07-12-p2-8-settings-stabilization.md`: frontend
  accounting is exactly `41 files / 366 tests` (`+3 / +19`), focused suites,
  typecheck/build, static boundaries, no-PG smoke, and five-viewport browser
  gates are closed. Reviewer canonical A/B is identical
  (`30 failed / 4074 passed / 74 skipped / 18 warnings / 7 errors`, empty
  bidirectional failure diff, backend collect `+0/-0`). Portfolio 1.1 Slice 1
  is now merged and live through `fa052dc` under
  `docs/superpowers/specs/2026-07-13-portfolio-1-1-observation-activity-design.md`;
  its account-overview Slice 2 is **MERGED / LIVE 2026-07-15 through
  `35b3115`**: canonical A/B, responsive/privacy, and single-sidecar
  real-Gateway gates are closed. Portfolio 1.1 Slice 3 is **MERGED / LIVE
  2026-07-16 through `e5045dc`**, making the full three-slice Portfolio 1.1
  line LIVE COMPLETE. **P2.8 Slice 2 shell convergence is MERGED to `master`
  2026-07-17 through post-merge scroll hotfix `95662b1`; code, independent review, exact
  frontend accounting, responsive/live, static, backend-byte-identity, and
  merged-tree verification gates are closed.** The external Claude Design
  companion sync remains the one separate post-merge gate. **Slice 3 Research
  workspace is MERGED / LIVE 2026-07-18 through `5bbc753`; independent
  canonical A/B, merged-tree frontend/no-PG/static gates, responsive browser
  verification, and the one-call provider smoke are closed. Its external
  Claude Design companion sync remains separately pending**
  (`docs/superpowers/plans/2026-07-18-p2-8-slice-3-research-workspace.md`).
  The bounded NEWS `content_availability` Unit 2 is **MERGED / LIVE COMPLETE
  2026-07-18 through closeout base `b3d9728`**. Alpha Picks article
  reconciliation and comment-continuity line is **MERGED / LIVE COMPLETE
  2026-07-19 through `bf378f1`**. Independent canonical A/B, copied-DB
  migration/state-machine, repeated-Quick, merged popup/privacy, no-PG, and
  stopped-service production migration gates are closed. Production is schema
  v2 with integrity/FK clean and legacy logical facts preserved. DB-derived
  universe/`tickers_core.json` retirement is **MERGED / LIVE COMPLETE
  2026-07-19 through `cddea39`**. The stopped-service production cutover
  preserved the user-edited JSON in retained mode-0600 backups, imported only
  reviewed compatibility annotations, established exact 150-ticker
  DB-derived/export parity, and retired all runtime readers and writers. **P2.8
  Slice 4.1 Settings navigation correction is FAST-FORWARD MERGED to `master`
  through evidence tip `bfdc32c` (reviewed product tip `4931050`) on
  2026-07-20.** It supersedes Slice 4's rejected all-groups long page with
  three accessible workflow tabs and one mounted group, preserves guarded
  drafts/in-flight work, and makes `總經資料` the sole detailed FRED owner.
  Independent review reproduced exact frontend accounting `+42/-12`
  (`63/606 -> 65/636`), all static and immutable byte gates, and a separate
  41-check browser matrix. Merged-tree focused `18/164`, full `65/636`,
  typecheck, and build are green. **The corrected Settings surface is LIVE
  COMPLETE after the merged-master normal desktop check passed all five user
  acceptance points and a separate merged-master sandbox replay passed 41
  interactions.** Design Kit sync #5 is complete. The app-wide i18n decision
  document is written and independently review-approved. **I18N-0 foundation
  is LIVE COMPLETE at reviewed product tip `69a8ca2`, fast-forward merged
  through evidence tip `64017f4`.** Independent review completed canonical
  backend A/B at exact `+7/-0`; merged-tree backend `7`, frontend `73/680`,
  typecheck, build, scanner, no-PG, and isolated startup smoke are green.
  **I18N-1 Shell + common UI is LIVE COMPLETE at reviewed product tip
  `5a76528d`, fast-forward merged through evidence tip `6542e6e2`.**
  Independent review found zero issues. Exact frontend accounting is `+15/-0`
  (`73/680 -> 74/695`), focused is `12/108`, scanner is
  `1649/1563/1562/1`, and the both-locale six-viewport gate is green after one
  reviewed single-line topbar wrap correction. Fresh merged-tree gates and the
  normal merged-master desktop smoke are green. **I18N-2 Settings is LIVE
  COMPLETE at reviewed product tip `9f78a9ff`, fast-forward merged through
  evidence tip `d2195cd0`.** Independent implementation review returned GREEN
  with zero findings. Exact frontend accounting is `+83/-0`
  (`74/695 -> 77/778`), focused is `35/376`, scanner is
  `1033/973/954/20` with 28 migrated scopes, and the isolated both-locale
  six-viewport matrix is green. Merged-master reruns reproduced full `77/778`,
  focused `35/376`, scanner totals, typecheck/build, no-PG, and all protected
  byte gates; the normal desktop restarted in zh-Hant without creating
  `profile_settings.ui_locale`. Its bounded scope includes the currently
  reachable Investor Profile panel, all nine Settings anchors, and
  Settings-owned backend-copy mappings. The public selector remains
  unauthorized. **P2.8 Slice 5 is LIVE COMPLETE at reviewed product tip
  `b214e1fc`, fast-forward merged through evidence tip `de8485be`.** Independent
  implementation review returned GREEN. Exact accounting is backend `+53/-1`
  to collect `4621` and focused `219`; frontend sequential `+88/-3` to
  `83 files / 863 tests`; scanner `1033/973/954/20` with 30 scopes. After a
  retained `0600` online backup and explicit user approval, merged code migrated
  production calibration schema v1 to v2 with marker 2, clean integrity/FK,
  preserved message/terminal-proposal digests, legacy active-state
  supersession, and zero inferred coverage/base state. Fresh merged-tree gates
  and the normal zh-Hant desktop Summary smoke are green. **I18N-3 Explore is
  LIVE COMPLETE at reviewed product tip `be89a9b5`, fast-forward merged through
  evidence tip `eb4dee7b`.** Independent implementation review returned GREEN
  with zero findings. Exact frontend accounting is `+84/-3` to `90/944`,
  focused is `17/173`, scanner is `703/656/637/20` with 39 scopes, and the
  six-surface bilingual runtime matrix is green after one reviewed narrow-width
  CSS correction. Merged-tree focused/full/typecheck/build/scanner/no-PG/byte
  gates are green; the normal desktop restarted from merged master and passed
  zh-Hant Home/News/Ticker/Card smoke without creating
  `profile_settings.ui_locale`. **The combined I18N-4/5 remaining-surfaces
  unit is LIVE COMPLETE at Tranche A `34ddf08f`, reviewed product
  `20666d33`, and fast-forward/evidence tip `5f35e8b1`.** Independent review
  returned GREEN with zero findings; merged-tree gates and the normal zh-Hant
  Research/Holdings/Capture/System smoke are green. The former standalone
  I18N-5 cycle and AppRecords frontend backlog are closed. **The I18N-6 Release
  is LIVE COMPLETE at reviewed product tip `7b9c13b2`, fast-forward merged
  through evidence tip `2997c926`.** Repaired independent review returned
  GREEN and explicit approval preceded integration. Fresh merged-tree focused
  `13/148`, full `95/1056`, typecheck/build, resources `681/1781`, scanner
  `36/20/0/20`, backend collect `4621`, no-PG, protected byte/CSS, and diff
  gates are green. The production selector is visible in Settings; semantic
  read-only smoke left it at `zh-Hant` and production still has no stored
  `ui_locale`. The user's first intentional selection owns that first write.
  **Design Kit locale synchronization is COMPLETE (sync #6, 2026-07-25), and the
  app-wide i18n line is CLOSED with no open unit.** The `codex/i18n-6-release`
  branch and its worktree are removed. Localized surfaces are the web app as
  rendered in Electron. The SA browser extension popup is **explicitly English by
  decision**, while the CLI, logs, and sidecar diagnostics sit **outside the locale
  contract with no guarantee either way** — see the decision doc's scope boundary,
  which also fixes one canonical prose owner per condition so immediate popup
  feedback and durable localized health can coexist.
  The first focused irritation slice, **Models Routing UX,
  is LIVE COMPLETE 2026-07-12 (merged through `2fb1c4f`)**.
- **Authority correction (Sol, verified)**: `DESKTOP_APP_VISION_DRAFT.md` header itself says **"DRAFT — NOT a spec"** and parts are stale (written pre-Holdings). It is the *intent* source for triage — NOT implementation authority. The P2.8 phase-1 spec becomes the implementation authority.
- **Objective audit (completed 2026-07-12)**: `Alerts`/`Notes` remain disabled nav
  placeholders; the right rail remains a planned-content placeholder; the
  topbar exposes sidecar/tool-count/raw-apiBase diagnostics; `Settings.tsx` is
  now 3,839 lines; Research is a fixed 220px/1fr/320px layout; Holdings and
  Investor Profile both consume class families absent from `styles.css`; saved
  reports have storage/API but no GUI home; and five frontend ownership areas
  still use `window.confirm`. Full evidence, repair ownership, and acceptance
  gates live in the canonical spec.
- **User correction added 2026-07-11 — preserve the boundary that already exists**: Settings already separates `Providers` from `Models` (`SETTINGS_SECTIONS` + section dispatch in Settings.tsx). `Providers` owns credential login/management and credential-scoped discovery; `Models` separately owns per-task provider/model/effort routing after availability is known. The user understands and explicitly requires that dependency; the complaint is that the **existing Models section's display and controls are not intuitive**, not that login and routing need new pages or workflow education. P2.8 must preserve this ownership split and audit the Models surface itself (task cards, verified/advanced choices, source state, effort, test/reset, and page-level save/import/export) only after collecting the user's concrete problematic interactions. The S3 lifecycle hotfix may add only its owned row-level 重新登入 affordance under Providers; it must not become an accidental Models redesign.
- **First concrete irritation slice designed 2026-07-11; review round 1 absorbed**: live inspection showed the current route provider determines the only effective list shown, so OpenAI-routed tasks expose four OpenAI value models while Anthropic is hidden inside `手動覆寫`; a checkbox controls advanced visibility; and `已驗證可用` is visually conflated with the API-key-only `測試此模型`. Draft authority: `docs/superpowers/specs/2026-07-11-model-routing-settings-ux-design.md`. Proposed fix preserves the Providers/Models split while making each task provider-first: visible OpenAI/Anthropic segmented control, read-only active-credential context, one grouped model selector, distinct discovery/task-eligibility/actual-call states, contextual discovery refresh, and an auth-aware task-scoped test. Review refinements pin effort plus read-only thinking behavior (no new route field), explicit per-model eligibility/reason schema, frontend-only missing-credential save veto, one-turn/no-tools OAuth canaries, `model_not_visible` only after successful live discovery, and reverse version-skew fallback. S3 ships first; this is the next focused P2.8 slice after user approval.
- **Focused slice closeout 2026-07-12**: delivered provider-first task cards,
  one grouped selector, active-credential context, model-specific effort,
  bounded actual-call tests, stale-result invalidation, and explicit
  visible/eligible/callable states. Live OAuth proof: Luna was listed but
  failed honestly, while the mini control passed. Follow-up is transport-level:
  investigate third-party ChatGPT OAuth client identity/version compatibility
  versus an official Codex app-server path; do not silently fallback.
- **Phase 1 output delivered for written review**: objective current-UI audit +
  user irritation decisions → one canonical shell/interaction spec. Locked shape:
  compact progressive disclosure; user-default/developer shell modes;
  Explore/Research/Monitor/System navigation; stage-aware bounded progress plus
  a Research-first background-work drawer; adaptive Research rails; grouped but
  fully scannable one-page Settings; summary-first Investor Profile with guided
  scenario calibration; separate NavigationTarget and persistent Reference
  contracts; 960px shell breakpoint; and workflow-sized implementation slices.
- **Triage rule**: each item → (a) matches Vision Tier A intent (schedule), (b) contradicts a lock (ruling), or (c) new (design addendum). Micro-fixes not touching locked architecture may ship without reopening the Vision decision.
- **Sequencing note**: new feature surfaces (Track B UI, Alerts, Notes) should land on the unified shell primitives from this pass, not before it.
- **Claude Design project (inventoried 2026-07-10; P2.8 conformance + Slice 1 companion sync COMPLETE 2026-07-12)**: claude.ai/design "ArkScope Design System" (`f9c9ea59-…`, user-owned) remains the synchronized visual companion, while the canonical shell spec owns shell/IA/interaction behavior. The approved conformance sync updated `readme.md`, added the 960px shell token to `tokens/spacing.css`, and added `guidelines/shell-contract.html`. Slice 1 companion plan `plan_f9c9ea59626141ed_14c603dcda45` then reconciled the legacy `radius-lg` alias from 10px to the 8px cap and added the seven reviewed primitive specimens. Screens remain incremental outputs of their owning slices rather than prebuilt. Earlier truth-sync evidence remains in `ce122b8`.

### P2.9 AI execution usage observability
- **Status**: design adopted 2026-07-12; implementation deliberately deferred and must not interrupt unfinished core product work. Authority: `docs/superpowers/specs/2026-07-12-ai-execution-usage-observability-design.md`.
- **Why**: ArkScope already tracks tokens for agent loops and persists AI Research usage, but card synthesis/translation and other model tasks discard available duration/usage metadata. There is no complete per-user-action call tree or daily aggregate authority.
- **Adopted shape**: metadata-only `execution -> child execution -> provider call event` ledger in `profile_state.db`; every call links to one root user/system action and carries code-reviewed `feature / operation / trigger` classifications. Store nullable input/output/cache/reasoning tokens, wall/provider duration, provider/model/effort/auth and terminal state; never copy prompts or answers. Later translation/retry/regeneration is a new execution linked to the source artifact/execution.
- **Product surface**: future `System -> AI Usage` period dashboard plus contextual summaries in Research/Card. Settings is reserved for separately approved controls. V1 has no pricing, dollar cost, warnings, budgets, hard limits, backfill, or retention UI.
- **Future decomposition**: (1) all-seam capture foundation and coverage/health contract, (2) aggregation API and contextual summaries, (3) System dashboard. Do not open an implementation plan until this map explicitly promotes slice 1.

### P2.10 Portfolio 1.1 account observation and activity
- **Status**: **PORTFOLIO 1.1 LIVE COMPLETE 2026-07-16. Slice 1 capture foundation is MERGED / LIVE through `fa052dc`; Slice 2 account overview is MERGED / LIVE through `35b3115`; Slice 3 activity/journal is MERGED / LIVE through `e5045dc`. All review, exact canonical A/B, responsive/privacy/no-PG, copied-profile, and required real-Gateway gates are closed.** Authorities: `docs/superpowers/specs/2026-07-13-portfolio-1-1-observation-activity-design.md`, `docs/superpowers/plans/2026-07-13-portfolio-1-1-slice-1-capture-foundation.md`, `docs/superpowers/plans/2026-07-14-portfolio-1-1-slice-2-account-overview.md`, and `docs/superpowers/plans/2026-07-15-portfolio-1-1-slice-3-activity-journal.md`.
- **Why now**: Holdings V1 persists canonical positions, but the existing IBKR call already discards account-value tags and broker realized P&L, while no execution/commission ledger exists. IB Gateway execution history is current-day bounded, so every uncaptured day creates a permanent activity gap.
- **Adopted architecture**: retain `portfolio_accounts` / `portfolio_positions` / `portfolio_position_notes` as current-state authority; add a non-cascading append-only observation layer in `profile_state.db` for capture runs, account snapshots, complete broker-position observations, immutable executions, commission revisions, corrections, unmatched changes, and atomic manual-adjustment journals. Observations never rebuild canonical holdings. `ibkr_review` derives preview at read time; `ibkr_auto` may apply only a complete position leg after observation commit.
- **Capture**: one read-only Gateway capture service (`portfolio_capture=+70`, distinct from `holdings=+60`), startup catch-up, configurable 5-1440 minute cadence with 15-minute default, and manual trigger. Account/execution/position legs report truthful five-state terminal outcomes; coverage gaps are explicit and never mean zero activity.
- **Product surface**: Holdings-first four-tab layout (`持倉` / `活動` / `帳戶明細` / `同步紀錄`), per-account IBKR NLV/cash/margin/P&L, separate manual-account subtotal, broker/canonical dual timestamps, correction-aware order/fill activity, user-confirmed intent, and no fake overall net worth. Data Sources does not duplicate the scheduler.
- **Sequence**: Slice 1 observation/capture foundation and minimal controls -> Slice 2 account overview -> Slice 3 activity/journal UI. Each slice gets its own plan, review, canonical A/B, and live Gateway gate. Flex history, performance return calculations, agent activity tools, multiple Gateways, and order placement are deferred. Future trading is expected but must use a separately authorized client-id domain, order ledger, confirmation, and kill switch; capture stays read-only.

---

## 5. P3 — Paused / Deferred

### P3.1 RL productionization
- **Status**: paused as of 2026-04-25.
- See `docs/design/RL_COLLAPSE_FINDINGS.md` §11 Decision Record and `memory/project_rl_paused.md`.
- Re-investment requires reward redesign + walk-forward training + multi-seed agreement + baseline outperformance threshold pre-committed.

### P3.2 ArkScope rename Phase 2 (local + code)
- **Status**: blocked on stable git state + no in-flight long-running tasks. Currently both conditions fail.
- See git history + memory `project_rename_arkscope.md` (the executed runbook `ARKSCOPE_RENAME_PHASE2.md` was removed 2026-06-07, recoverable via git).

### P3.3 Social / alternative data (Reddit, StockTwits, Twitter/X)
- **Status**: not approved. Noise-to-signal ratio + scraping governance cost too high without a clear use case.

### P3.4 MCP integration
- **Status**: not needed near-term. HTTP API more direct for OpenClaw / external clients.

---

## 6. Cross-Cutting: open-source-first principle

Where the option is "build it" vs "use OSS", default to a **time-boxed OSS spike (≤1 day) before committing to custom**. Applies to:

- S3 admin dashboard (P1.5)
- Future job queue if S2 outgrows in-process scheduler (RQ / Dramatiq / Prefect)
- Knowledge graph backend if NetworkX outgrows in-memory (Neo4j Community vs Memgraph vs Kùzu)
- Future analytics views (Superset / Metabase vs custom)

The spike should produce a decision doc, not just opinion. If OSS doesn't fit, document **what specifically failed** so future spikes don't repeat.

---

## 7. Subscription Strategy

Two **separate** decision frames. Don't mix.

### 7.1 Operational subscriptions (must-keep)

Question: "Would breaking this disrupt our current product workflow?"

| Service | Status | Why |
|---------|--------|-----|
| IBKR market data | active | quotes / options / price core; cannot be replaced |
| SA Alpha Picks | active | proven research workflow, extension already integrated |

### 7.2 Research subscriptions (evidence-gated)

Question: "Is the signal hypothesis pre-validated, with OOS plan and kill criteria?"

This is the bucket that ties to *signal-research investment plans* — distinct from operational must-keeps. Each candidate requires:

1. Written hypothesis (what edge / what regime / what universe).
2. OOS validation plan (windows, baseline, metric thresholds).
3. Kill criteria (when to cancel if hypothesis fails).
4. Cost vs value estimate.

| Candidate | When to consider | Notes |
|-----------|------------------|-------|
| Polygon Premium | only if real-time / minute-bar gap blocks intraday work | duplicate IBKR for many uses |
| Unusual Whales | only if options/event-driven monitor is built and validates options-flow signal | $50/mo sample; needs use case |
| Benzinga | only if news latency / analyst-rating gap is empirically proven | overlaps IBKR/Finnhub/SA |
| Financial Datasets API | already integrated as fallback; keep on-demand toggle | not pure subscription, pay-as-go cache |
| Tavily / Exa | already in use, keep | agent-side enrichment, not signal source |

### 7.3 Not approved near-term

Reddit / X / StockTwits paid access — defer until a specific signal hypothesis is committed to writing.

---

## 8. Dependency Graph

```
                  ┌─────────────────────────────────────┐
                  │  P0.1 Replay Harness (foundation)   │
                  └────────────┬────────────────────────┘
                               │
            ┌──────────────────┼─────────────────────────┐
            │                  │                         │
            ▼                  ▼                         ▼
  P1.4 Compression B    P2.1 Unified Runner C    (any agent edit)
            │                  │
            │                  ├──→ P2.2 Knowledge Graph A
            │                  │     (consumer)
            │                  └──→ Phase D Analysis Pipeline
            │
  P0.2 S2 jobs persist
            │
            ├──→ P1.5 S3 dashboard (OSS-first)
            │           │
            │           └──→ P2.3 OpenClaw integration
            │
            └──→ telemetry for SA / freshness / reports

  P0.3 SA Comment Stage 1 ──┐
  P0.4 SA Market News      ─┴──→ P2.2 Knowledge Graph (consumer)

  P1.1 Signals tool       (independent, near-zero deps)
  P1.2 FRED/calendar      (independent)
  P1.3 SA Latest articles (independent)
```

---

## 9. Sequencing Recommendation

**Status (2026-05-02, post-pivot)**: P0.1-P0.4 + P1.1-P1.5 (spike) + Phase C spec all landed. **Phase C is paused**; product re-positioned to **local-first investment research workbench**. **Active sequence**: (1) `docs/design/LOCAL_FIRST_RESEARCH_WORKBENCH_AUDIT.md` — done 2026-05-02; six sections (north star + 5-layer architecture + 5 axes with sync surface as main axis + Hermes-capability gap); (2) `docs/design/LOCAL_FIRST_RESEARCH_WORKBENCH_SPEC.md` — locks product positioning, deployment model, storage strategy (**two-SQLite split (`workbench.db` app state + `sa_cache.db` SA ingest, isolated writer locks) + transient DuckDB on parquet** — no persistent `warehouse.duckdb` in v1; PG → archive/import), sync policy (v1 manual zip-and-go, v2 selective sync), page IA, **bidirectional DTO inventory (read + write actions, even if writes not yet implemented)**, **scheduler storage interface + process interface + deferral trigger** (NOT model A/B/C choice — that stays deferred per audit §7.4), Phase C resume gate, "first SQLite/DuckDB migration cut" kill criterion, and explicit decisions for all seven open questions (no spec-internal opens); (3) **docs governance cleanup** — audit at `docs/design/DOCS_GOVERNANCE_AUDIT_2026_05.md` (this commit) + follow-up archive/delete/rewrite commit, BEFORE code work begins (removes pre-pivot entrypoint pollution that would otherwise pull cut #1 back toward PG/RL/CLI-first); (4) read-only UI skeleton against existing PG reads — validates product shape without committing to write paths; (5) two-SQLite migration first cut: `research_reports` first (small + already-files-on-disk) + zip-and-go smoke; (6) cross-machine smoke insertion-point: **one table migrated → immediate zip-and-go test on second machine** (do NOT defer to end). Phase C resumes only after the three-part gate (workbench ship + 2 weeks stable + 1 verified migration). P0.4 deferred items stay deferred; P2.2 Knowledge Graph stays deferred; P2.4 Key Markets opportunistic-only. Operational subscriptions (IBKR / SA Alpha Picks) unchanged; research subscriptions still evidence-gated.

**Locked decisions for this pivot (2026-05-02)**:
- **PG runtime fate**: not optional — v1 runtime is SQLite/DuckDB-only; PG schema kept as legacy/archive/import path for one-shot data migration; active PG path deleted incrementally once SQLite/DuckDB smoke passes. Dual-backend mode rejected (multiplies query/migration/test cost).
- **DuckDB scope**: added but strictly OLAP/columnar — news / price / macro / backtest / cross-source exploration. NOT for scheduler state, UI state, memory, settings, watchlist (those go to SQLite). DuckDB can `ATTACH` SQLite for cross-engine joins.
- **Packaging**: deferred. Spec only locks deployment target (local web app or desktop wrapper must read same local profile directory); PyInstaller / launcher script / Tauri choice waits until scheduler daemon model + storage profile are fixed. Premature packaging choice would prematurely bind process model.
- **Audit-now**: not optional — without an audit, the spec is a wishlist. Five axes, ~60-90 min, sync surface is the main axis (not an appendix) per "跨機同步是根本問題" framing.

The historical week plan below is kept for context.

---

A concrete week-1 / week-2 plan to fit the dependency graph (historical):

**Week 1**
- Day 1-2: P0.1 Replay Harness — enables everything downstream.
- Day 3-4: P0.2 S2 jobs persist — DB schema + history API.
- Day 5: P0.4 SA Market News reliability patches (interleaved).

**Week 2**
- Day 1-3: P0.3 SA Comment Intel Stage 1.
- Day 4: P1.1 Multi-factor signals tool/API (fast win).
- Day 5: P1.2 FRED + calendar (fast win).

**Week 3+** (re-evaluate against this map)
- Likely candidates: P1.3 SA Digest, P1.5 S3 dashboard OSS spike, P1.4 Compression Phase B.

This was intentionally aggressive on P0 to clear the foundation block; P1 items individually are 1-day fast wins that demonstrate momentum.

---

## 10. Decision Log

> **Order**: NEWEST FIRST. New entries go at the TOP of this section,
> directly under this header. Reverse chronological matches the
> "what just happened?" reading mode — most recent decisions front-loaded.
> When adding an entry, do NOT scroll to the bottom; insert immediately
> below this note.

- **2026-07-30 (PRICE COLLECTION DETERMINISTIC TIER RUNNER AMENDMENT WRITTEN - FOCUSED REVIEW NEXT)**: Independent review cleared blocker `fa42d44a` with zero findings and confirmed that T0/T1 violated the reviewed termination bound while T2 raced an operator-added identity assertion; all remain `invalid`, with no admitted node result. The user selected a single SHA-pinned Python module that acts as both parent controller and pytest progress plugin (`pytest_runtest_logstart`/`pytest_runtest_logfinish`). Structured progress crosses one inherited atomic pipe and is persisted as `progress.jsonl`; it is control evidence only, while the unchanged final reporter remains the sole collected/seen/non-passing authority. The runner owns `Popen(start_new_session=True)`, stable PID/PGID/SID validation, pre-first-node and per-node monotonic 150-second deadlines, 120-second per-item dump distinction scoped to the current progress window, process-group SIGINT, full 10-second grace, optional SIGKILL, atomic attempt records, and immediate refusal after the first `invalid`. A breach with the current-window dump remains `unresolved_stall`; a collection/final-teardown breach without it is `invalid`. Four mandatory pinned probes cover natural pass, SIGINT termination, SIGKILL fallback, and unchanged collect-only identity. Environment allowlist, reporter, outcome table, tier map, banking tuple, retry, A/B accounting, product/tests/providers/Gateway/production data, and both main drafts remain protected. Focused spec review is the sole next gate; exact runner source belongs to a later independently reviewed plan, and Task 0/product RED remain unauthorized.

- **2026-07-30 (PRICE COLLECTION TIERED TASK 0 BLOCKED - INVALID RUNNER EXECUTION)**: Focused review cleared tiered-contract tip `3863b3be` with zero findings. The authorized restart reproduced all four canonical collections, backend/frontend focused gates, scanners, tools, and no-PG smoke exactly; the immutable builder/reporter/map remained `0f0421f8...` / `09d2bc52...` / `3d7adb7e...`, with all `4722` nodes mapped exactly once. Runtime admission did not close. T0 and T1 emitted their 120-second dumps at `test_route_rejects_unreviewed_interval_with_typed_422` and `test_scheduler_start_stop`, but manual orchestration failed to enforce the reviewed 150-second termination boundary; both partial transcripts are therefore `invalid`, not `unresolved_stall`, and no node result is banked. A following T2 launch exposed an added immediate PID/PGID/SID assertion racing `setsid` (`145/1/1`), exited without a terminal summary or reporter JSON, and is also `invalid`. T1/T2 ran only because T0 was misclassified before sealing; T3-T7, retries, monolithic diagnostics, protected Step 6, and product RED did not start. A 46-file scratch manifest at `/tmp/price-truth-tier-v1/task0-invalid-manifest.sha256` has SHA `1f3f5c61...`; isolated `data/` is empty, no tier process remains, and no product/test/provider/Gateway/production-data/main-draft path changed. The next gate is a separately reviewed deterministic control-runner amendment using an in-process monotonic no-progress deadline and stable post-`setsid` identity handshake. Re-running the same manually supervised wrapper is unauthorized.

- **2026-07-30 (PRICE COLLECTION TIERED VERIFICATION CONTRACT WRITTEN - FOCUSED REVIEW NEXT)**: Focused closeout review cleared diagnosis tip `e6d4b7fa`, `master` fast-forwarded without a merge commit, and merged collection-only verification reproduced exact backend `4722/fcdb1b7d…`. The price branch rebased from old tip `f7458727` onto that restart base: range-diff kept the first seven patches exact and changed the final two only to retain later diagnosis decision-log entries; the three price authority files remained byte-identical before this amendment. A collection-only prototype then assigned all `253` backend test files to eight deterministic whole-file tiers with node loads `591/591/590/590/590/590/590/590`; builder `0f0421f8…`, map `3d7adb7e…`, concatenated count `4722`, unique count `4722`, and sorted-union hash `fcdb1b7d…` prove zero dropped or duplicated nodes. A stdlib-only pytest reporter `09d2bc52…` preserves exact node IDs, including embedded spaces, and makes transcript token parsing inadmissible for non-passing sets. A second full collection under the proposed allowlisted `env -i` boundary again produced exact `4722/fcdb1b7d…` while leaving isolated `data/` empty; runtime tiers therefore inherit neither provider credentials nor ambient DB paths. End-to-end pass and intentional-failure probes both validated the exact `setsid`/terminal-summary/reporter command, including the space-containing failure ID; neither is admitted as a product baseline. The proposed `price-truth-tier-v1` contract reuses one immutable map for base/tip, runs tiers sequentially in fresh processes, banks only complete natural-exit tiers under an unchanged identity, permits one deferred retry per stalled tier, and preserves Stop Condition 11 at tier granularity. Partial transcripts remain inadmissible; historical monolithic and fresh-process tiered contexts are explicitly not comparable. One bounded monolithic attempt remains diagnostic-only. Product/test code, node/resource budgets, predicted hashes, providers, Gateway, production data, and the two main-worktree drafts remain unchanged. Focused contract review is the sole next gate; Task 0 and product RED remain unauthorized.

- **2026-07-30 (LIFESPAN DIAGNOSIS REVIEW GREEN - CLOSEOUT REVIEW NEXT; PRICE VALIDATION DECOUPLED)**: Independent raw reconstruction returned GREEN with zero findings: both manifests, schedules, source hashes, eight `10/10` matching-stall cells, E0 `0 -> 0`, E1 `0 -> 1`, zero invalids, and V6 precedence were reproduced without relying on evidence prose. A reviewer then reported three non-stall A0B0 replays, but the handoff omitted raw coordinates; a fresh pinned-controller closeout replay now supplies them and produced the opposite current state, `3/3` six-flag matching stalls at `80.032...s`, return `-9`, no pyrate thread, no data files, with nine-file manifest `ed2f1067...`. These observations are partitioned from the frozen 80-trial result and jointly show a time-varying condition without identifying its mechanism or a currently clean window. The user ruled that this infrastructure stall may no longer be the price-truth fix's sole acceptance gate: the full suite remains instrumented and partial output is never called a pass, but the price spec/plan must define a separately reviewed tiered verification contract before product RED. V6 selects no SEC/import/TestClient seam. Closeout-only scope now transfers the unresolved incident to `EIR-005`; its machine-state observer is nonblocking and ordered after the price line's next review checkpoint unless the real desktop exhibits the same startup failure. Diagnosis merge, price rebase, and any product action remain unauthorized pending focused closeout review.

- **2026-07-29 (LIFESPAN STALL CAUSAL DIAGNOSIS REVIEW-READY - INDEPENDENT RAW REVIEW NEXT)**: The two reviewed, counterbalanced matrices reached their pre-registered block-10 terminal point with `80` total valid trials, `0` invalid attempts, and `0` boundary violations. Phase 1 recorded `10/10` matching portal-signature stalls in each of `A0B0/A0B1/A1B0/A1B1`; mandatory Phase 2 recorded the same `10/10` in each of `E0B0/E0B1/E1B0/E1B1`, while all E0 snapshots proved leaker transition `0 -> 0` and all E1 snapshots proved `0 -> 1`. The controller and independent verifier agree exactly, so reviewed precedence selects `V6 ambient_or_machine_state_dominates`: SEC collection, the mount predecessor, and direct `edgar` import are each not necessary for the observed stalls, but the shared mechanism remains unknown and no tested factor is declared harmless in every process state. Terminal summary hashes are Phase 1 `16c1e179…` and Phase 2 `60c55eac…`; independent reconstructions are `47e7bab3…` and `d2c2bb0…`; phase manifests are `79ab780f…` and `e7aac43c…`; the 345-entry complete manifest is `9725dd4f…`. Product/test/dependency files, production resources, main drafts, and price tip `f7458727` remained protected. This verdict authorizes no fix, merge, dependency change, TestClient conversion, machine-state observer, or price-truth restart. Independent review must reconstruct the raw records before any product/fix decision passes the approved four-question gate.

- **2026-07-29 (LIFESPAN STALL DIAGNOSIS PLAN WRITTEN - INDEPENDENT PLAN REVIEW NEXT)**: Independent review of design `222636a7` returned GREEN with zero findings after reproducing the complete `4722/fcdb1b7d…` collection and all four `1/2/7/8` cell identities. The experiment plan at `docs/superpowers/plans/2026-07-29-lifespan-stall-causal-diagnosis.md` pins a standard-library scratch controller, an identical-byte import-only plugin, two position-balanced 20-block schedules, closed portal-signature classification, exact block-10/15 truncation thresholds, one replacement for an invalid exposure attempt, and raw-record reconstruction before verdict. Prototype-only validation compiled the controller, plugin, and independent verifier, reproduced E0 `0 -> 0` and E1 `0 -> 1` leaker transitions without changing collection identity, and fixed controller/plugin/verifier/schedule SHA-256 values before any behavioral trial. Phase 2 remains mandatory; finite no-stall samples cannot become a fix claim. Real lifespan coverage, product/test/dependency files, production resources, the two main drafts, and price tip `f7458727` remain protected. Task 0 and all behavioral trials remain unauthorized pending independent full-plan review.

- **2026-07-29 (FULL-SUITE LIFESPAN STALL CAUSAL DIAGNOSIS WRITTEN - INDEPENDENT SPEC REVIEW NEXT)**: The price-collection partial-truth branch remains frozen at blocker packet `f7458727` after its restarted full-suite baseline again produced the reviewed AnyIO portal-spawn stall, now at the first untouched lifespan family in `tests/test_api.py`. The bounded diagnosis design at `docs/superpowers/specs/2026-07-29-lifespan-stall-causal-diagnosis-design.md` does not preselect a fix or convert another TestClient family. It pins a fresh-process `2 x 2` experiment over SEC-test collection and the immediately preceding real-app route-mount node, followed by a SHA-pinned import-only `edgar` plugin `2 x 2` that preserves the predecessor factor. Trials are hermetic, counterbalanced, capped at 20 per cell, dump before outer termination, classify non-stall failures separately, and cannot promote a finite no-stall sample into a fix claim. Grounding names both repository imports of `data_sources.sec_filings`, confirms active SEC DAL/agent paths do not consume `SECFilingsClient`, and separates the later product decision about edgartools-backed 10-K/10-Q content from test hygiene. Real lifespan coverage is a protected user-facing product contract; the final packet may state only a reviewed causal verdict, after which causal seam, SEC capability value, coverage integrity, and ownership require a separate user-approved fix decision. Product/test/dependency edits, main-worktree drafts, production data, merge, and price RED remain unauthorized pending independent spec review and a separately reviewed experiment plan.

- **2026-07-29 (PRICE COLLECTION TASK 0 RESTART BLOCKED - TEST_API LIFESPAN STALL)**: Focused review of `7844429a..5fecce65` returned GREEN and authorized the full Task 0 restart. Clearance `5fecce65` reproduced all four collections exactly (backend `4722/151`, frontend `96/1074`, focused `86`), backend `151 passed`, frontend `86 passed`, scanner twice `36/20/0/20`, tools `53/54/54`, and no-PG `23/23`. The instrumented full suite passed the converted `test_agents` route exposure, then stalled at `tests/test_api.py::TestHealth::test_status`. Its 120-second dump placed the pytest thread in `TestClient.__enter__` from `tests/test_api.py:41` while the AnyIO portal thread remained in the event loop; this identifies the blocking boundary without identifying the ambient lifespan cause. The operator interrupted only after the dump. The unique diagnostic transcript is `204` lines / `18895` bytes, SHA `1e2f8907…`; no normalized non-passing set was derived. Isolated `data/` remained empty, no pytest process remained, and no product/test file, provider, Gateway, production DB, or user-owned draft changed. Stop Condition 11 blocks protected Step 6 and product RED pending a separately reviewed resolution.

- **2026-07-29 (PRICE COLLECTION PARTIAL-TRUTH REBASED - FOCUSED PLAN CONFIRMATION NEXT)**: Independent harness implementation review returned GREEN with zero findings, and `master` fast-forwarded to exact reviewed tip `2edf12e1`. Merged verification reproduced full `4722/fcdb1b7d…`, agents `31/78d7cdbe…`, owned `2/5e1e62ac…`, and both route nodes passed. The price-truth branch rebased onto that base; five newest-first conflicts were resolved by preserving every reviewed harness and price-truth entry in chronological order. The historical Task 0 stall remains diagnostic-only. The first `test_agents` lifespan exposure is removed, while the harness tip's later stall at untouched `test_api.py::TestHealth::test_status` prevents any global termination claim. Exactly the Task 0 base and Task 5 tip full-suite commands now add `faulthandler_timeout=120`; it emits stacks but does not terminate pytest, and Stop Condition 11 remains binding. All node IDs, counts, deltas, resources, and predicted hashes are unchanged. No price product code, provider, Gateway, production data, or user-owned draft changed. Focused confirmation of this docs-only handoff is the sole next gate; Task 0 must then restart from Step 1 before product RED.

- **2026-07-29 (QUERY ROUTE HARNESS IMPLEMENTED - INDEPENDENT REVIEW NEXT)**: Test-only implementation `31230232` replaces exactly the two `TestQueryEndpoint` nodes' full-app fixture with the reviewed minimal real-router ASGI transport; product code, `conftest`, and the other five `TestClient` families are byte-quiet. Structural RED failed only on `TestClient`; source GREEN, direct `2 passed`, and two independent 20-iteration runs (`80/80` repeated node executions total) passed. Full, agents-file, and owned collections remain byte-identical at `4722/fcdb1b7d…`, `31/78d7cdbe…`, and `2/5e1e62ac…`, exact `+0/-0` with empty comms. The instrumented tip full suite passed both owned nodes, then produced the expected diagnostic stop at the next untouched lifespan family, `tests/test_api.py::TestHealth::test_status`; its dump again showed `TestClient.__enter__` waiting on the AnyIO portal. This slice therefore removes the reproduced first exposure without claiming global suite termination. No partial failure set, production data, provider, Gateway, scheduler, or main-worktree draft was used. Independent implementation review is the sole next gate; merge and the price-truth rebase remain unauthorized.

- **2026-07-29 (QUERY ROUTE HARNESS TASK 0 GROUNDED - STRUCTURAL RED NEXT)**: Clearance `db7f2240` reproduced backend `4722/fcdb1b7d…`, agents `31/78d7cdbe…`, and owned `2/5e1e62ac…` exactly in the isolated worktree. The pre-change two-node selection again failed to terminate (bounded 20-second run exit `124`; the planned 30-second observation was shortened only because the orchestration envelope closed before its inner timeout could report). More decisively, the instrumented full suite collected all `4722`, stalled at `test_providers_endpoint`, and emitted its 120-second all-thread dump: the pytest thread waited in `starlette.testclient.TestClient.__enter__` through AnyIO's blocking portal while the portal thread remained in asyncio `select()`. The operator interrupted only after the dump; no pytest process, data file, product edit, or partial A/B baseline remained. This locates the observed blocking boundary without claiming which ambient state caused it. The reviewed source-boundary RED is now the sole next step.

- **2026-07-29 (QUERY ROUTE HARNESS PLAN REVIEW GREEN - TASK 0 CLEARED)**: Independent plan review of `bed13bc4` returned GREEN with zero findings. The reviewer reproduced all three collection hashes (`4722/fcdb1b7d…`, agents `31/78d7cdbe…`, owned `2/5e1e62ac…`) and executed the prescribed minimal FastAPI + real router + async DAL/personalization stubs + `ASGITransport` shape directly, observing the unchanged `200` provider map and typed `400` unknown-provider response without lifespan entry. The structural source RED, wrong-RED rules, 20-iteration bound, instrumented full-suite protocol, reversible isolated-data handling, protected adjacent harnesses, and reviewed integration/rebase sequence are cleared. Task 0 may begin from this docs-only clearance; product test edits remain unauthorized until grounding completes and the structural RED fails for the specified reason.

- **2026-07-29 (QUERY ROUTE HARNESS SPEC REVIEW GREEN - IMPLEMENTATION PLAN NEXT)**: Independent review of design tip `3216c1b9` returned GREEN with zero findings and verified all six gates: exact backend `+0/-0` at `4722/fcdb1b7d…`, unchanged two-node identity and HTTP assertions, no deterministic root-cause claim, diagnostic-only `faulthandler_timeout=120`, strict one-file scope, and no duplicate EIR owner. Code grounding additionally confirmed that unknown-provider rejection currently follows personalization resolution and that FastAPI resolves `get_dal` before the handler, making both deterministic stubs required rather than optional. The RED-first plan at `docs/superpowers/plans/2026-07-29-query-route-harness-termination.md` now pins the full collection plus `tests/test_agents.py` `31/78d7cdbe…` and owned `2/5e1e62ac…`, uses a source-boundary RED rather than adding a node, prescribes the reviewed minimal real-router ASGI transport verbatim, repeats the two nodes 20 times, and instruments base/tip full-suite runs without accepting partial output. Product test edits remain unauthorized until independent plan review clears Task 0.

- **2026-07-29 (QUERY ROUTE HARNESS LIFESPAN-EXPOSURE DESIGN WRITTEN - INDEPENDENT REVIEW NEXT)**: Price-collection partial-truth Task 0 correctly stopped after its required backend full-suite baseline ceased progress at `tests/test_agents.py::TestQueryEndpoint::test_providers_endpoint`; a bounded single-node run also timed out at fixture setup, while a later independent rerun passed, so no deterministic ambient root cause is claimed. The bounded design at `docs/superpowers/specs/2026-07-29-query-route-harness-termination-design.md` keeps both existing node IDs and HTTP assertions but replaces only their full-app `with TestClient(create_app())` fixture with a minimal real-router `httpx.ASGITransport` harness that cannot enter application lifespan. Expected backend accounting remains exact `+0/-0`, `4722`, and collection SHA `fcdb1b7d…`; product code, global pytest configuration, and the other five lifespan-using test families are protected. After reviewed merge, the price-truth docs branch rebases and adds `faulthandler_timeout=120` to its base/tip full-suite captures; this emits diagnostic stacks but does not terminate a stall or permit partial A/B evidence. The active micro-slice is the sole owner, so no duplicate EIR entry is opened unless this work is paused or abandoned. Product test edits remain unauthorized pending independent spec review and a separately reviewed RED-first plan.

- **2026-07-29 (PRICE COLLECTION PARTIAL-TRUTH TASK 0 BLOCKED - FULL-SUITE HARNESS HANG)**: Clearance commit `15933c31` reproduced all four canonical collections exactly (backend `4722/151`, frontend `96/1074`, focused `86`) plus backend `151 passed`, frontend `86 passed`, scanner twice `36/20/0/20`, tools `53/54/54`, and no-PG `23/23`. The required same-environment full run, launched from empty isolated `data/` with unbuffered verbose output, stopped at `tests/test_agents.py::TestQueryEndpoint::test_providers_endpoint` for more than 70 seconds and was interrupted with exit `130`; a bounded single-node replay timed out after 20 seconds with exit `124` at the same fixture setup. The partial transcript (`58` complete lines / `5348` bytes plus the active line, SHA `7c4f83d2...`) is retained only as EIR-002 diagnostic evidence and is not an A/B baseline. An ignored deterministic 143,360-byte profile fixture from earlier tests was proven distinct from the 43,962,368-byte production DB and moved reversibly to `/tmp`; production data and both main-worktree untracked authority drafts remained untouched. Stop Condition 11 prohibits protected Step 6, product RED, provider work, or implementation until the harness hang is resolved or the baseline protocol is formally amended and reviewed.

- **2026-07-29 (PRICE COLLECTION PARTIAL-TRUTH PLAN CLEARED - TASK 0 NEXT)**: Independent focused review cleared reviewed plan tip `9d1e648a` after the sole required correction aligned the mounted Settings node ID with its existing `Settings provider config authority` describe prefix; all four predicted final collection hashes then remained mechanically consistent. The advisory 26-slot mutation now requires its exact temporary diff in evidence so a dead reconciliation-only mutation cannot masquerade as classification coverage. Exact budgets remain backend `+17/-0` to `4739` (`168` focused), frontend `+2/-0` to `1076` (`88` focused), and Settings `704 -> 706` with Explore unchanged at `380`. Full-suite A/B commands now use unbuffered verbose output: a repeated hang triggers Stop Condition 11, preserves the final node line as EIR-002 diagnostic evidence, and cannot become a partial baseline. The main-worktree `SCRIPTS_RETIREMENT_DECISION.md` and `IBKR_PACING_AND_ERROR_SEMANTICS.md` remain untracked, user-owned, and forbidden to edit, stage, delete, or cite. Task 0 grounding is authorized; product edits remain blocked until it closes with a complete same-environment baseline.

- **2026-07-28 (PRICE COLLECTION PARTIAL-TRUTH PLAN WRITTEN - INDEPENDENT PLAN REVIEW NEXT)**: Final spec re-review returned GREEN with zero findings at `1a695141`. The RED-first plan at `docs/superpowers/plans/2026-07-28-price-collection-partial-truth.md` grounds backend `4722/151` and frontend `96/1074` plus focused `86`. Planned accounting is exact backend `+17/-0` to `4739` (`168` focused), frontend `+2/-0` to `1076` (`88` focused), and two Settings leaves per locale (`704 -> 706`, total `1783 -> 1785`, Explore unchanged at `380`). Seven direct nodes separately cover partial sibling preservation, all-issue failure, resolved former targets, one-row low-volume success, non-target rows, stable original target identity, and mixed exception/unresolved rollup. The worker and scheduler validate a closed payload, retain lock-busy skip, map semantic price partial to durable `partial` plus failed audit without continuation, and pin normalized-news audit behavior unchanged. Eight mutation probes include fixed-26-slot false-partial detection. Adapters, Coverage v2, schema, provider health, source catalog/cadence, Gateway locking, scripts, production data, and repair remain protected. Product edits remain unauthorized until independent plan review records clearance.

- **2026-07-28 (PRICE COLLECTION PARTIAL-TRUTH SPEC AMENDED - FINAL RE-REVIEW NEXT)**: Independent review confirmed the day-presence reconciliation, fail-closed price projection, collection/Coverage dual-state boundary, and anti-false-partial direction, then exposed one pre-existing repository split: normalized-news durable `partial` currently persists `job_runs.status='succeeded'`, while SA degraded and the proposed prices partial persist `failed`. Normalized-news partial is not uniformly resumable; covered shapes include both real continuation and `continuation=null`. Data Sources also renders the generic job-run check/cross beside the durable badge, so news can show check-plus-partial while prices will show cross-plus-partial. The spec now records this inconsistency without expanding the stop-the-lie slice: normalized news, SA, provider health, global job history, schema, and generic glyph behavior remain protected. A later bounded convergence decision must inventory history, health, failure counters, backoff, status APIs, and UI consumers before choosing any enum, projection, schema, or presentation change. RED-first evidence now requires three separately named anti-false-partial shapes (idempotent no-target, a resolved former zero-bar target, and a one-row low-volume day), plus a mutation proving that replacing day presence with a fixed 26-slot rule fails. The disproven provider-health impact is not adopted. Final full-document re-review is the sole next gate; implementation planning and all product/runtime/data work remain unauthorized.

- **2026-07-28 (PRICE COLLECTION PARTIAL-TRUTH SPEC WRITTEN - INDEPENDENT REVIEW NEXT)**: Coverage v2 correctly reported 2026-07-27 as `149 complete / 0 partial / 1 indeterminate (LCID)`, while the direct-local collector persisted success after inserting 3,874 sibling rows and leaving LCID at its 2026-07-24 frontier. A later ordinary run reported one gap, added zero rows, and again persisted success. The bounded design at `docs/superpowers/specs/2026-07-28-price-collection-partial-truth-design.md` rechecks only each run's original zero-bar day targets after insertion, assigns stable `price_day_unresolved_after_fetch` without guessing IBKR/Polygon/halt/no-trade cause, and derives succeeded/partial/failed from distinct per-ticker outcomes. Partial preserves successful rows, prevents unresolved ticker success/error clearing, maps to failed in three-value audit tables, remains partial in durable scheduler state, and exposes a bilingual unresolved count without a continuation or repair control. `rows_added=0` alone remains legal for idempotent and already-present days; slot-level low-volume/no-trade truth, adapter outcome protocols, provenance, Coverage v2, provider policy, schema, and production repair remain outside scope. The observed `provider_sync_meta.provider='ibkr'` cannot prove whether Polygon fallback produced a row because the current prices schema has no provenance. This contract-violation slice precedes EIR-002 and root scripts retirement. Independent full-document review is the sole next gate; implementation planning, product edits, provider calls, production writes, merge, and push remain unauthorized.

- **2026-07-28 (SA FEED STORE-TRUTH LIVE COMPLETE - ALPHA PICKS ALIGNMENT REMAINS OPEN)**: Independent test-closure re-review returned GREEN with zero findings, and `master` fast-forwarded to exact reviewed tip `aaee26b7` without a merge commit. Merged verification reproduced backend `4722/108`, frontend `96/1074` and focused `27`, resources `380/704/1783`, scanner twice `36/20/0/20`, tools `53/54/54`, no-PG `23/23`, typecheck/build, and protected byte gates. A direct merged-code positive smoke returned the normal populated feed; a separate absent-path negative smoke retained real read-only profile history, returned typed `store_missing`, preserved normalized request facts, leaked no path/diagnostic, and created no temporary store. The bilingual News surface rendered its populated state with one feed GET per locale, no locale PUT, and no runtime exception. Live Firefox extension ingestion wrote during an earlier permission wait; after its next cycle completed, a bounded final pre/UI/post window preserved both production DB fact packets exactly, with integrity `ok` and FK zero. No release control triggered refresh, recovery, scheduler, provider, or repair work. The untracked `SCRIPTS_RETIREMENT_DECISION.md` remains untouched for the next line of work, and Alpha Picks missing-store availability alignment remains an open product-contract follow-up.

- **2026-07-28 (SA FEED STORE-TRUTH REVIEW GREEN - TEST-CLOSURE RE-REVIEW NEXT)**: Independent implementation review of `664635a5` returned GREEN with zero required product changes and reproduced all four hashes, exact `+30/-0` and `+2/-0` comm, ordered behavior, mutations, resources, scanner, tools, no-PG, and protected boundaries. Its sole non-blocking advisory found that the reachable outer `get_sa_feed` catch-all still lacked a direct regression node for the original fixed-`days=30` and raw-`error=str(e)` failure class. One test-only node now drives an `_sa_db` property exception through that path and requires normalized `days/query`, typed `store_query_failed`, and no marker/error field. Independent RED probes restored each old defect separately and turned only the new contract red; product source remains byte-identical to reviewed product tip `841d7241`. Final backend accounting is `4691 -> 4722`, exact `+31/-0`, focused `108`; final hashes are full `fcdb1b7d…` and focused `b0ec2b6f…`. The matched full run is `4623 passed / 27 failed / 72 skipped`, with the reviewed non-passing set unchanged (`27/27`, new `0`, gone `0`). Frontend `96/1074`, resources `380/704/1783`, scanner, tools, no-PG, browser evidence, and every product boundary are unchanged. Only this one-node test/evidence delta awaits focused re-review; merge and post-merge production smoke remain unauthorized.

- **2026-07-27 (SA FEED STORE-TRUTH IMPLEMENTATION REVIEW-READY - INDEPENDENT REVIEW NEXT)**: Product tip `841d7241` derives one ordered read-only SA feed truth contract without changing the global SA connector, schema, extension/native-host, providers, scheduler, repair, Alpha Picks, or CSS. Canonical accounting is backend `4691 -> 4721`, exact `+30/-0`, focused `107`; frontend remains `96` files and moves `1072 -> 1074`, exact `+2/-0`, focused `27`. The seven activity authorities are separately status-insensitive and mechanically complete; missing history reads create nothing; on-disk open, five-table capability validation, and query share one `mode=ro` connection; every unavailable reason remains typed HTTP `200`, while feature-disabled remains `503`. Resources are Explore `380`, Settings `704`, total `1783`; scanner is twice `36/20/0/20`; tools are `53/54/54`; no-PG is `23/23`. A 24-case bilingual `390/960/1440` Chrome matrix used adversarial positive-count unavailable fixtures and found no hidden statistics, facets, rows, valid-empty copy, pagination, overflow, runtime exception, console error, or HTTP failure. In-place locale switching preserved mode, filters, focus, DOM identity, and exact SA request count `3 -> 3`. Backend base/tip non-passing sets are byte-identical `27/27`, with an exact `+30` passing delta. No production data or service was touched. Independent implementation review is the sole next gate; merge and post-merge negative smoke remain unauthorized.

- **2026-07-27 (SA FEED STORE-TRUTH TASK 0 GROUNDED - RED-FIRST IMPLEMENTATION ACTIVE)**: Isolated worktree `codex/sa-feed-store-truth` at clearance `a364268c` reproduced backend `4691/77`, frontend `96/1072` and focused `25`, all four normalized hashes, scanner twice `36/20/0/20`, tools `53/54/54`, and no-PG `23/23`. No production data, external service, browser profile, or user configuration entered the worktree. Task 0 also repaired four stale nonexistent protected-path names in the plan before product edits, replacing them with the current shell/primitives/token owners so the byte gate is executable. Implementation is now active RED-first and remains blocked from merge or production interaction pending independent implementation review.

- **2026-07-27 (SA FEED STORE-TRUTH PLAN REVIEW GREEN - IMPLEMENTATION CLEARED)**: Independent review reproduced all five collection/resource baselines and approved the design. Two verified test-layer advisories are binding before Task 0. First, one real FastAPI transport node now proves every typed unavailable feed reason remains HTTP `200`; the existing handler-direct node only owns populated behavior and feature-disabled `503`. This supersedes backend accounting in the plan-written snapshot by `+1/-0`: backend is now exact `+30/-0`, `4691 -> 4721`, focused `77 -> 107`; frontend remains `+2/-0`, `1072 -> 1074`, focused `25 -> 27`. Second, the closed `SAFeedEmptyReason` consumer must be an exhaustive `switch` with a `never` guard, not a `Set.has()` classifier that would silently accept future union growth. The existing recovery-target node gains that source-level proof without changing frontend accounting. Isolated RED-first implementation may begin from this docs-only clearance commit; merge, production fault injection, database writes, and the separate Alpha Picks availability follow-up remain unauthorized.

- **2026-07-27 (SA FEED STORE-TRUTH IMPLEMENTATION PLAN WRITTEN - INDEPENDENT PLAN REVIEW NEXT)**: The RED-first plan at `docs/superpowers/plans/2026-07-27-sa-feed-store-truth.md` grounds clean `a7806dd9` collections at backend `4691/77` and frontend `96 files / 1072 nodes` plus `2 files / 25` focused. Planned accounting is backend `+29/-0` to `4720` (`106` focused) and frontend `+2/-0` to `1074` (`27` focused), with seven activity names held by seven independent status-insensitive nodes. One no-create `mode=ro` history primitive feeds an ordered SA-store classifier; direct on-disk open, five-table capability validation, and the feed query share one read-only connection without changing the global SA connector. The existing generic feed error retires into a closed reason union, and mounted adversarial unavailable fixtures must suppress counts, facets, rows, valid-empty copy, and pagination at render time. Resources move Explore `379 -> 380` and total `1782 -> 1783`; Settings, scanner `36/20/0/20`, tools `53/54/54`, no-PG `23`, routes, schema, extension/native-host, Alpha Picks, providers, and CSS remain unchanged. Post-merge negative verification is limited to a unique absent temporary SA path with the real profile authority read-only and exact before/after production DB invariants. Product edits remain unauthorized until independent plan review records a clearance commit.

- **2026-07-27 (SA FEED STORE-TRUTH SPEC REVIEW GREEN - IMPLEMENTATION PLAN NEXT)**: Independent full-document review returned GREEN with zero required changes. Two useful advisories are binding in the spec: FTS capability is checked independently of the current `q` so one damaged store cannot alternate between available and incompatible by request shape, and post-merge verification includes a real-profile-history/absent-temporary-SA-path negative smoke while preserving both production DBs and proving the temporary path remains uncreated. The seven-name activity owner, no-create mutation proof, ordered reason contract, safe diagnostics, exact resource delta, and rendered `feed.available` statistics gate are cleared for RED-first planning. Product edits remain unauthorized until the implementation plan receives independent review.

- **2026-07-27 (SA FEED STORE-TRUTH SPEC WRITTEN - INDEPENDENT REVIEW NEXT)**: The user-approved bounded design at `docs/superpowers/specs/2026-07-27-sa-feed-store-truth-design.md` replaces the current missing-file-as-empty projection with one ordered read-time contract. Only a readable, capability-compatible on-disk `sa_capture.db` whose requested query succeeds may return `available=true`; valid zero rows remain `no_items_in_window`. Missing storage is neutral only when a no-create `profile_state.db` witness finds no surviving SA activity evidence; prior activity or unreadable history fails closed as degraded. Unreadable, incompatible, and post-validation query failures receive distinct typed reasons, raw SQLite/path diagnostics are excluded, normalized request facts survive failures, and the News surface may not render zero/facet claims while unavailable. The existing `requires_local_sa` state remains first-class. The seven-name activity owner is mechanically checked against extension protocol operations, SA-enabled service jobs, and the audited repair owner; the inference explicitly depends on unpruned job history and records double-store loss as an epistemic boundary. Scope is read-only and excludes global connector, schema, extension, provider, CSS, and Alpha Picks behavior. The Alpha Picks missing-store `is_partial + refresh_hint` inconsistency is now a named future **SA availability alignment** contract in this map, not an Engineering Issue Register item and not part of this micro-slice. Independent full-document review is the sole next gate; implementation planning and product edits remain unauthorized.

- **2026-07-27 (LEGACY SCHEDULER + IV PRODUCT RETIREMENT LIVE COMPLETE - PRODUCTION CUTOVER EXECUTED)**: After ArkScope desktop and SA auto-sync were stopped, merged code at `1142ac95` produced exact read-only preview `0ed0916d2cd165574e7ddbce1dbefe755526ced0aa105e82db34d452814aca0b`; both DBs were integrity-clean/FK-zero and SQLite matched the four Parquets value-for-value. The user explicitly approved that manifest. Apply archived the two scheduler rows, one schedule setting, one IV sync row, 24 SQLite IV rows, and four Parquets under `data/backups/legacy_scheduler_iv_retirement_20260727T123347933126Z/` (manifest SHA-256 `30c01ea8fd009a3d47c5ac96ffd4dd9b0282a1adef03faafb91c3dd50dd92fad`, directory `0700`, artifacts `0600`), then removed only those targets. A second apply returned `already_applied=true`; postchecks preserved all 1,352 historical target `job_runs` plus exact profile/market non-target digests, retained exactly four active scheduler sources, and passed scheduler/options gates `174 passed / 1 skipped`. The user restarted ArkScope and confirmed the retired Settings identities and legacy IV surface are gone. Future provider-neutral IV remains gated and must use a new semantic ID/schema; the archive is rollback evidence, never a seed.

- **2026-07-27 (LEGACY SCHEDULER + IV PRODUCT RETIREMENT MERGED - PRODUCTION CUTOVER PENDING)**: Independent implementation review returned GREEN with zero product findings. Two docs-only transcription defects were corrected before integration: migrated-scopes SHA-256 is `02e335bebcadfba523d502a7af86a5c184d1ac024230cfec9199dd19b4416c13`, and the memory-bound node is `test_logical_database_digest_is_memory_bounded`. `master` fast-forwarded from `0bdd5261` to exact reviewed tip `28b136d1`, with no merge commit. Merged verification: retirement/scheduler/options/no-PG `218 passed / 1 skipped`; frontend `96/1072`; typecheck/build exit 0; resources/scanner/foundation `43/43`; scanner twice `36/20/0/20`; runtime no-PG `23/23`, `ok=true`, `pg_attempts=[]`. Backend full observed `4629 passed / 9 failed / 53 skipped`; the absolute environment-bound count is not an allowlist, while the reviewed canonical base/head node-ID comparison remains the regression authority. A non-migration production `mode=ro` smoke left both DBs and four Parquet files size/mtime-identical, with integrity `ok`, FK 0, and exactly four merged schedule sources. The physical legacy targets remain intentionally present: 24 IV rows, four Parquets, one sync row, two scheduler-state rows, one setting, and all 1352 historical target job runs. **Not LIVE COMPLETE:** production preview/archive/apply is still blocked on stopped writers, a fresh exact manifest, and the user's second explicit approval.

- **2026-07-26 (LEGACY SCHEDULER + IV RETIREMENT IMPLEMENTED - INDEPENDENT REVIEW NEXT)**: The cleared two-tranche plan is implemented on isolated branch `codex/legacy-scheduler-iv-retirement`, with named checkpoint `TRANCHE_1_TIP=747aa51acc056d98da316153be3edd9c96f90cda`. Canonical composition is backend base->T1 `+6/-24`, T1->final `+23/-63`, total `+29/-87` to `4691` (`605` focused); frontend is `+3/-3`, then `+1/-1`, total `+4/-4` at `96 files / 1072 nodes` (`139` focused). The one reviewed backend deviation is a RED-first memory-bound test: a copied 3.3 GB market DB exposed a roughly 6 GiB full-table logical-digest implementation, which was replaced by length-framed streaming SHA-256 and held the production-shaped preview near 125 MiB RSS. Final resources are Settings `704`, Explore `379`, total `1782`; tools are `53/54/54`; no-PG is `23`; scanner is deterministic `36/20/0/20`. Copied-data preview/archive/apply/idempotence/restore proves exact preservation of all `1352` historical target `job_runs`, non-target digests, SQLite/Parquet parity, archive modes/hashes, and restore of the 24-row legacy IV domain. Isolated bilingual runtime gates show exactly four schedule rows and no retired IV request/UI at 390/960/1440 while preserving locale-switch state. No production migration, deletion, provider/Gateway/PG call, merge, or push occurred. Independent implementation review is the sole next gate; production archive/apply remains separately blocked on merge, stopped writers, fresh preview, and explicit approval.

- **2026-07-26 (LEGACY SCHEDULER + IV RETIREMENT PLAN REVIEW GREEN - IMPLEMENTATION CLEARED)**: Independent review reproduced all four baseline hashes, all 81 existing node IDs, both tranche ledgers, resources, tools, and no-PG arithmetic. Two docs-only defects are closed at clearance `5f528475420c8de407125bceb32d94050cfa8e14` without moving any target: the unknown `NewsWriteMode` RED now asserts the real `status/code/reason_code` scheduler envelope instead of indexing a nonexistent `ok` field, and Tranche 2 explicitly removes the unreachable in-process market-data job poller end to end (`_JOBS`, both starters, `get_job`, route, and frontend DTO/helper). The latter's seven tests were already included in the reviewed 40-node retirement set. A separately discovered query-shape typo in the illustrative retained-Greeks request is also corrected to the real `S/K/T/sigma` contract without changing accounting. Task 0 may start in an isolated worktree; implementation still performs no production write, and production archive/apply remains blocked on independent implementation GREEN, merge, stopped writers, fresh preview, and a second explicit approval.

- **2026-07-26 (LEGACY SCHEDULER + IV RETIREMENT PLAN WRITTEN - INDEPENDENT PLAN REVIEW NEXT)**: Independent spec review is GREEN with zero remaining findings. The RED-first implementation plan at `docs/superpowers/plans/2026-07-26-legacy-scheduler-iv-domain-retirement.md` rederives the current `7bb7cc29` base and makes the two-tranche attribution executable. Tranche 1 is exact backend `+6/-24` (`4749 -> 4731`, focused `663 -> 645`) and frontend `+3/-3`, with the two additive `NewsWriteMode` safety nodes separately identified from retirements, a real `prices_runtime.parse_args()` argv seam, resources `1814 -> 1806`, and a full-hash `TRANCHE_1_TIP`. Tranche 2 is backend `+22/-63` to `4690` and frontend `+1/-1`, removes exactly three history-backed tools and two old-store analysis scripts while leaving broader scripts retirement open, changes resources to `1782` and no-PG `24 -> 23`, and adds 16 migration plus 4 boundary nodes. Preview/archive/apply/idempotence/restore each have independent mutation-sensitive proof; copied-data restore runs under an exact detached pre-retirement code worktree. Product edits, production migration, provider/Gateway/PG calls, and merge remain unauthorized until independent plan review clears Task 0.

- **2026-07-26 (LEGACY SCHEDULER SOURCES + IV DOMAIN RETIREMENT SPEC WRITTEN - INDEPENDENT REVIEW NEXT)**: The user approved full retirement rather than continued read-only tombstones. The single design document at `docs/superpowers/specs/2026-07-26-legacy-scheduler-iv-domain-retirement-design.md` defines two independently attributable tranches: Tranche 1 removes `price_backfill`, `local_incremental`, and scheduler `iv_history` from the active seven-row catalog, collapses schedule controls to the four real direct collectors, and records a named `TRANCHE_1_TIP`; Tranche 2 removes the stale 24-row legacy IV read domain across SQLite/Parquet, API/UI/status/health, evidence packets, DAL/backends, three history-backed agent tools, and the two old-store-coupled analysis scripts while preserving Greeks, live option chain, IV skew, pure option math, all non-IV market payload, and every historical `job_runs` row. Retiring those two scripts is a scoped supersession of the B6 survivor table and aligns with the separate gradual scripts-retirement direction; it does not absorb other scripts. Fresh read-only production grounding found `collect.local_incremental=1350 succeeded`, `collect.price_backfill=2 failed`, no IV collect runs, two target scheduler-state rows, one dead schedule preference, one IV sync-meta row, and exact value parity between the SQLite 24 rows and four Parquet files. The spec corrects the initial zero-resource assumption: Tranche 1 removes 8 Settings leaves per locale (`714/1814 -> 706/1806`), while Tranche 2 removes 2 Settings plus 22 Explore leaves (`704/379/1782` final), changes central/bridge tool counts `56/57 -> 53/54`, and changes the no-PG smoke inventory `24 -> 23` by retiring exactly the IV-history check. Production removal is separately approval-gated and requires a mode-0600 restore-ready mini-DB/Parquet/state archive, non-target digests, idempotent apply/restore proof, and byte-preserved `job_runs`. Future provider-neutral IV remains gated under the existing proof-packet/S-D/S-E decisions and must use new semantic IDs/schema. Independent full-document review is the sole next gate; implementation planning and all product/data edits remain unauthorized.

- **2026-07-26 (COVERAGE V2 MERGED / LIVE COMPLETE)**: User-approved integration fast-forwarded `master` from `f019f9fa` to independently reviewed product tip `3f0fb18f`, with no merge commit and no push. Merged-tree verification reproduces backend focused `230/230`, backend collection `4749` with node hash `e7dc826f…`, frontend focused `8/118`, frontend full `96/1072`, scanner `36/20/0/20` twice, resources `714/1814/44`, typecheck/build, and no-PG `24/24`. A production read-only schedule-owner smoke returns `price_backfill` as read-only/disabled with neutral blank history; profile and market DB size/mtime remained identical, integrity is `ok`, FK violations are zero, and no scheduler/repair/provider action ran. The Superpowers worktree was removed and the branch deleted non-forcibly. Coverage v2 is closed; `/sa/feed` missing/unreadable-versus-empty semantics remains the next bounded observability item, while Settings Developer diagnostics sanitizer alignment follows separately.

- **2026-07-26 (COVERAGE V2 FOLLOW-UP REVIEW GREEN - P3 TEST CLOSURE COMPLETE)**: Independent follow-up review confirmed all product findings closed and found one test-only mutation gap in the four-field blank-state predicate. Two named scheduler tests now cover `last_status`-only, `continuation`-only, `last_error`-only, and `last_result`-only legacy shapes; removing any one field independently makes its owning test fail, while product source remains byte-identical after probes. Final backend accounting is `+76/-40` to `4749`, focused `230/230`; normalized full/focused node hashes are recorded in evidence. Frontend/resources/runtime/production behavior is unchanged. The historical `Price Gap Backfill` identity remains, with the read-only chip, description, and absent controls owning current semantics. The branch is unmerged; commit and integration are the only remaining actions.

- **2026-07-26 (COVERAGE V2 REVIEW FOLLOW-UP COMPLETE - RE-REVIEW NEXT)**: Independent implementation review was substantively GREEN but found that the historical `price_backfill` stable ID still exposed misleading schedule/Run controls and that a production-shaped row with only attempt metadata projected a fabricated `legacy_unproven_gap` failure. The RED-first follow-up adds closed `scheduled/read_only/retired` control modes, rejects non-scheduled operator routes, ignores stale enabled flags, preserves historical telemetry, and renders truthful bilingual read-only copy with no controls. Completely blank outcome fields remain neutral; every non-empty unproven legacy shape still fails closed. Current accounting is backend `+74/-40` to `4747`, focused `228`; frontend `+12/-3` to `96/1072`, focused `118`; resources `714/1814/44`. Full frontend, scanner, build, no-PG, and production `mode=ro` integrity/mtime checks pass. The observed backend full result is environment-bound `4612 passed / 61 failed / 74 skipped`, not an acceptance constant. The branch remains unmerged and awaits independent follow-up review.

- **2026-07-26 (COVERAGE V2 IMPLEMENTATION REVIEW-READY - INDEPENDENT REVIEW NEXT)**: Review-repair product tip `cb33a193` replaces maximum-relative coverage with a pinned offline XNYS authority, reviewed official fixtures, exact half-open RTH slot identity, an ordered six-state day enum, separate unknown/unmatched/provider facts, and one atomic V2 backend/frontend contract. The legacy `missing_tickers` planner feed and `src/scheduler_planner.py` are gone; unknown/provider facts and unproven legacy continuations cannot create repair work. Post-evidence read-only review additionally closed empty-universe 500 behavior, optional-provider-diagnostic poisoning (including invalid UTF-8), diagnostic-source trimming, and locale-derived React remounts. Canonical accounting is backend `4713 -> 4746` (`+72/-39`) and frontend `95/1063 -> 96/1072` (`+11/-2`); focused `227` and `118`, resources `713/1813/44`, scanner `36/20/0/20`, typecheck/build/no-PG, twelve HTTP truth fixtures, two-locale four-width browser evidence, and production read-only `150 x 26` witnesses are green. Production market/profile size, mtime, integrity, FK, and relevant digests were unchanged. This is review-ready only: no merge, LIVE claim, provider/Gateway action, repair, or production write is authorized before independent implementation review.

- **2026-07-26 (COVERAGE V2 SPEC REVIEW GREEN - IMPLEMENTATION PLAN NEXT)**: Independent full-document review returned GREEN with zero findings. The ordered seven-path precedence model, separate `unmatched_rth_row_count`, 13:29/13:30 actual-close buffer boundary, atomic `missing_tickers` retirement, and `legacy_unproven_gap` continuation rejection are cleared for planning. The sole advisory is accounting, not design: new bilingual Settings states must carry an exact resource-leaf delta, and the existing inventory-count node must evolve in place from the rederived `settings=694 / total=1794` baseline. A RED-first implementation plan is the sole next gate; no product, dependency, database, provider, or scheduler change is yet authorized.

- **2026-07-26 (COVERAGE V2 SESSION-TRUTH SPEC WRITTEN - INDEPENDENT REVIEW NEXT)**: The design at `docs/superpowers/specs/2026-07-26-coverage-v2-session-truth-design.md` converts the reviewed inventory into one bounded contract. It selects pinned `exchange_calendars` XNYS as the provider-independent runtime adapter, official NYSE-derived fixtures as test authority, `us_listed_equity_proxy` plus `rth` as explicit DTO scope, read-time half-open session projection, exact slot identity, a 30-minute actual-close buffer, and one pure classifier. Missing exact observations remain `unknown`; in-window off-grid rows are reported separately as `unmatched_rth_row_count`. The ordered day enum is `unknown / non_trading / in_progress / partial / indeterminate_tickers / complete`, with exact-match consumers and separate unknown counts. Maximum-relative fields and `missing_tickers` retire atomically, and neither unknowns nor provider errors may reach planner candidates or exclusions; legacy unversioned gap continuations fail closed until a separately reviewed repair authority exists. Calendar health has one composition owner, official fixture review must ship 12 months forward, and a running build below six months is degraded without erasing reviewed historical results. The route remains read-only with no schema, provider, Gateway, PG, or repair work. Independent spec review is the sole next gate; implementation planning and product changes remain unauthorized.

- **2026-07-25 (COVERAGE V2 GROUND-TRUTH INVENTORY REVIEW GREEN - DESIGN SPEC NEXT)**: Independent review returned GREEN with zero substantive findings. The sole precision question is resolved by read-only production replay: GOOG stores 64 rows from 08:00-23:45 UTC on 2026-07-02, while QBTS stores 63 rows over the same endpoint span on 2026-07-24; the latter is one interval short of its own 64-slot observed range. The inventory now names both witnesses rather than implying they are one number. It is cleared as the evidence basis for a separately reviewed design spec, but it adopts no runtime calendar, exchange proxy, RTH policy, dependency, schema, provider, API, or repair decision by itself. Coverage v2 design is the sole NEXT action; implementation planning and product edits remain unauthorized.

- **2026-07-25 (COVERAGE V2 GROUND-TRUTH INVENTORY WRITTEN - INDEPENDENT REVIEW NEXT)**: The evidence-only inventory at `docs/design/COVERAGE_V2_GROUND_TRUTH_INVENTORY.md` traces the current hand-built holiday owner, fixed 16:30 ET completion rule, maximum-relative daily diagnostic, zero-day-only planner, provider retrieval semantics, storage schema, frontend contract, and focused `82/82` baseline. Read-only production inspection found 2,386,679 `15min` rows across 151 stored symbols and a 150-symbol active universe at observation time, with integrity clean and database size/mtime unchanged. Known early-close dates are modal 14-bar sessions, while recent days contain one or two 58-64-row extended-hours outliers beside 26-row RTH majorities; because `prices` stores no provider/session provenance, raw daily maximum is not an expected-session authority. Official NYSE calendars/memos, IBKR `SCHEDULE`/`useRTH`, Massive all-session/no-trade-bar semantics, and the two principal offline calendar packages are compared. A pinned offline session owner plus official golden fixtures, optional IBKR corroboration, explicit RTH policy, and read-time session filtering is the working recommendation, not an adopted decision. Independent review must first validate the three-layer model and authority candidates; no spec, dependency, schema, provider, or product change is authorized.

- **2026-07-25 (CALIBRATION ANTHROPIC REFUSAL LIVE COMPLETE - COVERAGE V2 GROUND-TRUTH INVENTORY NEXT)**: Independent implementation review returned GREEN with zero findings, and `master` fast-forwarded through reviewed evidence tip `8e73dba4`. Fresh merged-tree verification reproduced focused `48/48`, backend collection `4713`, and no-PG `ok:true / pg_attempts:[]`; merge and closeout did not touch production data, Providers, prompts, frontend code, or schema. EIR-004 remains the separate owner for the generic frontend refusal title. Coverage v2 is now the sole NEXT work item, but its first deliverable is a ground-truth inventory rather than a design spec: identify and compare the actual candidates for per-day expected session length, including full and early-close sessions, then select or reject an authority through review. Existing `26` and `13-14` bar observations and the deferred-B+ comment are evidence to investigate, not acceptance constants or a preselected calendar.

- **2026-07-25 (CALIBRATION ANTHROPIC REFUSAL IMPLEMENTED - INDEPENDENT REVIEW NEXT)**: Product commits `5fa47c5b` and `1a634ced` detect Anthropic `stop_reason="refusal"` before text extraction and preserve it through the calibration route as HTTP `502` plus durable `model_refusal`, fixed safe diagnostic, and existing retryable turn state. RED-first proof is distinct at both seams; private model/category/detail fixtures remain absent from storage and HTTP payloads, and the responder is called once with no fallback or effort retry. Collection is exactly `4711 -> 4713`, focused `46 -> 48`, additions are the two reviewed IDs, and removals are zero. Virgin full A/B exposed only the registered `test_agents` classification instability; fresh same-file base/head replay was byte-equivalent at `23 pass / 1 fail / 7 error`, after which common-node drift is empty and canonical passed delta is exactly `+2`. Focused `48/48`, no-PG `ok:true` / `pg_attempts:[]`, diff check, and every protected byte gate pass. Evidence is at `docs/superpowers/evidence/2026-07-25-calibration-anthropic-refusal.md`. EIR-004 now owns the still-generic frontend turn-error title for the next Investor Profile UI slice; this backend-only branch remains unmerged and stops at independent implementation review.

- **2026-07-25 (ISSUE REGISTER + CALIBRATION REFUSAL PLAN REVIEW GREEN - IMPLEMENTATION CLEARED)**: Independent review reproduced every initial register entry and returned GREEN. A fresh resource comparison confirms `160` identical zh/en leaves, but the proposed `24` multi-word and `2 aria / 6 routing / 5 runtime` actionable classification has no persisted exact key ledger or reproducible classification rule, so it remains outside the evidence-only register until those facts are written. The calibration micro-plan independently reproduces `4711` full / `46` focused, exact `+2/-0`, the stop-reason-only shared helper, all three existing refusal seams, and the two distinct RED sites. Its one UX advisory is accepted without widening scope: after the backend makes calibration `model_refusal` real, review-ready closeout will register the still-generic turn-error title for the next Investor Profile-owned UI slice. Backend-only RED-first implementation is cleared; frontend, prompt, schema, retry/fallback, shared refusal owners, and provider policy remain protected.

- **2026-07-25 (ENGINEERING ISSUE REGISTER + CALIBRATION REFUSAL MICRO-PLAN WRITTEN)**: `docs/design/ENGINEERING_ISSUE_REGISTER.md` is now the single owner for small, reproducible engineering debt, with evidence-only admission, dated observations, mechanical promotion, named-test requirements, evidence-backed closure, and an owner/next-action anti-graveyard rule. Its initial open set is deliberately only three independently grounded entries: unreachable `.page-head*` CSS, the environment-dependent non-green backend baseline, and the I18N-2-era 89-item Settings copy-quality audit. Existing popup contrast rules remain in their canonical spec, the SA evidence environment discrepancy is resolved, unverified same-locale copy is not admitted, and Coverage v2 stays a formal map-owned workstream. Calibration refusal is excluded because it violates an existing contract. Its short backend-only RED-first plan at `docs/superpowers/plans/2026-07-25-calibration-anthropic-refusal.md` is grounded on `4711` full / `46` focused and targets exact `+2/-0`: one direct Anthropic seam node and one route/durable-state node. It reuses `is_refusal` and `AnthropicRefusalError`, records `model_refusal`, adds no retry/fallback, and keeps frontend, prompts, schema, shared refusal owners, and provider policy byte-identical. Independent register and plan review are the sole next gates; implementation is not authorized.

- **2026-07-25 (SA EXTENSION RELIABILITY + POPUP CONTRAST LIVE COMPLETE)**: The reviewed reliability branch and its bounded post-merge contrast repair are merged through `2ef8bb69`. Merged master reproduces backend collection `4711`, focused `328/328`, frontend `95/1063`, typecheck/build, scanner `36/20/0/20`, package closure, and protected boundaries. Production historical repair is terminal and idempotent at `88 already_present / 30 repaired / 0 unavailable / 0 retryable`, with all `118` bodies present and integrity/FK clean; the contrast closeout did not rerun it. The merged Firefox installer rebuilt a 15-file artifact containing `article_identity.js`; Chrome source and Firefox artifact were reloaded through normal browser UI, produced no reload-triggered job, and the operator confirmed both popups' normal, explanatory, and Advanced controls are clear. Firefox is the single auto-sync owner because scrolling capture may activate its working tab; Chrome remains manual fallback to isolate daily browsing and prevent duplicate schedules. The popup stays English, the app owns durable localized health, and an embedded/app-owned collector remains unadopted P2.6 Part 2 work. The temporary worktree and merged branch were removed non-forcibly; no push was performed.

- **2026-07-25 (SA EXTENSION RELIABILITY IMPLEMENTATION REVIEW GREEN - MERGE APPROVED)**: Independent review reproduced backend `4621 -> 4710`, focused `238 -> 327`, frontend `1056 -> 1063`, focused `62 -> 69`, exact `+91/-2` and `+8/-1`, typecheck/build/scanner/no-PG, atomic packaging failure, four-state reason validation, derived degradation, protected boundaries, and popup static contracts. In the reviewer's matched data-bearing environment, backend full was base `31 failed / 4516 passed / 74 skipped` and tip `31 failed / 4605 passed / 74 skipped`; the 31-ID failure sets are byte-identical and the `+89` pass delta exactly equals the backend net node delta. This resolves the evidence advisory: absolute failure-versus-error classification varies with mounted data/config, while equal-environment failure-set equivalence is authoritative. The reviewer explicitly did not rerun the isolated Chrome/Firefox Task 8 browser matrix and accepted the recorded artifact/runtime evidence for that scope. Fast-forward merge is approved; installation, a fresh historical preview, and any exact-manifest repair remain subsequent gates.
- **2026-07-25 (SA EXTENSION RELIABILITY IMPLEMENTATION REVIEW-READY - INDEPENDENT REVIEW NEXT)**: Product tip `c85f4bed` completes deterministic Firefox dependency closure, closed JS/Python run truth, bounded idempotent telemetry, structured localized health, no-age recorded-ID repair, 168-hour-capped incident discovery, and five grouped English popup controls with exact bounds and contextual/Advanced recovery. Canonical accounting is backend `4621 -> 4710` (`+91/-2`, focused `327`) and frontend `1056 -> 1063` (`+8/-1`, focused `69`); full A/B keeps an identical pre-existing backend failure/error set, frontend/typecheck/build/scanner are green, and protected formatter/runtime boundaries hold. Copied-DB gates found `25` legacy source runs / `450` failure records / `118` unique targets / `88` already present / `30` missing without committing IDs or licensed content. The gate also found and RED-first fixed exact historical-run lookup being incorrectly limited by the latest 200 rows, with zero node-ledger change. Real Chrome/native lifecycle, exact-XPI Firefox accessibility, two-locale four-width six-state health, cleanup, and production non-write evidence are complete. Independent implementation review is now the sole NEXT gate; merge, production browser installation, and any historical repair remain separately blocked.
- **2026-07-25 (SA EXTENSION RELIABILITY PLAN REVIEW GREEN - IMPLEMENTATION CLEARED)**: Independent full-plan review returned GREEN with one required baseline correction and two grounding advisories. Independent replay confirms frontend full `95 files / 1056` hashes to `5f9a1624...`; the prior `f6351647...` value was misrecorded, while frontend focused `025e8717...`, backend full `488eeaab...`, and backend focused `ca36c2cc...` remain exact. Hash recipes now distinguish raw pytest `file::node` IDs from Vitest web-relative `file<TAB>name` rows. The review correctly requested grounding for the Alpha additional-recovery defaults, but the defaults are real: `AgentConfig.sa_comments_backfill_per_full_scan=10` and `sa_comments_backfill_per_backfill_scan=50`, with profile overrides and matching DAL fallbacks; DAL excludes parked pending rows from Full and includes them in Backfill. Test accounting remains backend `+91/-2` to `4710` and frontend `+8/-1` to `1063`. Isolated RED-first implementation is now the sole next action; merge, extension installation, and production repair remain blocked on their later gates.
- **2026-07-25 (SA EXTENSION RELIABILITY IMPLEMENTATION PLAN WRITTEN - INDEPENDENT PLAN REVIEW NEXT)**: The RED-first plan at `docs/superpowers/plans/2026-07-25-sa-extension-reliability-control-clarity.md` is grounded on clean `c49a2417` with backend `4621`, focused `9 files / 238`, frontend `95 files / 1056`, focused `4 files / 62`, resources Settings `681` / total `1781`, and scanner `36/20/0/20`. Its exact target is backend `+91/-2` to `4710` and focused `14 files / 327`, plus frontend `+8/-1` to `95 files / 1063` and focused `4 files / 69`; all three removed node IDs and successors are named. Dependency closure lands first and records `PACKAGING_GATE_TIP`; later review must compare base -> packaging checkpoint -> final. The plan separates capture truth from audit delivery, uses JS/Python parity fixtures, bounds the telemetry outbox at 100 records / seven days with visible eviction, derives database status server-side, creates resumable hash-pinned repair manifests without schema changes, and preserves five permanent popup actions plus contextual/Advanced recovery. Historical repair remains a distinct post-merge approval over a fresh exact manifest. Product work, browser installation, merge, and production repair remain blocked on independent full-plan GREEN.
- **2026-07-25 (SA EXTENSION RELIABILITY DESIGN REVIEW GREEN - IMPLEMENTATION PLAN NEXT)**: Independent review reproduced every grounded source/production fact, including the 25 runs / 450 failure entries / 118 unique IDs / 88 now present / 30 still missing observation, and returned GREEN with one disclosure amendment. The amendment is applied with a source-level correction: Alpha Picks `doRefresh()` is not capped by the Market News `18/30/80` detail budgets. The misleading `Repair Comment Gaps` label becomes `Deep Repair Scan`; its disclosure now states 200 article-list load rounds, no separate global Alpha article-detail cap, at most 20 extra reconciliation items, per-article comment limits of 140 scrolls / 120 seconds / five stable-bottom rounds, and the resolved configured recovery batch (default 50). Quick and Full receive the same honest no-global-detail-cap treatment. The three advisories are also binding: legacy `detail_not_saved` cannot seed modern outcome reasons, the historical manifest uses no-age recorded-ID repair and is not truncated by the 168h rediscovery cap, and the plan must audit every consumer of the intentional degraded-to-database-failed mapping. Plan grounding additionally separated pre-POST capture outcome from telemetry persistence: a locally complete capture may be audit-pending and cannot become a durable healthy anchor until persisted, while repair audit creation/finalization remains a hard lifecycle gate. The design status is GREEN; a separately reviewed RED-first implementation plan is the sole next gate.
- **2026-07-25 (SA EXTENSION RELIABILITY + CONTROL CLARITY DESIGN WRITTEN - INDEPENDENT REVIEW NEXT)**: The combined authority at `docs/superpowers/specs/2026-07-25-sa-extension-reliability-control-clarity-design.md` is grounded against `38178f65` and the current read-only databases. Dependency order is fixed: deterministic Firefox packaging closure first; structured phase/item outcomes with aggregate status derived rather than independently written; three recovery layers (routine 24h, no-age recorded-ID retry, and actual-gap incident recovery capped at 168h); a five-button English popup grouped by Alpha Picks and Market News with one accessible description owner, explicit bounds/non-guarantees, contextual Retry, and normally collapsed Advanced recovery; then a separately approved historical repair through the shipped audited path. Market News repair uses four states plus stable reasons; 403/paywall/login/modal/empty-DOM failures stay retryable, while source-unavailable requires explicit 404/410 or reviewed removed evidence. `job_runs` owns immutable hashed/resumable manifests without a schema change, and last healthy means a real derived-complete sync rather than any outer `succeeded` row. The incident set is deliberately not pinned to the earlier 27: a fresh 2026-07-25 reconstruction found 450 failure records, 118 unique IDs, 88 now body-present, and 30 still missing; implementation must freeze and obtain approval for a fresh redacted manifest. Known missing details and metadata rediscovery remain separate result axes, and zero known IDs cannot hide a real discovery interval. Implementation remains blocked on independent spec review and a separately reviewed RED-first plan.
- **2026-07-25 (APP-WIDE I18N LINE CLOSED — SA EXTENSION RELIABILITY SPEC NEXT)**: The external Design Kit companion is synchronized and the repo closeout is done, so the i18n line has no open unit. Design Kit sync #6 (locale) rewrote the readme snapshot to record I18N-0…6 as shipped, added the `p28-locale-control` specimen (four states: each locale active, write-in-flight disabled, and failed-write rollback with the reason in the restored locale), reconciled `type-bilingual` to one-active-locale semantics, and demoted `components/data/Bilingual` to kit-legacy because its `mode="both"` parallel rendering is not a shipped pattern. It also corrected two kit statements that the release had made false — the audience no longer "wants both at once" and the two languages no longer "coexist in one line" — and recorded the layout consequence that English copy runs longer than 繁體中文, so chrome must bound and wrap. Sync #7 closed the one remaining kit gap by documenting P2.8 Slice 5 (`p28-investor-workspace`, `p28-investor-calibration`): one anchor with four ephemeral command modes, Summary as the only landing state, `context_preview` rendered byte-for-byte, and a base-value conflict that writes nothing and leaves the proposal pending. Read-back verified both syncs. `codex/i18n-6-release` was confirmed fully merged (zero unmerged commits, clean worktree) before its worktree and branch were removed; the leftover worktree profile DB was an empty 148 KB scaffold with no `ui_locale`, never production. **Scope ruling: the SA browser extension popup stays English.** It cannot read `profile_settings.ui_locale`, so giving it a switch would either create a second locale authority — contradicting the line's central locked decision — or add a new IPC surface to the component that just failed silently for ~26 hours; the extension is an operator tool, and the boundary is now stated in the decision doc rather than left implicit in byte gates. The division of labor is that the extension reports machine-readable state while the web app owns its localized human-facing rendering, which is also the shape the pending degraded-observability work needs. A separate audit compared the pre-i18n visible-literal snapshot at `ac578581` against today's zh resources: of 1374 CJK-bearing originals, 1121 match exactly, 66 are contained in longer values, 26 belong to the retired AppRecords panel, 63 were recomposed, and 8 fall in the era after byte-preservation became explicit — all 8 trace to named reviewed changes (優先級→優先順序, the `結果：{{destination}}` de-duplication, and the typed-error replacements that stopped rendering raw `e.message`). The remaining 89 are I18N-2-era Settings copy predating that rule; they are a copy-quality note, not a release blocker, and the originals stay recoverable from `ac578581`. Five cross-locale integrity axes — interpolation-token symmetry, plural completeness, empty values, `${}` leakage, and `undefined`/`NaN` — return zero findings across 1781 keys per locale. **Next is the SA extension reliability and control-clarity spec**: packaging integrity so a missing `article_identity.js` fails the build, degraded observability so `detail_failed > 0` cannot hide behind `succeeded`, function-grouped renaming of the popup controls, and a targeted retry-known-failed-details or one-off audited repair for the 27 out-of-window articles rather than a general Backfill button that a 24-hour-bounded catchup cannot honor. Calibration's unhandled Anthropic refusal seam — currently misclassified as a generic validation/responder failure — follows that batch; Settings sanitizer alignment and Coverage v2 remain separately queued.
- **2026-07-25 (I18N-6 RELEASE LIVE COMPLETE — DESIGN KIT LOCALE SYNC NEXT)**: Repaired independent implementation review returned GREEN after independently reproducing both guard mutations, the dual-use credential-status clipping probe, canonical raw `+12/-4`, focused/full hashes, scanner, no-PG, byte gates, and production read-only evidence. With explicit approval, `master` fast-forwarded through evidence tip `2997c926` (product tip `7b9c13b2`). Fresh merged master passed focused `13/148`, full `95/1056`, typecheck/build, resources Settings `681` / total `1781`, scanner twice `36/20/0/20`, backend collect `4621`, no-PG `ok:true / pg_attempts:[]`, protected byte gates, the exact three-line `styles.css` exception, and `git diff --check`. Semantic production smoke found exactly one Settings selector at `zh-Hant` with fixed `zh-Hant` / `en` options and did not change it; the DB remains at 16 profile-setting rows with `ui_locale` absent. The first production locale write remains the user's action. Product release is LIVE; external Design Kit locale-control and bilingual-state synchronization is the sole next i18n action.
- **2026-07-25 (I18N-6 RELEASE REPAIR REVIEW-READY — INDEPENDENT RE-REVIEW NEXT)**: Product tip `7b9c13b2` closes both independent findings without widening scope. The existing foundation node again owns repo-wide direct-`changeLanguage()` and kebab-selector dimensions; separate temporary mutations each produced exact `1 failed / 10 passed`, then clean removal restored `11/11`. The credential repair is the precise `.credential-status-pill.missing` override: a fresh-profile mounted Settings gate measured unavailable copy at zh-Hant `85/85` and en `139/139` client/scroll width with zero clipping at `390/760/1024/1440`, while the available masked credential retained computed `max-width: 96px`. Final virgin `cf7050ed -> 7b9c13b2` is `94/1048 -> 95/1056`, raw `+12/-4`, focused `13/148`, backend `4621 -> 4621`; typecheck/build, resources `681/1781`, scanner `36/20/0/20`, no-PG, exact CSS/byte/formatter/source/privacy gates, and production read-only proof are green. Production still has no `ui_locale`; merged master and Vite `8430` were untouched. Independent re-review is the sole NEXT gate; merge, LIVE, first production write, and Design Kit sync remain blocked.
- **2026-07-25 (I18N-6 RELEASE RE-REVIEW FINDINGS — BOUNDED REPAIR NEXT)**: Independent re-review reproduced all twelve original reviewer-focus gates, canonical A/B, authority lifecycle, responsive checks, and production read-only evidence, but withheld merge for two verified findings. The renamed foundation node had dropped its predecessor's repo-wide direct-`changeLanguage()` and kebab-selector dimensions; it will be strengthened in place with zero node delta and mutation proof. English `Currently unavailable` is clipped by the generic `96px` masked-key cap now that EN is publicly reachable; one RED-first Shell CSS node and a `.credential-status-pill.missing`-scoped override are authorized. A mounted repair probe proved the qualifier is load-bearing because the same span renders an available credential's masked token, which must retain the generic cap. Revised target is raw `+12/-4`, frontend `95/1056`, focused `13/148`, Shell CSS `11/11`; resources, scanner `36/20/0/20`, backend, controller/provider/bootstrap, formatters, and all other CSS remain unchanged. An element-level unavailable-copy clipping census at `390/760/1024/1440` plus masked-token cap proof is required. Merge, LIVE status, production first write, and Design Kit sync remain blocked on repaired independent GREEN.
- **2026-07-25 (I18N-6 RELEASE IMPLEMENTATION REVIEW-READY — INDEPENDENT REVIEW NEXT)**: Product tip `b5228e63` completes the single Settings PageHeader locale selector without changing the existing controller/provider/bootstrap/cache owners. Virgin `cf7050ed -> b5228e63` evidence is exact: frontend `94/1048 -> 95/1055`, raw `+11/-4`, focused `13/147`, backend collect `4621 -> 4621`, resources Settings `681` / total `1781`, scanner `36/20/0/20` with unchanged artifacts, typecheck/build, no-PG, absolute byte gates, and formatter freeze all pass. The isolated authority lifecycle covers both durable write/reload directions, both rollback-language directions, recovery, cache/DB correction, GET fail-open, StrictMode dedupe, and stale-GET ownership. The bilingual release matrix passes `384/384` plus targeted 760px fixtures `24/24`; deep-state probes preserve content, drafts, focus, work, Drawers, and formatter output. Production was queried read-only and still has no `ui_locale`; no production selector interaction occurred. The branch remains unmerged. Independent implementation review is the sole NEXT gate; merge, LIVE status, production first write, and Design Kit locale sync remain blocked.
- **2026-07-25 (I18N-6 RELEASE PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent full-plan review reproduced both normalized node-list hashes, frontend `94/1048`, focused `12/140`, backend collect `4621`, every existing evolution anchor, resource/scanner arithmetic, fixed-translator typing, real-controller test wrapper shape, and zero-CSS premise. Two zero-accounting advisories are incorporated: the durable selector whitelist now scans every production `src` file and excludes only the Settings mount plus selector owner, while `LocaleSelector` receives the initialized i18n instance from `useTranslation("settings")` rather than importing the singleton. Raw target `+11/-4`, final `95/1055`, focused `13/147`, resources `681/1781`, scanner `36/20/0/20`, production read-only closeout, and formatter freeze are unchanged. Isolated RED-first implementation is cleared; merge and production locale writes remain blocked on independent implementation review and explicit user approval.
- **2026-07-25 (I18N-6 RELEASE IMPLEMENTATION PLAN WRITTEN — INDEPENDENT PLAN REVIEW NEXT)**: The RED-first plan at `docs/superpowers/plans/2026-07-25-i18n-6-release.md` is grounded on behavior base `cf7050ed` with frontend `94/1048`, focused `12/140`, backend collect `4621`, resources Settings `679` / total `1779` per locale, and scanner `36/20/0/20` with global `src/**` scope and zero debt. Its exact target is raw frontend `+11/-4` to `95/1055`, focused `13/147`, resources Settings `681` / total `1781`, and unchanged scanner artifacts. The four removals are explicit selector/action-absence ID evolutions; a fifth known resource-inventory node evolves in place with zero node delta. One Settings-local native selector uses fixed-locale resource autonyms and the existing controller only; all controller/provider/bootstrap/index files, formatters, backend, API, CSS by default, dependencies, desktop, and extensions remain byte-identical. Runtime evidence is split into an isolated full authority lifecycle with failure/reload/race coverage and a bilingual all-surface matrix using canonical six widths plus targeted `759/760/761` fixtures that mount the real Model Routing credential ghost action and record `.page-head*` as dead CSS. Production closeout is read-only and cannot create the first `ui_locale` value; that intentional first write belongs to the user. Product implementation and the selector remain blocked pending independent full-plan GREEN.
- **2026-07-25 (I18N-6 RELEASE DESIGN REVIEW GREEN — IMPLEMENTATION PLAN NEXT)**: Independent full-document review returned GREEN with zero required product changes. Two zero-accounting advisories are adopted in the docs-only clearance: the targeted `759/760/761` Model Routing fixture must mount both `.settings-grid` and `.model-credential-summary .btn-ghost`, while `.page-head` / `.page-head-actions` are recorded as dead CSS with no reachable consumer; and the existing resource-inventory node is named as a fifth guaranteed in-place evolution from Settings/total `679/1779` to `681/1781` per locale. The four selector/action absence nodes remain the only known ID-sensitive guard evolutions. The reviewed RED-first implementation plan is the sole next gate; product implementation and the public selector remain unauthorized.
- **2026-07-25 (I18N-6 RELEASE DESIGN WRITTEN — INDEPENDENT REVIEW NEXT)**: The final release design is written at `docs/superpowers/specs/2026-07-25-i18n-6-release-design.md` against clean merged base `cf7050ed`. It locks one Settings-local native selector in the PageHeader action slot, fixed-locale resource autonyms, controller-only writes, rollback-language error copy, isolated first-write/reload evidence, production read-only closeout, and no third locale. Formatter policy is now declaratively closed: `ui_locale` changes interface copy only, while every owner in `I18N_FORMATTER_INVENTORY.md` retains its current browser/fixed/source formatting until a separately reviewed owner-specific unit. Exact baselines are frontend `94/1048`, resources Settings `679` / total `1779` per locale targeting `681/1781`, and scanner `36/20/0/20` with global `src/**` scope and zero debt. The release matrix uses both locales across the canonical six widths plus targeted `759/760/761` gates for actual 760px media-query owners. Four existing selector/action-absence nodes are explicitly evolved; one glob autonym guard and registry structure guards remain unchanged. Independent full-document review is the only next gate; implementation and the public selector remain unauthorized.
- **2026-07-24 (COMBINED I18N-4/5 LIVE COMPLETE — I18N-6 RELEASE NEXT)**: Independent implementation review returned GREEN with zero findings after the required virgin two-stage comparison from base <code>93cda668</code> through <code>TRANCHE_A_TIP=34ddf08f</code> to product <code>20666d33</code>. With explicit user approval, branch/evidence tip <code>5f35e8b1</code> fast-forwarded to <code>master</code>. Fresh merged-tree verification passed B focused <code>15/232</code>, Shell CSS <code>10/10</code>, full frontend <code>94/1048</code>, typecheck/build, resource contracts, scanner <code>36/20/0/20</code> twice with global <code>src/**</code> scope and zero debt, no-PG <code>ok:true / pg_attempts:[]</code>, protected-byte gates, exact five-symbol <code>api.ts</code> retirement, AppRecords zero references, and diff checks. The final 24-file A set collects 310 nodes because B adds five reviewed shared scanner/foundation nodes; all 305 frozen A-owned nodes remain present and green. The normal desktop restarted from merged master and passed zh-Hant Research, Holdings, Portfolio Capture, and System / Health smoke without creating <code>profile_settings.ui_locale</code>. A coordinate-driven closeout click accidentally invoked the existing review-mode apply action for valid run <code>307</code>; read-only impact analysis proved the ten-position set and every non-market field stable across all 49 observations from runs <code>258</code> through <code>307</code>, with no add/remove/close/manual-adjustment effect and only mark-to-market values plus sync timestamps refreshed. No unverifiable rollback was attempted; production mutation controls are now prohibited from coordinate-driven smoke. The standalone I18N-5 and AppRecords frontend backlog are closed. I18N-6 Release is the sole next i18n unit; the public selector remains absent.
- **2026-07-24 (COMBINED I18N-4/5 TASK 8 APPRECORDS FRONTEND RETIRED — TASK 9 NEXT)**: The pre-delete stop gate proved `AppRecordsSection` had no production consumer and its three preview/result interfaces plus two preview/apply wrappers were consumed only by that unreachable component. One dedicated commit removes exactly that frontend component and those five `api.ts` exports. Backend routes, local storage, archive data, migration code, and offline tools remain intact and byte-identical. This closes the standing "App Records panel full removal deferred" frontend backlog; resources and scanner manifests intentionally remain unchanged until Task 9 closes scanner debt.
- **2026-07-24 (COMBINED I18N-4/5 PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent full-plan review returned GREEN. Its sole advisory is incorporated with zero accounting change: the existing AppShell bilingual System node keeps its ID but now mounts Dashboard in both locales and asserts all 20 `system` leaves, so the roughly fourteen non-sidecar System/Health labels are not covered only by runtime screenshots. The reviewed contracts remain A `+44/-2` at exact scanner `483/448/429/20` and focused `305`, then B `+60/-0` to final `36/20/0/20`; independent implementation review must compare both segments and rerun the frozen A focused suite at final. Isolated RED-first implementation is cleared. Merge and the public selector remain blocked on independent implementation review GREEN and explicit user approval.
- **2026-07-24 (COMBINED I18N-4/5 IMPLEMENTATION PLAN WRITTEN — INDEPENDENT PLAN REVIEW NEXT)**: Independent full-document review returned GREEN with zero required changes. Both plan-level advisories are adopted: Tranche A's bilingual runtime matrix must mount long English chrome, an error banner, active progress, and long source content in the same viewport; sidecar structured-outcome copy is owned by the `system` namespace even though `App.tsx` stores the state and `Dashboard.tsx` renders it. The single RED-first plan at `docs/superpowers/plans/2026-07-24-i18n-4-5-remaining-surfaces.md` locks product base `93cda668`, frontend `90/944`, backend collect `4621`, scanner `703/656/637/20`, exact A checkpoint `483/448/429/20` with 48 scopes, and final `36/20/0/20` with global `src/**` scope. Its raw frontend ledger is base-to-A `+44/-2`, A-to-final `+60/-0`, total `+104/-2` to `94/1046`; both removals are stale exact-count test names replaced one-for-one, so net behavior coverage remains `+102`. Resources close at Common `61`, Shell `37`, Settings `679`, Research `207`, Explore `401`, Portfolio `373`, and System `20` per locale. `TRANCHE_A_TIP`, AppRecords' own retirement commit, the five-symbol-only `api.ts` exception, scanner hardening, formatter inventory-only boundary, protected-byte gates, and two separate canonical comparisons are mechanical contracts. Product edits remain blocked pending independent plan review GREEN; the public selector stays reserved for I18N-6.
- **2026-07-24 (COMBINED I18N-4/5 REMAINING-SURFACES DESIGN WRITTEN — INDEPENDENT REVIEW NEXT)**: User-approved sequencing collapses the final Research and Portfolio/System localization work into one bounded unit without collapsing its evidence. The design at `docs/superpowers/specs/2026-07-24-i18n-4-5-remaining-surfaces-design.md` reconciles the current manifest ceiling exactly: Tranche A Research/shared-model `230`, Tranche B Portfolio/System/common owned debt `374`, unreachable AppRecords frontend retirement `30`, and `api.ts` scanner adjudication `3`, totaling `637`; current source is `636` because one API row is stale. A separate direct audit found 22 English table labels missed by the current scanner, so the unit first adds `header`/tuple-backed-column RED coverage and migrates those values instead of falsely treating `637` as a complete copy inventory. Tranche A freezes a full-hash `TRANCHE_A_TIP` with scanner/resource/test snapshots; independent implementation review must compare `93cda668 -> A_TIP -> final`, and Tranche B cannot mask or modify frozen A owners. AppRecords retirement is a dedicated commit that removes only the unreachable frontend component and five dead client exports while preserving backend/offline capabilities. Shared model copy explicitly reopens `modelRoutingUx.ts`/`modelPicker.ts` while preserving Decision 37 metadata and no decorated-label parsing. Formatter work is inventory-only. Final scanner authority is `36/20/0/20` with migrated scope `src/**`, no new allowlist entry, and no public selector. The former standalone I18N-5 cycle is absorbed; independent full-document review of this design is the sole NEXT gate, after which one separately reviewed RED-first plan may open.
- **2026-07-24 (I18N-3 EXPLORE LIVE COMPLETE — I18N-4 RESEARCH NEXT)**: Independent implementation review returned GREEN with zero findings for product `be89a9b5` / evidence tip `eb4dee7b`, and the user explicitly approved integration. `master` fast-forwarded from `061e8a76` through `eb4dee7b`. Merged-tree focused `17/173`, full frontend `90/944`, typecheck, production build, scanner `703/656/637/20` twice with 39 scopes, no-PG `ok:true` / `pg_attempts:[]`, protected byte gates, and `git diff --check` are green. The normal desktop restarted from merged master; Home, News, MXL Ticker Detail, and AI Card rendered cleanly in zh-Hant while source/generated content remained original. Production `profile_settings.ui_locale` stayed absent before and after the smoke, so the public selector remains reserved for I18N-6. The one reviewed runtime deviation is bounded to the existing `max-width:760px` `styles.css` hunk plus one Shell CSS node, with `759/760/761` and 390px geometry verified. I18N-4 Research is now the sole NEXT unit; deferred SA extension repair, `/sa/feed` empty-versus-bad, Settings sanitizer alignment, App sidecar raw-error cleanup, formatter policy, and calibration advisories remain separate backlog items.
- **2026-07-23 (I18N-3 EXPLORE IMPLEMENTATION PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent full-plan review reproduced frontend/focused/backend/resource/scanner baselines, the exact `+83/-3` node ledger, all 39 operation IDs, and the reviewed `703/656/637/20` scanner target. Two docs-only advisories are closed without accounting changes: the Universe warning explicitly aligns `theme 標籤 -> 主題標籤`, and the News/Ticker copy ledger now records complete rendered before/after sentences rather than fragments. The clearance commit authorizes isolated RED-first implementation from product base `c0448213`; merge, the public selector, backend/API/CSS/package changes, and all deferred backlog work remain unauthorized pending independent implementation review and explicit approval.
- **2026-07-23 (I18N-3 EXPLORE IMPLEMENTATION PLAN WRITTEN — INDEPENDENT PLAN REVIEW NEXT)**: The RED-first plan at <code>docs/superpowers/plans/2026-07-23-i18n-3-explore.md</code> grounds product base <code>c0448213</code> at frontend <code>83/863</code>, focused <code>9/85</code>, backend collect <code>4621</code>, resources Common/Settings/Research <code>18/713/5</code>, and scanner <code>1033/973/954/20</code> with 30 scopes. Its exact target is frontend raw <code>+83/-3</code> (net <code>+80</code>) to <code>90/943</code>, focused <code>16/165</code>, Common/Settings/Research/Explore leaves <code>32/702/5/352</code>, scanner <code>703/656/637/20</code>, and 39 scopes. The three removals are explicit one-for-one test-name corrections for two retired resource counts and the former zh-only stance presenter; no semantic test is lost. The plan enumerates all 39 operation-specific error IDs, the fail-closed diagnostic/path boundary, every reviewed zh-Hant correction, seven new mounted test files, the I18N-2 Decision 35 node's in-place single-owner evolution, zero-refetch locale switching, fake-backed explicit Card translation, and the six-surface bilingual responsive matrix. Backend/API/CSS/packages/prompts/extension/desktop bytes and the public selector remain protected. Product implementation is blocked pending independent plan review GREEN.
- **2026-07-23 (I18N-3 EXPLORE DESIGN REVIEW GREEN — IMPLEMENTATION PLAN NEXT)**: Independent full-document review verified the 889-line design against the actual eight-file debt surface, mounted-test inventory, structured `ApiError`, recovery anchors, terminology values, scanner accounting, and all six locked decisions. Its one required docs-only pin is now incorporated: existing rendered zh-Hant chrome moves byte-for-byte by default, every wording change must be named in the implementation-plan copy ledger, terminology wins on conflict, and Watchlist explicitly records `設定優先級 -> 設定優先順序`. The plan must also give the I18N-2 Decision 35 stance/mismatch byte-contract node an explicit evolution-or-retirement entry. I18N-3 implementation planning is the sole NEXT gate; product implementation and the public selector remain unauthorized.
- **2026-07-23 (I18N-3 EXPLORE DESIGN WRITTEN — INDEPENDENT REVIEW NEXT)**: The bounded authority at <code>docs/superpowers/specs/2026-07-23-i18n-3-explore-design.md</code> records six locked decisions: localize application chrome while preserving source/user/generated content; replace raw Explore errors with operation-specific copy plus fail-closed Developer diagnostics; render stable semantic IDs at read time through one shared personalization owner; make locale switching a pure display change with state/focus/in-flight work intact; use reactive per-surface hooks and namespace-typed pure presenters; and preserve Explore behavior/IA unless measured overflow earns a reviewed CSS deviation. Grounding is frontend <code>83 files / 863 tests</code>, backend collect <code>4621</code>, scanner <code>1033/973/954/20</code> with 30 scopes, and exactly <code>317</code> debt signatures / <code>330</code> occurrences across the eight fully owned files. The reviewed architecture adds those eight files plus <code>src/explore/**</code> for exactly 39 migrated scopes; <code>Research.tsx</code> and <code>ResearchEvidenceDrawer.tsx</code> receive only bounded reactive personalization wiring and remain I18N-4 debt. The selector stays absent, backend/CSS are protected by default, and no implementation plan may open before independent full-document GREEN. Explicit deferred work remains visible: the SA extension packaging/Backfill/degraded-observability/27-article batch, <code>/sa/feed</code> empty-versus-bad semantics, Settings diagnostic sanitizer alignment, <code>App.tsx</code> sidecar raw-error cleanup in I18N-5, formatter policy, calibration Anthropic refusal normalization, and its hard-coded fallback-model review.
- **2026-07-22 (P2.8 SLICE 5 LIVE COMPLETE — I18N-3 EXPLORE NEXT)**: Independent implementation review returned GREEN for product `b214e1fc` / evidence tip `de8485be`, and the user explicitly approved integration and production migration. `master` fast-forwarded from `bba7bf60` through `de8485be`. A retained `0600` online backup (`profile_state-v1-pre-investor-profile-v2-20260722T143200Z.db`, SHA-256 `09afc65ad34f78b35c8e329300eccad6962f3edd792e72abdf9d9f18667a446a`) preceded merged-code-only calibration v1-to-v2 migration. Production now reports marker `(1,2)`, `integrity_check=ok`, zero FK violations, preserved `12` messages / `1` approved proposal and exact ordered digests, the unsupported legacy active session honestly superseded, and zero inferred coverage/base state. Fresh merged-master gates passed backend focused `219` / collect `4621`, frontend focused `11/144` / full `83/863`, typecheck/build, scanner `1033/973/954/20` with 30 scopes, no-PG `ok:true` / `pg_attempts:[]`, and immutable boundaries. The normal desktop restarted from merged master and rendered the zh-Hant Investor Profile Summary cleanly with both profile endpoints returning 200; a post-startup read-only probe reconfirmed migration invariants. The backup remains retained pending user confirmation. Two non-blocking backlog observations remain: normalize Anthropic calibration `stop_reason: "refusal"` into the established typed refusal family, and revisit the hard-coded `claude-sonnet-4-6` fallback when calibration gains a registry. I18N-3 Explore is the sole NEXT unit; I18N-6 remains the selector-release owner.
- **2026-07-22 (P2.8 SLICE 5 IMPLEMENTATION REVIEW-READY — INDEPENDENT REVIEW NEXT)**: Product tip `b214e1fc` completes the bilingual summary-first Investor Profile workspace, durable adaptive calibration, covered-field-only proposal approval, and exact current/per-run personalization transparency. Canonical virgin A/B is exact: backend collect `4569 -> 4621` with `+53/-1`, identical existing `49 failed / 74 skipped / 7 errors / 18 warnings`, and head `4491 passed`; frontend `77/778 -> 83/863` with sequential `+88/-3`. Focused backend is `219`; typecheck/build and scanner `1033/973/954/20` with 30 scopes are green. A `0600` copied-production migration preserved ordered message/terminal digests, produced marker 2 with integrity/FK clean, inferred zero legacy coverage, and was idempotent; transaction probes proved no SQLite write lock across Provider await and atomic profile/proposal rollback. No-PG returned `ok:true` / `pg_attempts:[]`; a 67-check `zh-Hant`/`en` six-viewport matrix, one bounded isolated Provider turn, and fake-backed Research/Card null/empty/exact snapshot proof passed. All temporary DBs, profiles, screenshots, ports, and processes were cleaned. The branch is unmerged, production schema is not migrated, normal desktop is untouched, and Slice 5 remains blocked on independent implementation GREEN plus explicit user approval.
- **2026-07-22 (P2.8 SLICE 5 IMPLEMENTATION PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent review approved the RED-first plan after two required docs-only authority alignments. The versioned opening prompt now uses the spec-owned single-question wording byte-for-byte in backend canonical English and the exact zh-Hant resource; the rejected longer plan draft is not an implementation option. Per-run null snapshot semantics now cover both pre-Slice-5 history and new queued runs that never reach prompt assembly, while executed-disabled runs remain the distinct empty-string state. No test/resource/scanner ledger changed. Slice 5 remains the sole NEXT unit and may proceed from the docs-only clearance commit; implementation must stop at independent implementation review-ready before merge or production migration.
- **2026-07-22 (P2.8 SLICE 5 IMPLEMENTATION PLAN WRITTEN — INDEPENDENT PLAN REVIEW NEXT)**: The RED-first implementation plan at `docs/superpowers/plans/2026-07-22-p2-8-slice-5-investor-profile-workspace.md` grounds the approved design against backend `4569` / focused `167`, frontend `77 files / 778 tests`, Settings resources `623`, and scanner `1033/973/954/20` with 28 scopes. Its exact ledger is backend `+53/-1` to `4621`, frontend `+47/-1` to `81 files / 824 tests`, Settings `712` leaves, new Research namespace `5` leaves, and scanner counts unchanged with 30 scopes. The plan makes calibration schema v2 an explicit startup phase, keeps Provider waits outside SQLite locks, clamps partial proposals through the backend topic policy, samples stance trace and exact context together at prompt assembly, and assigns Evidence Drawer chrome to the `research` namespace. Slice 5 remains the sole NEXT unit; product implementation is blocked until independent plan review returns GREEN.
- **2026-07-22 (P2.8 SLICE 5 DESIGN REVIEW GREEN — IMPLEMENTATION PLAN NEXT)**: Independent full-document review approved the 696-line Investor Profile workspace design with one required clarification and two plan advisories. The required pin is now part of the authority: catalog order controls display only, interview order is adaptive within the still-uncovered set, and a model-selected next topic becomes server current only after catalog membership and uncovered-state validation; failure leaves coverage/current unchanged and the turn retryable. The implementation plan must align stance trace and exact context snapshot sampling at prompt assembly, and must place new Evidence Drawer chrome in a `research` resource namespace rather than `settings.*`. Slice 5 remains the sole NEXT unit; product implementation is blocked until the RED-first plan receives independent review GREEN.
- **2026-07-22 (P2.8 SLICE 5 INVESTOR PROFILE WORKSPACE DESIGN WRITTEN — INDEPENDENT REVIEW NEXT)**: The bounded design at `docs/superpowers/specs/2026-07-22-p2-8-slice-5-investor-profile-workspace-design.md` records the approved summary-first single-anchor IA, command-driven Edit/Calibration/Proposal modes, deterministic versioned opening question, eight-topic backend field-policy catalog, partial-proposal clamp and optimistic conflict contract, idempotent turn state machine, and exact current/per-run personalization transparency. It explicitly preserves evidence-gathering purity, keeps `freeform_notes` non-causal, distinguishes legacy-null from new-disabled empty context snapshots, supersedes unsupported legacy active/draft calibration state without inferring coverage, and ships all new chrome bilingual from birth. The terminology authority and canonical shell pointer are synchronized in the same docs change. Slice 5 remains the sole NEXT unit, but implementation planning and product edits are blocked until independent full-document review returns GREEN.
- **2026-07-21 (I18N-2 SETTINGS LIVE COMPLETE — P2.8 SLICE 5 NEXT)**: Independent implementation review returned GREEN with zero findings for product `9f78a9ff` / evidence tip `d2195cd0`, and the user explicitly approved integration. `master` fast-forwarded from `45c3f2d8` through `d2195cd0`. Merged-tree closeout reproduced focused `35/376`, full `77/778`, scanner `1033/973/954/20` with 28 scopes, clean typecheck/build, no-PG `ok:true` / `pg_attempts:[]`, and all four protected-byte gates. The normal desktop restarted from merged `master`, rendered Settings in zh-Hant with Sidecar connected, and read-only checks proved `profile_settings.ui_locale` stayed absent before and after restart. The selector remains absent. P2.8 Slice 5 is the sole next unit and ships bilingual from birth. I18N-6 records one release guard from the runtime review: its selector must use `useUiLocale().setLocale()` / the locale-controller path, never direct `i18next.changeLanguage()`, so `<html lang>`, cache, and DB authority stay synchronized.
- **2026-07-21 (I18N-2 SETTINGS IMPLEMENTATION REVIEW-READY — INDEPENDENT REVIEW NEXT)**: Product tip `9f78a9ff` completes the bounded Settings migration without exposing a locale selector. Exact gates are frontend `77 files / 778 passed`, focused `35/376`, node comm `+83/-0`, scanner `1033/973/954/20` with 28 migrated scopes, clean typecheck/build, byte-identical backend/shared presenters/legacy/CSS/desktop/extensions/packages, and no-PG `ok:true` / `pg_attempts:[]`. Decision 65 reconciles the one-signature scanner delta to the reviewed typed-`ApiError` presenter removal; Decisions 66-67 preserve diagnostic ownership, baseline zh-Hant copy, and credential-scoped discovery tests. An isolated scheduler-off matrix passed `zh-Hant` and `en` at 1440/1024/961/960/959/390 with all three groups/nine anchors, one tabpanel, responsive rail/Drawer polarity, cross-language search/focus, zero locale-switch refetch, and Provider/model/Investor/OAuth/ConfirmDialog state and privacy intact. The isolated profile, DBs, browser, screenshots, and harnesses were removed. Merge, LIVE status, selector work, Slice 5, and I18N-3 remain blocked on independent implementation GREEN plus explicit user approval.
- **2026-07-21 (I18N-2 TASK 8 DECISIONS 62-63 PAPER GREEN — REPAIR RED NEXT)**: Round-two narrow review verified the object-identity plus monotonic calibration-generation contract, exact seven Investor RED node names plus two resource nodes, current Task 6-8 ledger, eight-leaf sufficiency, `investor 61 / settings +622 / 623 total`, unchanged canonical `+83/-0`, and unchanged scanner `1034/974/954/20`. Its only finding was a stale status date; after the one-line sync to `2026-07-21`, readback and `git diff --check` returned PAPER GREEN. Task 8 may now write the exact `4 files / 50 tests`, `9 failed / 41 passed` repair RED; product repair and merge remain blocked.
- **2026-07-21 (I18N-2 TASK 8 FIRST GREEN REJECTED — DECISIONS 62-63 NARROW REVIEW NEXT)**: Exact initial RED `2 files / 37 tests`, `5 failed / 32 passed` reached a first focused `3/41` GREEN with the four reviewed files only, but independent spec review found real behavior defects hidden by the tests: delayed initial calibration could overwrite a user edit, completed calibration alerts borrowed action labels, the pending proposal badge duplicated its heading, and existing Investor privacy/guidance/unset/running/save semantics were lost. Decisions 62-63 authorize an object-identity guard for edits, a monotonic calibration request generation that makes explicit actions defeat stale initial reads, and exactly eight missing Investor copy leaves. Final resources become `investor 61`, `settings +622 / 623 total`; canonical frontend remains `+83/-0`, scanner remains `1034/974/954/20`, and backend/CSS/shared presenters remain byte-identical. The repair must first reproduce exact `4 files / 50 tests`, `9 failed / 41 passed` at the two resource nodes and seven explicitly named Investor nodes; product repair is blocked until this paper amendment receives independent narrow GREEN.
- **2026-07-21 (I18N-2 TASK 7 COMPLETE — TASK 8 NEXT)**: Cast-free Decision 61 repair passed exact focused `4 files / 31 tests`, resources `615/dataStorage 48/macroStorage 31`, typecheck, three-owner direct scanner `[]`, `git diff --check`, and the reviewed global `GPT-5.6`-only phase failure. Final product spec review returned GREEN and code-quality review returned APPROVED with no remaining finding. Product commit `7d33cfd3` closes Storage/Macro localization while preserving requests, state, formatters, DOM, CSS, backend, desktop, and extensions. Task 8 current Investor Profile localization is the single NEXT implementation unit; Task 9 still owns the atomic scanner manifest update.
- **2026-07-21 (I18N-2 TASK 7 TYPE-SAFE SUCCESS INTERPOLATION AMENDMENT GREEN — CAST-FREE REPAIR CLEARED)**: Independent narrow review verified that pinned i18next `26.3.6` reserves `count` as `number` while the replacement placeholders accept strings, reproduced exact `4/31`, `1 failed / 30 passed`, and confirmed typecheck plus `615/48/31`, `+83/-0`, and scanner phase accounting. Product repair may evolve the two existing locale values to `pricesValue/newsValue/ivValue/fundamentalsValue` and remove the cast; no resource path/count/copy or other behavior boundary changes. Final quality re-review still gates the Task 7 product commit.
- **2026-07-21 (I18N-2 TASK 7 TYPE-SAFE SUCCESS INTERPOLATION STOP — NARROW REVIEW PENDING)**: The first Decision 60 product repair correctly routed the Market Data success summary through its single resource owner and passed focused `31/31`, but used `count: formattedValue as unknown as number` because i18next reserves `count` as numeric. That is the same false typed-contract pattern previously rejected for FRED interpolation. Decision 61 evolves only the two existing `dataStorage.update.succeeded` values from `count/value/sourceId/providerId` to string placeholders `pricesValue/newsValue/ivValue/fundamentalsValue`, passes the same four formatted strings without a cast, and strengthens the same Data node with an unsafe-cast negative assertion. Exact RED remains `4/31`, `1 failed / 30 passed`; typecheck otherwise passes. Resource paths/counts/copy remain `615/48/31`, canonical frontend remains `+83/-0`, and scanner, request, state, formatter, DOM, CSS, backend, desktop, and extension contracts remain unchanged. Cast-free product repair is blocked until narrow review GREEN.
- **2026-07-21 (I18N-2 TASK 7 SUCCESS SUMMARY OWNER / LOCALE-SWITCH AMENDMENT GREEN — PRODUCT REPAIR CLEARED)**: Independent narrow review reproduced exact `4/31`, `1 failed / 30 passed`, confirmed the sole failure is the Data success-owner pin, and verified unchanged node identity plus `615/48/31` resources. The mounted Data/News locale-switch assertions already pass and prove stable anchor identity, source/date values, and request counts. Product repair is limited to routing the four unchanged formatted values through existing `dataStorage.update.succeeded` and deleting the manual array join. No key, node, scanner, request, state, formatter, DOM, CSS, backend, desktop, or extension change is authorized; final quality re-review still gates the Task 7 product commit.
- **2026-07-21 (I18N-2 TASK 7 SUCCESS SUMMARY OWNER / LOCALE-SWITCH PROOF STOP — NARROW REVIEW PENDING)**: Final code-quality review found that `DataStorageSection.syncLine()` manually composes the four-leg successful update summary despite the existing complete owner `dataStorage.update.succeeded`, and that Data/News English nodes switched locale only before mount rather than proving an in-place switch cannot refetch or alter source values. Decision 60 routes unchanged formatted values through the existing resource and strengthens those two existing nodes with mounted identity/source/date/request-count assertions plus a source-level owner pin. Exact RED is `4/31`, `1 failed / 30 passed` solely at the Data owner assertion; the locale-switch behavior is already GREEN. Resources remain `615/48/31`, canonical frontend remains `+83/-0`, and scanner phase/final `1034/974/954/20`, requests, state, formatters, DOM, CSS, backend, desktop, and extensions remain unchanged. Product repair is blocked until independent narrow review GREEN.
- **2026-07-21 (I18N-2 TASK 7 SCANNER PHASE-ORDER AMENDMENT GREEN — FINAL QUALITY RE-REVIEW NEXT)**: Independent narrow review returned PAPER GREEN with no finding. It confirmed that Task 7 must leave `settingsCopy.ts` and scanner manifests to Task 9, that the direct Macro scan `[]` plus the exact known global `GPT-5.6` failure is a sufficient and executable phase-local proof, and that header/map/accounting remain self-consistent. No product/resource/test/scanner-terminal contract changed. Task 7 now proceeds to final code-quality re-review; product commit remains blocked until that gate approves.
- **2026-07-21 (I18N-2 TASK 7 SCANNER PHASE-ORDER STOP — NARROW REVIEW PENDING)**: Decision 58's product repair is behaviorally GREEN at focused `4/31`, resources `615/dataStorage 48/macroStorage 31`, typecheck, and direct `MacroStorageSection.tsx` scanner output `[]`. The global scanner is fail-fast and now exposes the already reviewed `["src/settings/settingsCopy.ts","presenter_return","GPT-5.6"]` stable identifier. Decision 45 already assigns that candidate to the final allowlist, while Task 9 owns the atomic debt/allowlist/migrated-scope update and first full GREEN at `1034/974/954/20`. Task 7 therefore must not edit `settingsCopy.ts` or scanner manifests to force an early global GREEN. Its exact scanner proof is direct Macro `[]` plus a global failure containing only the known `GPT-5.6` candidate and no Macro candidate. No product/resource/test/accounting contract changes; Task 7 product commit is blocked until this phase boundary receives independent narrow review GREEN.
- **2026-07-21 (I18N-2 TASK 7 MACRO SUMMARY SINGLE-OWNER AMENDMENT GREEN — PRODUCT REPAIR CLEARED)**: Decision 58's single-owner design and exact RED received independent PAPER GREEN after `c8de865b` corrected the sole wording issue by naming `616` as the Decision 56 checkpoint rather than the final contract. Final resources are `settings +614 / 615 total`, `dataStorage 48`, `macroStorage 31`; cumulative Task 7 path delta is `+10/-2`. RED remains exact focused `4/31`, `2 failed / 29 passed` plus one exact Macro scanner debt. Product repair is limited to removing `macroStorage.snapshot.lastFetch` in both locales and rendering the existing snapshot through `counts.summary`; canonical `+83/-0` and all non-copy behavior boundaries remain fixed.
- **2026-07-21 (I18N-2 TASK 7 MACRO SUMMARY SINGLE-OWNER STOP — NARROW REVIEW PENDING)**: Final code-quality re-review found one remaining i18n-literal ratchet violation after focused `31/31`: `MacroStorageSection` composes a visible ` · ` separator around `snapshot.lastFetch`, while `macroStorage.counts.summary` already owns the complete count-plus-last-fetch sentence. The suffix resource has no independent consumer and is a redundant second owner. Decision 58 removes `macroStorage.snapshot.lastFetch` in both locales and reuses `counts.summary`. This exposes a real owner drift: the duplicate suffix used `Last fetched`, while the shared full-sentence owner uses sentence-case `last fetched`; normalizing the snapshot to the shared owner is the sole reviewed visible-copy change. Final resources become `settings +614 / 615 total`, `dataStorage 48`, `macroStorage 31`; cumulative Task 7 resource delta is `+10/-2`. RED is exact focused `4/31`, `2 failed / 29 passed` at the resource-count and strengthened English Macro nodes plus one exact `check:i18n-literals` debt at the Macro JSX expression. Canonical `+83/-0`, requests, state, polling, formatters, DOM, CSS, backend, desktop, extensions, and locale-switch/refetch behavior remain unchanged. Product repair is blocked until independent narrow review returns GREEN.
- **2026-07-21 (I18N-2 TASK 7 COVERAGE AUTHORITY COPY AMENDMENT GREEN — PRODUCT REPAIR CLEARED)**: Decision 57's paper/RED contract received independent GREEN. The reviewer reproduced exact `4 files / 31 tests`, `2 failed / 29 passed`, with failure identity limited to the existing zh-Hant and English Data Storage nodes; resources stay `616/dataStorage 48/macroStorage 32`, no key or node is added, and canonical frontend remains `+83/-0`. Product repair from `02e90318` is limited to evolving the two existing `dataStorage.coverage.description` values so backend `coverage_status` remains the explicit row-classification authority.
- **2026-07-21 (I18N-2 TASK 7 COVERAGE AUTHORITY COPY STOP — NARROW REVIEW PENDING)**: Decision 56's first repaired `31/31` tip restored the reviewed information, DOM, unit, and locale punctuation, but product spec review found one unprotected baseline authority statement missing in both locales: each coverage row is classified by backend `coverage_status`. Decision 57 strengthens the existing zh-Hant and English Data Storage nodes and evolves only `dataStorage.coverage.description`; exact bounded RED is `4/31`, `2 failed / 29 passed`. Resources remain `616/dataStorage 48/macroStorage 32`, canonical frontend remains `+83/-0`, and requests, state, polling, formatters, DOM, CSS, backend, desktop, extensions, and locale-switch/refetch behavior remain unchanged. Product repair is blocked until independent narrow review returns GREEN.
- **2026-07-21 (I18N-2 TASK 7 COPY/DOM QUALITY AMENDMENT GREEN — PRODUCT REPAIR CLEARED)**: Decision 56's strengthened contract is independently GREEN after three narrow-review rounds. The review first found a stale terminal authority table (`613/dataStorage 46`) and an unprotected relative-to-best-covered-ticker explanation; follow-up found one Decision 55 sentence still calling historical `614` final. Commits `4024d98f` and `14a3abb9` close all three without changing node accounting or failure identity. Exact repair RED remains `4 files / 31 tests`, `6 failed / 25 passed`; final resources are `settings +615 / 616 total`, `dataStorage 48`, `macroStorage 32`, with cumulative Task 7 path delta `+10/-1`. Product repair may resume in the existing six-file boundary; canonical `+83/-0`, requests, polling, formatters, CSS, backend, desktop, extensions, and locale-switch/refetch behavior remain fixed.
- **2026-07-21 (I18N-2 TASK 7 COPY/DOM QUALITY STOP — NARROW REVIEW PENDING)**: The cleared Task 7 product reached exact `4 files / 31 passed`, resources `614 / dataStorage 46 / macroStorage 32`, typecheck GREEN, and unchanged Macro request counts, but independent code-quality review found three real Decision 28/32 defects hidden by the behavior suite. Previously unused compact resources deleted zh-Hant Data/News/Macro ownership guidance, six-state coverage/drill-down explanation, and the Universe ticker unit; the implementation also restructured the coverage header DOM and shared ASCII detail punctuation across both locales. Decision 56 restores the baseline DOM and all information, evolves existing description/lookback/read-only/Universe leaves in place, and adds exactly `dataStorage.coverage.drilldown.missingDetail|partialDetail`. Cumulative Task 7 resource delta becomes `+10/-1`, final resources `settings +615 / 616 total`, `dataStorage 48`, `macroStorage 32`. Five behavior nodes plus the resource-count node are strengthened in place; repair RED is exact `4/31`, `6 failed / 25 passed`. Canonical `+83/-0`, scanner, requests, polling, formatters, CSS, backend, desktop, extensions, and locale-switch/refetch behavior remain unchanged. Product repair is blocked until independent narrow review returns GREEN.
- **2026-07-21 (I18N-2 TASK 7 TEST-CONTRACT AMENDMENT GREEN — PRODUCT GREEN NEXT)**: The cleared Task 7 RED suite collected the reviewed `4 files / 31 tests` and initially produced the expected seven new behavior failures plus the resource-count failure. Before GREEN, the implementer correctly stopped on three contradictory baseline assertions: two still required the bilingual headings that Task 7 removes, and one still required a raw request exception that the reviewed normal/Developer diagnostic boundary forbids. Those three baseline nodes now evolve in place to exact single-locale headings and authored generic load failure with a raw-detail negative assertion. Honest RED accounting is eleven failures (`7` new + resource count + `3` evolved baseline) while collection, added-node ledger `+7`, final canonical frontend `+83/-0`, resources `614`, scanner, requests, polling, formatters, CSS, backend, desktop, and extensions remain unchanged. Independent narrow review reproduced exact `4/31`, `11 failed / 20 passed` identity and returned GREEN with no finding. Task 7 product GREEN implementation is cleared.
- **2026-07-21 (I18N-2 TASK 7 RESOURCE-INVENTORY AMENDMENT GREEN — RED NEXT)**: Task 7 stopped before RED or product edits when a complete owned-copy audit proved the reviewed `dataStorage 40` inventory could not render five Market Data summary-row labels, the standalone `Days / 天數` lookback control label, or the existing financial-cache expired count without a resource bypass or semantic loss. First narrow review confirmed those findings but rejected the six-leaf amendment because Macro still lacked exact owners for per-table unavailable and database/required-table unavailable; it also caught the missing typed `expiredCount` field and one stale ledger sentence. Decision 55 now defines exact resource path `+8/-1`: six Data Storage additions, two Macro unavailable additions, and replacement of the unused Macro `availability.available` path. It evolves `summary.financialCache` with typed `expiredCount`, reuses `coverage.drilldown.partial` only for the semantically identical partial-ticker heading, and explicitly maps News run states through the existing schedule-history four-state copy. Second narrow review found one remaining proof gap rather than a resource gap: the planned English Macro node did not require both unavailable branches. That existing node now exercises required-tables-present/non-required-table-missing and database-or-required-table-missing fixtures, asserts their distinct resource owners, and rejects the transport-level unavailable fallback in both cases. Independent re-review returned GREEN with no remaining finding. Final resources remain `settings +613 / 614 total`, `dataStorage 46`, and `macroStorage 32`; the existing resource-count node evolves in place, so canonical frontend remains `+83/-0`, Task 7 remains `+7` nodes with eight RED failures, and scanner/request/polling/formatter/CSS/backend/desktop/extensions remain unchanged. Task 7 RED-first implementation is cleared.
- **2026-07-21 (I18N-2 TASK 7/8 DIAGNOSTIC PROP-WIRE AMENDMENT GREEN — TASK 7 RED NEXT)**: Independent narrow review confirmed the three omitted production props, Macro's lack of retained raw rejection payload, and the zero-node test strategy. Two review rounds corrected the Task 8 proof shape: the existing normal-mode PageHeader/diagnostic node now preserves its assertions, then mounts Personalization with `developerMode=true` and records the Investor mock prop; the Task 8 RED command includes both files and expects exact `37 tests / 5 failures`. Task 7's planned Market/News nodes exercise the real `SettingsView` in both modes. Decision 54 changes no owner, persistence, resource, request, or canonical `+83/-0` accounting. Task 7 RED-first implementation is cleared.
- **2026-07-21 (I18N-2 TASK 7/8 DIAGNOSTIC PROP-WIRE STOP — NARROW REVIEW PENDING)**: Pre-Task-7 grounding found that the reviewed normal/Developer diagnostic tests could pass through direct component construction while the real Settings workspace never forwarded `developerMode` to Market/News Storage or Investor Profile. Decision 54 keeps `Settings.tsx` in both task-local file maps, forwards only its existing bit, requires Task 7's planned Market/News diagnostic nodes to exercise `SettingsView` in both modes, and strengthens the existing Settings workspace PageHeader/diagnostic node for the Task 8 Investor wire: it preserves its normal-mode assertions, then mounts Personalization with Developer Mode and records the mock's received boolean. Macro has no raw diagnostic payload and receives no prop. This adds no owner, persistence, resource, request, or test node; canonical accounting remains `+83/-0`. Task 7 product RED is blocked until narrow review returns GREEN.
- **2026-07-21 (I18N-2 TASK 6 COMPLETE — TASK 7 NEXT)**: Decision 53 repair `22fb1e00` closed the last Task 6 finding with a genuine existing-node RED (`Base=？`) and locale-neutral `Base=—` GREEN. Final independent spec review is GREEN and code-quality re-review is APPROVED. The quality amendment suite is `58/58`, the complete Task 6 suite is `80/80`, typecheck/build/diff checks pass, resources remain exactly `607` with `dataSources 149`, and the canonical frontend ledger remains `+83/-0`. Task 6 changed no CSS, API/request/polling, backend, desktop, or extension behavior. Task 7 Storage/Macro panels is the single NEXT implementation unit, subject to its pre-edit file-map grounding.
- **2026-07-21 (I18N-2 TASK 6 NULL CLIENT-ID QUALITY STOP — BOUNDED REPAIR REVIEW PENDING)**: Product repair `db1f28b3` passed the exact `58/58` amendment suite, `80/80` Task 6 suite, typecheck, build, and independent spec review. Final code-quality re-review nevertheless found one reachable punctuation fallback outside the reviewed seven-site inventory: an unparsable environment-owned IBKR client-ID base produces `effective_id=null`, and the UI renders hard-coded full-width `？` even in English. Decision 53 replaces that unknown numeric ID with the locale-neutral unavailable marker `—` and strengthens the existing English integration node with a null-ID fixture plus a `？` negative assertion. This adds no resource, test node, request, DTO, formatter, polling, CSS, backend, or extension change; ledgers remain `607` resources / `dataSources 149`, frontend `+83/-0`, and scanner `1034/974/954/20`. RED-first product repair is blocked until this paper amendment receives narrow review GREEN.
- **2026-07-21 (I18N-2 TASK 6 FINAL QUALITY AMENDMENT GREEN — RED-FIRST REPAIR NEXT)**: Cast-free repair `14716ddd` passed the exact `21/21`, `80/80`, typecheck, build, and spec-compliance review, but final code-quality review found two further real compatibility/localization defects. A future provider-health status could fall through both switches and render an undefined label/badge instead of the baseline stable identifier; English Data Sources still inherited zh-Hant `（）：、` punctuation from punctuation-only JSX. The bounded amendment makes unknown status a muted stable identifier with no badge, adds exactly two semantic resource leaves for source wrapping and client-ID separation, and moves all seven affected sites (one list separator, two source wrappers, two external values, auto-refresh, guard title) into locale-correct resource ownership. First narrow review caught two paper inconsistencies (four-vs-seven site count and an obsolete common-state sentence); amendment `47403a42` enumerated all seven sites, required grouped-config/running fixtures, and preserved all seven known mappings. Re-review returned GREEN. Final resources become `settings +606 / 607 total` with `dataSources 149`; canonical frontend remains `+83/-0`, scanner remains `1034/974/954/20`, and requests/polling/backend/CSS/extensions remain unchanged. RED-first product repair is the single NEXT action.
- **2026-07-21 (I18N-2 TASK 6 TYPED-INTERPOLATION AMENDMENT GREEN — SUPERSEDED BY FINAL QUALITY STOP ABOVE)**: Task 6 product commit `9494d961` reached exact `21/21`, `80/80`, 605 resources and the reviewed 15-file/+8-node boundary, but independent spec review found two real semantic regressions: both config `effective_source="missing"` branches bypassed the sole `providerKeySourceLabel` owner, and FRED `series_count` stopped using the existing formatter and turned unknown into zero. Repair `7cd198e3` closed both with existing-node RED evidence (`2 failed / 31 passed`), but re-review correctly rejected its `as unknown as number` workaround as a violation of the typed interpolation contract. The bounded amendment adds one string-typed `seriesCount` interpolation field for `snapshotAvailable`; resource paths/counts, visible copy, frontend `+83/-0`, scanner `1034/974/954/20`, requests/polling, backend, CSS, and extensions remain unchanged. Independent narrow review returned GREEN with no findings. Removing the cast through the reviewed placeholder change was the next action at that checkpoint; the later final-quality stop above now controls.
- **2026-07-21 (I18N-2 TASK 6 RESOURCE-INVENTORY RE-REVIEW GREEN — RED-FIRST IMPLEMENTATION NEXT)**: A second pre-edit Task 6 stop proved the reviewed resource ledger was internally false: `dataSources 85` claimed provider/config/schedule table chrome, source captions, compact durable states, and progress accessibility copy that had no corresponding leaves. A complete owned-presenter audit also found 23 compatibility routing outputs that must remain exported/tested by the 07-12 stabilization ruling and therefore cannot be deleted, hidden in locale branches, or left as unclassified source copy. The first 84-path amendment was correctly rejected by independent narrow review because provider-health `key_source=mixed` is a real DTO/backend state and still lacked an owned label. The corrected bounded amendment adds exactly 85 leaves per locale (`dataSources +62`, `dataStorage.routing +3`, `newsStorage.routing +16`, `macroStorage.routing +4`), exhaustively maps all six key-source states through the sole `providerKeySourceLabel` owner, evolves existing resource/history/provider-source nodes in place, and adds one mechanical `DataStorageSection -> coverageStatusLabel` translator wire. Independent re-review returned GREEN after one final paper-only correction from two to three evolved nodes. Final resources become `settings +604 / 605 total`; frontend `+83/-0`, scanner `1034/974/954/20`, requests/polling, backend, CSS, and extension bytes remain unchanged. Task 6 RED-first implementation is now the single NEXT action.
- **2026-07-21 (I18N-2 TASK 6 FILE-MAP STOP — REVIEW GREEN, SUPERSEDED AS CURRENT STATUS BY RESOURCE STOP ABOVE)**: Task 6 grounding caught a plan omission before any product edit: `DataSourcesSection` must receive the already-owned Settings `developerMode` bit to enforce the reviewed normal/diagnostic boundary, but the task-local file map omitted its sole production caller `Settings.tsx`. The global I18N-2 map and Locked Decision 24 already authorize that one prop wire. Independent narrow review confirmed exactly one production caller and one already-owned direct test construction. The bounded amendment adds only `Settings.tsx -> DataSourcesSection developerMode`; resources, frontend node accounting, scanner arithmetic, polling/requests, and all other boundaries remain unchanged. Task 6 implementation was cleared at that checkpoint; the later resource stop above now controls.

- **2026-07-21 (I18N-2 TASK 5 COMPLETE — TASK 6 NEXT)**: The bounded Provider, credential, OAuth, discovery, and reopened scanner/state repair is closed. Independent spec review is GREEN and code-quality review is APPROVED after removing the raw route-source <code>title</code> while retaining the reviewed <code>default</code> scanner candidate inside the localized aria-label composition. Fresh closeout reproduced the exact eight-file <code>120/120</code> suite, typecheck, five-file direct scan at <code>13</code> occurrences / <code>12</code> signatures, protected byte gates, and a clean worktree. Resources remain <code>520</code>, the canonical frontend ledger remains <code>+83/-0</code>, backend remains byte-identical, and the final scanner contract remains <code>1034/974/954/20</code>. Task 6 is now the single NEXT implementation unit. This entry supersedes the blocked status in the earlier Task 5 stop-and-amend entry without rewriting that historical decision.

- **2026-07-21 (I18N-2 TASK 5 SCANNER STOP-AND-AMEND)**: Task 5 spec review rejected two green-test blind spots: translator-driven presenter composition created new literal-scanner signatures, and locale-switch tests could pass through detached pre-switch DOM references. A complete audit of already-completed I18N-2 scopes found the same punctuation-composition shape in Task 4/Task 2 seams and five omitted stable identifiers: <code>sk-…</code>, <code>sk-ant-…</code>, <code>claude-…</code>, <code>gpt-…</code>, and interpolated model family <code>GPT-5.6</code>. The reviewed repair uses only punctuation joins/adjacent JSX, requires post-switch live-DOM identity assertions, restores the originally planned live <code>anthropic</code>/<code>default</code> candidates, and adds the five honest stable-identifier allowlist entries. Resources remain <code>520</code>, frontend remains exact <code>+83/-0</code>, backend remains byte-identical, while the final scanner contract is corrected from <code>1029/969/954/15</code> to <code>1034/974/954/20</code>. Task 6 remains blocked until this bounded repair receives spec and quality re-review.

- **2026-07-20 (I18N-2 TASK 2 QUALITY REVIEW — BOUNDED REPAIR)**: Task 2's spec gate passed, but independent code-quality review exposed two integration holes hidden by the exact <code>31/31</code> test result: the initial backlog copy flattened away <code>tone</code> and <code>earliest_next_retry_at</code>, while the initial model-reason mapping covered only a subset of the nine live reason codes and mislabeled <code>model_not_visible</code>. The accepted bounded repair adds one structured backlog projection, one exhaustive model-reason mapper, and compile-time <code>never</code> guards for closed frontend unions. Preserving the existing zh-Hant backlog presentation requires two omitted visible leaves (queue prefix and earliest-retry label), so the current resource ledger is <code>dataSources 85 / settings +487 / 488 total</code>; frontend remains exact <code>+83/-0</code>, focused remains <code>35/376</code>, backend/scanner/other subtrees remain unchanged, and I18N-2 stays the single implementation unit.

- **2026-07-20 (I18N-2 DEEP PLAN RE-REVIEW — SUPPLEMENTAL CLEARANCE)**: A second grounded review found one arithmetic-bearing plan error: production <code>HORIZONS</code> has five values and no placeholder, not six. The exact resource ledger is therefore corrected from <code>investor 54 / settings +486 / 487 total</code> to <code>investor 53 / settings +485 / 486 total</code>; frontend <code>+83/-0</code>, focused <code>35/376</code>, backend <code>4569</code>, scanner <code>1029/969/954/15</code>, files, and every other subtree remain unchanged. The search advisory is adopted because the draft alias-only helper would have dropped current title/description matches: both locale titles, descriptions, and aliases now remain indexed, with an existing node strengthened against every baseline title/description/keyword. The model-picker review correctly requested explicit metadata coverage but incorrectly claimed additive fields would break the current assertions; those assertions use projections or <code>objectContaining</code>, so the existing ordering/compatibility nodes are strengthened in place without a node-ledger change. I18N-2 remains the single NEXT implementation unit and must descend from this supplemental docs-only clearance.

- **2026-07-20 (I18N-2 SETTINGS PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent written review validated every grounded baseline and exact ledger, then identified one real documentation-authority gap: the plan introduced canonical Settings IA and Investor enum translations without scheduling their terminology authority update. Clearance adopts the deduplicated amendment rather than copying the review count literally: 12 previously absent Settings workflow/section/subsection pairs, seven Assistant Stance pairs, and four risk-mismatch pairs now live in <code>ARKSCOPE_TERMINOLOGY.md</code>; already-owned <code>Macro Data</code>, <code>Risk appetite</code>, and <code>Risk capacity</code> are not duplicated. The existing stance/mismatch mapping node is strengthened in place to require zh-Hant byte parity with the byte-identical shared presenter, so planned accounting remains frontend <code>+83/-0</code> (<code>74/695 -> 77/778</code>), focused <code>32/293 -> 35/376</code>, backend <code>4569</code>, resources then recorded as <code>settings +486</code> (superseded by the deep re-review entry above), and scanner <code>1029/969/954/15</code>. I18N-2 implementation is the single NEXT unit, must descend from the latest docs-only clearance, and must stop at independent implementation review; no public locale selector is authorized.

- **2026-07-20 (I18N-2 SETTINGS PLAN WRITTEN — INDEPENDENT REVIEW PENDING)**: The bounded plan at <code>docs/superpowers/plans/2026-07-20-i18n-2-settings.md</code> localizes the complete reachable Settings workspace, including the current Investor Profile panel, while preserving the three-tab IA, guarded drafts and in-flight work, polling/request semantics, formatter ownership, and selector-last release contract. It introduces exact Settings-only semantic-copy and backend-copy seams, adds typed HTTP error metadata without changing legacy <code>Error.message</code>, and confines raw source diagnostics to Developer Mode. Planned accounting was frontend <code>+83/-0</code> (<code>74/695 -> 77/778</code>), focused <code>32/293 -> 35/376</code>, backend byte-identical at <code>4569</code>, resources initially recorded as <code>settings +486</code> leaves per locale (later corrected by deep review), and scanner <code>1649/1563/1562/1 -> 1029/969/954/15</code> with 22 new migrated scopes. Independent plan review was the next gate; no feature worktree, product implementation, or public locale selector was authorized at that point.

- **2026-07-20 (I18N-1 SHELL + COMMON UI LIVE COMPLETE — I18N-2 SETTINGS NEXT)**: Independent implementation review returned GREEN with zero findings at product tip <code>5a76528d</code>, reproducing exact frontend <code>+15/-0</code>, <code>74/695</code>, typecheck/build, scanner <code>1649/1563/1562/1</code>, the one-line wrap RED/GREEN proof, immutable gates, and a separate 37-check runtime matrix. With explicit user approval, <code>master</code> fast-forwarded through evidence tip <code>6542e6e2</code>. Fresh merged-tree focused <code>12/108</code>, full <code>74/695</code>, typecheck, build, scanner, no-PG <code>ok:true / pg_attempts:[]</code>, byte gates, and exact one-line <code>shell.css</code> accounting passed. The normal desktop was then reduced to one merged-master launcher owning Vite <code>8430</code>, Electron, and sidecar; health/locale requests return <code>200</code> and the captured <code>1440x900</code> Shell is correctly <code>zh-Hant</code> with no selector. I18N-2 Settings, including the current Investor Profile surface, is the single NEXT planning unit; the public selector remains absent.

- **2026-07-20 (I18N-1 SHELL + COMMON UI IMPLEMENTATION REVIEW-READY)**: Frozen product tip <code>5a76528d</code> completes the bounded Shell/Common migration without exposing a locale selector. Virgin frontend node A/B is exact <code>680 -> 695</code>, <code>+15/-0</code>; full frontend is <code>74/695</code>, focused is <code>12/108</code>, backend remains byte-identical at <code>4569</code>, resources remain exact <code>common +17</code>/<code>shell +37</code>, and the literal scanner is <code>1649/1563/1562/1</code>. The English <code>390px</code> runtime gate exposed a real topbar overflow and correctly triggered the plan stop condition. Reviewed scope amendment <code>06c99f98</code> authorized one named RED-first CSS node and one declaration, <code>.shell-topbar-primary { flex-wrap: wrap; }</code>. Real geometry then disproved the provisional claim that Traditional Chinese should remain one flex row: old nowrap hid the problem by compressing the work button to about <code>51x80px</code> and making the primary bar <code>92px</code> high; the reviewed layout gives both locales two clean rows at <code>390px</code>, one row at every canonical width <code>>=959px</code>, complete labels, and zero overflow. Both-locale six-viewport, Drawer/focus/state/privacy, typecheck, build, no-PG, immutable-tree, and process-cleanup gates are green. Independent implementation review is the single NEXT gate; merge, I18N-2, and the public selector remain unauthorized.

- **2026-07-20 (I18N-1 PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent written review approved the complete I18N-1 plan with no must-fix. The one advisory is resolved in the clearance commit: the assertion-ledger summary now explicitly names all three reviewed evolutions, namely the Background Work result-copy correction, navigation authority labels-to-IDs/views, and missing Research thread title to semantic <code>null</code>. Exact scope and accounting remain unchanged at frontend <code>+14/-0</code> (<code>73/680 -> 74/694</code>), focused <code>10/87 -> 11/101</code>, backend byte-identical at <code>4569</code>, resources <code>common +17</code>/<code>shell +37</code>, and scanner <code>1709/1621/1621/0 -> 1649/1563/1562/1</code>. I18N-1 implementation is now the single NEXT unit, must descend from this docs-only clearance, and must stop at review-ready; I18N-2 and the public selector remain unopened.

- **2026-07-20 (I18N-1 SHELL + COMMON UI PLAN WRITTEN — INDEPENDENT REVIEW PENDING)**: The bounded plan at <code>docs/superpowers/plans/2026-07-20-i18n-1-shell-common-ui.md</code> localizes only the shipped Shell plus generic Drawer/BoundedProgress chrome, preserves source content and all formatting owners, and keeps the public locale selector absent. It fixes exact resources at <code>common +17</code> and <code>shell +37</code> leaves per locale, uses one exhaustive static label seam for four navigation groups and eight views, stores missing Research thread titles as semantic nulls rather than captured translations, and promotes App/Shell/Drawer/BoundedProgress into the literal ratchet with ArkScope as the sole stable-identifier allowlist. Planned accounting is frontend <code>+14/-0</code> (<code>73/680 -> 74/694</code>), focused <code>10/87 -> 11/101</code>, backend byte-identical at <code>4569</code>, and scanner <code>1709/1621/1621/0 -> 1649/1563/1562/1</code>. Independent plan review is the single next gate; no implementation branch or product change is authorized yet.

- **2026-07-20 (I18N-0 FOUNDATION LIVE COMPLETE — I18N-1 NEXT)**: Independent implementation review approved product tip <code>69a8ca2</code> and completed canonical virgin backend A/B: collect <code>4562 -> 4569</code>, exact <code>+7/-0</code>, with isolated reruns proving the apparent <code>test_agents</code> differences were pre-existing order-sensitive fixture noise. With user approval, <code>master</code> fast-forwarded through evidence tip <code>64017f4</code>. Merged-tree verification passed backend <code>7</code>, frontend focused <code>8/44</code>, full frontend <code>73/680</code>, typecheck, build, literal scanner, no-PG, and isolated synchronous-first-paint startup smoke at desktop/mobile widths. The normal desktop was concurrently active, so SQLite byte-hash equality was not observable; read-only inspection proved <code>profile_settings.ui_locale</code> remains absent and the isolated profile reported the default without persisting it. I18N-0 adds no selector or translated surface, so Design Kit is unchanged. I18N-1 Shell + common UI is now the single NEXT unit; I18N-2, bilingual-from-birth Slice 5, and later migrations remain queued.

- **2026-07-20 (I18N-0 IMPLEMENTATION REVIEW-READY — INDEPENDENT REVIEW NEXT)**: Product tip <code>69a8ca2</code> completes the reviewed foundation without translating a surface or exposing a selector: key-specific profile locale authority, synchronous cache-first bundled i18next bootstrap, monotonic StrictMode-safe controller/provider, and a TypeScript-AST visible-literal debt ratchet. Exact ledgers are backend <code>+7/-0</code> (<code>4562 -> 4569</code>) and frontend <code>+44/-0</code> (<code>65/636 -> 73/680</code>); frontend full/typecheck/build, scanner, static boundaries, no-PG, and isolated first-paint/runtime smoke are green. This environment reproduces the known canonical pytest hang symmetrically on base and head at <code>test_providers_endpoint</code>, so independent review owns canonical full-suite completion per the reviewed plan. I18N-1 and the public locale selector remain queued and unopened.

- **2026-07-20 (I18N-0 PLAN REVIEW GREEN — IMPLEMENTATION CLEARED)**: Independent written review approved the complete I18N-0 foundation plan with no must-fix. The reviewed scope remains profile-backed locale authority, synchronous cache-first bootstrap, exact bundled typed resources, monotonic StrictMode-safe controller, and the standalone TypeScript-AST literal ratchet with exact debt/allowlist separation. Implementation is now the single NEXT unit, must descend from the plan-clearance commit, and must stop at review-ready; I18N-1 and the public selector remain unopened.

- **2026-07-20 (I18N-0 IMPLEMENTATION PLAN WRITTEN — INDEPENDENT REVIEW PENDING)**: The bounded foundation plan at <code>docs/superpowers/plans/2026-07-20-i18n-0-foundation.md</code> records the reviewed profile-locale API, synchronous cache-first bootstrap, exact pinned i18next resources, monotonic StrictMode-safe controller, selector-typing fallback, and a standalone TypeScript-AST visible-literal ratchet. The ratchet separates generated legacy translation debt from exact non-localized allowlist entries, forbids debt growth, and requires zero findings in migrated scopes; fixtures independently pin JSX text, visible props, ARIA labels, placeholders, templates, message sinks, ignored contexts, and dynamic-key rejection. Planned accounting is backend <code>+7/-0</code> (<code>4562 -> 4569</code>) and frontend <code>+44/-0</code> (<code>65/636 -> 73/680</code>). I18N-0 plan review is the single next gate; dependency installation and product implementation remain unauthorized until GREEN.

- **2026-07-20 (APP-WIDE I18N DECISION REVIEW GREEN — I18N-0 PLAN NEXT)**: Independent full-document review approved `docs/superpowers/specs/2026-07-20-app-wide-i18n-decision.md` with no must-fix. Two non-blocking implementation advisories are assigned to I18N-0: typed selector support must have a standard typed dot-path fallback if the pinned i18next selector API proves unstable, without reopening the mechanism decision; and the TypeScript-aware visible-literal scanner must be a standalone RED-first deliverable with fixtures for JSX text, visible props, ARIA labels, placeholders, and template strings. The decision and terminology autonym addendum are now APPROVED. I18N-0 foundation planning is the single next unit; no product implementation is authorized until that plan receives independent review GREEN.

- **2026-07-20 (APP-WIDE I18N DECISION WRITTEN — INDEPENDENT REVIEW PENDING)**: The bounded decision at `docs/superpowers/specs/2026-07-20-app-wide-i18n-decision.md` records the four approved product choices: profile DB authority plus write-through first-paint cache; synchronous bundled `i18next + react-i18next` resources with no detector/Suspense; selector-last rollout after mechanically complete app coverage; and one compact autonym selector in the Settings PageHeader rather than registry, a fourth tab, or Shell topbar. It also fixes the backend-copy boundary, typed semantic-key discipline, exact two-locale resource/literal/DTO gates, test-ledger framework, and migration sequence: I18N-0 foundation, Shell, Settings, bilingual-from-birth Slice 5, remaining surfaces, then release. `ARKSCOPE_TERMINOLOGY.md` records the narrow `繁體中文` / `English` autonym exception. Design Kit sync #5 is now complete; no partial i18n screen is pre-rendered. Next gate is full written-decision review; implementation and implementation-plan drafting remain unopened until GREEN.

- **2026-07-20 (P2.8 SLICE 4.1 SETTINGS CORRECTION LIVE COMPLETE)**: The merged-master user check passed all five acceptance points: three clear workflow tabs, one mounted panel instead of an all-groups long page, compact FRED truth under Data Sources, detailed FRED content under `總經資料`, and cross-tab exact search/navigation. An independent sandbox built from merged `master` separately replayed 41 interactions, including keyboard tabs, dirty-confirm privacy, responsive `961/960/959/390` behavior, and exact OAuth/FRED targeting, without touching the normal desktop or production DB. Slice 4 remains historical merged infrastructure whose rejected IA is superseded; the corrected Settings surface is LIVE through Slice 4.1, and Design Kit sync #5 has since completed. The app-wide i18n decision document is the single next design unit, while Slice 5 stays queued.

- **2026-07-20 (P2.8 SLICE 4.1 FAST-FORWARD MERGED — DESKTOP USER CHECK PENDING)**: Independent implementation review approved product tip `4931050` after reproducing the exact `+42/-12` ledger, `65/636` frontend suite, static/immutable gates, code-level Tabs/guard/FRED contracts, and a separate 41-check responsive interaction matrix. With user approval, `master` fast-forwarded through evidence tip `bfdc32c`; merged-tree focused `18/164`, full `65/636`, typecheck, and build are green with only the existing chunk-size warning. The correction replaces the rejected all-groups Settings page with three automatic-activation workflow tabs and one mounted group, protects dirty and busy work during navigation, and gives detailed FRED state one owner under `總經資料`. LIVE status, Design Kit sync #5, and the app-wide i18n decision remain gated on the normal desktop user check; Slice 5 stays queued.

- **2026-07-19 (P2.8 SLICE 4.1 WRITTEN REVIEW GREEN — IMPLEMENTATION PLAN NEXT)**: Independent full-document review approved the 4.1 correction spec and the single terminology authority after one sentence-level repair. The backend boundary is now absolute: `src/`, `data_sources/`, and all backend tests must remain byte-identical; the former open-ended “reviewed fixture adjustment” exception is removed. Every negotiated navigation, preference, Tabs, FRED-owner, honest-state, terminology, test-ledger, and six-viewport contract is otherwise unchanged. Plan review must focus on the self-added dirty-draft/in-flight guarantee: Provider OAuth and discovery, provider-config drafts, Investor Profile drafts, and runtime drafts need an explicit owner-by-owner preserve-or-block design under one-mounted-group semantics, with OAuth lifecycle treated as the highest-risk seam. Next gate is a separately reviewed Slice 4.1 implementation plan; implementation remains unopened.

- **2026-07-19 (P2.8 SLICE 4.1 IMPLEMENTATION PLAN WRITTEN — INDEPENDENT REVIEW PENDING)**: The bounded implementation plan at `docs/superpowers/plans/2026-07-19-p2-8-slice-4-1-settings-navigation-correction.md` is grounded on merged product base `f797673` and freshly reproduced frontend `63/606`. It resolves the written-review lifecycle warning owner by owner: model route/discovery/task-test state remains parent-preserved; Provider/runtime/Data Sources report value-free dirty/busy facts; OAuth, probes, and mutations hard-block switching; dirty local drafts require explicit discard confirmation; immutable Investor Profile uses its existing `aria-busy` plus a conservative Settings-owned edit boundary, with precise dirty ownership deferred to Slice 5. The plan locks shared automatic-activation Tabs, commit-keyed exact focus, one mounted group, single-owner FRED detail, absolute immutable paths, raw test accounting `+42/-12` (`63/606 -> 65/636`), and the six exact visual viewports. Implementation remains unopened until independent plan review GREEN and a separate clearance commit.

- **2026-07-19 (P2.8 SLICE 4.1 SETTINGS NAVIGATION CORRECTION DESIGN ADOPTED — WRITTEN REVIEW PENDING)**: The desktop user check rejected Slice 4's technically valid but operationally poor all-groups long page, so Slice 4 remains merged but NOT LIVE. The adopted correction at `docs/superpowers/specs/2026-07-19-p2-8-slice-4-1-settings-navigation-correction-design.md` replaces collapse with three automatic-activation workflow tabs and one mounted group, preserves nine stable anchors, keeps empty-query navigation scoped while making `搜尋所有設定` explicitly cross-group, and requires search/NavigationTarget to switch group before exact focus. It introduces shared `ui/Tabs` with Settings as first consumer while leaving Holdings byte-identical and tracking its local tablist as named debt. It also corrects FRED ownership: Data Sources retains compact provider/config health from existing payloads only, while `總經資料` becomes the sole detailed `/macro/snapshot` owner and no longer promises a Calendar feature. The retired collapse key is never read; a fail-closed active-group key replaces it. `docs/design/ARKSCOPE_TERMINOLOGY.md` is now the single terminology authority: Traditional Chinese uses natural grammar with professional English retained where translation is lossy, duplicate `譯文 · Original` labels are disallowed by default, and invisible EN aliases remain searchable. Verification must include the canonical six viewports, explicitly including 960x768. Next gate is full written-spec review, then a separately reviewed Slice 4.1 implementation plan. After 4.1, the single next design line is app-wide i18n (Shell + Settings first); Slice 5 remains queued and Design Kit sync #5 remains held.

- **2026-07-19 (P2.8 SLICE 4 SETTINGS WORKSPACE FAST-FORWARD MERGED — DESKTOP USER CHECK / LIVE CLOSEOUT PENDING)**: Independent implementation review reproduced the pure-move `60/572`, final `63/606`, exact `+34/-0`, all byte/static gates, and a separate 35-check browser interaction matrix. With the user's merge approval, `master` fast-forwarded from `7e56bce` through review docs tip `8a0fc9c` (product tip `2611464`) without a merge commit or push. Merged-tree focused `18/135`, full `63/606`, typecheck, build, four byte-identity gates, and all reviewed static ratchets passed. The old desktop process group exited through its launcher cleanup path; Vite `8430` and old sidecar `37255` were closed before merged `master` started a fresh Vite `8430`, Electron, and sidecar `45343`. Health and initial product requests return `200`. The plan remains short of LIVE until the user checks the normal desktop Settings directory/search/collapse, Models action placement, Providers, Investor Profile, and Data Sources table. Slice 5 is not started; external Design Kit sync remains a separate closeout record.

- **2026-07-19 (P2.8 SLICE 4 SETTINGS WORKSPACE IMPLEMENTED FOR REVIEW — NOT MERGED / NOT LIVE)**: Product tip `2611464` on isolated branch `codex/p2-8-slice-4-settings` completes the reviewed single-page Settings workspace. The pure-move stage reduced `Settings.tsx` from 3,742 to 671 lines without collection drift, then one static registry established three workflow groups, nine anchors, static search, fail-closed remembered collapse, and the canonical wide-directory/narrow-Drawer split. Model actions now stay with their owner, catalog failure is bounded to AI content, and both prior Settings `window.confirm` paths use `ConfirmDialog`. Exact fresh accounting is `60/572 -> 63/606` (`+34/-0`), focused is `18/135`, typecheck/build pass with only the existing chunk warning, and backend/API/Investor Profile/Dashboard/extension trees remain byte-identical. The six-viewport browser gate found and RED-first repaired four real integration defects: the `IBKR client id` search alias, provenance-only retired-sync rendering, expand-before-focus navigation, and fixed-grid overflow at 1024/961px. Search, collapse persistence, exact System-to-Data-Sources navigation, focus return, scrolling, long-table fit, and no-overlap checks now pass at 1440/1024/961/960/959/390 widths. Isolated services are stopped; the copied production profile and disposable browser data are removed. Next gate is independent implementation review before any merge, LIVE status, Design Kit sync, or Slice 5 work.

- **2026-07-19 (P2.8 SLICE 4 SETTINGS WORKSPACE PLAN REVIEW GREEN — cleared for implementation, implementation not started)**: Independent review cleared `docs/superpowers/plans/2026-07-19-p2-8-slice-4-settings-workspace.md` with no must-fix and locked exact frontend accounting at `+34/-0` (`60/572 -> 63/606`, focused `101 -> 135`). The frontend-only plan first extracts the 3,742-line Settings leaf sections under strict zero-behavior/zero-collection equivalence, then replaces the one-category switcher with one searchable single page containing exactly three non-empty workflow groups (`AI 與模型`, `個人化`, `資料與同步`) and nine stable anchors. It preserves Providers versus Models ownership, keeps Developer Mode in System / Health, omits disabled App Records/Permissions and any empty advanced group, isolates model-catalog failure from unrelated settings, moves route save/import/export into the Models owner, remembers fail-closed collapsed groups, and uses the canonical 960px authority for a wide directory versus transient narrow Drawer. Both remaining Settings `window.confirm` owners migrate to `ConfirmDialog`; Investor Profile semantics remain Slice 5. Next gate is isolated-worktree TDD implementation, followed by an independent implementation review before merge.

- **2026-07-19 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT MERGED / LIVE COMPLETE)**: User-approved stopped-service cutover fast-forwarded `master` through docs tip `cddea39` (reviewed product tip `8f2f9be`). Before the destructive path transition, mode-0600 online backups of both production DBs plus the exact dirty JSON and patch were retained under `data/backups/` with timestamp `20260719T105534Z`. Merged-code pre-apply preview was byte-identical to the approved report (`152` JSON active, `150` accepted, no missing overview or approval rows, all four fingerprints unchanged); `apply --approve-none` produced exact 150-ticker transition parity, zero legacy memberships, and `335` compatibility annotations. Integrity/FK checks passed, and logical multiset comparison preserved every pre-existing row across 43 profile tables and 23 SA tables. ATGE/BRK.B/LC remain absent; BRK B/BTSG/HAPN retain their reviewed distinct provenance. Merged verification closed at universe/caller `405/405`, no-PG `ok:true / pg_attempts:[]`, clean static retirement ratchets, a no-provider 150-ticker dry run, and authenticated desktop live smoke (`/profile/universe`, symbol search, seven-source schedule, DB-derived export). The only broader focused failures were the three already-reviewed missing NVDA fundamentals fixture nodes. The desktop now runs merged `master`; implementation worktree/branch are removed, while backups remain. Next main UI unit: P2.8 Slice 4 Settings.

- **2026-07-19 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT IMPLEMENTED FOR REVIEW — NOT MERGED / NO PRODUCTION CUTOVER)**: Product tip `8f2f9be` completes the reviewed readers-first/writer-last slice in isolated branch `codex/db-derived-universe`. Exact collection is `4514 -> 4562` (`+54/-6`, semantic `+48`); final virgin HEAD is `29 failed / 4452 passed / 74 skipped / 18 warnings / 7 errors`. The one base-only failure is a reviewed deterministic same-node clock repair in `test_route_wires_universe_and_db`, so no HEAD-only failure/error identity exists. Frontend remains byte-identical at `60/572`, typecheck/build pass, no-PG is `ok:true / pg_attempts:[]`, and all runtime/static retirement ratchets are clean. A mode-0600 copied-DB preview found JSON `152` versus accepted snapshot `150`, no missing overview or approval rows, and preserved exact pre-veto provenance for hidden `BRK.B`; copied apply exported `150`, wrote zero active legacy memberships plus `335` annotations, was semantically idempotent, and failed closed before write after a fingerprint mutation. Independent review's sole provenance blocker was fixed RED-first in `8f2f9be`; re-review is GREEN with no new blockers. Production DBs and services were not touched, and main's unstaged `config/tickers_core.json` remains byte-identical at SHA-256 `00d197cf9cc04bf1cb83a877aea0a647ee25c958ba122607bd33e45be325964f`. Next gate is explicit user merge/cutover approval followed by the plan's stopped-service backup, fast-forward, fingerprinted apply, production verification, and restart sequence.

- **2026-07-19 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT PLAN REVIEW GREEN — cleared for implementation)**: Independent review reproduced full collect `4514` and focused `388`, verified all six runtime caller landings, every referenced seam and removal/evolution node, and locked raw accounting at `+54/-6` (semantic `+48`). The reviewed plan at `docs/superpowers/plans/2026-07-19-db-derived-universe-tickers-core-retirement.md` may now enter Task 0 from this docs-only clearance commit in an isolated worktree; behavior A/B remains anchored at `5d8748f`. Three non-blocking execution notes are explicit: route tests use direct handlers rather than `TestClient`, Task 7 removes stale retirement comments as part of the zero-reference gate, and caller-landing node accounting is semantic rather than mechanically one node per caller. The main checkout's unstaged `BTSG` JSON edit remains outside implementation and cannot transition until copied-DB evidence, independent implementation GREEN, stopped services, and explicit user approval.

- **2026-07-19 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT IMPLEMENTATION PLAN OPENED — review pending, implementation not started)**: The written-review-GREEN design is translated into a writer-last RED-first plan at `docs/superpowers/plans/2026-07-19-db-derived-universe-tickers-core-retirement.md`. One structured read-only accessor owns active manual lists, open equity-like holdings, current non-stale Alpha Picks, reviewed legacy membership, source status/provenance, exact hidden veto, and sanitized typed unavailability. A separate compatibility module owns paired tier/category annotations, explicit LC→HAPN/BTSG/ATGE preview decisions, deterministic exact-set export, and no runtime JSON authority; a maintenance-only audit CLI owns semantic fingerprints and copied-DB/live cutover gates. The plan additionally preserves archived-list UI history without qualifying it, migrates symbol-search `tracked` truth, enumerates all six existing resolver landings, and keeps SEC autocomplete reference data available without fabricating active truth. Reviewed accounting target is raw backend `+54/-6` (semantic `+48`), focused `388→436`, canonical collect `4514→4562`, with frontend/extension exact `60/572` byte identity. The user's unstaged `BTSG` JSON edit remains protected until copied-DB preview, retained backup, independent implementation GREEN, stopped services, and explicit merge/cutover approval. Next gate is independent plan review; no Task 0 or product code yet.

- **2026-07-19 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT WRITTEN REVIEW GREEN — implementation plan next)**: Independent review found no must-fix and fresh verification closed its three sentence-level gaps plus one live-data classification. `portfolio_open` now admits only reviewed equity/news identities (`stock`, `etf`, and an option's stored underlying symbol); cash/future/FX/bond/unknown classes are excluded with a source warning rather than silently injected. All six existing `resolve_active_universe()` callers have explicit unavailable-source landings, including durable scheduler failure without loop escape, pre-provider collector/direct aborts, pre-loop daily-update exit, and a sanitized HTTP 503 coverage response. The exporter explicitly drops the unread top-level legacy settings block. Commit `7150ba7` and migrated state prove `LC -> HAPN`, so preview classifies `LC` as `superseded_by_rename` and defaults to no import rather than reviving a dead symbol. The user-owned unstaged `BTSG` JSON edit remains untouched. A separate RED-first implementation plan may now open; no product code is authorized before independent plan review.

- **2026-07-19 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT FRESHLY GROUNDED — written review ready, implementation not started)**: The former P2.8 Slice 3 sequencing block is satisfied, so the adopted 2026-07-17 design has been re-grounded against merged Alpha Picks schema v2 and the live dirty JSON before any implementation plan opens. Exact runtime inventory remains three live JSON read surfaces, one dormant DAL compatibility reader, and one shared writer reached by native-host plus fallback call paths; the maintained collectors' existing profile resolver is not yet the target because it includes archived membership, omits portfolio/Alpha Picks, and collapses unavailable into `[]`. A read-only `2026-07-19T11:48:38+08:00` snapshot found active JSON `152`, active manual membership `148`, open-position symbols `10`, current non-stale Alpha Picks symbols `44`, raw DB-source union `151`, and hidden-filtered union `150`. Exact differences are JSON-only `ATGE`/`LC`, DB-only `HAPN`, with `ATGE` and `BRK.B` explicitly hidden; the user's unstaged `BTSG` line is already represented by the Alpha Picks DB source and must not become duplicate legacy membership. The revised contract makes parity hidden-aware against a generated transition snapshot, separates all-entry annotations from approved JSON-only membership, preserves exact `BRK.B` versus `BRK B` identity, gives unavailable sources a typed failure instead of fake empty, and explicitly declines to promote the 46 `legacy_reference`-only symbols into active membership. All 17 legacy-overview tickers are currently covered by active manual lists and receive a cutover subset gate. Next gate is independent written review; only GREEN may open a separate RED-first plan. The dirty JSON remains modified only by the user and is excluded from this docs commit.

- **2026-07-19 (ALPHA PICKS RECONCILIATION + COMMENT CONTINUITY MERGED / LIVE COMPLETE)**: User-approved fast-forward integration moved `master` through reviewed docs tip `bf378f1` without changing product bytes after `821b2f3` or touching the user's unstaged `config/tickers_core.json`. Merged-tree focused `262`, extension fixtures `24`, no-PG `ok:true / pg_attempts:[]`, and an isolated merged popup/privacy smoke all passed before production state changed; the popup showed `166` review events, kept Advanced collapsed, and exposed no raw relationship/comment/watermark identity. All app/browser/native-host/sidecar processes were stopped, including stale disposable-gate PID `2289588`. A retained mode-0600 v1 online backup lives at `data/backups/sa_capture-v1-pre-alpha-reconciliation-20260719T025246Z.db`. Merged code alone migrated production to schema v2: integrity is `ok`, FK violations are zero, all pre-existing table multiset digests match the backup, `103` lineages cover all `114` picks, and no accepted links/decisions or recovery flags were invented. Desktop Vite `8430`, sidecar `41017`, the normal native-host launcher, and Chrome Profile 1's unpacked extension now resolve to merged main and are healthy. The Advanced manual URL path remains until real automatic coverage supports a separate removal ruling; DB-universe/`tickers_core.json` retirement remains independent.

- **2026-07-19 (ALPHA PICKS ARTICLE RECONCILIATION + COMMENT CONTINUITY MERGE-READY — PRODUCTION STILL V1)**: Independent implementation review is GREEN at product tip `821b2f3`; SF-2 prerequisite tip is `9e4f945`, with final gate evidence recorded by this entry. Canonical virgin A/B against `848ffd4` is exact at collect `4412 -> 4513` (`+101/-0`), head passed `4402`, and the existing `30 failed / 74 skipped / 18 warnings / 7 errors` identities are unchanged. Review SF-2 now documents the root `npm ci`/same-lockfile hoisted-`jsdom` prerequisite for 24 extension fixture nodes. Online-backup migration preview reached schema 2 with integrity/FK clean, preserved `114` picks / `400` articles / `42,794` comments / `2` refresh-meta rows, created `103` lineages and no accepted links, and exposed a `166`-event review queue; the second-copy ten-step continuity probe passed Quick raise/repair, Full two-miss park, Backfill-only terminalization, terminal audit retention, and future re-anchor. The real-provider gate initially exposed a stale Chrome MV3 worker from `18fcfb4`; those two runs are explicitly excluded. After `chrome.runtime.reload()` and source pins proved the reviewed worker loaded, the first Quick advanced four checkpoints to current counts with zero new comments/failures, and the immediate unchanged Quick returned `comments_refreshed=0 / net_new_comments=0 / failed=0`. Popup/native payloads exposed no watermark/comment identity. All gate processes and disposable artifacts are removed; ports `8423/9224` are closed. Formal config/DB hashes and logical aggregates are byte-identical to pre-gate, production remains schema v1, and the user's dirty `config/tickers_core.json` is untouched. Next: user-approved fast-forward merge, merged-tree gates, then an exclusive merged-code production v1-to-v2 migration before app/browser restart. DB-universe/JSON retirement remains separate.

- **2026-07-19 (ALPHA PICKS COMMENT CONTINUITY IMPLEMENTED FOR REVIEW — LIVE EVIDENCE PENDING)**: Product commits `3149623` and `821b2f3` implement the reviewed `+16/-0` addendum: explicit zero-versus-unknown provider-count provenance, a usable-scan checkpoint, a frozen pre-upsert row-ID watermark, Quick evidence-only repair, Full two-miss parking, Backfill-only five-round terminalization, terminal audit/re-anchor, and recovery-first bounded scheduling with no provider-minus-inventory trigger. Full collection is exactly `4497 -> 4513`; the Task 8 eight-file focused command passes `182`, while the broader Task 9 fourteen-file command passes the reviewed `262` (the original Task 8 expectation had copied the broader command's count). Compile/JS syntax/diff/static/protected-file gates are green. The implementer full run is `4408 passed / 31 failed / 74 skipped / 18 warnings`; the one extra visible failure is a byte-identical date-window test whose fixed 2026-06-22 input expired on 2026-07-19, so no canonical equivalence claim is made. Independent code review comes next, followed by disposable copied-DB state-machine and repeated-Quick provider proof. Production remains schema v1; no browser/profile/provider/production DB gate ran in this implementation phase.

- **2026-07-19 (ALPHA PICKS COMMENT CONTINUITY ADDENDUM WRITTEN REVIEW GREEN — TASK 8 CLEARED)**: The provider gate disproved the old comment scheduler's completeness assumption: a representative article reported `983` lifetime comments, a bounded browser pass exposed about `207`, and the local deduplicated inventory had accumulated `592`. `provider_count > stored_count` creates permanent work, but a pure count checkpoint would also abandon recoverable post-enable outages. User ruling: waive pre-enable and provider-unreachable historical deficits, while repairing middle intervals when current rendering can still bridge them. The approved contract freezes the pre-upsert maximum comment row ID when a usable count-change scan has prior local comments but zero overlap. Quick may raise or evidence-clear pending; Full has no terminal authority and parks after two usable no-overlap attempts; explicit Backfill alone may terminalize after five stable-bottom rounds with no growth, loader, or activated load-more control. Unusable scans freeze the state machine. Terminalization re-anchors future capture while retaining its audit; unchanged-count overlap and stale TTL alone cannot restart a terminal gap. No fixed calendar cutoff or provider-minus-inventory trigger exists. Review repair standardized the baseline-overlap result key and strengthened the existing park node to prove nested count changes cannot re-freeze the watermark/start time; exact accounting remains `+16/-0`. Existing production remains schema v1, and the not-yet-shipped v1-to-v2 migration seeds only count checkpoints with no recovery flag. Task 8 RED-first implementation is authorized; core tip `18fcfb4` remains merge-blocked pending implementation, independent review, and copied-DB/live continuity proof.

- **2026-07-18 (ALPHA PICKS RECONCILIATION PLAN ROUND 2 CLEARED — TASK 0 AUTHORIZED)**: Independent rereview confirmed all three MF-A closures: the existing idempotence node's v1-marker/v2-artifact fail-closed arm, the pre-merge real-provider gate's disposable-worktree plus online-backup-copy boundary, and the post-merge merged-code-only production migration window. The installed launcher/environment seams and `_try_ticker_sync()` containment were re-grounded, exact `+78/-0` accounting remains unchanged, and `config/tickers_core.json` remains unmodified/unstaged. The status-sync commit containing this entry is the sole `PLAN_REVIEW_CLEARANCE_COMMIT`; implementation branches from it while canonical behavior A/B remains anchored at `848ffd4`. Task 0 is authorized; product implementation must stop review-ready before merge.

- **2026-07-18 (ALPHA PICKS ARTICLE RECONCILIATION IMPLEMENTATION PLAN OPENED — review pending, implementation not started)**: The approved BTSG-grounded design is translated into the eight-task RED-first plan `docs/superpowers/plans/2026-07-18-alpha-picks-article-reconciliation-implementation.md`. It introduces lifecycle-stable `(symbol_key,picked_date)` lineages, distinct exit events per `closed_date`, independent list/detail ticker provenance, deterministic bounded date/role matching, retained rejection/replacement history, a review-first extension popup, and an event-scoped Advanced URL escape hatch. Both legacy same-ticker/nearest-date auto writers retire in the same product change. Pick, article-list, and body/comment captures each commit before a separate reconciliation call; a matcher failure cannot turn a successful capture into failure. Normal body work is limited to the current list scan, while matcher enrichment is a separate deduped `4/12/20` cap. The plan also pins a one-time cross-process v1→v2 migration, copied-DB preview/backup gates, no manual-symbol injection into provider or synthetic content evidence, and strict event resolution before manual fetch. Grounded baseline is backend `4412` collected / `4301` passed with existing non-passing families, focused `161`, and web app `60/572`; reviewed target is exact backend/extension `+78/-0` with web-app byte identity. The unrelated dirty `config/tickers_core.json` remains protected. Next gate is independent plan review; no product/schema/extension implementation is authorized yet, and DB-universe/JSON retirement remains separate.

- **2026-07-18 (P2.8 SLICE 3 RESEARCH WORKSPACE MERGED / LIVE — external Design Kit sync pending)**: Independent implementation review returned PASS and explicitly approved local integration at product tip `341e760` / docs tip `5bbc753`; `master` fast-forwarded from `5be79a6` through `5bbc753` while preserving the user's unstaged `config/tickers_core.json` edit. Reviewer canonical A/B remained exact at backend `+34/-0` with identical `30 failed / 74 skipped / 18 warnings / 7 errors`; merged-tree frontend is `60 files / 572 tests`, typecheck/build pass with only the existing chunk warning, no-PG is `ok:true / pg_attempts:[]`, and static privacy/layout/API ratchets are clean. A fresh merged-tree disposable profile repeated all six responsive widths and passed exact archived navigation, history filtering, lifecycle mutations, active-run rejection, typed-error privacy, Developer sanitization, and legacy linkage; ports `8421/8432/9223` refused connections after cleanup. Focused backend on the merged worktree produced `126 passed` plus the same known bare-environment `eventkit` setup error from the canonical error family. Slice 3 is live; the external Claude Design Research-screen/pinnable-Drawer/progress-error companion sync remains explicitly pending because this session has no authenticated DesignSync capability. Slice 4, Alpha Picks reconciliation, and DB-universe retirement remain separate candidate lines and have not started implicitly.

- **2026-07-18 (P2.8 SLICE 3 RESEARCH WORKSPACE IMPLEMENTED FOR REVIEW — all implementer gates closed)**: Branch `codex/p2-8-slice-3-research-workspace` stops at product tip `341e760` without merge. The shipped transcript/run authorities now support deterministic bounded history, exact run/message linkage, rename/archive/confirmed delete, typed failures, complete per-run provider/model/effort selection from the shared Models-UX authority, factual bounded progress, one conversation workspace, on-demand History, and wide-only pinnable Evidence. Canonical virgin A/B at `c78203a` versus the backend product stack retained identical `30 failed / 74 skipped / 18 warnings / 7 errors`, empty bidirectional failure/error identity diff, and exact `4378 -> 4412` collection / `4267 -> 4301` passed (`+34/-0`). Frontend is exactly `56/533 -> 60/572`, raw `+54/-15`; the extra reviewed node is a RED-first StrictMode exact-target regression found by the browser gate and fixed in `9dcdbfc`. Final self-review then strengthened an existing Evidence node without changing collection and fixed persisted semantic effort `default` to render consistently as `Provider 預設` in `341e760`. Full frontend, typecheck/build, no-PG, static privacy/layout/API ratchets, six responsive boundaries, lifecycle/history mutations, typed error privacy, and process cleanup passed. One authorized live call used `chatgpt_oauth / gpt-5.4-mini / default`, observed `creating -> queued -> running -> succeeded`, retained a local draft, used zero tools, preserved exact tuple/run linkage across leave/return, made no fallback, and deleted its disposable thread through ConfirmDialog. Independent implementation review is the remaining gate; do not merge, mark Slice 3 LIVE, sync the Design Kit, or begin Slice 4/Alpha Picks/universe implementation yet.

- **2026-07-18 (ALPHA PICKS ARTICLE RECONCILIATION WRITTEN REVIEW GREEN — implementation plan next)**: Written review verified the BTSG parser diagnosis, the unbounded same-ticker writers, and the live DB identity problem. Two spec-level must-fixes are closed. First, `_sync_canonical_to_picks()` and the mutating branch of `audit_unresolved_symbols()` retire or route through the new matcher in the same slice; article/comment capture commits before separate reconciliation, and no dual-writer window is permitted. Second, links no longer reference unstable `sa_alpha_picks.id`: a stable `(symbol_key, picked_date)` lineage spans current/closed rows, each distinct `closed_date` remains its own exit event, and changed picked dates form a review-visible new lineage rather than triggering symbol-only migration. The accepted-link/rejection/review keys and compatibility projection are updated accordingly. The independent RED-first implementation plan may now open; DB-universe/JSON retirement remains separate.

- **2026-07-18 (ALPHA PICKS BTSG LIVE EVIDENCE CORRECTS PARSER GROUNDING)**: User screenshots prove the Alpha Picks analysis list exposes `BTSG` beside the publication time and the detail header exposes `BrightSpring Health Services, Inc. (BTSG) Stock`, despite the generic article title. The current list parser drops the ticker because its anchored expression assumes ticker immediately follows the date, while the detail parser does not return the security-header ticker at all; the manual URL flow succeeds only because it injects the supplied symbol. The reconciliation draft now treats list and detail ticker metadata as separate explicit provider evidence, requires conflict review, retains title/body matching only as a fallback, and pins sanitized BTSG fixtures plus a no-manual-injection live gate. No other symbol is asserted from analogy. Relationship/event-date semantics remain unchanged; implementation receives fresh written review before plan work.

- **2026-07-18 (P2.8 SLICE 3 PLAN REVIEW GREEN — cleared for Task 0, implementation not started)**: Independent review reproduced backend `4378` / focused `93` and frontend `56/533`, verified the exact `+34/-0` and `+52/-15` accounting, and found one plan-only omission: max-tool exhaustion has four provider/auth shapes. The plan now recognizes only reviewed code-owned facts: the Anthropic API-key exact done sentinel, OpenAI API-key anchored `MaxTurnsExceeded:` prefix, ChatGPT OAuth full max-turn message, and Claude SDK typed `error_max_turns` subtype normalized to explicit `tool_limit_reached`. The existing Claude driver test is strengthened in place and the central classifier's single table-driven node covers positive/near-miss shapes, so accounting is unchanged. Unknown future SDK prose remains `provider_call_failed` and triggers review rather than fuzzy inference. Plan status is REVIEWED / CLEARED; implementation must start with isolated Task 0 and continue to protect the unrelated dirty `config/tickers_core.json`.

- **2026-07-18 (P2.8 SLICE 3 RESEARCH WORKSPACE PLAN OPENED — review pending, implementation not started)**: With NEWS content availability merged/live and the canonical sequence unblocked, the approved Research workspace contract is translated into an eight-task RED-first plan at `docs/superpowers/plans/2026-07-18-p2-8-slice-3-research-workspace.md`. The bounded slice replaces the permanent history/trace columns with on-demand History and wide-pinnable Evidence, adds server-side deterministic metadata filtering plus rename/archive/confirmed delete, links new messages to exact runs, persists typed Research error codes, presents factual session-bound progress, and fixes the known model-selection effect race through one complete-tuple resolver over the same Models-UX effective authority used by Settings. It explicitly does not invent structured claim/citation data, collections, LLM classification, a second model catalog, or a second job registry. Grounded baselines are backend `4378` collected / Research-focused `93` and frontend `56 files / 533 tests`; reviewed targets are backend exact `+34/-0` and frontend raw `+52/-15` (15 obsolete silent-fallback policy tests replaced one-for-one), ending at `60 / 570`. The unrelated dirty `config/tickers_core.json` remains protected. Next gate is independent plan review; do not implement, merge, mark Slice 3 LIVE, or start Alpha Picks/universe code yet.

- **2026-07-18 (NEWS CONTENT AVAILABILITY UNIT 2 MERGED / LIVE COMPLETE)**: The reviewed stack fast-forwarded to `master` through `b3d9728`. One shared derived-at-read classifier, explicit migration/projection-map resolution, SQL-before-pagination content filtering/facets, additive API fields, and a market-only version-skew-safe NEWS selector now ship with honest full/headline-only/unknown and retryable/terminal presentation. Finnhub/Polygon never receive a false recovery promise; IBKR remains the sole reviewed body-recovery source. Canonical virgin A/B at `012dc69` versus final implementation/test tip `eb011d7` retained identical `30 failed / 74 skipped / 18 warnings / 7 errors`, empty bidirectional failure/error identity diff, and exact backend `4248 -> 4267` / collect `+19/-0`; frontend is exact `55/527 -> 56/533` (`+6/-0`). Independent review RED-first closed empty-unknown selection, old-sidecar badge suppression, map precedence, legacy-FTS authority, and mutation-proven SQL predicate/bounded pagination without changing node accounting. The user's independent C-1 SA gate passed search/ticker/day/type/pagination/degraded behavior and proved zero Unit 2 UI leakage. Fresh merged-tree `94`, frontend `56/533`, typecheck/build, no-PG `pg_attempts:[]`, and 1440/1024/390 market/SA visual gates passed; filtered pagination remained `50 -> 100`, with no horizontal overflow or toolbar overlap. Temporary ports `8421/8432/9223` were stopped, and the unrelated dirty `config/tickers_core.json` remained untouched. P2.8 Slice 3 may now open.

- **2026-07-18 (SA MISSING-DB EMPTY-VS-UNAVAILABLE SEMANTICS — NON-BLOCKING BACKLOG)**: The C-1 regression gate confirmed a pre-existing truthfulness gap outside Unit 2: when the configured Seeking Alpha DB file is absent, `/sa/feed` currently returns `available:true` with `no_items_in_window`, so the UI presents a valid empty result rather than an unavailable/not-ready source. It does not crash or create the file, and all normal SA behavior is intact. A future bounded SA-owned fix should distinguish missing/unreadable storage from a valid empty database and map that fact through the established unavailable/degraded UI state; it must not be folded into market `content_availability`. The narrow TICKER placeholder clipping is cosmetic P2.8/NEWS surface debt and does not block this line.

- **2026-07-17 (NEWS CONTENT AVAILABILITY UNIT 2 IMPLEMENTATION PLAN OPENED — review pending, implementation not started)**: The approved derived-at-read design is translated into Task 0 plus seven RED-first implementation/verification tasks at `docs/superpowers/plans/2026-07-17-news-content-availability-implementation.md`. One code-reviewed classifier owns the six body states and the independent recovery-capability axis; V1's scheduled body-recovery set is exactly IBKR, so Finnhub/Polygon pending or failed rows remain honestly headline-only/terminal. The existing local NEWS query dynamically consumes only explicit migration/projection maps, degrades missing normalized evidence to unknown, filters in SQL before totals/facets/pagination, and retains legacy FTS/search authority. The additive route/UI is market-only, hides under old-sidecar version skew, adds no force action, and leaves SA, normalized schema/writer/store/IBKR retry/entitlement/scheduler owners unchanged. Grounded baselines are backend focused `75`, canonical `4359` collected with `4248` passed, and frontend `55 files / 527`; reviewed-plan targets are exact backend `+19/-0` and frontend `+6/-0`. A real roughly 400k-row read-only query-plan/timing sanity is an explicit gate without an invented threshold. The user's dirty `config/tickers_core.json` remains excluded and protected. Next gate is independent plan review; do not implement yet.

- **2026-07-17 (DB-DERIVED UNIVERSE / `tickers_core.json` RETIREMENT ADOPTED — written review pending, implementation after P2.8 Slice 3)**: The user approved automatic DB capture and automatic V1 `sa_alpha_picks_current` eligibility with no second default-on universe switch. Source withdrawal removes only that membership; all captured/history data survives. Feed retirement remains a named additive source-policy follow-up and is never inferred from age. Authority draft: `docs/superpowers/specs/2026-07-17-db-derived-universe-tickers-core-retirement-design.md`. It establishes one structured accessor over active lists, open holdings, current non-stale Alpha Picks, and imported legacy-config membership; readers migrate first under strict JSON-set == derived-set parity; the protected extension writer stops last; and the app owns only a generated-labelled on-demand export. Grounding corrected the loose "ten readers" claim to three live production read surfaces, one dormant DAL compatibility reader, and one native-host writer. `ARKSCOPE_TOOL_CATALOG.md` is superseded in the same docs change: the former UI-opt-in/follow-tool promise is retired. The existing dirty `config/tickers_core.json` must be previewed and preserved, never staged/reverted/overwritten during setup.

- **2026-07-17 (ALPHA PICKS ARTICLE RECONCILIATION MINI-DESIGN DRAFTED — fresh written review required)**: `docs/superpowers/specs/2026-07-17-alpha-picks-article-reconciliation-design.md` replaces unbounded nearest-date/single-canonical semantics with event roles: entry anchors to `picked_date`, exit anchors to `closed_date`, accepted links live in a relationship model, and automatic acceptance requires a unique bounded date+ticker/content+role evidence shape. Ambiguity remains reviewable; the raw `TICKER URL` textarea becomes an Advanced event-scoped escape hatch rather than the default workflow. Capture always commits independently of matching. This content was not pre-approved by the universe ruling and receives its own review; implementation remains after P2.8 Slice 3 and separate from JSON retirement.

- **2026-07-17 (NEWS CONTENT AVAILABILITY UNIT 2 DESIGN APPROVED — implementation plan opening)**: `docs/superpowers/specs/2026-07-17-news-content-availability-design.md` adds no stored authority: market NEWS derives `full / headline_only / unknown` plus retryable/terminal secondary meaning through the explicit legacy migration/projection maps. Written review found and closed one load-bearing correction: recovery is `body_status × code-reviewed provider capability`, not body status alone; only IBKR currently has a scheduled article-body retry path, so Finnhub/Polygon pending or failed rows are honestly terminal at read time rather than promised future processing. Filtering is SQL-before-pagination/facets; unmapped legacy rows remain unknown; old-sidecar absence never becomes a false headline-only badge; title search remains available while nonexistent body text cannot match. It deliberately leaves the legacy feed/search authority, retry policy, SA mode, and force actions unchanged. This bounded Unit 2 implementation completes before P2.8 Slice 3.

- **2026-07-17 (DATA SOURCES SCHEDULE POLLING HOTFIX MERGED — natural API boundary PASS, user visual confirmation pending)**: `master` fast-forwarded through reviewed docs tip `a4aadc6`. Fresh merged-tree verification retained focused `3 files / 33`, full frontend `55 files / 527`, typecheck/build, exact `+10/-0`, backend/API byte identity, and zero polling `aria-live`. With the restarted desktop and Data Sources left mounted, a natural IBKR News schedule run entered `running` at `2026-07-17T14:22:02Z` and completed at `14:25:25Z`; an independent read-only `/schedule` monitor observed both transitions within the designed cadence without pressing Run or mutating provider/DB state. The final LIVE label remains gated on the user's visual confirmation that the mounted row itself changed automatically; the monitor has been stopped. Unit 2 design is now open, but the polling plan is not falsely marked LIVE before that UI confirmation.

- **2026-07-17 (DATA SOURCES SCHEDULE POLLING HOTFIX PLAN OPENED — review pending, implementation not started)**: The user's post-merge observation separated scheduler truth from stale renderer state: the 120-minute IBKR News run had completed and durable state had advanced, but `DataSourcesSection` only polls every five seconds after its already-rendered snapshot says some source is running. A background run that begins while the view believes all sources are idle therefore has no polling path that can discover it; the existing page-level `重新整理` eventually reveals correct backend truth. The bounded frontend-only authority is `docs/superpowers/plans/2026-07-17-data-sources-schedule-polling-hotfix.md`: `/schedule` only at 30-second idle / 5-second active cadence plus focus refresh, one in-flight request, sequenced schedule acceptance, and one existing full `load()` whenever a source starts or a durable completion revision appears. Polling failures preserve prior truth and no `aria-live`, API/backend, scheduler, Gateway, database, CSS, or Design Kit change is allowed. Grounded baseline is Data Sources `19` and full frontend `54 files / 517 tests`; reviewed target is exactly `+10/-0` to `55 / 527`. This is Unit 1 of the user's accepted sequence; Unit 2 (derived NEWS `content_availability` mini-design) remains queued, and P2.8 Slice 3 does not resume before both bounded units close.

- **2026-07-17 (IBKR NEWS 10172 RETRY RECALIBRATION MERGED — desktop restart/natural-cycle check pending)**: Independent review closed every gate at `2a84248`; `master` fast-forwarded `77ea897..2a84248` while preserving the user's unrelated ticker-config edit. The first merged-tree focused command exposed an existing real-checkout-only test contamination: fake diagnostic probe tests loaded `config/.env` provenance into process-global `env_keys._loaded_keys`, making later scheduler dummy keys appear forbidden. Adapter -> scheduler reproduced `59` failures; each file alone passed; clearing only that provenance set made the pair `123/123`. Test-only commit `80dd6e8` now bypasses real env/profile application in the two fake-source probes, changes no production path or collection count, and makes the exact merged-tree focused suite `192/192`. Full collection remains `4359`; no-PG is `ok:true / pg_attempts:[]`. The two-attempt policy and old-authority supersession are merged, but LIVE COMPLETE waits for the user to restart the desktop app and for one natural due-cycle read-only telemetry check; no Gateway retry was forced.

- **2026-07-17 (IBKR NEWS 10172 RETRY RECALIBRATION IMPLEMENTED FOR REVIEW — canonical A/B PASS)**: Branch `codex/ibkr-news-10172-recalibration` implements the reviewed two-total-attempt policy at code tip `dda3c00`: first typed `10172` retains the six-hour hedge, second is terminal, and an explicit entitled-only SQLite reconciliation runs under a short market lock after successful provider discovery and before retry selection. Reconciliation failure aborts all retry/fresh work; its aggregate count remains internal and no schema, scheduler, writer, adapter, API, frontend, provider-identity boundary, retry budget, or Gateway cadence changed. Focused execution is `192/192`; full collection is `4352 -> 4359`. Raw node accounting is `+9/-2` because two reviewed policy tests were renamed one-to-one; rename-normalized accounting is exactly `+7/-0`. Symmetric virgin canonical A/B is failure/error-set identical at `30 failed / 74 skipped / 18 warnings / 7 errors`, with passed `4241 -> 4248` (`+7`). Static/no-PG gates pass, and a copied-DB probe reconciled `76 -> 0` matching rows (`76` then `0` changes) while all protected digests remained stable. No live Gateway retry or production DB mutation was used. Independent implementation review is the remaining merge gate; the live three-attempt authorities remain unchanged until merge.

- **2026-07-17 (IBKR NEWS 10172 RETRY RECALIBRATION PLAN OPENED — spec approved, implementation not started)**: Written review approved `docs/superpowers/specs/2026-07-17-ibkr-news-10172-retry-recalibration-design.md` with no design finding. The later read-only database observation strengthened the evidence: one natural `25`-row retry batch moved the typed cohort from `96 failed-attempts-2 / 136 terminal` to `71 / 161`, while fetched bodies at attempts two or more remained zero. Implementation authority is `docs/superpowers/plans/2026-07-17-ibkr-news-10172-retry-recalibration.md`: RED-first change the terminal threshold to the second total typed `10172`, transactionally reconcile currently entitled matching legacy rows after successful provider discovery and before selection, preserve the reversible entitlement block, and fail closed before retry/fresh provider work if reconciliation fails. Reconciliation returns an internal count for tests and copied-DB evidence only; it is deliberately absent from child stdout, parent allowlists, scheduler state, API DTOs, and Settings. Grounded collection is `185` focused / `4352` full, with reviewed-plan accounting target `+7/-0`. Next gate is independent plan review; no production code, live policy, or Gateway behavior has changed.

- **2026-07-17 (IBKR NEWS 10172 RETRY RECALIBRATION ADOPTED — written spec review pending)**: The user's live reruns exposed policy churn rather than a stuck worker: one run moved exactly `25` rows from `32 due / 83 scheduled` to `7 / 108`, and the next due batch also used all `25` retry calls with zero bodies fetched while fresh ingestion continued to succeed. Read-only production evidence now shows zero fetched IBKR bodies at `fetch_attempts >= 2`, versus `96` typed-10172 rows waiting after attempt two and `136` terminal after attempt three (`0/232` successful retry outcomes). The adopted bounded correction retains one six-hour hedge but makes the second total typed `10172` terminal `unavailable`, idempotently reconciles existing failed typed rows at attempts two or more, preserves headlines/metadata, leaves generic errors and the reversible entitlement block untouched, and does not add a force-retry action. Authority draft: `docs/superpowers/specs/2026-07-17-ibkr-news-10172-retry-recalibration-design.md`. No production code has changed; written spec review precedes the implementation plan.

- **2026-07-17 (P2.8 SLICE 2 POST-MERGE SCROLL REGRESSION CLOSED)**: The user's first desktop restart caught a real test-shaped hole: every ordinary `.main` page had stopped scrolling because the new viewport-bounded `.app-shell-content { overflow: hidden }` wrapper clipped an unconstrained full-height child; the old shell had made `.main` the constrained grid item directly. RED-first hotfix `95662b1` makes the wrapper a column flex container and restores its direct page root as the bounded scroll owner (`flex: 1 1 auto; min-height: 0`). Real Chromium proved Settings PageDown movement at 840px (`0 -> 682`), 1440px (`0 -> 815`), and 390px (`0 -> 686`) with no horizontal overflow, while Research retained its viewport-bound outer workspace and independently scrollable message region. `master` fast-forwarded through the hotfix; fresh merged-tree frontend is exactly `54 files / 517 tests`, with typecheck and production build green. The external Design Kit companion sync remains pending and Slice 3 has not started.

- **2026-07-17 (P2.8 SLICE 2 SHELL CONVERGENCE MERGED — external Design Kit companion sync pending)**: Independent review closed every reviewer gate at docs tip `8016aaf`, and the user selected local integration. `master` fast-forwarded from `29f37f8` through that reviewed tip while preserving the unrelated `config/tickers_core.json` worktree edit. Fresh merged-tree verification passed frontend `54 files / 516 tests`, typecheck, and production build with only the existing `559.14 kB` chunk warning; backend-owned paths remain byte-identical to base. The shipped shell now has one four-group IA, no planned Notes/Alerts controls or placeholder rail, normal/developer diagnostic separation, exact current-surface navigation, and a Research-only background-work notification Drawer. The external Claude Design companion sync remains a separately recorded post-merge gate because this execution session has no authenticated DesignSync capability; no Slice 3 implementation has started.

- **2026-07-17 (P2.8 SLICE 2 SHELL CONVERGENCE IMPLEMENTED FOR REVIEW — all implementer gates closed)**: Branch `codex/p2-8-slice-2-shell-convergence` completes the approved frontend-only shell through code tip `90e953e`: one four-group shipped IA; no disabled Notes/Alerts placeholders; no global placeholder rail; normal top bar separated from persisted display-only Developer Mode diagnostics; one sequenced `NavigationTarget` dispatcher for views/tickers/Research threads/enabled Settings sections; and a Research-only background-work Drawer backed by minimal session identities and 5-second direct reconciliation. RED-first commits account exactly for frontend `46 / 453 -> 54 / 516` (`+63/-0`); full frontend, typecheck, and build pass with only the existing chunk warning. Backend owners are byte-identical to `29f37f8` (`+0/-0`, constructive equivalence rather than a claimed canonical A/B), and static IA/privacy/confirm/breakpoint ratchets are clean. Disposable sidecar/Vite/Chrome gates at 1440/1024/961/960/959/390 prove the single 960px authority, no overflow/duplicate nav/blank third column, Drawer focus restoration, Developer Mode reload persistence, repeated exact Research/Settings targets, and no work-payload leakage; measured terminal observation was 1.283s after DB completion. Evidence screenshots are `/tmp/arkscope-p2-8-shell-{1440x900,1024x768,961x768,960x768,959x768,390x844}.png`; all three isolated test services were stopped and the normal desktop/profile remained untouched. Independent implementation review is the remaining gate; do not merge, mark LIVE, sync the Design Kit, or start Slice 3 yet.

- **2026-07-16 (P2.8 SLICE 2 SHELL-CONVERGENCE PLAN OPENED — review pending, implementation not started)**: With Portfolio 1.1 and the bounded IBKR-news correction closed, the canonical P2.8 sequence resumes through the eight-task RED-first plan at `docs/superpowers/plans/2026-07-16-p2-8-slice-2-shell-convergence.md`. The bounded frontend-only slice establishes one four-group shipped IA; removes disabled Notes/Alerts placeholders and the empty right rail; splits the normal top bar from a display-only Developer Mode; adds actionable current-surface `NavigationTarget` resolution; and composes a Research-only background-work notification Drawer from the shipped transient Drawer and compact BoundedProgress primitives. Existing `GET /research/threads` active summaries plus `GET /research/runs/{id}` are sufficient: the shell stores at most 50 minimal run identities in `sessionStorage`, observes create/hydrate/replay seams to close fast-run races, preserves prior truth on poll failure, and does not claim complete history, copy answers/errors, cancel work, or include fixed tasks. Settings IA, Research execution/transcript behavior, backend schema/routes, Notes/Alerts product surfaces, and the external Design Kit remain unchanged until their owning gates. Grounded baseline is frontend `46 files / 453 tests`; reviewed-plan accounting target is `+63/-0` to `54 / 516`, with backend equivalence proved by byte identity. Next gate is independent plan review; do not implement, merge, mark Slice 2 LIVE, or start Slice 3 yet.

- **2026-07-16 (IBKR NEWS ENTITLEMENT-AWARE RETRY MERGED / LIVE — unavailable subscriptions no longer consume retry budget)**: Independent review returned GREEN with canonical virgin A/B exact at backend `+13/-0`, identical pre-existing `30 failed / 74 skipped / 18 warnings / 7 errors`, and empty bidirectional failure differences. Because Portfolio Slice 3 merged first, reviewed tip `b7d590a` was rebased onto Portfolio closeout `4dab7d1`; `git range-diff` kept all four product commits equivalent and retained both decision histories, then `master` fast-forwarded through `a73939f`. Combined merged-tree verification passed focused backend `214`, full frontend `46 files / 453 tests`, typecheck, production build, static privacy/authority gates, and no-PG `ok:true / pg_attempts:[]`. The copied-DB Gateway gate proved `78` unavailable-provider rows made zero body calls, the unchanged `25` budget was spent only on currently available sources, and a provider-set expansion made a blocked row immediately eligible without body-state or attempt mutation; real market/profile digests remained byte-identical. Capability blocks remain derived/reversible, headlines and metadata remain retained, terminal `10172` semantics remain distinct, and provider identity remains inside the isolated child. This bounded IBKR-news correction and Portfolio 1.1 are both closed.

- **2026-07-16 (PORTFOLIO 1.1 LIVE COMPLETE — all three slices merged, all gates closed)**: `master` fast-forwarded `b657349..e5045dc` after independent review returned GREEN without reservation. Slice 3 delivers a provider-free correction-aware activity projection, grouped positive-`perm_id` orders, commission/correction and unmatched detail, manual-adjustment history, explicit coverage/history markers, user-owned intent annotations, the fourth Holdings `活動` tab, and a real-content-only recent summary without changing capture or canonical Holdings authority. Reviewer canonical A/B at base `c5cd91f` versus reviewed branch tip proved exact backend `+37/-0`, identical pre-existing `30 failed / 74 skipped / 18 warnings / 7 errors`, and empty bidirectional failure differences. Merged-tree verification passed focused backend `119`, focused frontend `6 files / 84 tests`, full frontend `46 files / 451 tests`, typecheck, production build, and no-PG `ok:true / pg_attempts:[]`. After the external reset window recovered, a scheduler-disabled sidecar against a fresh SQLite backup completed two immediate captures with all three legs complete; run history advanced while executions, commissions, two logical order groups, and the complete activity projection stayed unchanged, raw broker account identity remained absent, and the real profile digest stayed byte-identical. No late correction was fabricated. The concurrent IBKR-news entitlement hotfix remains a separate review branch and is not part of this merge; no further Portfolio 1.1 implementation slice remains.

- **2026-07-16 (IBKR NEWS ENTITLEMENT-AWARE RETRY IMPLEMENTED FOR REVIEW — exact A/B and copied-DB gate pass)**: Branch `codex/ibkr-news-entitlement-hotfix` implements one strict per-run provider-capability observation, reversible SQLite queue filtering, aggregate-only `provider_not_entitled` telemetry, and an honest Settings explanation. Focused backend is `214`; full collection is exactly `4302 -> 4315` (`+13/-0`); canonical base/head runs retain identical `30 failed / 74 skipped / 18 warnings / 7 errors`; frontend is `44 files / 428 tests` (`+2/-0`) with typecheck/build green. The copied-DB one-Gateway gate classified `78` unresolved rows as entitlement-blocked with zero blocked body calls, spent the unchanged `25` budget only on currently available sources, proved automatic limit-1 re-entry without body-state/attempt mutation, and left real market/profile DB digests unchanged. Three responsive Settings viewports pass without overlap or a false action. No queue table, terminal rewrite, licensed identity boundary change, 10172 policy change, force action, PG path, or trading API was added. Independent review is the remaining merge gate; Portfolio Slice 3 stays isolated.

- **2026-07-16 (IBKR NEWS ENTITLEMENT-AWARE RETRY DESIGN APPROVED / PLAN OPENED)**: Live read-only diagnosis proved the durable queue was active rather than stuck: one run spent all `25` retry slots (`0` fetched), while its independent fresh leg fetched `40` bodies. Seventeen retries returned typed `10172`; eight FLY retries returned IBKR `321`, and a successful `reqNewsProviders` observation did not include FLY while a recent entitled-provider control remained fetchable. The approved correction treats provider absence as a reversible capability block, retains headline/metadata, excludes blocked rows from retry budget, reports only an aggregate `provider_not_entitled` count, and automatically restores eligibility under the existing `25`-item limit after a later successful provider observation includes the source. It does not rewrite rows to terminal `unavailable`, parse licensed error text, add a queue table/action/config, or alter `10172`. The five-task RED-first plan is `docs/superpowers/plans/2026-07-16-ibkr-news-entitlement-aware-retry.md`; implementation proceeds on isolated branch `codex/ibkr-news-entitlement-hotfix`, then stops review-ready before Portfolio Slice 3 resumes.

- **2026-07-15 (Portfolio 1.1 SLICE 3 ACTIVITY/JOURNAL PLAN OPENED — review pending, implementation not started)**: The final approved Portfolio 1.1 outcomes are grounded into a seven-task RED-first plan at `docs/superpowers/plans/2026-07-15-portfolio-1-1-slice-3-activity-journal.md`. One provider-free read model projects immutable execution correction families, latest exact commission revisions, unmatched position changes, manual journals, and derived coverage/history markers without rebuilding canonical holdings or calling Gateway. Only user-owned intent/note annotations add mutable storage, behind the profile-state write gate; grouped fills use positive `perm_id`, missing/non-positive identities stay independent, provider totals are all-legs-or-null, position effects fail closed when context is ambiguous, and raw broker account ids remain absent. The Holdings surface gains the locked `活動` tab plus a real-content-only wide recent summary using existing P2.8 primitives. Grounded baselines are backend `4302` collected and frontend `44 files / 426 tests`; planned accounting is backend `+33/-0` and frontend `+24/-0`. Next gate is plan review; do not implement or mark Portfolio 1.1 LIVE COMPLETE yet.

- **2026-07-15 (IBKR NEWS DURABLE BODY RETRY MERGED / LIVE — all gates closed)**: `master` fast-forwarded through reviewer tip `c5cd91f`. Independent canonical A/B proved identical pre-existing families and exact backend `+27/-0`; merged-tree verification passed focused `183`, frontend `44 files / 426 tests`, typecheck, and production build. The copied-DB one-Gateway gate naturally exercised retryable 10172 without mutating the real DB or exposing provider identity, and six responsive states passed after a scoped wrapping fix. The user opened the merged desktop and reported no issue. Hotfix A plus durable retry are closed; Portfolio 1.1 Slice 3 activity/journal planning resumes next.

- **2026-07-15 (IBKR NEWS DURABLE BODY RETRY IMPLEMENTED FOR REVIEW — live copied-DB gate pass; reviewer canonical A/B pending)**: Branch `codex/ibkr-news-durable-body-retry` implements the SQLite-derived queue, independent 25-item retry leg, sanitized child/parent telemetry, and separate Settings backlog presentation through code head `3f9ea7e`. Focused backend is `183`, explicit reliability `8`, full head collection `4302`, frontend `44 files / 426 tests`, no-PG smoke is clean, and clean archive collection accounting is exactly `4275 -> 4302` (`+27/-0`; frontend `+7/-0`). Six responsive states pass without overlap or false action. A one-Gateway copied-DB run naturally observed retryable 10172, advanced the copied row exactly once, still ran fresh ticker work, exposed no licensed/provider identity, and left the real DB byte-identical. The visual gate also caught and fixed a scoped table-cell wrapping specificity collision. This environment's base full pytest hung in the known TestClient/lifespan family after 31 cases, so no canonical A/B PASS is claimed; independent reviewer canonical A/B is the final merge gate. Portfolio 1.1 Slice 3 resumes only after this bounded interruption is merged/live.

- **2026-07-15 (IBKR NEWS DURABLE BODY-RETRY PLAN OPENED — review pending, implementation not started)**: The approved follow-up is grounded into a six-task RED-first plan at `docs/superpowers/plans/2026-07-15-ibkr-news-durable-body-retry.md`. Existing normalized SQLite rows are the only queue authority; a deterministic 25-item local-ID retry batch runs before, but never replaces or consumes the budget of, the fresh headline leg. Child/parent allowlists expose only leg statuses, counts, and earliest retry time; future-due backlog may coexist with a succeeded run, while an eligible in-run failure including 10172 deterministically makes the run partial. Queue-query failure runs fresh work when possible and reports unavailable rather than zero. No queue table, provider-ID boundary change, force retry, cadence/config change, PG path, tool/prompt, or trading API is added. Grounded baselines are backend collection `4275` and frontend `44 files / 419 tests`; planned accounting is backend `+27/-0` and frontend `+7/-0`. Next gate is plan review; do not implement or merge yet.

- **2026-07-15 (IBKR NEWS PARTIAL-STATUS HOTFIX A MERGED / LIVE — exact +7, live/restart gate pass)**: Independent review repeated the product, boundary, privacy, and frontend gates and returned GREEN. `master` fast-forwarded through `c035ff2`; merged-tree verification passed focused `40`, full frontend `44 files / 419 tests`, typecheck, and production build. The frontend now derives truthful ticker/body/cursor observations only from durable `last_result.collect.continuation`; actionable price `deferred[]` remains the sole `補抓` source, malformed counts fail closed, and process-local results are never guessed into a durable run. Backend owners remain byte-identical to `e5ccd12`; `api.ts` is type-only and `Settings.tsx` is byte-identical. The single-sidecar live/restart gate used the natural durable count `2`, rendered correctly at 1440/1024/390 without overlap or a continuation button, and stayed correct after process-local state reset. Hotfix A is closed; the SQLite-derived durable body-retry follow-up remains pending and requires its own reviewed implementation plan.

- **2026-07-15 (IBKR NEWS PARTIAL-STATUS HOTFIX A PLAN OPENED — review pending, implementation not started)**: Written review of `docs/superpowers/specs/2026-07-14-ibkr-news-partial-retry-design.md` is GREEN after making §4.4 deterministic: an eligible body failure during the run, including retryable 10172, makes that run partial; a future-due backlog that was not attempted does not. The bounded frontend-only plan at `docs/superpowers/plans/2026-07-15-ibkr-news-partial-status-hotfix.md` describes the already-returned durable `last_result.collect.continuation` counts, preserves actionable price `deferred[]` as the only manual `補抓` source, and deliberately does not consume process-local results because the DTO has no shared run ID. It adds no backend/API request/Gateway/storage/retry behavior. Grounded frontend baseline is `44 files / 412 tests`; reviewed-plan accounting is `+7/-0`. Next gate is plan review; do not implement or merge yet.

- **2026-07-15 (Portfolio 1.1 SLICE 2 ACCOUNT OVERVIEW MERGED / LIVE — real-Gateway gate closed)**: `master` fast-forwarded through `35b3115` after independent canonical A/B proved identical pre-existing failure families and exact backend `+17/-0`; merged-tree verification passed focused backend `81`, frontend `44 files / 412 tests`, typecheck, and production build. The single-sidecar live gate captured and applied real run `61` without a second Gateway read, preserved distinct broker-observation and canonical-approval timestamps, rendered provider-reported NLV/cash/buying-power/margin/daily-P&L fields while keeping absent Settled Cash null, separated Manual subtotal from IBKR values, and exposed neither raw account id nor a fake overall net worth in API or DOM. The user's Gateway has one account, so multi-account/mixed-currency remains reviewed fake-backed evidence rather than manufactured live data. Slice 3 activity/journal remains pending. The next bounded work is the already adopted IBKR News partial-status Hotfix A, followed by its durable body-retry plan; neither reopens Portfolio authority.

- **2026-07-14 (Portfolio 1.1 SLICE 2 ACCOUNT OVERVIEW IMPLEMENTED FOR REVIEW — canonical A/B and responsive gates pass)**: Branch `codex/portfolio-1-1-overview` implements the additive provider-free account overview through code `3cbe753`: latest captured values per visible IBKR account, broker/canonical dual timestamps, provider-reported ET daily P&L, a strictly separate manual-account subtotal, safe account labels, complete account details, account-aware canonical positions, and the three completed Holdings tabs (`持倉 / 帳戶明細 / 同步紀錄`). The shipped Accounts/Currency-basis block was removed rather than duplicated; no overall net worth, unfinished activity placeholder, Gateway read, write route, scheduler, tool/prompt input, or order API was added. Automated evidence closes at backend focused `81`, frontend `44 files / 412 tests`, typecheck/build, no-PG `pg_attempts:[]`, static/privacy boundaries, and five browser widths. Clean sequential backend A/B at base `a0daf69` versus head `3cbe753` is exact: cases `4258 -> 4275`, passed `4147 -> 4164`, while both sides retain identical `30 failed / 74 skipped / 18 warnings / 7 errors` and empty bidirectional failure/error test-ID differences. The normal desktop sidecar was left running and was not displaced by a competing branch process, so one post-review real-Gateway single-sidecar gate remains; this is not merged or LIVE. Slice 3 activity/journal remains pending.

- **2026-07-14 (Portfolio 1.1 SLICE 2 PLAN REVIEW-CLEARED — implementation-ready, not started)**: Independent plan review verified the grounded backend/frontend seams and exact `+17/-0` backend, `+18/-0` frontend ledgers, then found one paired plan/test omission: the shipped Accounts status-grid would otherwise survive outside the new tabs with duplicate aggregate toggles and a mixed-account `Currency basis` summary that contradicts the approved manual-only aggregate. Task 6 now explicitly replaces that whole block with `PortfolioAccountSummary`, removes `currencySummary()`, and strengthens the existing toggle test to prove the old metric is absent and each fixture account has exactly one toggle; collection accounting is unchanged. The plan is implementation-ready. The currently approved sequencing interruption remains IBKR News Hotfix A followed by its bounded durable-body-retry plan; Portfolio Slice 2 product code has not started.

- **2026-07-14 (IBKR NEWS PARTIAL-STATUS HOTFIX + DURABLE BODY RETRY DESIGN ADOPTED — written review pending)**: Live inspection explained the repeated `部分完成（待補抓 0）`: the latest run really saved 16 new articles and 16 bodies, while ten known bodies remained under the bounded 10172 six-hour retry policy; the frontend falsely applied the price-backfill `deferred[]` label to the sanitized IBKR count-only continuation. The immediate frontend-only Hotfix A renders actionable ticker continuation separately from non-actionable body counts and never invents zero. A deeper reliability trace found that known-body retries still depend on the article reappearing in IBKR's rolling 300-headline response because article IDs intentionally do not cross sanitized worker stdout. The approved follow-up derives a bounded due-body queue directly from normalized SQLite inside the child worker, keeps IDs out of parent state/API/logs, preserves fresh-headline work with an independent budget, and separates run outcome from scheduled retry backlog. This closes retries for known metadata only; headlines never observed after a long provider-tail gap remain unrecoverable under the existing runbook boundary. Sequence: written review -> Hotfix A independently -> durable-retry plan -> resume Portfolio 1.1 Slice 2 implementation. Authority: `docs/superpowers/specs/2026-07-14-ibkr-news-partial-retry-design.md`.

- **2026-07-14 (Portfolio 1.1 SLICE 2 ACCOUNT-OVERVIEW PLAN OPENED — review pending, implementation not started)**: The approved Slice 2 outcomes are now grounded into a seven-task RED-first plan at `docs/superpowers/plans/2026-07-14-portfolio-1-1-slice-2-account-overview.md`. The additive design joins every non-archived account to its latest persisted broker snapshot and canonical position-sync time without any Gateway call; reuses the shipped totals algorithm for included manual accounts only; emits a daily P&L total only when both provider legs exist; redacts raw-id legacy labels; and exposes one new `GET /portfolio/overview` read. Holdings gains the always-visible account summary plus the three completed tabs (`持倉 / 帳戶明細 / 同步紀錄`); the locked `活動` position remains unrendered until Slice 3 rather than shipping a disabled placeholder. Optional cross-IBKR aggregation, performance claims, activity/fills, new mutation paths, Data Sources controls, tools/prompts, and order APIs remain excluded. Grounded baseline is backend full collection `4258` and frontend `43 files / 394 tests`; reviewed-plan accounting is backend `+17/-0` and frontend `+18/-0`. Next gate is plan review; do not implement or merge yet.

- **2026-07-14 (Portfolio 1.1 SLICE 1 MERGED / LIVE — capture foundation closed, Slice 2 next)**: Independent canonical virgin A/B at base `281382d` versus reviewed head `0aa161e` closed the final gate with identical `30 failed / 74 skipped / 18 warnings / 7 errors`, empty bidirectional failure-set diff, and passed `4074 -> 4147`, exactly matching backend collect `+73/-0`; frontend independently passed `43 files / 394 tests`, typecheck, production build, no-PG `pg_attempts:[]`, and the order-API/PG/raw-id/duplicate-scheduler/FK/reader-factory gates. The user delegated local integration, so `master` fast-forwarded through docs tip `fa052dc`. The real-Gateway evidence and RED-first live fixes remain as recorded in the immediately following review-ready entry: `account="All"` and `Currency=BASE` no longer create a pseudo-account/false partial, and legacy account labels cannot leak the raw broker id into agent payloads. Slice 1 now accumulates append-only account, position, execution, commission, correction, and manual-adjustment facts from activation while preserving Holdings V1 authority and read-only capture. This does not mark all Portfolio 1.1 live: next is a separate Slice 2 account-overview implementation plan; Slice 3 activity/journal UI remains pending.

- **2026-07-14 (Portfolio 1.1 SLICE 1 IMPLEMENTED FOR REVIEW / LIVE GATE PASS — canonical reviewer A/B pending)**: Branch `codex/portfolio-1-1-capture` completes the capture foundation through code tip `34c50e4`: non-cascading observations and manual-adjustment journal, one read-only `portfolio_capture=+70` reader, serialized startup/scheduled/manual service, redacted additive APIs, and the Holdings-owned run/review controls. Automated gates close at `173` focused backend tests, full collect `4185 -> 4258` (exact `+73/-0`), frontend `41/366 -> 43/394` (exact `+28/-0`), typecheck/build, no-PG `pg_attempts:[]`, five responsive viewports, and zero order/PG/duplicate-scheduler matches. Real Gateway validation used one sidecar and client id 71: 15-minute durable scheduling ran repeatedly; adjacent captures were idempotent while snapshots appended; run-48 review/apply preserved all nine note rows byte-for-byte; manual capture worked while periodic capture was disabled; restart retained the last run and recomputed next due as completion + 15 minutes. Live data exposed and RED-first closed two mock gaps: IBKR's `account="All"` aggregate plus `Currency=BASE` sentinel had created a pseudo-account/false partial (`f64cd4a`), and the agent tool dropped the raw-id field but leaked a legacy label containing it (`34c50e4`). The two pre-fix test runs/pseudo shell were assertion-guarded out of the real DB with canonical 9-position/9-note state and digest preserved. No trades occurred, so execution/commission/correction/unmatched paths remain fake-backed and were not manufactured; the real review account leaves auto mode test-backed. The implementation environment still reproduces the known `tests/test_api.py` lifespan hang, so no canonical A/B PASS is claimed. Stop for independent implementation review plus canonical virgin A/B; do not merge, mark the spec LIVE, or start Slice 2.

- **2026-07-13 (Portfolio 1.1 SLICE 1 PLAN REVIEW-CLEARED / IMPLEMENTATION NOT STARTED)**: Independent review verified the 8-task/49-checkpoint plan against the live seams and cleared it with no must-fix. One accepted should-fix is now explicit: the mounted legacy apply path deliberately stops treating a reappearing archived account as new, so it preserves archive state and user-owned account fields; a direct `tests/test_portfolio_ibkr.py` RED pin raises the backend ledger from `+70/-0` to `+71/-0`. The external claims that `connectAsync()` already awaits executions and that the current client-id base lacks the required upper bound were independently verified; the archived-account reset chain was also reproduced end to end. Client-id base 0 remains compatible in this slice because capture uses effective id `base+70`, never special IB client id 0; the two-second PnL wait remains a live-gated best-effort boundary. Implementation remains not started and must stop review-ready.

- **2026-07-13 (Portfolio 1.1 WRITTEN SPEC APPROVED / SLICE 1 IMPLEMENTATION PLAN OPENED)**: Written review approved the complete dual-layer observation design without reopening Holdings V1 authority. Slice 1 planning is now the only promoted implementation unit: append-only observation storage and reconciliation, atomic manual-adjustment journals, one read-only `portfolio_capture=+70` Gateway reader, startup/scheduled/manual capture, guarded run/config APIs, and a minimum Holdings-owned sync-record control surface. Account overview and activity/annotation UI remain Slice 2/3; `portfolio_activity_annotations` therefore lands with its first consumer in Slice 3 rather than as an unused Slice 1 table. Data Sources receives no duplicate Portfolio scheduler, capture remains isolated from research/tools and all order APIs, and implementation waits for review of `docs/superpowers/plans/2026-07-13-portfolio-1-1-slice-1-capture-foundation.md`.

- **2026-07-13 (Portfolio 1.1 DESIGN APPROVED / WRITTEN SPEC OPENED — observation first, not event sourcing)**: The user approved the six-section design and a three-slice sequence at `docs/superpowers/specs/2026-07-13-portfolio-1-1-observation-activity-design.md`. Locked architecture: shipped Holdings tables remain canonical current-state authority; a non-cascading append-only `profile_state.db` observation layer records account snapshots, full broker position observations, immutable executions, commission revisions/corrections, unmatched changes, and manual financial adjustments from first successful capture onward. Gateway capture is one read-only `portfolio_capture=+70` service with startup catch-up, 15-minute default configurable 5-1440, and manual trigger; captures account/execution/position legs independently, commits valid observations before separately applying a complete position leg under existing sync-mode rules, and never rebuilds positions from an incomplete event stream. Review pins include derived-at-read `ibkr_review` diffs, explicit five terminal run states plus stale-running reconciliation, `execId`/`permId` identity, correction-aware residual matching, unique unmatched windows, finite-number discipline, raw-account-id exclusion, and static order-API bans. User selected the Holdings-first tabbed UI: per-account IBKR NLV/cash/margin/daily P&L; a separate manual-account subtotal; activity with grouped/expandable fills and explicit coverage gaps; and Portfolio schedule controls owned only by Holdings. Future ArkScope trading remains intended but is a separate permission/client-id/order-audit line, not an upgrade of capture. Gate: written-spec review -> Slice 1 implementation plan; do not open Slice 2 or P2.8 shell work first.

- **2026-07-13 (P2.8 bounded Settings stabilization LIVE COMPLETE / MERGED — FF through `ca3db2f`)**: After the implementation re-review closed durable-`skipped` truthfulness and authored scheduler-copy leakage, reviewer canonical virgin A/B (`554e94b` versus `cef39e6`) passed with identical `30 failed / 4074 passed / 74 skipped / 18 warnings / 7 errors`, empty bidirectional failure diff, and backend collect `+0/-0`; the final branch tip added only the canonical evidence ledger. The user delegated the local integration decision, so `master` fast-forwarded `332a92b..ca3db2f`. Post-merge verification passed `41 files / 366 tests`, typecheck, production build (existing chunk warning only), and no-PG `24/24` with `pg_attempts:[]`. The design and plan are LIVE; user-facing `risk appetite` is standardized as `風險意願` while `risk capacity` remains `風險承受能力`. Next promoted unit is Portfolio 1.1 design (cash, net liquidation/account value, realized P&L, and transaction effects); P2.8 Slice 2 does not preempt it.

- **2026-07-13 (P2.8 bounded Settings stabilization IMPLEMENTED FOR REVIEW — canonical reviewer A/B pending)**: Branch `codex/p2-8-settings-stabilization` completes the authorized frontend-only repair in six RED-first task commits plus review fix `fe83011`: truthful real/indeterminate scheduler progress; pure Data Sources common-state mapping without synthesized `interrupted`; disabled provider/schedule neutral text and durable-history separation; five explicit wide-table scroll owners; removal of rendered source badges, two vestigial News toggles, and migration/storage narration while preserving API/profile/env compatibility; and `風險胃納` -> `風險意願` with schema/prompt behavior unchanged. Independent review found and RED-first closed two gaps: durable writer-lock `skipped` had been mislabeled as `尚未執行`, and authored scheduler help exposed `job_runs`/`data/locks`/app-CLI internals. Durable skip now renders neutral `上次已跳過` without a state badge; the help text retains only the one-job-at-a-time user contract. Gates closed at `9 files / 63` focused tests, `41 files / 366` full frontend tests (exact `+19/-0`), typecheck/build, backend/API byte boundary, static semantic ratchets, no-PG `24/24` with `pg_attempts:[]`, and real-Chrome 1440/1024/961/959/390 checks with no overlap or page overflow. Canonical single-process base pytest reproduced the known FastAPI `TestClient`/lifespan hang in this Codex environment and no canonical pass is claimed. A credential-scrubbed virgin file-isolated fallback is strictly equal base/head: `4185=4185` collected, `3894 passed / 27 failed / 1 error / 73 skipped / 20 warnings` on both completed subsets, identical problem sets and normalized per-file results, and identical four timeout files (`test_agents.py`, `test_api.py`, `test_monitor.py`, `test_signal_factors_p1.py`). Stop for implementation re-review plus reviewer canonical A/B; do not merge yet. Sequence remains stabilization merge -> Portfolio 1.1 -> P2.8 Slice 2.

- **2026-07-13 (P2.8 bounded Settings stabilization PLAN REVIEW-CLEARED — implementation authorized)**: Plan review found one real spec contradiction: the draft mapped provider-disabled and generic scheduler-skip states to `interrupted`, while the approved surface spec explicitly forbids synthesizing that state and the canonical meaning is cancelled/interrupted work. Corrected contract: disabled provider/schedule = muted domain text; duplicate already-running skip = `blocked`; other real skip families (Gateway busy, writer-lock busy, pending manual continue) remain muted domain text. Skip presentation and durable history now have separate pure mappings, so a transient skip cannot overwrite the prior durable outcome. The copy-test observation was partly inaccurate (`本地市場資料庫` really is the current heading), but coverage was incomplete because `本地市場庫` is a separate live label; scoped RED assertions now pin both plus all authored migration strings across the four enabled owner functions while preserving `config/.env` provenance and excluding App Records/compatibility code. Test accounting remains `+18/-0`; Task 0 is authorized.

- **2026-07-12 (P2.8 bounded Settings stabilization IMPLEMENTATION PLAN DRAFTED — review pending)**: The review-cleared design at `docs/superpowers/specs/2026-07-12-p2-8-settings-stabilization-design.md` is translated into a frontend-only, RED-first plan at `docs/superpowers/plans/2026-07-12-p2-8-settings-stabilization.md`. Planned units are: truthful known/indeterminate source progress; pure provider/SA/schedule common-state mapping with no synthesized `interrupted` state (disabled provider/schedule = neutral text, duplicate already-running skip = blocked, other skipped attempts retain muted domain text); Data Sources wiring with no storage-route badges; explicit scroll ownership for provider/SA/FRED/config/schedule tables; normal Market/News/Macro/Data Sources copy with both migration-era News checkbox branches removed from rendering while API/profile/env/status compatibility stays byte-identical; and `風險胃納` -> `風險意願`. Baseline is `38` frontend files / `347` tests; plan contract is `+18/-0` and backend `+0/-0`. Sequence remains stabilization merge -> Portfolio 1.1 design -> P2.8 Slice 2/3/4/5.

- **2026-07-12 (P2.8 bounded Settings stabilization DESIGN APPROVED / REVIEW-CLEARED — mixed sequence adopted)**: User inspection after Slice 1 found four distinct facts rather than one unfinished-layout problem: (1) Data Sources running progress puts current item/count/percentage into one no-wrap chip and covers adjacent information; (2) SA extension and FRED fixed-layout tables have no scroll owner and overlap at constrained widths; (3) enabled Market/News/Macro/Data Sources copy still narrates PG/SQLite/local/legacy migration history after PG-EXIT closed; (4) Investor Profile's `風險胃納` is not acceptable user language and becomes `風險意願` while `風險承受能力` remains. Adopted bounded design: user-selected two-line real progress with truthful indeterminate fallback; existing StatusBadge and surface-level common-state mapping; scroll-owner/min-width/wrapping repair without wholesale DataTable migration; normal copy preserves actionable source ownership but removes storage/migration narration; News Ingestion becomes a concise news-data status surface without deciding future consolidation; both migration-era News checkbox branches are deliberately removed from normal rendering while profile/env/API/status compatibility remains untouched; disabled schedules use muted text rather than a fabricated common-state badge; enabled directory descriptions are included, unreachable App Records is excluded. Startup was measured, not guessed: fresh scheduler-disabled sidecar probes show median ready time about 2.24s at `91ca631` versus 3.11s now (145→158 routes; eager card/Research/provider SDK imports dominate), but the user accepts the roughly one-second increase and defers optimization/loading-screen work. Sequence ruling: stabilize these UI debts first, then open Portfolio 1.1 (cash/net value/realized P&L/transaction effects), then return to P2.8 Slice 2/3/4/5. Authority: `docs/superpowers/specs/2026-07-12-p2-8-settings-stabilization-design.md`; implementation plan is authorized.

- **2026-07-12 (P2.8 Slice 1 UI primitives MERGED / ALL GATES CLOSED — FF through `3449e32`)**: Final code tip `df9c39b` establishes the first-consumer-depth UI authority: one JSON token source (960px shell overlay breakpoint, <=8px general frame radius), compact Button/IconButton/PageHeader/status/alert primitives, transient Drawer, consequence-aware ConfirmDialog, stage-owned BoundedProgress, and typed DataTable row actions/inline expansion. Holdings migrated without payload/domain changes and repaid only its `window.confirm`; Investor Profile received presentation/state/class repair only. Whole-branch review found and RED-first closed multiple-open row menus, missing Profile mutation state, narrow proposal overflow, cancellation-copy drift, and a real-Chrome focus timing hole hidden by jsdom; final re-review was GREEN. Reviewer canonical virgin A/B (`820d1b3` versus `c7d9c41`) completed with identical `30 failed / 4074 passed / 74 skipped / 18 warnings / 7 errors`, empty bidirectional failure diff, and backend collect `+0/-0`. Frontend closed at `38 files / 347 tests`, typecheck/build and no-PG smoke passed, static boundaries remained clean, and required 1440/1024/961/959/390 visual checks passed. Authenticated DesignSync plan `plan_f9c9ea59626141ed_14c603dcda45` reconciled `radius-lg` to 8px and added seven primitive specimens with read-back. User approved the local fast-forward merge; next P2.8 unit is Slice 2 shell work, not yet opened.

- **2026-07-12 (P2.8 Slice 1 plan REVIEW-CLEARED — isolated TDD implementation authorized)**: Review found and the plan now closes one paired implementation/test defect plus four interaction edge cases before code work starts: Drawer close visibly invokes `onClose` and restores trigger focus; polling elapsed time is no longer an `aria-live` region; stacked overlays respect `defaultPrevented`; busy confirmation cannot be escaped around its disabled Cancel action; and DataTable placement tests install their geometry stub before the layout effect runs. Shared Holdings layout classes are explicitly promoted into the `ui-*` namespace, while every native command button and the dead `btn-primary`/`btn-secondary`/`table-wrap` tokens must disappear during migration. The exact frontend ledger remains six new test files and +46 tests (`32/296` to `38/342`). Behavioral A/B stays anchored at approved-design base `4e229a8`; the implementation worktree starts from the review-cleared master tip so its plan and ledger travel with the branch. Stop review-ready; merge still requires implementation review and canonical A/B.

- **2026-07-12 (P2.8 Slice 1 UI primitive plan OPENED — implementation waits for review)**: Plan `docs/superpowers/plans/2026-07-12-p2-8-slice-1-ui-primitives.md` targets only the minimum foundation plus the two audited first repairs. One JSON token authority supplies TypeScript behavior and pre-mount CSS variables; shell overlay classification is exactly 960px with 961/959 gates; modal/overlay radius is not granted an exception and stays at or below 8px. Focused primitives are compact controls/PageHeader, nine machine-state presentations over the common eight-state vocabulary, transient Drawer, consequence-aware ConfirmDialog, compact stage-owned BoundedProgress, and typed DataTable row actions/inline expansion. Holdings is the first full consumer and repays its `window.confirm`; Investor Profile receives presentation/state/class repair only, with Slice 5 semantics explicitly excluded. Frontend baseline was re-run at `32 files / 296 tests`; the plan's exact ledger is six files and +46 tests (`38/342`). Static gates freeze App/Research/Settings/api/styles/backend, retain four known legacy confirm owners, prohibit copied shell breakpoints/radius exceptions/hard delete, and require class coverage. Next: plan review; no implementation or branch merge is authorized yet.

- **2026-07-12 (P2.8 canonical shell WRITTEN APPROVED; Claude Design companion synchronized; Slice 1 planning authorized)**: User spot-check passed the committed canonical spec `02ccbf3`. The external "ArkScope Design System" then completed a three-file conformance sync (plan `dfd45b4f7dbb`): `readme.md` now records repo-spec authority and resolves the topbar/nav/right-rail rulings; `tokens/spacing.css` records the sole 960px shell breakpoint and 8px general-frame cap; new `guidelines/shell-contract.html` summarizes the four navigation groups, normal/developer topbar, eight common states, primitive set, and mechanical hard rules. One reconciliation is deliberately left to Slice 1: the old `radius-lg: 10px` token is not an authorized modal exception and must not enter app primitives. Screen examples remain incremental slice outputs. Next gate: reviewed Slice 1 implementation plan, then isolated TDD implementation; no code implementation is authorized by this entry alone.

- **2026-07-12 (P2.8 bounded UI audit + canonical shell design discussion COMPLETE; written review gate open)**: Objective audit covered the merged real-data Home/Watchlist/Research/Holdings/News/System/Ticker/Settings surfaces and found structural, not cosmetic, debt: permanent dev diagnostics, planned disabled nav controls, an empty global rail, fixed Research side columns, undefined Holdings and Investor Profile class families, a 3,839-line Settings owner, browser confirmation sites, raw-error debt, and reports with no GUI home. Seven user-reviewed design sections now lock the target in `docs/superpowers/specs/2026-07-12-p2-8-canonical-shell-interaction-design.md`: compact progressive disclosure; normal vs Developer shell modes; final Explore/Research/Monitor/System navigation without planned placeholders; transient/pinnable drawers; stage-owned bounded progress including the server-confirmation grace state; Research-first global work notification with owning surfaces retaining result authority; adaptive Research plus deterministic history and per-run provider/model/effort selection; one-page grouped/expanded Settings; summary-first Investor Profile with scenario+dialogue calibration and approved-profile-only injection; separate NavigationTarget vs persistent Reference contracts; a 960px shell breakpoint; and six bounded implementation slices. Reports IA and the Reference store remain Notes V1-owned; Intraday Behavior remains its independent research feature. **Gate**: written spec review → Claude Design kit sync → Slice 1 implementation plan. No UI implementation or plan is authorized yet.

- **2026-07-12 (Subscription-backed cards + fixed-task runtime LIVE COMPLETE; FF merge approved; next queue ruled)**: Reviewer canonical A/B for the final stack passed with identical failures `30=30`, passed `4042→4074` (`+32` exactly), and flat `74/18/7` skip/warning/error counters. Fresh closeout reran backend `232`, frontend `296`, typecheck/build, stale-bound gates, and no-PG `24/24` with `pg_attempts:[]`. Real single-sidecar proof now covers both subscription transports: Claude Sonnet 5 `max` completed the MU card and first `zh-Hant` translation under independently persisted `900s` DB bounds with no managed child left; ChatGPT OAuth `gpt-5.4-mini/low` generated AAPL `run_id=7` and wrote its first translation (`cached=false`) with no billing/model/effort fallback. The original Sonnet 5 `max` routes were restored exactly, while AI Research kept its separate runtime authority. Parent subscription-card-routing and fixed-task-runtime plans are both `LIVE COMPLETE`; user approved local FF merge. **Queue ruling after merge**: bounded P2.8 current-UI audit/canonical shell spec → Notes V1 → Alerts V1 → Track B skills. The audit excludes a visual rewrite and component implementation; P2.5/`DEVELOPMENT.md` may be filler, while P2.9 usage observability, IV, and Phase C remain deferred.

- **2026-07-12 (AI execution usage observability DESIGN ADOPTED, implementation deferred)**: The fixed-task live gate exposed an observability gap: Sonnet 5 card generation and translation both completed under the new 900-second bound, but the structured subscription adapter discarded SDK `duration_ms`/usage metadata, preventing exact post-run comparison. Audit confirmed this is partial prior work, not a greenfield idea: `TokenTracker` already covers agent loops, Research persists usage and elapsed time, desktop carryover preserved per-query/cache indicators, and an earlier UI cleanup explicitly deferred token monitoring; however no app-wide daily authority exists. User adopted a metadata-only, all-call hierarchy: one root execution per user/system behavior, independently recorded provider call/retry events, child executions for subagents, and new-but-linked executions for later translation/retry/regeneration. Classification is the three-axis `feature / operation / trigger`; missing provider metrics remain null; cache/reasoning stay separate; no prompt/response duplication. Future UI belongs under System with contextual Research/Card summaries. Pricing, dollar estimates, alerts, budgets, hard limits, backfill, and retention controls are deferred. Sequencing ruling: write the design and backlog now, but do not implement or open a plan until higher-priority unfinished product work permits promotion.

- **2026-07-12 (Fixed AI task runtime limits IMPLEMENTED FOR REVIEW — canonical A/B pending reviewer)**: Branch `codex/card-oauth-routing` replaces the card/translation 90/210/300-second constant chain with one code-reviewed fixed-task registry and independent DB-backed limits (`card_synthesis`, `card_translation`; default 900s, range 60-3600, `env > db > default`). Routes resolve before work, all API-key and subscription paths receive an explicit task timeout, API-key SDK clients use request-scoped `max_retries=0`, and timeout returns a typed `model_timeout` envelope without effort/model/provider fallback or partial persistence. Settings now exposes both task limits with independent authority badges; App's existing runtime snapshot drives browser waits at effective seconds +60, including old-sidecar 900s compatibility. TDD commits through `c311b23`; focused backend `232 passed`, frontend `32 files / 296 tests`, typecheck/build green, stale-constant and explicit-call-site gates clean, no-PG smoke 24/24 with `pg_attempts:[]`. Virgin collect is `4153→4185` (`+32/-0`); this Codex environment's canonical base run reproduced the known single-process TestClient/lifespan hang, so reviewer A/B remains mandatory and no A/B pass is claimed. Next: reviewer checkpoint, then the one-sidecar Sonnet-5 `max` live gate; do not merge or close the parent subscription-card-routing follow-up before both pass.

- **2026-07-12 (Fixed AI task runtime limits plan OPENED — spec review hardened the true-bound contract)**: Live subscription-card validation showed that timeout policy is a product/runtime setting rather than a safe constant: the same MU five-item EvidencePacket completed on Claude Sonnet 5 `low` in 31.6s with 13 cited claims, while `max` exceeded both 90s and 210s. AI Research already has its own DB-managed `session_timeout_s` (default 900s); cards did not. Adopted spec `docs/superpowers/specs/2026-07-12-fixed-ai-task-runtime-limits-design.md` adds provider-agnostic per-task `model_timeout_s` for card synthesis and translation, each defaulting to 900s with a 60-3600 range. Review hardened four load-bearing points: API-key calls must combine the request timeout with `max_retries=0` so 900 seconds is a real bound rather than up to three SDK attempts; one code-reviewed registry owns fixed-task membership while reusing existing `TaskId` values; subscription adapter timeouts become required keyword-only arguments; and the stale fixed 210/300/240-second constant story must disappear. TDD plan `docs/superpowers/plans/2026-07-12-fixed-ai-task-runtime-limits.md` now covers the SQLite resolver, guarded atomic API, all four execution paths, typed no-partial timeout, Settings UI, derived browser wait, full A/B, and a real Sonnet-5 `max` gate. No effort/model/provider/billing fallback, no unlimited mode, and no mixing with AI Research runtime. Next gate: plan review, then implementation against base `c136316`.

- **2026-07-12 (Subscription-backed card routing IMPLEMENTED FOR REVIEW — transport repaired; runtime-limit follow-up opened)**: The selected task provider plus that provider's active credential now determine transport and billing for card synthesis and translation. Provider-matching ChatGPT/Claude OAuth paths use bounded structured-output adapters; API-key paths stay separate; no provider, credential, model, effort, or billing fallback exists. Live repair proved and fixed runtime HTTP-401 reauthentication classification, Claude's two-turn internal `StructuredOutput` exchange, inherited billing isolation, bounded preflight/cleanup, and exact-PID subprocess reaping (zero zombie after host canary). OpenAI re-login succeeded and then honestly reported Plus `usage_limit_reached`. Claude task canaries passed, but real MU `max` synthesis exceeded both 90s and 210s while the identical five-item packet completed at `low` in 31.6s. Timeout policy is therefore continued by the newer fixed-task runtime design entry above; this transport slice remains unmerged until that runtime implementation and the real max card/translation gate complete.

- **2026-07-12 (Models-UX LIVE COMPLETE — merged FF through `2fb1c4f`; OAuth Luna compatibility split out)**: Implementation review and canonical A/B were already green. The final user-run branch gate completed the required list-vs-execute pair after the model-specific effort/client-cleanup amendment: ChatGPT OAuth `gpt-5.6-luna` returned the redacted `provider_call_failed` surface in 906 ms, while `gpt-5.4-mini` returned `ok` in 1,589 ms. Fresh closeout verification: backend focused 187, frontend 284, production build green, no-PG smoke 24/24 with `pg_attempts: []`. This closes the focused Models UX slice because its contract is to distinguish discovery visibility, deterministic eligibility, and actual execution without hidden fallback. Separate non-blocking follow-up: current third-party ChatGPT OAuth requests lack the first-party Codex client identity/version contract implicated by open upstream reports; evaluate a gated compatibility canary versus routing subscription execution through the official Codex app-server/CLI. Until ruled, Luna remains "visible but actual call failed" under OAuth and works through the API-key path.

- **2026-07-12 (Models-UX live gate amendment — model-specific effort + OAuth client cleanup, re-test pending)**: User visual inspection passed the provider-first task cards. Live ChatGPT OAuth task tests reproduced `gpt-5.6-luna` `Model not found` for every selected effort while `gpt-5.4-mini` passed; the failure occurs at model resolution before effort validation and therefore does not prove that OAuth forbids effort. Code inspection found three real pre-merge defects: OpenAI capability entries shared one stale effort tuple (`minimal` everywhere, no GPT-5.6 `max`), ChatGPT OAuth silently coerced `xhigh|max` to `high` and omitted explicit `none`, and bounded canary teardown left its `AsyncOpenAI` client to GC, producing an unhandled `AsyncClient.aclose()` task after the event handler closed. RED-first amendment on `codex/models-ux`: current OpenAI entries now use model-specific sets (GPT-5.6 `none/low/medium/high/xhigh/max`; 5.4/5.5/5.2 without `minimal|max`; 5.2-Codex `low/medium/high/xhigh`), catalog/v2/Settings/Research/route validation all consume the selected model contract, `default` is visibly distinct from explicit `none`, OAuth sends the selected wire value unchanged, and stream ownership closes the execution client even when the consumer stops early. No silent model or effort fallback was added. Verification: backend focused 207, close-path `-W error` 72, frontend 284, typecheck/build, no-PG smoke 24/24 with `pg_attempts:[]`. Remaining gate: user live re-test on the branch, then final review/A-B/merge.

- **2026-07-12 (Models-UX implementation REVIEW GREEN on `codex/models-ux`; canonical A/B PASS, NOT MERGED, live gate next)**: Implemented the provider-indexed v2 effective catalog and derived legacy adapter, bounded `/config/model-task-test`, provider-first task cards, one grouped model selector, active-credential summary, dirty-only save veto, stale-result invalidation, and old-sidecar compatibility. The UI now separates provider choice, discovery visibility, deterministic task eligibility, and actual-call evidence; the advanced checkbox and duplicate manual override are gone. Verification: focused backend 126, frontend 281, typecheck/build, no-PG smoke 24/24 with `pg_attempts: []`, and desktop/mobile Playwright inspection. Reviewer canonical virgin single-process A/B is authoritative: failures `30=30`, passed `3948→3995 = +47`, collect `4059→4106` (`+47/-0`), all other counters flat. A review gate caught production module `model_task_test.py` being collected by pytest; RED pin plus rename to `model_task_canary.py` removed the phantom test. Implementation review had no must-fix. Two rulings were absorbed: successful discovery may disable an advanced entry omitted from that credential's listing (`model_not_visible`, with seed/unknown and route-pin exceptions), and the equivalent never-discovered copy/action is accepted. Remaining pre-merge work is only the plan §6 live gate.
- **2026-07-11 (Models-UX implementation plan OPENED — provider-indexed effective view v2 + task-scoped test + task-card rebuild)**: Plan `docs/superpowers/plans/2026-07-11-models-ux-implementation.md` (Claude authors+implements, user reviews) against the approved spec. Grounding pass corrected one spec mechanism (§4.4 `--tools ""` → the Claude bound is Agent-SDK options `tools=[]` + empty `mcp__ark__*` allowlist when `registry=None`; both subscription drivers already accept `max_turns/timeout_s/registry` through the factory — no driver changes needed). Key design rulings encoded: **v2 is the single computation source and the P2.7 task-level verified/advanced becomes a DERIVED alias** (the ten existing test_model_effective pins stay green untouched = the adapter regression net; alias removal = separate gated slice); route-pinned unknown models are `eligible=true` + `model_not_in_registry` warning (acceptance-9 ruling: current route never becomes unselectable); provider-level veto (`missing_active_credential`/`task_auth_mode_unsupported`) split from per-model `task_capability_missing`; task-test endpoint is NEW (`/config/model-task-test`, backend resolves the active credential itself) with a dispatch matrix whose OAuth-research arm is a bounded canary (max_turns=1, registry=None, ≤45s, tool event = failed canary → `task_test_unsupported`) and zero-call arms proven by construction-counting fakes. Frontend: §4 fixed card order, checkbox + duplicate manual selector REMOVED with an exact absence test, old-sidecar compat mode pinned. Four P2.7 UI tests named as obsolete siblings with intent-preserving replacements. Live gate pre-pins the S3 discovery: task-test `gpt-5.6-luna` via chatgpt_oauth must yield `provider_call_failed` while `gpt-5.4-mini` passes — the list-vs-execute pair rendered honestly.
- **2026-07-11 (S3 credential-lifecycle hotfix SHIPPED — merged FF `e7e144b`; §7 live gate passed; ai_research route → gpt-5.4-mini)**: I implemented (roles per plan), the user ran two implementation review rounds on top of the three plan rounds. Round 4: 1 BLOCKING + 2 must-fix + 1 should-fix — the blocker was real (discovery could commit a stale listing AFTER a re-login/delete and resurrect old-account entitlement, user-repro'd both ways); fixed with a **per-credential lifecycle epoch** in the discovery cache (`model_discovery_epochs`, bumped by every `delete_scope` in-transaction, validated by `record_run(expected_epoch=…)` INSIDE the write transaction — closes this hazard cross-process, beyond the process-local lock). Round 5: 3 must-fix + 1 should-fix, all user-repro'd — epoch capture failing OPEN (`expected_epoch=None` disabled the guard → now capture failure skips the cache write entirely), rollback trusting keyring's collapsed `delete()→False` (now verify-read: still-loadable/unreadable = unproven "may still hold"), cancel's `manual_completable=False` clobbered by the cancel-woken wait thread (flag now monotonic), and a poll-abort race (re-check after the await). Discipline ledger: the round-4 A/B's exact accounting (+38 vs announced +40) exposed two delete-route behaviors implemented WITHOUT tests — handled by reverting the code, proving both tests RED, restoring, GREEN. Final A/B `97f9919`→`d85d8e8`: failure sets identical 30=30, passed +45 = exact collect diff. **§7 live gate (real profile DB + keyring, single sidecar, user's real browser auth): all five steps PASS** — local:7 re-login in place (same id, alias/active preserved, both token hashes replaced, expires renewed to 07-21, epoch 1, cache zero), fresh discovery live 7 backend models, **research smoke through the subscription backend PASS with gpt-5.4-mini (S3 live P1/P2 CLOSED; the honest-copy claim stands)**, delete cascade clean with epoch bump on a zero-cache credential. Live finding feeding the Models-UX spec: the ChatGPT backend LISTS `gpt-5.6-luna` but execution rejects it — a real list-vs-execute divergence, the exact 「此登入可見 ≠ 實際呼叫通過」 split §3.4 designs for. Post-merge ruling applied: ai_research route `gpt-5.6-luna`→`gpt-5.4-mini` with local:7 kept active (subscription-first, no accidental API spend). Next: the focused Models-UX P2.8 slice (spec approved, implementation plan to open).
- **2026-07-11 (Models routing UX design drafted + review round 1 absorbed — provider-first without moving credential ownership)**: User live inspection identified the concrete P2.7 picker failure: four OpenAI entries appear normal because all three routes currently point to OpenAI, while Anthropic is discoverable only through a collapsed manual override; the advanced checkbox and API-key-only `測試此模型` make discovery visibility, task eligibility, and actual execution look like one concept. The user approved the direction. Draft spec `docs/superpowers/specs/2026-07-11-model-routing-settings-ux-design.md` keeps login/activation/re-login and detailed credential discovery in Providers, but makes Models task cards explicitly provider-first, shows the chosen provider's active credential read-only, replaces the checkbox + duplicate selector with one grouped selector, and distinguishes `此登入可見` / `可供此任務使用` / `實際呼叫通過`. Review round 1 fixed the two under-specified contracts: thinking is registry-derived read-only behavior beside effort (no route-schema change), and every model entry now carries status + eligible + reason + thinking mode, with ineligible rows visible but disabled. It also pins frontend-only missing-credential save blocking, bounded one-turn/no-tools OAuth canaries, seed-safe `model_not_visible`, and old-sidecar degradation. No per-task credential binding, automatic recommendation flip, or hidden billing fallback. Sequence remains S3 lifecycle hotfix first, then this focused P2.8 slice.
- **2026-07-11 (S3 credential-lifecycle hotfix plan REVIEW-CLEARED after round 3 — chatgpt_oauth re-login in place + fail-closed delete cascade)**: P2.7 live verification surfaced local:7's dead token; the user triaged the credential lifecycle and found the real gaps: no re-login exists (`OAuthStartRequest` = make_active only; `complete_login` always `add_oauth_credential` → duplicate row), delete orphans token-store + discovery-cache state, dead-token errors are free text, and Settings/caller/test commentary still claims Research is unwired while query.py:208 routes it through `_openai_subscription_stream`. **Scope ruling (user): re-login is chatgpt_oauth-ONLY** — anthropic claude_code_oauth remains add-only setup-token import; shared delete covers both providers. Plan `docs/superpowers/plans/2026-07-11-s3-credential-lifecycle-hotfix.md` (Claude authors+implements, user reviews) passed the round-3 re-check with no open findings: D1 same-id target threads through both completion paths; D2 refresh/relogin/delete share one **process-local** lifecycle lock, compensation after metadata failure is explicitly best-effort under double storage failure, and live re-login requires exactly one sidecar; D3 cache invalidation is required before token replacement; D4 only a real stored-token absence on an otherwise wired OAuth credential becomes `reauth_required`, while missing driver store/id is non-reauth wiring failure; D5 delete is fail-closed with successful `token_deleted: true|null`; D6 row/result 重新登入 shares the existing busy-excluded flow. Out-of-slice: durable needs_reauth flag; account-id fingerprints; full credential-scoped inter-process locking; broad Models UX is the separate P2.8 spec above. Task 1 is cleared to start.
- **2026-07-11 (P2.7 live verification — server-side COMPLETE, every contract holds on real credentials)**: Sidecar restarted on the merged build; I drove the round via the API. Discovery across ALL 7 credentials: openai api_key ×2 → `ok` with **129 vs 131 live models — per-credential entitlement differences are real**, validating the fingerprint-scoped cache; anthropic api_key ×2 → `ok` (10 models incl. fable-5/sonnet-5); anthropic claude_code_oauth ×2 → `seed_only` (all-seed classification, `cached: true`); openai chatgpt_oauth → **honest failure live** (token-refresh 401 re-login needed: `cached: false`, zero additive fields, error message [REDACTED]-sanitized). profile_state.db proof: exactly 6 rows — **the failed credential has NO row**, seed_only rows carry models=0, four distinct per-key fingerprints, oauth scopes pinned to fp="oauth". Effective view flipped [] → verified sol/terra/luna/gpt-5.4-mini for all three (openai-routed) tasks, advanced emptied, **pinned_only zero leak** (gpt-5.5/5.4/nano present in the discovered 129, rendered nowhere). All three badge polarities proven by route flip + exact restore: anthropic-routed task → `seed_only` + 4 `seed` + 2 `advanced` entries with verified staying EMPTY; unknown custom id `gpt-9-experimental` → advanced-only with `route` badge, gone after restore. Task 5C smoke: gpt-5.6-luna ok 2,895ms + claude-sonnet-5 ok 1,804ms (effort low accepted natively, fallback_effort null; **Fable smoke NOT run per ruling**). Observations filed: (a) route PUT accepts an unknown custom id with warning=null — `route_capability_warnings` doesn't flag not-in-catalog ids (custom ids allowed by design, picker treats them honestly as route pins; cosmetic); (b) chatgpt_oauth needs re-login (S3 line state). Remaining user-side: eyeball the Settings picker in the desktop app — Chrome extension wasn't connected for me to drive the UI; rendering is pinned by the 250-test frontend suite.
- **2026-07-11 (P2.7 SHIPPED — model capability registry + discovery cache + effective picker merged FF `7e4f5b7`)**: Implementation survived two review rounds before the user's GREEN verdict. Round 1 (6 must-fix + 2 should-fix, two user-repro'd): the legacy full-seed selector bypassed the verified-first picker → now a collapsed 手動覆寫 `<details>`; no catalog refetch after discovery → refetch wired; `resolve_active_credential` fabricated identity (first-available fallback + pool coerced to api_key) → active-only + real auth_type; dated discovery ids (e.g. `claude-haiku-4-5-20251001`) vanished from verified → classified via `capability_for(discovered_id)` keeping the provider's real id in the option; 129 registry-unknown openai models flooded Advanced → excluded except route-pin-with-badge; **refusal leaked into the effort-retry heuristic** (stop_details wording matched `looks_like_effort_error`, a second paid call fired) → `except AnthropicRefusalError: raise` ahead of the heuristic at both synthesis seams + `call_count == 1` pins. Round 2 (2 must-fix + 1 test gap, MF1 user-repro'd `pool_fingerprints_equal False`): resolver hashed the whole `OPENAI_API_KEYS` string while discovery's write side hashed the selected single key → resolver unified on `_resolve_api_credential` (pool keeps `api_key_pool` identity, fingerprint = selected key, cache round-trip test proves read-back); oauth route reported `cached: false` despite a landed cache write → `out["cached"] = True` set only in the record_run try/except-else, failure path pinned uncached with no additive fields; gap closed with a cross-provider manual-override interaction test + extracted `runDiscoveryAndRefreshCatalog` helper (2 unit tests). Final virgin A/B `e92778b`→`7e4f5b7`: failure sets identical 30=30 (both direction diffs empty), passed 3853→3901 = +48 = exact collect diff (48 added / 0 removed), skips/warnings/errors flat. Registry `src/model_capabilities.py` is now the single capability authority; MEMORY.md Active-Models rewritten as a registry pointer (2026-03-22 STALE banner retired). Remaining: live verification next session (sidecar restart → Settings discovery round per credential → per-task picker: verified-first / advanced toggle / 最後驗證可見 / seed_only badge on the active claude_code_oauth credential) + Task 5C smoke (gpt-5.6-luna $1/$6 + claude-sonnet-5 intro $2/$10; **Fable 5 smoke $10/$50 stays user-gated**).
- **2026-07-10 (P2.7 Task 0 verified + acked; five ruled fixes locked; three standing directions filed)**: Task 0 executed against official per-model pages + read-only live discovery (`b4e2ed4`): Fable 5 ($10/$50, 1M/128K, thinking always-on, refusal=HTTP 200 stop_reason contract), Sonnet 5 ($3/$15, intro $2/$10 → 2026-08-31, 1M/128K, thinking default-on with explicit-disable), gpt-5.6 Sol/Terra/Luna (1,050,000/128K; $5/$30, $2.50/$15, $1/$6; official alias `gpt-5.6`→Sol) — all five VISIBLE to the user's api_key credentials (anthropic 10 models, openai 129). User acked both tables and ruled: **Fix C adopted** (opus-4.8 compaction True per beta list), **Fix D adopted** (sonnet-4.6 max output 64K→128K per models table — docs are authority for existing-model facts), **Fix E adopted** (sonnet-4.6 effort tuple gains `max`; CLI inherits incidentally). Three standing directions filed: (1) **Context-management strategy audit (new backlog)** — official server compaction is opt-in convenience, NOT our context strategy: compacted context may need post-compaction re-search on complex problems, summaries are model-bound (switch = useless), and Claude Code/Codex pair provider compaction with their own management logic which we do NOT inherit — whether ArkScope's client-side ContextManager/memory layers are coherent with server compaction is an unanswered audit question. (2) **CLI surface = deprioritized/removal candidate** (user: app is the design; no CLI-only investment; consider full removal if CLI design constrains long-term features — Track B's CLI 收編 remains the nearer-term step). (3) **seed_only OAuth channels MAY borrow same-provider api_key entitlement as a badged import hint** (user proposal, deferred enhancement — entitlements differ per channel so it can only be a hint). Live note: the ACTIVE anthropic credential is claude_code_oauth → Anthropic picker will read seed_only until an api_key credential is made active. Task 1 (registry module) starts now; roles unchanged (Claude implements, user reviews).
- **2026-07-10 (P2.7 implementation plan opened — model capability registry / discovery cache / effective picker)**: Opened `docs/superpowers/plans/2026-07-10-model-capability-catalog.md` against the adopted P2.7 architecture (roles: Claude authors+implements, user reviews). Six tasks: (0) live-verify new-generation facts (Fable 5 / gpt-5.6 family) via provider docs + real discovery — **no id enters the registry from memory; unverifiable ids are dropped, never invented**; (1) `src/model_capabilities.py` pure-code registry (prefix-keyed, tier current/legacy, provenance per entry); (2) behavior-identical convergence of the 7 drift sites (equivalence table pinned per existing id; single-source grep gate; one ruled bug fix: CLI `get_effort_options("claude-opus-4-8")` None → opus tuple); (3) `model_discovery_cache` in profile_state.db (per provider+auth_mode+credential, replace-on-success / preserve-on-failure, no secret columns); (4) effective picker — verified = cache ∩ registry-current, legacy/custom/seeds into Advanced with badges, empty-cache keeps verified EMPTY with a discovery nudge, saved route model always selectable, `/config/model-catalog` changes additive-only; (5) new models land + CLI/discord/`agents/config.py` stale-default sweep + optional user-gated live smoke; (6) gates + full A/B + MEMORY Active-Models rewrite. Grounding surveyed before writing: both catalogs' exact contents, the five capability tables, discovery's live-only path, import directions (top-level registry is cycle-free), and the frontend-fixture isolation that keeps `/config/model-catalog` additive changes test-safe.
- **2026-07-10 (Backlog refined per gpt-5.6 Sol cross-review; fictional calibration profile DISABLED)**: Sol reviewed my A.5/P2.7/P2.8 closeout; I verified every code claim before adopting (all held: the three extra capability tables at `anthropic_agent/agent.py:102` / `openai_agent/agent.py:64` / `context_manager.py:58`; Settings.tsx exactly 3,245 lines; Alerts/Notes in NAV but not ENABLED; right-rail 規劃中 placeholder; topbar dev leaks; Vision header literally says NOT a spec). Actions: (1) **the fictional profile my A.5 live verification approved was disabled immediately** (`PUT {"enabled": false}` → `profile_active: false`, `effective_stance: off`; data preserved for reference) — Sol correctly flagged it was live-affecting research prompts, not test residue; user should recalibrate with real data when wanted. (2) **P2.7 re-architected and moved ahead of P2.8**: code-reviewed capability registry + per-credential DB discovery cache + effective picker (intersection), NOT a single DB authority; picker default adopted = verified-usable only, legacy/custom into Advanced-with-warnings; drift-site list extended from 4 to 7. (3) **P2.8 phase 1 recast as current-UI audit + canonical shell/interaction spec** (Vision demoted to intent source per its own DRAFT header), user irritation list still a required input; new surfaces (Track B/Alerts/Notes) wait for the unified shell primitives. Claude Design (claude.ai/design) ArkScope project sync was proposed by the user — DesignSync access needs re-auth (`/design-login`; the later `/login` dropped design scopes); ruling on timing: inventory the design project when access returns, substantive update rides with P2.8 phase 1 output rather than duplicating the audit.
- **2026-07-10 (Track A.5 calibration chat LIVE verified — two live-only defects found and fixed, `9bd47ea`)**: Ran the full calibration loop against a live sidecar (standalone-8420, OpenAI gpt-5.4-mini per the cheap-live-smoke convention): start session → describe style with a deliberate appetite/behavior contradiction → assistant asked targeted follow-ups (問而不薦 held) → structured proposal → approve → profile written. **Live-only defects the units could not see (responder fully mocked): (1) the system prompt showed one example value per field but never the allowed enum sets — two different models emitted `holding_horizon: "years"` and the whole turn 400'd; fixed by embedding exact allowed tokens in `CALIBRATION_SYSTEM_PROMPT` + a pin test asserting every PRESETS/STANCES/HOLDING_HORIZONS member appears verbatim in the prompt. (2) `parse_calibration_model_json` returned the NORMALIZED patch, which always contains the derived `risk_mismatch`; `create_proposal` re-validates and rejects that key — so no model could EVER produce an acceptable proposal (seam-mock lesson again: the mocked seam's real output shape was never fed to the real consumer). Fixed with strip-before-normalize (models also emit the banned key despite the prompt) + pop-after-normalize; the old parse-level reject pin updated to the tolerant contract (model value discarded, server derives on save — direct-API hard reject in `normalize_proposal_payload` unchanged and separately pinned.** Result quality was real: appetite 8 vs capacity 5 → server-derived `appetite_above_capacity`; the NVDA-drawdown insomnia anecdote became `drawdown_sensitive`; rationales quote the user's own words per field. Provenance verified: proposal `approved` + `approved_at` + 12-entry `changed_fields`; post-approve `profile_active: true`, `effective_stance: growth_opportunity`, populated context_preview (research-injection input armed). Full A/B on the fix: see evidence in this entry's commit. **Filed, not fixed**: responder-side near-miss robustness family (orphan user turns on rejected rounds ×2 in the journal — accepted artifact; misleading 400 code `invalid_calibration_message` when it is the MODEL's output that failed; assistant text lost when parse rejects); pre-existing hermeticity repro — `test_calibration_router_mounts_on_real_app` fails when its file runs standalone (Python 3.10 `asyncio.run()` clears the MainThread loop; first `ib_insync` import then dies in eventkit — concrete standalone repro of the import-time-loop fragility recorded 07-09; passes in full suite only by import-order luck). **Incidents**: the openai package was upgraded to 2.45.0 mid-day while the desktop sidecar kept running → stale-process ImportError (`deepcopy_with_paths`) on first calibration call; a fresh interpreter imports cleanly and `pip check` shows no agents-SDK conflict — restart resolves. My cleanup `pkill` also took down the user's desktop-managed sidecar (pid 806078; it was pre-upgrade stale and needed a restart anyway) — **user must restart the desktop app/sidecar before using calibration from the UI**. Note: model proposed `default_stance: growth_opportunity` unprompted (imitating the prompt example's field list) — acceptable because the form review/edit surface is where the user confirms, but worth watching in real use.
- **2026-07-10 (Holdings line LIVE COMPLETE — V1 + row actions both verified against the real Gateway)**: User completed the full live checklist after sidecar restart + frontend rebuild: IBKR-row notes/thesis/tags edited through the inline editor and **survived a real re-sync** (closing the Holdings V1 note-survival item); manual financial edits worked including blank-clears-avg-cost, while `abc` avg cost was blocked client-side (均價 error, zero requests); manual close required confirmation, disappeared from the default view, reappeared under 顯示已關閉 with the 已關閉 badge and **totals unchanged**; IBKR rows showed broker·synced with no close control. Both plan headers flipped to LIVE COMPLETE (`2026-07-08-holdings-portfolio-v1.md`, `2026-07-10-holdings-row-actions.md`). The holdings line (design → V1 → worker-loop hotfix → financial columns → row actions) is closed end-to-end. Backlog returns to: P2.5 provider capability display / P2.6 SA extension health surface part 2 / Track B skill suggestions (+ Track A.5 calibration live verification still pending on the user).
- **2026-07-10 (Holdings row actions MERGED — inline editor, manual soft-close, closed-row view; live verification pending)**: FF-merged `claude/holdings-row-actions` (`03e1277..fcdbd94`, +1,641/−53, 12 files) with explicit user approval — **roles reversed: Claude implemented, user reviewed**. User review caught 2 must-fix on top of a fully green build: (1) closed rows leaked into totals under `include_closed=true` (latent since the V1 repair round's totals-match-display choice, made reachable by threading `include_closed` through `GET /portfolio`; ruling superseded: **totals ALWAYS count open rows only, closed rows are display-only**) — fixed at the single choke point in `snapshot()` (open-subset before `_totals`) so tool + route inherit; the repair-round tool assertion pinning the old contract flipped 2→1; (2) non-numeric Avg Cost input (`"abc"` → `Number`→NaN → JSON `null`) silently cleared the stored value — client now finite-guards quantity (non-zero) and avg cost, only a truly blank input sends `avg_cost:null`. Fix `fcdbd94` verified by a 5-way workflow fan-out: adversarial refuter A ran 38 attacks on closed-totals (all 5 scope combinations × include_closed, both close paths, currency-bucket leak probe via closed EUR row, broker_base side-channel, byte-identical totals sweep) — zero refutations; refuter B confirmed the NaN guard (incl. `1e999`→Infinity rejected, whitespace-only trims to blank-clear avoiding the `Number("  ")===0` silent-zero trap, negative avg cost fails loud server-side); independent backend gates (48 passed, no-hard-delete, smoke `pg_attempts:[]`) and frontend gates (26 files/242 + typecheck + build). Final virgin A/B (03e1277 vs fcdbd94): failure sets IDENTICAL 30=30, passed 3833→3851 = +18 exact (17 first-round + 1 fix-round backend tests), warnings 18=18. Worktrees and branches removed. **Live verification pending (user): restart sidecar + rebuild frontend → edit an IBKR row's note/thesis/tags → 預覽+套用 a second sync → all three survive; edit a manual row's financials and clear avg cost once (blank) and try `abc` once (blocked with 均價 error, no request); close a manual row (confirm dialog) → disappears from default view → reappears under 顯示已關閉 with 已關閉 badge and totals unchanged; verify IBKR rows show broker·synced and no close button. Then flip the row-actions plan to LIVE COMPLETE and close the parent Holdings V1 note-survival item.**
- **2026-07-10 (Holdings row actions IMPLEMENTED FOR REVIEW — inline editor, manual soft-close, closed-row view)**: Branch `claude/holdings-row-actions` implements the row-actions plan in three TDD commits (RED verified before each implementation): store `update_position()` — one transactional presence-aware update (key absent = unchanged; explicit null clears `avg_cost`/`strategy_bucket`/`target_allocation`; `None` notes/thesis/tags = no-change, empty string/list clears) with typed `BrokerPositionManagedBySync` raised before any write when manual-financial keys hit a broker row, `update_position_notes()` kept as a compatibility wrapper, and `close_position()` tightened to manual-only + idempotent; routes — `PositionUpdateBody.updates()` via `model_fields_set`, PATCH maps KeyError→404 / managed-by-sync→400 / ValueError→`invalid_portfolio_position`, new DELETE `/portfolio/positions/{id}` = manual-only soft-close (write-gated, action:"close"), `GET /portfolio` threads `include_closed`; frontend — per-row 編輯 opens an inline uncontrolled-ref editor (user fields for every row; +5 financial fields only when `broker=="manual"`; blank Avg Cost serializes `avg_cost:null`), 關閉 with `window.confirm` renders only for open manual rows (IBKR rows show a broker·synced badge; closed rows show 已關閉), one 顯示已關閉 checkbox reloads `/portfolio?include_closed=true`, saves reload the snapshot (no optimistic mutation). Evidence: backend focused 47 passed (17 new: 8 store + 9 route); frontend 12/12 Holdings + full suite 26 files/241 passed + typecheck + build; static no-hard-delete gate zero matches; PG-unreachable smoke `ok:true` `pg_attempts:[]`. IBKR sync-survival test extended to assert notes+thesis+tags all survive a second broker snapshot. Full virgin A/B (03e1277 vs tip) pending in this entry's review round; branch stays unmerged for user review + live verification (edit an IBKR row's note → re-sync → note survives; close a manual row → reappears under 顯示已關閉).
- **2026-07-10 (Holdings row-actions plan opened — user fields for all rows, manual CRUD via soft-close)**: Live use confirmed the remaining Holdings lifecycle gap: notes can only be entered during manual creation in the UI, even though the guarded backend PATCH can update user-owned fields on any position; manual positions are insert-only after creation, and the existing store soft-close has no route/UI. Adopted follow-up contract: every row may edit notes/thesis/tags; manual rows may additionally edit symbol/asset class/quantity/average cost/currency and be soft-closed; IBKR rows must never expose manual financial-field edits or a manual close action because sync would overwrite/reopen them; closed manual rows remain inspectable through an include-closed toggle; hard delete and restore are deferred. The implementation plan is `docs/superpowers/plans/2026-07-10-holdings-row-actions.md`. It locks one atomic presence-aware PATCH (`model_fields_set`, explicit null clears nullable values), server-side broker ownership rejection, manual-only DELETE-as-soft-close, profile-state write gates, inline table editor tests, PG-unreachable smoke, and full A/B. This is a separate follow-up slice; it does not reopen Holdings V1 architecture, tool ledgers, IBKR connection behavior, or trading scope.
- **2026-07-10 (Holdings financial-columns fix MERGED — preview/positions now show cost & P&L)**: FF-merged `codex/holdings-financial-columns` (`6c46c6a`, frontend-only). Live UI verification surfaced that the preview table rendered only Change/Symbol/Qty (the `change.after` financial fields were captured but unrendered) and the positions table lacked an Avg Cost column; the local store was also still empty — the user had been reading the preview as if it were synced holdings. Fix: preview table gains Avg Cost / Market Value / Unrealized P&L with `before → after` deltas on updates, an explicit 「預覽結果，尚未寫入本地持倉；按「套用同步」後才會保存」 hint, positions table gains Avg Cost, and apply now clears the preview + reloads the snapshot so persisted rows appear immediately. Reviewer diagnosis had pinned the split beforehand (Gateway probe showed all 9 positions carrying avg_cost/market_value/unrealized_pnl — data layer complete; store empty; display-only gap). Verification: full frontend suite 26 files / 235 tests passed (zero collateral), typecheck + build green; backend untouched so pytest A/B is identical by construction. Remaining for LIVE COMPLETE: rebuild/restart the app surface, run preview (now with financials) → 套用 → confirm 9 positions persist with notes surviving a second sync → flip the plan header. Noted for later: account `base_currency` comes back None (accountSummary's default tag set lacks BaseCurrency) — harmless under v1 per-currency totals; IBKR options avgCost is per-contract (×multiplier) when options display lands.
- **2026-07-09 (IBKR worker-loop hotfix MERGED — shared `IBKRDataSource` now worker-thread-safe)**: FF-merged `codex/ibkr-worker-loop-fix` (`88713b5..d1680c3`). I verified the diagnosis both ways with a probe mirroring sidecar conditions (main-thread import, worker-thread call, real Gateway read-only): master reproduced `IBKRHoldingsUnavailable` exactly; hotfix returned **1 account / 9 positions** — re-run green after the review fix too. Review found one introduced defect via **A/B warnings accounting** (+4 `PytestUnraisableExceptionWarning`): `__del__` → `disconnect()` → `_release_owned_event_loop()` raised AttributeError on `__new__`-constructed instances (news-adapter test shape; also any post-`__init__`-exception GC) — fixed with a `getattr` guard + a pinned tolerance test (`d1680c3`). Final virgin A/B: failure sets IDENTICAL 30=30, passed +2 exact, **warnings 18=18 back to baseline**, unraisable=0. Bonus effect: the same fix un-breaks sidecar `get_current_quote`'s IBKR path (previously silently fell back to local bars in worker threads). **Fragility note (recorded, not actionable now)**: `import ib_insync` itself needs an event loop at import time (eventkit module-level `get_event_loop()`) — every process today first-imports it on the main thread (the sidecar via eager route import at startup); if that import ever goes lazy into a worker thread, the import-time path re-opens and `_ensure_event_loop` cannot save it. Remaining for LIVE COMPLETE: restart sidecar → full Holdings UI verification (preview should now list the real account/positions) → flip plan header.
- **2026-07-09 (IBKR AnyIO worker-loop hotfix IMPLEMENTED FOR REVIEW — live read probe green)**: The first merged Holdings preview exposed a shared `IBKRDataSource` runtime defect: FastAPI sync handlers run in AnyIO worker threads, while `ib_insync.IB.connect()` internally calls `connectAsync` through the current asyncio loop; Python provides no loop in those threads, so connection failed before `managedAccounts`/`positions`/`portfolio`/`accountSummary` and emitted an unawaited-coroutine warning. Branch `codex/ibkr-worker-loop-fix` adds an owned temporary event loop only when the calling thread lacks a usable loop, releases it on disconnect or connection failure, and pins the lifecycle with a real worker-thread test. Focused IBKR/holdings/quote suites: 46 passed + 8 intentional skips. A direct read-only probe using the existing S-J provider config and `holdings=+60` client id reached the real Gateway and returned 1 visible account / 9 positions; no DB write and no order API. This shared fix also prevents current-quote and other synchronous IBKR consumers from hitting the same hidden worker-thread failure. Pending: review/merge, sidecar restart, then Holdings preview→apply→note-preservation UI verification before marking the parent slice LIVE COMPLETE.
- **2026-07-09 (Holdings + Portfolio V1 MERGED — local portfolio authority, read-only IBKR sync, Holdings page, holdings tool)**: FF-merged `codex/holdings-portfolio-v1` (`91ca631..446cf5e`, +2,844/−71, 33 files; codex implemented, I reviewed two rounds). First-round review found **6 must-fix + 4 should-fix despite a clean initial A/B (30=30, +17 exact)** — the holes were test-shaped, caught by reviewing against spec/plan contract language with concrete repros: preview persisted discovered broker accounts with no write gate (repro'd; hit the plan's own stop-loss), broker-side deletions never closed local rows (`closed_at` had zero non-NULL writers), the agent tool leaked raw broker account ids into prompts (repro'd; spec §11), `include_in_total` was dead end-to-end (no totals effect, no API, no UI), account PATCH ignored its path id (could silently create accounts), and the options-honesty/read-only copy was missing (spec §13). Repair `40b5b8a` closed all ten with 13 pinned backend + 2 frontend tests: preview is strict zero-write (new-account changes carry `account_id=null`), missing conIds preview as `remove` and soft-close on apply (notes preserved; close scoped `broker=account.broker AND broker_con_id IS NOT NULL` so manual rows are untouchable; previously-closed rows re-open as `update`), tool redacts `broker_account_id` (hash/label only) with scope/totals-consistent payloads, snapshot honors include-in-total and reports `included_account_ids`, PATCH targets its path id with 404/400 structured errors + archive/aggregate toggles, Holdings page gains 唯讀同步/不會下單 copy + a separate options section with the advanced-risk warning; the two dead sync-history tables were REMOVED and the deferral recorded in spec §9 (no hollow audit surface); store gains WAL best-effort + busy_timeout 5000. I re-verified everything myself: three repros re-run green (preview zero-write / payload redaction / full-liquidation soft-close), static no-order/no-PG gates zero matches, catalog parity zero missing (registry 56 / bridges 57), focused 74 passed, frontend 5 passed + typecheck + build, smoke `pg_attempts:[]`. **Final virgin A/B: failure sets IDENTICAL 30=30, passed 3801→3831 = +30 = exactly the new tests.** Worktree and branch removed. **Live verification pending (user): restart the sidecar → 持倉 page → fresh profile shows Manual account → add a manual position and refresh (persists) → with Gateway up run IBKR 預覽 (discovered accounts appear as review changes, zero writes) → 套用 → user notes survive a second sync → ask the agent for a holdings summary and confirm the trace calls `get_portfolio_holdings` with no raw account id and no IBKR access.** Next per map: P2.5 provider capability display / P2.6 SA extension health surface / Track B skill suggestions — user's call.
- **2026-07-09 (Holdings V1 first review-repair pass — six blockers closed, final A/B pending)**: Review found concrete contract gaps in the first build despite a clean initial A/B. The repair makes IBKR preview zero-write for newly discovered accounts; represents broker deletions as `remove` and soft-closes them only on apply; redacts raw broker account ids from agent output and keeps account scope/closed-position totals internally consistent; makes account PATCH target its path id with include-in-total/archive controls and structured errors; applies aggregate inclusion in the store and UI; and adds explicit read-only/no-order copy plus a separate options section with the advanced-risk warning. Two unused sync-history tables were removed and deferred rather than shipped as a false audit surface; SQLite now uses best-effort WAL and a 5-second busy timeout. TDD evidence: backend failures reproduced before implementation; holdings/config core `74 passed`, broader focused/ledger set `254 passed`, frontend `5 passed` plus typecheck/build, static no-order/no-PG gates empty, PG-unreachable smoke `ok:true` with `pg_attempts:[]` across 24 checks. Branch remains review-ready and unmerged; reviewer reruns final virgin A/B against the repair tip.
- **2026-07-08 (Holdings + Portfolio V1 IMPLEMENTED FOR REVIEW — local portfolio store, read-only IBKR sync, Holdings page, local holdings tool)**: Branch `codex/holdings-portfolio-v1` implements the adopted holdings design and the six-task plan. Delivered: `PortfolioStore` in `profile_state.db` with fresh Manual account, multi-account shape, conId identity for broker-backed positions, broker-owned vs user-owned field split, and currency-basis totals; read-only IBKR snapshot/sync preview/apply using `IBKRDataSource(..., readonly=True)` and `holdings=+60` client-id domain; `/portfolio` API routes with `profile_state_write` gates and S-J `provider_config_missing` refusal; enabled desktop `Holdings` nav/page with manual position entry and IBKR review/apply controls; `get_portfolio_holdings` local read-only agent tool, registered in ToolRegistry and both bridges with Tool Catalog sync (registry 56, bridges 57). Verification before review: holdings/config backend core `61 passed`; targeted tool-ledger set `26 passed`; frontend `Holdings.test.tsx` `3 passed`, typecheck passed, build passed; static no-order/no-PG gates had zero matches; PG-unreachable smoke returned `ok:true` and `pg_attempts:[]` across 24 checks. Known verification note: full-file `tests/test_tools.py` still contains pre-existing data/network-dependent failures in this worktree, and `tests/test_api.py::TestHealth::test_status` hangs in the TestClient path; full virgin A/B remains the reviewer merge gate. Non-goals remain trading/order placement, alerts, research-note linking, multiple Gateway connection profiles, CSV import, and advanced derivatives risk.
- **2026-07-08 (Holdings + Portfolio V1 implementation plan opened — local store, read-only IBKR sync, Holdings page, local holdings tool)**: Opened `docs/superpowers/plans/2026-07-08-holdings-portfolio-v1.md` against the adopted holdings design spec. Plan sequencing locks the implementation into six reviewable slices: (0) reserve IBKR `holdings=+60` client-id domain and repair stale Settings guard text (`quotes=+50`/`holdings=+60`); (1) add `PortfolioStore` tables in `profile_state.db` with fresh Manual account, multi-account shape, conId identity, broker/user field split, and honest currency-basis totals; (2) add read-only IBKR position/account snapshot + review/auto sync diff using `IBKRDataSource(..., readonly=True)` and no order APIs; (3) expose `/portfolio` routes with `profile_state_write` gates for every mutation and S-J `provider_config_missing` refusal for unconfigured IBKR; (4) enable the existing disabled `Holdings` nav item as a real desktop work surface; (5) add one local read-only holdings agent tool with full registry/bridge/catalog ledger update (55→56 registry, 56→57 bridges); (6) run static no-order/no-PG gates, PG-unreachable smoke, frontend build, and full A/B. Non-goals remain trading, alerts, research-note linking, multiple Gateway connection profiles, and advanced options risk.
- **2026-07-08 (Holdings + Portfolio design opened — hybrid manual/IBKR, multi-account from v1)**: Opened `docs/superpowers/specs/2026-07-08-holdings-portfolio-design.md` after Track A.5. User ruled the holdings feature should support both manual entry and IBKR convenience, not one global mode. Locked product direction: local `profile_state.db` is the durable app authority; IBKR connection settings remain the existing S-J provider config in v1 (no duplicate `broker_connections` table), while `portfolio_accounts` are separate app objects; a fresh profile gets a Manual account; IBKR discovery may expose original accounts, subaccounts, managed accounts, or only the Gateway-visible account, so **multi-account is a v1 data-model requirement** even if UI stays simple. Sync is account-level by default (`manual` / `ibkr_review` / `ibkr_auto`) with broker-owned fields separated from user-owned notes/thesis/tags/targets; IBKR-backed position identity is account + `conId`, with symbol as display metadata. Holdings v1 is read-only with respect to IBKR and does not place/cancel/modify orders; this does **not** require the user's Gateway itself to be read-only, because future trading automation may reuse the same Gateway only through a separate execution product line. All readable IBKR positions should be visible, including stocks/ETFs/cash/options; broker-provided base-currency market value/P&L can feed totals, otherwise per-currency subtotals are required; advanced derivatives risk remains a later slice. Client-id ruling: holdings needs its own read-only IBKR client-id domain, and any future trading/execution line must use independent execution ids/order correlation ids/audit ids, never the holdings sync identity; adding bands must respect the legacy 100+ id collision zone documented in `data_sources/ibkr_client_id.py`.
- **2026-07-08 (Investor Profile Calibration Chat MERGED — Track A.5 dedicated profile journal loop)**: FF-merged `codex/investor-profile-calibration` (`5ec1417..2bb0ca7`, +1,509/−14; codex implemented, I reviewed across two rounds). Opt-in calibration chat: append-only `investor_profile_calibration_sessions/messages/proposals` in `profile_state.db`; `/profile/investor/calibration` state/start/send/approve/reject; Settings 投資人設定 gains a calibration conversation whose draft proposal pre-fills the existing profile form for review/edit; approval is the ONLY mutation path (`require_profile_state_write` + `InvestorProfileStore.save()` → `risk_mismatch` stays server-derived; changed-fields provenance from a fixed 14-field tuple, not raw asdict diff). Dedicated non-streaming loop (NOT research_threads/runs) so calibration can't leak into research history/hydration; one active session at a time with explicit `supersede_active`; raw dialogue never enters research/card prompts. Live responder is no-tool: OpenAI api-key → direct chat completion (json_object), ChatGPT OAuth → `build_driver(registry=None,dal=None)` (verified `_build_tools()`→[]), Claude Code OAuth → intentional hard-fail (no MCP/tools). Review found + fixed: (round 1 plan) changed_fields/test mismatch, non-Depends Protocol responder param would break app mount, frontend helper drift, app-factory test import; (round 2 impl) responder non-ValueError failures now wrap to structured 502 `calibration_responder_failed` (storage failures NOT misclassified). Boundary evidence (I ran the gates myself): zero research/tools/DAL imports in calibration modules; zero `calibration` hits in research/card prompt paths. Final A/B (base 1e3d8b8 vs 2bb0ca7): failure sets IDENTICAL 37=37, passed +18 = exactly the new tests. Live path is wired-but-unproven-by-execution (units fully mock `_call_calibration_llm`; symbols verified by hand) — **user live verification pending: restart sidecar, then Settings → 投資人設定 → calibration chat one round (OpenAI path) → review/approve a proposal → confirm approved profile drives research stance**. Known live-path notes deferred: Anthropic path has no json_object enforcement (relies on prompt); dangling user turn persists on responder failure (accepted journal artifact). Next: Track B (skill suggestions + CLI/discord) or holdings — user's call.
- **2026-07-08 (Investor Profile Calibration Chat IMPLEMENTED FOR REVIEW — dedicated profile journal loop)**: Branch `codex/investor-profile-calibration` implements Track A.5 before Track B: `src/investor_profile_calibration.py` adds append-only calibration sessions/messages/proposals in `profile_state.db`; `/profile/investor/calibration` exposes state/start/send/approve/reject; Settings 投資人設定 gains a calibration chat whose draft proposal pre-fills the existing profile form for user review/edit; approval is the only mutation path and calls `require_profile_state_write` + `InvestorProfileStore.save()` so `risk_mismatch` remains server-derived. Plan decisions landed: dedicated non-streaming calibration loop (not `research_threads`/`research_runs`), one active session at a time with explicit `supersede_active` for replacement, raw calibration dialogue never enters research/card prompts or hydration, and proposal provenance records `proposal_id`/`approved_at`/meaningful changed fields. No-tool boundary evidence: calibration modules do not import `ToolRegistry`, DAL, or research stores; grep over research/card prompt paths has zero `calibration` hits. Live responder paths stay tool-free: OpenAI API-key/env uses direct chat completion with no tools, ChatGPT OAuth uses `registry=None`/`dal=None`, and Claude Code OAuth intentionally raises `claude_code_oauth calibration no-tool path is not wired` rather than enabling MCP/tools. Verification so far: focused backend gate `101 passed`, frontend calibration/personalization gate `14 passed`, frontend typecheck passed, PG-unreachable smoke `ok:true`/`pg_attempts:[]`/24 checks; full virgin A/B remains the reviewer merge gate (expected failure set identical, passed delta = new backend tests). Not live/merged yet.
- **2026-07-08 (Investor Profile Calibration Chat design addendum — long-lived journal, wizard-sized v1)**: After live observation of the Track A Settings panel, user ruled that the form-first Investor Profile surface is too abstract for normal users. Updated `docs/design/INVESTMENT_SKILLS_PROFILE_DESIGN.md` with **Track A.5** before Track B: calibration is a long-lived profile journal in `profile_state.db`, but v1 remains wizard-sized (one active calibration conversation → structured proposal → user review/edit → approve/reject). Raw calibration messages are append-only review material and never shape research/card prompts directly; only the user-approved structured `investor_profile` can affect synthesis. Existing Track A machinery remains authoritative: proposal validation goes through the same normalization path, actual save goes through `profile_state_write`, `risk_mismatch` is server-derived, and proposals may include `default_stance` only as a user-confirmed field. Storage direction: dedicated calibration sessions/messages/proposals, not `research_threads`; approved provenance must carry `proposal_id` + `approved_at` and leave room for future profile-version trace. v1 calibration assistant has no market/news/web/code/write tools. Implementation-plan open point: choose execution path — server-owned run manager reuse with hard storage/UI isolation vs a dedicated lightweight calibration loop. This Track A.5 now precedes holdings/alerts/research-notes and Track B skill suggestions.
- **2026-07-08 (get_current_quote MERGED — first-class current-price capability)**: FF-merged `codex/current-quote-tool` (rebased onto Track A master, `aca7272`; codex implemented, I reviewed). New `get_current_quote(dal, ticker, source=auto|ibkr|local)` — IBKR `reqMktData` snapshot first (honest `mode`/`source_note`: live-vs-delayed depends on entitlement), `auto` falls back to the latest local bar EXPLICITLY marked `local_last_bar`/`stale`, strict `ibkr` never falls back, `local` never touches IBKR; no persistence/scheduling. Exposed at ALL five registration sites (registry 55 / bridges 56, both OAuth research allowlists, `GET /prices/{ticker}/quote`) + agent prompt guidance. Review history: round 1 caught the tool-count ledger only partially applied (21 failures across the ~11-file family) and the catalog's "introspection fact table" claim silently 3 tools stale (`get_sa_feed`/`get_sa_comment_focus`/`get_ticker_data_coverage` now rowed; counts 55/56; new PARITY TEST pins catalog↔`list_names()` forever). Full A/B round 1 then caught a second, distinct ledger family both plan and review had missed: the client-id DOMAIN enum contracts — `quotes` offset 5 violated the 10-wide-band spacing invariant → adjudicated to offset 50 (effective id 51 at seeded base 1, distinct from legacy absolute 50; commit = the module-mandated decision-log entry) and the provider-config view's exact enum pins updated in both sections. Final A/B: 37=37 sets, +9 passed = exactly the new tests. Settings client-id hint auto-shows 即時股價=51. Lesson banked in the tool-registration reference memory: sweep = new enum NAME across tests/, not just count literals.
- **2026-07-07 (Investor Profile Track A MERGED — opt-in personalization live on all research surfaces)**: FF-merged `track-a-investor-profile` (`3999cf9..b770826`, +2,245/−49; implementer=assistant, reviewer=owner/gpt-5.5 per the flipped two-party arrangement). Shipped: `src/investor_profile.py` singleton store in profile_state.db (appetite/capacity mismatch derivation, stance/skill-mode enums with `auto_with_trace` excluded from Track A), `/profile/investor` GET/draft/PUT (draft ungated, PUT `profile_state_write`, explicit-null clears via `model_fields_set`), personalization threaded through ALL research execution paths — api-key `run_query_stream`, both subscription helpers, AND the server-owned run manager (plan amendment: the workbench UI runs on createResearchRun, without this stance would silently not apply) — plus card synthesis (emphasis block appended after the EvidencePacket; `gather_evidence` untouched, monkeypatch-proven), trace persisted on research messages (`personalization_json`), run replay done-events (enriched BEFORE append), `ai_card_runs` (5 columns), and success/error/cancelled terminals; Settings 投資人設定 panel + Research/AICard stance selectors + trace chips. Conditional-omit rule enforced at every seam (route dispatch, sub-helpers, card synthesis internal — the last one caught by full A/B round 1 as 2 head-only failures in the un-run `test_card_synthesis.py`, lesson re-logged). Collateral fixes shipped: pre-existing fresh-checkout baseless-DAL crash (`_base/"config"` sslmode member — reproduced on merge base; PG-unreachable smoke now passes on VIRGIN checkouts), vitest `.test.tsx` include, `.gitignore` symlink-form `node_modules`. Review findings ×3 all fixed with tests (symlink untracked; explicit-null clear; cancelled-run trace). Final A/B: 37=37 sets, +38 passed = exactly the new tests. Preflight evidence recorded: `discord_bot.py` has zero launchers/imports → Track B retirement candidate. Live verification (enable profile → stance run → trace visible) = owner's next session; Track B (skill suggestions + CLI/discord collection) unopened.
- **2026-07-06 (Investment Skills + Investor Profile design opened — v1 profile/stance, v1.5 suggestions, v2 auto-trigger committed)**: Drafted `docs/design/INVESTMENT_SKILLS_PROFILE_DESIGN.md` after the hygiene line closed and the B6 ruling moved `config/skills/` out of cleanup and into product design. Grounding: Phase G already has a real skills substrate (`resources/skills/` = 10 packaged `SKILL.md`, including DCF/comps/earnings-analysis; `src/agents/shared/skills.py` = tiered registry + aliases + trigger index + custom dir), and `config/user_profile.yaml` already has early investment preferences, but the product lacks a durable Investor Profile, Assistant Stance, explainable skill suggestion, and controlled auto-trigger policy. Review correction folded in: web/workbench paths currently have zero skill-trigger use, but legacy CLI and Discord already auto-inject unique auto-applicable skills (`auto_apply=True` default), so Track B must bring those call sites under `skill_mode` or retire/defer Discord if it proves pre-pivot; `off = byte-identical` is scoped to the workbench surface until then. Ruling: **v1 = opt-in Investor Profile + Assistant Stance** (split user traits from assistant role; not merely tone, but investment judgment posture such as growth-oriented, conservative, complementary, strict risk-control, valuation-rationalist); **v1.5 = suggest-only skills with reasons**; **v2 = `auto_with_trace` skill auto-trigger is explicitly committed, not forgotten**, but gated on trace, stance-aware selection, opt-in controls, permission compatibility, and rerun/override. Evidence boundary locked: profile/stance may shape synthesis/chat emphasis only; it must not alter deterministic EvidencePacket gathering, hide evidence, or weaken required counter-thesis. Storage direction: profile/stance state should live in local `profile_state.db`; `config/skills/` becomes an import/legacy bridge to be resolved by the implementation plan, not a hygiene deletion. Next gate: user review of the design doc before any implementation plan.
- **2026-07-06 (B7 MERGED — options math lives at `src/options_math/`; hygiene B-line fully closed)**: FF-merged `codex/b7-options-math` (`e2315e1..cf4ec92`) after reviewer verification: move purity (option_pricing 100% rename, rate_curve 99% [docstring `:9` only], `__init__` docstring-only with `__all__` untouched), the `:251` patch-target string correctly rewired, T4 hard residue gate zero (reviewer re-ran), root `analysis/` gone (stale main-checkout `__pycache__` also cleaned post-merge), focused 94 passed + scripts compiled. **Full A/B = the pure-move ideal: failure sets 37=37 (diff empty both ways) AND passed counts EXACTLY equal 3730=3730** (skips 74 / warnings 18 / errors 7 also identical). Import surface verified live (`from src.options_math import …`). No running Python sidecar at merge time → no stale-lazy-import window; next app launch loads the new path. Worktrees/branches cleaned (incl. the leftover telemetry worktree); repo back to master-only. **The repo-hygiene line (audit → B1–B5 → B6 rulings → B7) is now fully closed.** Open follow-ups live elsewhere: Investment Skills + Investor Profile design spec (NEXT, product line), dev-environment docs, P2.5 provider capability display, docker-compose retire-or-keep re-judgment (deferred as ruled — judgeable any time now).
- **2026-07-06 (B7 options_math migration IMPLEMENTED FOR REVIEW — reviewer full A/B pending)**: Branch `codex/b7-options-math` moves the live options pricing/math package from root `analysis/` to `src/options_math/` with no shim and no root `analysis/` residue. Implementation preserved the `__init__` re-export surface, rewired all 13 functional sites (six `options_tools.py` lazy imports, two retained `scripts/analysis/*` imports, two test import sites, two in-function test imports, and the `patch("analysis.rate_curve...")` string target), and left the unrelated `src.analysis`/`analysis_cards` route surface untouched. Evidence so far: T1 RED matched the predicted `ModuleNotFoundError`; T2 option/rate suites passed (`94 passed`); T3 focused gate showed only the same 9 pre-existing `tests/test_tools.py` data/network failures with `113 passed`, targeted options tool tests passed, scripts compiled, and optional evidence/compressor suites passed; T4 real residue gate is zero and `analysis/` is gone. Scoped virgin A/B over `tests/test_option_pricing.py tests/test_rate_curve.py tests/test_tools.py` is identical (`9 failed, 113 passed` on both base/head; failure-set diff empty). Full A/B did not complete in the Codex environment and remains the reviewer merge gate; no LIVE COMPLETE claim until that passes.
- **2026-07-06 (B6 rulings received; B7 options_math plan opened; B4b accepted with one README nit fixed)**: Owner review accepted B4b-A end-to-end (archive dir + manifest semantics + restore proof + PUBLICATION_REVIEW flip + clean worktree independently verified) with one should-fix executed: docker README restore recipe said `<batch-dir>/dump.backup` but dump filenames differ per batch — now points at the manifest's `dump_file` field. B6 rulings: **(1) B7 OPEN** — `analysis/` → `src/options_math/` (live runtime out of a root-level package; TDD verbatim move, zero residue, full A/B); **(2) scripts survivor table STANDS** this round (migration/scoring/diagnostics have test consumers; re-audit only after B7 + skills design); **(3) `config/skills/` question moves to the Investment Skills + Investor Profile design line** (custom-skills home = product decision with profile DB + packaged skills, not hygiene). B7 plan drafted (`docs/superpowers/plans/2026-07-06-b7-options-math-migration.md`, pending review): 10 uncapped-inventoried import sites (incl. `tests/test_rate_curve.py`'s SECOND in-function site at `:241`), `__init__` 30+-name re-export surface preserved verbatim, `src/analysis` name-collision documented, acceptance = failure sets identical AND passed counts EQUAL (pure move, zero test delta). Next after B7: skills/profile design spec (product line, separate from hygiene), then dev-environment docs.
- **2026-07-06 (B4b-A EXECUTED — remote PG stopped after app-record dump; hygiene B-line closed except B6 approvals + B7)**: Owner ruled option A (dump-then-stop beats rotate: a stopped service exposes no credential). Executed: read-only snapshot of the three accept-as-archive tables (counts 2/2/1 — **the entire in-place archive was 5 rows**, all Feb–Apr era; live app records have been local in `profile_state.db` since the app-records localization) → `pg_dump` of exactly those tables to `data/pg_archive/app_records_20260706T121127Z/` (16,962 bytes, sha256 `486f6fae…`) → manifest with "legacy app-record archive, not runtime authority" semantics → scratch-DB restore proof on the source server (rows+columns matched, scratch dropped) → user stopped the container → connection-refused verified. PUBLICATION_REVIEW §5 flipped RESOLVED (no live service carries the compromised credential; rotation moot); docker README records "no live ArkScope PG anywhere — compose + dumps are the complete archive story"; repo `docker/` stays archive-only per review lean, re-judged after B6/B7. Also executed: B5b dangling-link must-fix (`9548c7f` — the gate lesson: name-mention grep ≠ link-resolution scan; a full markdown link scan now returns zero repo-wide). **B6 module disposition table DELIVERED** (`REPO_HYGIENE_B6_MODULE_DISPOSITION.md`, read-only): analysis/ = live runtime → proposed **B7 `src/options_math/` migration slice** (name collision with existing `src/analysis/` is real; 4 consumers inventoried incl. the two retained `scripts/analysis` files); config/ all live (reader counts; `config/skills/` question deferred to the skills design line — it is code-read Tier 3 custom dir); scripts survivor table = STANDS (per-subfolder evidence incl. `scripts.migration` 10 test consumers, `scripts.scoring` 4, `scripts/diagnostics` 1); resources/skills corrected to **10 packaged skills** (audit said 5 from a capped listing — truncation lesson again): builtin ×4 + equity-research ×3 + financial-analysis ×3 (**dcf-model/comps-analysis/earnings-analysis already exist as content**), Phase G tiered loader already covers the ruled boundary (Tier 1 builtin / Tier 2 category / Tier 3 custom + alias map + trigger index) — the Investment Skills + Investor Profile design spec starts from this inventory. Owner decisions pending: B7 approval, survivor-table-stands confirmation, config/skills deferral.
- **2026-07-06 (Repo hygiene B4a + B5a + B5b EXECUTED — docs sweep complete; only B4b rotation remains)**: **B4a `b5a4caf`**: docker repurposed archive-access-only — compose password is a required env (`ARKSCOPE_ARCHIVE_PG_PASSWORD:?`, no default; parse-with-dummy + loud-fail-without both gated), docker README rewritten (app needs no docker; two-stage scratch-DB restore recipe; rotation runbook), `config/.env.template` now ships local-first (DATABASE_URL = commented archive-only placeholder — the old template ACTIVATED PG with the compromised password for every new user), root README drops the "current stack" DB step AND the stale "today still PostgreSQL-backed" status block; repo-wide grep for the old dev password = zero tracked hits (the gate also caught MY hygiene docs quoting the literal — republishing a compromised secret in public plaintext; all mentions now descriptive); PUBLICATION_REVIEW gains §5 Known incidents (rotation pending = archive DB currently reachable with a public credential). **B5a `d076223`**: 111-doc disposition table with inbound-link evidence (42 keep-current / 55 keep-record+header / 2 relocate / 3 fold-then-delete / figures owner-choice / 2 IP fixes) — owner approved with figures=keep+link. **B5b `ef8c4a8`+`67604e9`+`d18e064`+`2397a5d`**: scripts survivor rule relocated to REFACTOR_PROTECTION_SMOKE_GATES §6 (scoping §5 keeps the execution record + pointer); current host write path relocated into the SA boundary doc (rewritten to current state — runbook text was PG-era); 22 status headers added (24 verified pre-existing); the two docs-governance process journals folded into PROJECT_HISTORY §"Docs governance lineage" and deleted; **execution-time verdict correction (fold-then-delete verification working as designed): `P0_1_FULL_V1_SPEC` was mislabeled in the table by name-pattern guess — it is the LIVE Replay Harness spec authority (`bridge_tools.py:15` citation, Phase C inherited contract) → KEPT, 2 of 3 approved deletions executed**; 5 orphan backtest figures linked from RL_COLLAPSE_FINDINGS (surviving visual evidence post-checkpoint-deletion); 2 pre-existing internal-gateway-IP literals + the hygiene plan's own description line generalized (IP gate = zero); live-doc stale-copy scan clean; dangling-link gates zero. **Open: B4b rotation (user-executed, runbook in docker/README) + B6 (analysis/config/scripts/resources recursive disposition — read-only table → approval → batches; per review ruling analysis/→src move = TDD slice e.g. src/options_math/, resources/skills = packaged data not src/, config = future db-ification slice, scripts survivor changes = standing-ruling changes).**
- **2026-07-06 (Repo hygiene B1–B3 EXECUTED + B4/B5 plan opened)**: Owner answered all 5 audit rulings (recorded in `REPO_HYGIENE_AUDIT_2026_07.md` §7): delete artifacts directly with a manifest and no external copy (data + training code remain; retraining cheap); gitignore stays necessary post-deletion (policy, not cleanup); `tickers_core.json` pending diff identified as rename-fix + SA Alpha Picks auto ticker-sync → committed as real app state (config db-ification stays a future readers-first migration, no pre-emptive deletion); docker → archive-access-only (not a dev quickstart — app runtime has zero PG) with rotation in-batch; docs sweep goes deeper under the 4-tier rule. Executed directly: **B1** `ff538b4` deleted the two zero-reference FNSPID-era root scripts; **B2** `17bcf00` deleted ~3.1G artifacts (trained_models 1.2G / huggingface output 984M / data_prep output 917M / comparison_results / data_lake; manifest with rebuild basis in audit §8), git-rm'd `trained_models/registry.json` + test-run metadata (rows pointed at deleted checkpoints; `_load_index` returns [] on missing), gitignored `trained_models/`, standing artifact policy recorded; **B3** `1c076bf` committed the months-old tickers_core state. Working tree fully clean for the first time since April; affected tests verified (23 passed, 4 by-design skips in `test_inference_offline`). **B4/B5 plan drafted** (`docs/superpowers/plans/2026-07-06-repo-hygiene-b4-b5.md`, pending review): B4 = compose de-hardcode (`${ARKSCOPE_ARCHIVE_PG_PASSWORD:?}` — no secret in repo ever) + README archive-only repurpose + user-executed rotation with live read-back verification; B5 = docs deep sweep in two steps (4-col disposition table over all tracked docs → owner approval checkpoint → relocations/headers/fold-then-delete batches with dangling-link gates); live-law relocation named up front (scoping §5 survivor table, SA runbook write-path detail).
- **2026-07-06 (Repo hygiene audit DELIVERED — module-level cleanup pass opened, inventory-first)**: User redirected the post-telemetry queue: before new features (gpt-5.5 had proposed P2.5 next), finish integrating/removing the repo's residual bulk. This executes the docs-governance line's long-pending module-level pass. Read-only inventory delivered at `docs/design/REPO_HYGIENE_AUDIT_2026_07.md`: training/ = paused-preserve (only its 917M gitignored `data_prep/output` is a disk question); trained_models/ = 131 untracked checkpoint files (1.2G) polluting git status since April → gitignore + pending M/D resolution; docker/ = archive-access-only now + the standing compromised-password rotation; scripts/ survivors ruled 07-06 stay; analysis/ = LIVE runtime (options pricing) not cleanup; config/ all live — `tickers_core.json` retirement guess REFUTED by reader check (~10 live readers; 3e-E only removed the default-scope role); two root FNSPID-era scripts (`audit_stock_news.py`, `filter_fns_data_by_date.py`) = zero-reference dead batch (the class the scripts consolidation didn't scan: root-level loose files); ~3.1G reclaimable gitignored artifacts; docs/design 60 files classified live-canonical / completed-record / paused / needs-look with archive+header sweep proposed. Execution split into batches B1–B5 (dead roots, gitignore+M/D, disk artifacts, docker+rotation, docs sweep), each Level-0-gated; 5 owner rulings listed in audit §7. Sequencing per user: hygiene before P2.5 provider capability display; Desktop App Vision decision doc and IV Task 1 follow.
- **2026-07-06 (SA extension telemetry plumbing + health surface SHIPPED + LIVE — the structural telemetry gap is closed)**: Implemented per the reviewed plan: codex Tasks 1–5 (`1dda248..c98a055`: host resolves telemetry target env > config > default-8420 with one-shot config-refused fallback + DAL-free ping carrying `telemetry_target/source`; Electron writes/clears per-run `api_base`/`api_token` via testable `stopSidecarCleanup` clearing BEFORE any early return; `JobRunsLocalStore.list_runs` gains `trigger_source` filter; read-only 7-segment health service + `GET /sa/extension-health` + Settings panel with tri-state warn) + codex live-discovered dev.js shutdown clear (`7b98b7a`) + three reviewer fixes: `8cd9210` (health probe strips `ARKSCOPE_API_*` so it reports what a browser-spawned host resolves, not the sidecar's env), `5ddd902` (import purity: `os.chdir`/`makedirs`/`logging.basicConfig` moved into `_init_script_runtime()` — importing the module at pytest collection had created `data/` in virgin A/B environments and flipped the pre-existing `test_agents` DAL-fixture noise family, breaking failure-set identity; import-purity subprocess pin added), `b804c27` (extension-health fetch removed from the Settings 5s schedule poll — each poll spawned a native-host subprocess; now mount-once + manual 重新檢查 per plan). Gates: focused 166 passed, node:test green, vitest 217/217 + build, smoke 24 checks `ok:true`/`pg_attempts:[]`; full A/B round 1 caught the import-side-effect diff (root-caused, fixed), round 2 strictly identical (37=37, +18 passed = exactly the new tests). **Live proof of both resolution paths**: with ONLY dev:desktop running, Quick Refresh landed `job_runs` `sa_alpha_picks_refresh/extension` run_id=13702 `persisted:true` via `source=config` (target = ephemeral 41229) — the outcome that was structurally impossible before this slice; clean quit cleared the config api fields (3 original keys remain); standalone `python -m src.api` on 8420 then landed run_id=13703 via `default`. P2.6 part 1 done; part 2 (embedded SA browser/session) remains an open Desktop App Vision decision.
- **2026-07-06 (SA extension telemetry plumbing + health surface plan opened — P2.6 part 1 promoted; embedded browser explicitly deferred)**: Opened `docs/superpowers/plans/2026-07-06-sa-extension-telemetry-health.md` after post-cutover tracing upgraded the telemetry incident to a structural finding: dev:desktop's Electron shell spawns the sidecar on an ephemeral port with a per-run token (`main.js:144-148`, observed live 34145), the browser-spawned native host inherits the browser env and POSTs to default 8420 with no token (`sa_native_host.py:721-738`, token check `app.py:151-153`), so extension telemetry can never land under the daily workflow — last success `2026-07-05T16:05Z` rode a standalone 8420 sidecar. Slice scope: shell writes/clears per-run `api_base`+`api_token` in `~/.config/arkscope/sa_native_host.json` (write-after-health, clear-on-clean-quit, 0600, tmp+rename), host resolves target env > config > default with a one-shot refused→default fallback (covers crash-stale config + standalone workflow), read-only `GET /sa/extension-health` + Settings 資料來源 per-segment 鏈路 panel (config/manifests/launcher/simulated ping/telemetry binding/last telemetry/capture read-back; no synthetic capture writes; token never displayed or logged). User ruling recorded: **embedded SA browser/session is NOT this slice and not now** — it is a new ingestion client (login/2FA/DOM drift/security boundary) deferred to a Desktop App Vision decision; current external-extension mode gets made diagnosable/configurable/verifiable first. Live acceptance = a `*/extension` `job_runs` row landing while ONLY dev:desktop runs (previously impossible), plus standalone-8420 regression proof.
- **2026-07-06 (Scripts runtime consolidation SHIPPED + LIVE — scoping §5 rule is final)**: FF-merged `codex/scripts-runtime-consolidation` at `bde732d` (9 commits: 5 codex Tasks 1–5 + 4 reviewer amendments), −8,219 lines. Moves: polygon/finnhub collectors → `src/collectors/`, `daily_update` → `python -m src.daily_update`, smoke/audit → `src/smoke/` + `src/audit/`, SA native host → `src/sa_native_host.py`. Dead batch deleted at `f9d00c7` (16 files incl. `collect_alphavantage_news.py`/`collect_eodhd_news.py` — recover via `git show f9d00c7^:scripts/<path>`; future Data Sources provider slice for both filed as §P2.5, which also owns the provider capability display work sourced from the restored `docs/data/NEWS_PROVIDER_DATA_DICTIONARY.md`). Reviewer amendments caught two uninventoried reference classes (shell installers + live docs): `install.sh`/`install_firefox.sh`/`native_host_launcher.sh` fallback re-pointed with a new self-maintaining pin `tests/test_extension_install_paths.py` (parses paths out of shell sources, asserts existence); README/PROJECT_STRUCTURE/smoke-gates/SPEC LOCK #9/provider catalog updated; DATA_DICTIONARY restored from deletion per user ruling with a durable-vs-stale header. Full A/B: virgin archives `e4ecc2b` vs `bde732d`, failure sets strictly identical (37=37, diff empty), passed −6 fully accounted (−14 deleted: 11 `test_news_scores` migrate classes + 2 old adapter params + 2 wrapper/refusal pins; +8 added: 2 new adapter params + 3 plan pins + 3 installer pins). Live cutover: sidecar stopped + Firefox closed → merge → `~/.config/arkscope/sa_native_host.json` `host_script` flipped to `src/sa_native_host.py` (browser manifests untouched — both point at the stable launcher; no extension reinstall needed) → simulated host round-trip (`ping` ok + real `sa_capture.db` read) → user Firefox Quick Refresh verified: `sa_refresh_meta` current 49+1 @02:07:56Z / closed 61+1 @02:08:14Z, `sa_articles` updated through 02:12:49Z, comments dedup `net_new=0` as expected. Known environmental note: `record_extension_job` telemetry POSTs got Connection refused because the sidecar was not running during the test (identical pre-merge behavior at 09:45 — best-effort by design, data path unaffected); telemetry resumes once the sidecar is up. New backlog: §P2.5 provider capability display, §P2.6 SA extension health/setup surface (boundary note `SA_EXTENSION_HEALTH_SETUP_BOUNDARY.md`). Post-PG-exit queue is now empty again; next standing threads per their own entries (news `ibkr_news` direct-or-retire, SA C-2 polish → C-3, IV Task 1 free inventory).
- **2026-07-06 (Scripts runtime consolidation plan opened — finish the ruled scripts/ → src/ line)**: Opened `docs/superpowers/plans/2026-07-06-scripts-runtime-consolidation.md` after the user flagged that the standing scoping §5 rule (runtime imports `src/` only; `scripts/` keeps only app-unrelated/historical material) was ruled but never queued to completion — the post-PG-exit queue was declared empty while polygon/finnhub news collectors still runtime-import `scripts.collection` (4 import sites + 2 adapter tuples), `daily_update.py` (already the F6 thin wrapper) still lives under `scripts/collection/`, and the smoke/audit harnesses plus the SA native host are app-related tooling living in `scripts/`. User rulings locked into the plan: zero residue (no wrappers survive a move; the S-A1 compatibility wrapper retires), smoke/audit/native-host also move under `python -m src.*`, alphavantage/eodhd collectors retire now with a recorded future-provider backlog pointer (delete>archive; git history is the archive), and the native host migrates LAST with an approval-gated Firefox native-messaging manifest re-point (host id `com.mindfulrl.sa_alpha_picks` unchanged). Task order: dead retirement batch (incl. dead-by-precedence `SourceDef.collector` plumbing and the migrate_* tombstones with their refusal tests, accounting named) → collectors → daily_update → smoke/audit → native host → §5 rule finalization. Full A/B acceptance: failure sets strictly identical (deleted tests passed on base, so any set diff is a finding).
- **2026-07-06 (IBKR news long-catch-up audit COMPLETE — current cadence safe, long quiet window capped)**: Closed `docs/superpowers/plans/2026-07-06-ibkr-news-long-catchup-audit.md` with a local read-only audit utility and no runtime ingestion changes. Live reports were run twice with explicit `--as-of 2026-07-06` and were byte-identical (`sha256 a89238907f2b54514824ac2b5a2c4a5da8fec05bc343bd4bcd60fe73b2dc14ef`). Evidence: `ibkr_news last_status=succeeded`, latest result `ticker_count=148`, `articles_seen=15068`, `articles_inserted=0`; local normalized IBKR rows `84,381`; 7d window max `129` with zero tickers >=200/250/300; observed quiet window `2026-06-25`→`2026-07-05` max `227` (`MU`) with zero tickers >=250/300; 30d window max `532`, `6` tickers >=300, `8` >=250. Methodology caveat is load-bearing: local SQLite counts are a lower bound, so already-missed provider-side tail rows cannot be recovered or counted after the fact. Ruling: `runbook_only` — keep normal IBKR news scheduling enabled, do not treat IBKR historical news as a complete multi-week backfill source, and do not raise writer budgets as a fake fix. No runtime follow-up is opened; future work would require a separate reviewed plan for real-time capture, warning surface, or cross-provider backfill if a zero-loss/news-tail requirement appears.
- **2026-07-06 (IBKR news long-catch-up audit plan opened — read-only evidence first)**: Opened `docs/superpowers/plans/2026-07-06-ibkr-news-long-catchup-audit.md` to close the final non-blocking post-PG-exit queue item. Static facts are already known: `IBKRDataSource.fetch_news()` calls `reqHistoricalNews(..., 300)` once per ticker, existing docs record that IBKR ignores requested date ranges and returns only the 300 most-recent headlines, and normalized IBKR worker budgets (`DEFAULT_MAX_ARTICLES=50_000`) are not the bottleneck. Live read-only grounding from local SQLite: current `ibkr_news` durable state is succeeded with no continuation; recent runs scan 148 tickers and now add 0 rows; current 7-day local IBKR volume has max 130 articles/ticker and zero tickers near 300, while the 30-day window has 6 tickers >=300 and 8 >=250. Plan ruling: initial slice is a reproducible local read-only audit utility + docs closeout only — no Gateway call, no runtime ingestion change, no fake continuation. If the report proves a material risk, a separate reviewed follow-up will decide between runbook-only, cadence warning, real-time capture, cross-provider backfill, or a live Gateway probe.
- **2026-07-06 (Dead-code/UI sweep SHIPPED — post-PG-exit stale paths closed)**: FF-ready branch `codex/dead-code-ui-sweep` (`dd00c04..38a06d9` before docs closeout) completed the narrow post-PG-exit sweep opened below. Runtime PG `sa_*` methods on `DatabaseBackend` are now tombstone-compatible stubs that never open PG, and SA market-news health no longer has a PG branch (missing local capture backend returns explicit `db_unavailable`). Local-first comments/copy were aligned without removing protected macro/SA capability or legacy `use_local_*` provenance keys. Retired Data Sources frontend helpers (`bootstrapMarketData`, `updateMarketData`, `validateMarketData`, `setUseLocalMarket`, `setUseLocalMacro`) and the unreachable generic `hintRows` provider-config path were removed while the grouped IBKR renderer remains the authority. Tool ledgers were corrected for the already-registered `get_ticker_data_coverage` and current analysis-category count. The single live-profile stale scheduler marker was approval-cleared: `price_backfill / failed / "REAL RUNNER CALLED"` backed up to `data/profile_state.db.bak-pre-dead-code-sweep-scheduler-marker-20260705T173056Z.db`, exact rowcount `1`, postcheck marker rows `0`. Gates: focused backend `65 passed`, frontend `29 passed`, `npm run build` passed, PG-unreachable smoke `ok:true` / `pg_attempts:[]` / 24 checks; reviewer full A/B round 2 reported identical failure sets except the two named base-only stale-ledger fixes and zero head-only deterministic failures. Next queue: IBKR news long-catch-up audit; future decisions remain FRED refresh cadence, Finnhub calendar enablement, and C-2/C-3 macro context.
- **2026-07-06 (Dead-code/UI sweep plan opened — post-PG-exit stale paths only)**: Opened `docs/superpowers/plans/2026-07-06-dead-code-ui-sweep.md` as the authority for the next cleanup slice after macro snapshot display. Scope is deliberately narrow: retire/stub dropped PG SA readers and the remaining SA health PG branch, align stale local-first comments/copy, remove orphan Data Sources frontend helpers/unreachable hint paths, refresh stale tool-count ledgers, and approval-gate the single live-profile `price_backfill / "REAL RUNNER CALLED"` scheduler-state marker. Non-goals are explicit: do not delete macro/SA protected capabilities, do not remove legacy `use_local_*` provenance keys, do not enable FRED refresh/Finnhub calendars/C-2 macro context, and do not start the IBKR news long-catch-up audit inside this sweep.
- **2026-07-06 (Macro snapshot display SHIPPED — FRED readable-snapshot semantics live)**: FF-merged `codex/macro-snapshot-display` at `8642b1a` (5 commits), implementing the 2026-07-05 ruling below. `GET /macro/snapshot` (11 curated FRED series), `/macro/series/{id}` + `get_macro_value()` read the local snapshot with refresh off (freshness `observation_date`+`fetched_at` everywhere), provider health shows FRED three-axis (`enabled=null`, `signals.auto_refresh_enabled=false`, `signals.local_snapshot`; 192h age judgment suppressed while refresh is off via an `_effective_threshold` sentinel that keeps all other providers byte-identical), Data Sources gains the FRED 本地快照 panel and the row no longer reads 「未啟用抓取」. PG-unreachable smoke now carries a standing `macro_snapshot` check (24 checks). Gates: backend 56+49, frontend 26+29+tsc+build, reviewer RED replays ×3, full A/B identical (39=39, +3 net-new tests), live UI verification on the real sidecar (row 正常/app/detail visible; panel 11 series no overflow; all four Data-Sources APIs 200). Queued future decisions unchanged: FRED refresh cadence, Finnhub calendar enablement, C-2/C-3 macro context. Sweep item added from live observation: stale durable `price_backfill: failed "REAL RUNNER CALLED"` row (2026-07-04) in the real profile — a past test-fake marker persisted pre-isolation; single-row cleanup + origin check.
- **2026-07-05 (Macro/FRED product semantics RULED — readable local snapshot, refresh disabled)**: `docs/design/MACRO_FRED_PRODUCT_SEMANTICS.md` drafted, fact-verified independently by the user, then re-ruled after the user reframed the question: not "keep dormant vs retire" but "why does a configured, free, data-rich provider display as unusable, and what should the app show and let be used?". Adopted semantics: three orthogonal display axes (provider/key 已設定 · local snapshot available — 11 core FRED series / 29,571 observations / last fetched 2026-06-25 · auto-refresh off = ops state, not provider-disabled); reads open with staleness honesty (`observation_date` + `fetched_at` on every value); refresh stays off (scheduler has no macro source — the flag today gates only reads, so opening reads creates zero refresh obligation); Finnhub economic/earnings calendars stay out (both tables empty, never ran). Named follow-up implementation slice queued: **"macro snapshot display"** — Data Sources row re-labeling, Macro Snapshot surface (FFR / 10Y / 2Y / spread / CPI / Core CPI / Unemployment / Payrolls / GDP / VIX), `get_macro_value` agent lookup, release-dates surface decided in-slice; gate mechanics (re-scope `macro_calendar_enabled` to refresh-only vs flip-plus-label) decided in the slice plan. Dead-code sweep boundary: macro layer = protected capability (carryover hard-lock #5); sweep may fix stale copy (e.g. `macro_calendar_tools.py` "requires PostgreSQL DAL backend") but not remove capability.
- **2026-07-05 (SA `use_local_sa` local-default collapse COMPLETE — S-J follow-up closed)**: FF-merged `codex/sa-local-default-collapse` at `4d2f6f1`. This closes the fresh-profile stranding hole found by S-J Phase 2 poison smoke: `use_local_sa` unset/explicit-false/env-unset now routes SA reads to local `sa_capture.db` by default, matching market/macro/job_runs/records collapse semantics; the legacy flag and `ARKSCOPE_USE_LOCAL_SA` are provenance only. The slice also removed the pre-cutover local-file-exists guard, tombstoned all PG-backed `scripts/migrate_sa_to_sqlite.py` modes (build, `--validate-only`, `--dry-run`), and made absent-file read-only `sa_capture_store.connect()` return an in-memory honest-empty schema so direct callers do not fall back to PG. Acceptance proof: fresh-profile poison E2E `pg_attempts` went `2 → 0`; routine real-profile E2E remained `ok:true` / `pg_attempts:[]`; focused SA gate `325 passed`; final A/B was identical (base `4883ac4`: 3,704 passed / 39 known failures; head `4d2f6f1`: 3,709 passed / same failure set). Review notes recorded: Task 1 routing flips were corrected from x3 to x5 mid-implementation, three absent-file tests were regression pins because the backend helper already handled absent files, and `4d2f6f1` fixed the plan blind spot where baseless DAL construction had become silent-half-built instead of loud-failing. Remaining queue: FRED product-semantics decision doc, dead-code sweep (including SA tombstone/dead PG readers), and IBKR news catch-up efficiency audit.
- **2026-07-05 (S-J Phase 2 provider-config strict flip COMPLETE)**: FF-merged `codex/s-j-phase2-strict` at `33a0cc4`. FieldDefs-managed provider configuration now defaults strict: `provider_env_fallback` unset/false means no `config/.env` runtime fallback for managed provider fields; explicit `provider_env_fallback=true` is the rollback lever. Missing required managed fields surface `provider_config_missing` across provider health, provider test route, scheduler `run_source`, worker entrypoints, and the Finnhub analyst tool; the canonical machine detail is the exact four-key contract `{code, status, provider, field}`. Live proof: primary profile preflight showed 8/8 managed fields from App config; fresh-profile poison smoke and rollback smoke passed; final A/B was clean (base `38d52c6`: 3,684 passed / 41 known failures; head `33a0cc4`: 3,704 passed / 39 known failures; diff = two base-only pre-existing analyst fixes, zero head-only regressions). The two analyst fixes were stale analysis tool-count expectations and a bridge test that implicitly depended on FileBackend auto-detection instead of a hermetic backend. PG-unreachable smoke gained a provider-config policy check so strict-mode regression is now part of the standing E2E gate. Follow-up now closed by the newest 2026-07-05 SA `use_local_sa` local-default collapse entry above.
- **2026-07-05 (P0-C.2 news burst hardening COMPLETE — startup burst false-failure path closed)**: Implemented and soaked the post-PG-exit news runtime hardening slice (`news-burst-hardening`, merged at `509cb5c`). Scope: normalized Polygon/Finnhub writer and IBKR news worker now pass `market_write_lock` as a short write-phase factory into `write_news_batch()` instead of holding an outer market DB lock across provider fetches; same-tick market-writer backpressure remains; lock-busy contention is retryable `skipped_lock_busy`, including through the IBKR sanitized subprocess seam (`retryable` + bounded `error` only for the known market lock message; generic provider/network `TimeoutError` remains non-retryable and does not expose raw text). Post-review P1 fix (`49425f5`) ensures `write_news_batch()` re-raises lock-busy from all resumable writer catch sites so it cannot degrade into `partial` / provider failure. Offline gates: focused suite `157 passed`; full A/B twice identical against `539a4ac` (known 41-entry failure set, head +13 tests). Live soak: startup burst fired one market writer and deferred the extras via `market_writer_backpressure`; later Finnhub and IBKR succeeded while Polygon continued a long run; three news sources ended durable `last_status=succeeded`, `last_error=None`; PG-unreachable E2E post-soak `ok:true`, `pg_attempts:[]`, 22/22. Non-blocking follow-up recorded: IBKR news long-catch-up efficiency / tail coverage audit, because a long quiet window observed ~35.7k IBKR items scanned across 148 tickers (near the provider's per-request cap); normal scheduled cadence remains fine, but catch-up behavior deserves a small separate audit.
- **2026-07-05 (PG-EXIT CLOSED — app-record archive ruling + use_local_records local-default collapse)**: Closeout ruling accepted the three remaining PG tables (`agent_queries`, `research_reports`, `agent_memories`) as out-of-runtime, archive-only — not a runtime dependency and not a PG-exit blocker. The closeout commit also fixed the last stranding hole found in post-batch-3 review: `use_local_records` was still unset→false, so `get_app_records_store()` on a fresh/reset profile (or a toggleless test double) would route app-record calls to `dal._backend` live PG SQL. Collapsed to local-default with batch-2 semantics — unset, explicit false, and toggleless dals all resolve `AppRecordsLocalStore` over profile_state.db; the legacy flag is provenance only. Old default-off→PG-backend test contracts flipped (TDD red→green); memory-tools fixtures rewritten from MagicMock PG backends to real local-store round-trips (seam-mock discipline). `PG_EXIT_COMPLETION_PLAN.md` now reads PG-EXIT IS CLOSED: all six DONE criteria hold, PG retains archive tables only. Follow-ups queue unchanged: news burst hardening / FRED product semantics / S-J Phase 2 strict flip / api.ts+hintRows dead-code sweep.
- **2026-07-05 (N9 batch-3 LIVE DROP EXECUTED — PG market-data physical exit complete; PG holds app-record archives only)**: Executed the reviewed `PG_EXIT_N9_BATCH3_PRICES_DROP_PLAN.md` end-to-end. Offline: `DatabaseBackend.query_prices`/`query_health_stats`/`get_available_tickers` retired to typed stubs; dedicated batch-3 gate CLI landed with the batch-2 validator as a strict subset plus scope/semantics checks; reviewer ran the full A/B (identical failure sets, +15 passed = exactly the new tests). Live gate caught one real blocker before approval: the first package (`b0ca91b9…`) was rejected because `get_recent_prices(character varying,character varying,integer)` holds a normal rowtype dependency on `prices` that a no-`CASCADE` `DROP TABLE` cannot pass — surfaced by external second-opinion review from the evidence's own dependency scan, empirically reproduced in a disposable DB, and closed by gate hardening (`179e745`): dependency-coverage machine refusal for uncovered normal `pg_proc` rowtype dependencies, `function_ddl.sql` archived via `pg_get_functiondef`, two-stage restore proof (restore table → apply function DDL → verify presence), and function-before-table drop order. Final approved package: fingerprint `028efacdc3a41f917eb6fa795ca97e5dff3e3c7f3508715d2d133a37711b902b`, archive `data/pg_archive/n9_batch3_prices_20260705T010022Z/`, dump sha256 `76b6cb6d…`, restore proof `ok`/`mismatches:[]`. Drop ran user-executed in one transaction (function then table, no CASCADE) with in-txn catalog verification; postcheck ok; pre-drop and post-drop PG-unreachable E2E each ran twice, all `ok:true`/`pg_attempts:[]`/22 checks; local NVDA smoke 104 bars. PG `prices` was frozen at `2026-07-02 14:15 UTC` with 2,314,293 rows and row fingerprint `e4b8e5d3…` (reviewer reproduced hours apart). **PG public schema now contains exactly `agent_queries`, `research_reports`, `agent_memories`. Next PG-exit item: app-record archive table policy (separate ruling) — accepting them as out-of-runtime archives closes PG-exit.**
- **2026-07-05 (N9 batch-3 prices archive/drop plan opened; no live drop authorized)**: Added `PG_EXIT_N9_BATCH3_PRICES_DROP_PLAN.md` as the reviewable authority for the final market-data PG physical drop. Scope: stub the final PG price methods (`DatabaseBackend.query_prices` and the prices branch of `query_health_stats`) before destruction; create a dedicated batch-3 CLI targeting only PG `prices`; protect app-record archive tables (`agent_queries`, `research_reports`, `agent_memories`); run pre/post PG-unreachable E2E smoke; produce a targeted `pg_dump` plus restore proof; and require explicit user approval before a no-`CASCADE` single-transaction drop. The plan locks three semantics: the PG `prices` dump is a frozen pre-cutover mirror archive, not a backup of current local `market_data.db` prices; `prices` is consciously removed from the batch-3 protected table set while app-record tables remain protected; and app-record archive policy remains a separate decision.
- **2026-07-05 (PG-unreachable E2E LIVE VERIFIED — next PG-exit item = batch-3 prices archive/drop)**: Completed the final non-destructive runtime proof before PG `prices` physical drop. Implemented `scripts/smoke/pg_unreachable_e2e.py` with a poisoned PG DSN and immediate `psycopg2.connect` trap, then triggered the plan's fallback rule after `TestClient` hung in the PG-unreachable environment: the harness now uses narrow handler-direct route calls with explicit dependencies while preserving the same required surface list and FastAPI `HTTPException(detail=...)` body shape. Live proof path: `scratchpad/pg-unreachable-e2e-20260704T172555Z/`; run-1 and run-2 both reported `ok:true`, `pg_attempts:[]`, 22 checks, 0 failures. Macro endpoints returned explicit config-disabled `503` because `macro_calendar.enabled=false` on this profile; this was classified as feature state, not PG failure. Harness lessons banked: direct script smoke needs repo-root `sys.path` bootstrap, connection-label sanitization must redact `psycopg2.connect(**kwargs)` credential values before recording, and PG-unreachable E2E should avoid `TestClient`/lifespan in this environment. Active PG-exit next item is **batch-3 PG `prices` archive/drop plan** with fresh dump/restore/approval; app-record archive tables remain a separate decision.
- **2026-07-05 (N9 batch-2 LIVE DROP EXECUTED — PG job_runs removed; remaining PG = prices + app-record archives)**: Completed the reviewed `PG_EXIT_N9_BATCH2_CLEANUP_PLAN.md`. Scope landed before live drop: local-default collapse for market/macro/job-runs (unset and explicit false no longer route normal runtime to PG), dead dropped-domain `DatabaseBackend` methods retired/stubbed, and `migrate_to_supabase.py` fail-closed for retired domains including `--prices`. Live gate: preview x2 byte-identical and then independently reproduced; final approved fingerprint `1bd083754c2e3c56fe93ea57c1b540c7c4c980117d737361509b451cc1963478`; archive `data/pg_archive/n9_batch2_20260704T162352Z/`; dump sha256 `dba8eb51bb3249f88d299047b30492867b11bdb531bfbb6d4d2a812e926ef71c`; restore proof `ok:true`, `mismatches:[]`; PG evidence `job_runs=13,652`, `news_search_vector_update()` present, protected tables (`prices`, `agent_queries`, `research_reports`, `agent_memories`) present; local takeover proof `profile_state.db.job_runs=13,672`; grep blockers `0`; poison smoke `186 passed`. Drop executed in one transaction with no CASCADE, removing PG `job_runs` and orphan `news_search_vector_update()`; postcheck `ok:true`, targets absent, protected tables present; post-drop poison smoke `186 passed`. Two gate lessons were fixed before the final approved drop: real CLI entrypoints must honor `sys.argv` (`5ff9581`), and read-only evidence prechecks must use a separate PG connection from the destructive transaction (`febec95`). Next PG-exit item is batch-3 physical PG `prices` archive/drop; app-record archive tables remain out of scope until separately decided.
- **2026-07-04 (N9 batch-2 cleanup plan opened; calendar soak converted to evidence gate)**: Authored `docs/design/PG_EXIT_N9_BATCH2_CLEANUP_PLAN.md` as the reviewable authority for the second N9 cleanup batch. Scope: collapse `use_local_market` / `use_local_macro` / `use_local_job_runs` so unset or explicit false no longer routes fresh profiles to dead PG fallback; remove/stub `DatabaseBackend` methods for already-dropped domains; retire `migrate_to_supabase.py` as a PG importer, including `--prices` (no `--archive-prices`, because post-P0-C PG prices is a static rollback/archive basis, not a sync target); and run an N9-style preview→dump→restore-proof→explicit-approval drop for frozen PG `job_runs` plus orphan `news_search_vector_update()`. Policy update: the original 2026-07-10 `job_runs` calendar soak is no longer the sole live-drop gate; early execution is allowed only if evidence proves PG `job_runs` is frozen or fully reconciled, local provider/SA/macro health reads local job history, runtime writes no PG `job_runs`, local-default collapse works with unset/false legacy flags, and a PG-poison/unavailable smoke proves Data Sources, Jobs, Provider Health, SA Health, Macro Health, prices, and news do not fall back to PG. If any evidence item is inconclusive, wait for the calendar soak and rerun the same gate. Non-goals remain explicit: PG `prices` physical drop is batch-3, and PG app-record archive tables require a separate decision.
- **2026-07-04 (P0-C.1 prices runtime hardening implemented; offline gate 160 passed)**: Closed the runtime follow-up opened after the first P0-C scheduler resume. Implementation restored the documented IBKR subprocess isolation for both `ibkr_prices` and `price_backfill` via sanitized `python -m src.prices_runtime`, kept the scheduler-owned Gateway lock by passing `--gateway-lock-held`, refactored `backfill_prices_direct()` so provider fetch occurs outside `market_write_lock`, added tick-level one-market-writer backpressure across normalized news + price writers, and classified worker-reported `market_data.db write lock busy (timeout)` as durable `skipped`/`skipped_lock_busy` instead of `failed`. Verification: `pytest tests/test_prices_runtime.py tests/test_data_scheduler.py tests/test_market_data_direct.py tests/test_provider_health.py -q` → `160 passed`. No PG price sync/fallback was reintroduced; PG `prices` physical drop remains batch-3 after soak and approval.
- **2026-07-04 (P0-C.1 prices runtime hardening opened after first scheduler resume)**: P0-C's data-authority cutover remains valid (`61bbf613…`, local prices authority, PG prices archive-only), but the first post-cutover scheduler resume exposed runtime issues that must be fixed before leaving IBKR price schedules enabled: `ibkr_prices` failed with `There is no current event loop in thread 'sched-ibkr_prices'`, normalized IBKR news showed `normalized IBKR worker failed (TimeoutError)`, and local writers surfaced `market_data.db write lock busy (timeout)`. Root cause: P0-C Task 4 selected the minimal in-process adapter for `ibkr_prices`, despite the scheduler's documented IBKR subprocess-isolation constraint (`ib_insync asyncio loop is safer in its own process`); `price_backfill` shares the same latent adapter bug; and `backfill_prices_direct()` holds `market_write_lock` across per-ticker provider fetch, starving news writers for minutes. Execution decision: open P0-C.1 runtime hardening (plan `PG_EXIT_P0C1_PRICES_RUNTIME_HARDENING_PLAN.md`) to move `ibkr_prices` + `price_backfill` behind a sanitized `python -m src.prices_runtime` worker, fetch provider rows outside the DB write lock, add one-market-writer-per-tick backpressure, and classify lock-busy as retryable skip rather than durable failure. This is a P0-C follow-up, not a roadmap rerank; PG `prices` physical drop remains batch-3 after soak.
- **2026-07-04 (P0-C prices direct-local cutover LIVE COMPLETE — app runtime no longer depends on PG market-data domains)**: P0-C completed after the audit-first path proved local `market_data.db.prices` was the correct authority. Final reviewed reconcile fingerprint `61bbf613c1fe94dd6558dc4bcc2bae7f9624e238df9c8114ed9a4e23da2580d2` reproduced byte-identically twice immediately before cutover: `unexplained_pg_only_count=0`, `value_checksum_mismatch_count=10`, `value_checksum_mismatch_row_count=19` (remaining 19 rows = June-23 volume revision noise; OHLC identical; keep-local). No bulk copy was performed. The only data mutation was the reviewed HAPN patch already applied under fingerprint `ebe085be…` (`315 INSERT + 269 UPDATE`) with backup `data/market_data.db.bak-pre-p0c-hapn-patch-20260703T232813Z.db`. Cutover backup `data/market_data.db.bak-pre-p0c-cutover-20260704T000609505759Z.db` created before merge. FF merge to master at `bc2ab0f` made runtime changes live: scheduler `ibkr_prices` routes to `src.market_data_direct.backfill_prices_direct`, `local_incremental` is retired, `/market-data/update` 409s instead of launching the PG mirror, `LocalMarketDatabaseBackend.query_prices()` is local-only, and `/market-data/status` + provider health mark prices as `authority=local`/mirror retired. Post-cutover smoke passed on live main checkout: NVDA 7-day price route function returned 81 bars; unknown ticker returned honest empty without PG; status reported `prices_authority=local`, `price_mirror_retired=true`, `pg_fallback_active=false`; scheduler source assertions passed. **PG runtime market-data dependency is now removed.** Remaining PG table `prices` is archive/rollback only until **batch-3**; physical drop requires a fresh targeted dump, restore proof, explicit approval, and conscious removal from N9 excluded-table protection. Next active PG-exit work returns to batch-2 after the 2026-07-10 job_runs soak gate; batch-3 prices drop follows separately.
- **2026-07-03 (P0-C 改判：Prices Reconcile + Direct-Local Cutover,不預設 bulk migration;batch-2 定 07-10)**: 開 P0-C plan 前的實測推翻了「N7-style migration」前提——**local prices 2,324,172 rows/149 tickers 多於 PG 2,314,293/150**（同 interval 15min、同日期終點 2026-07-02T14:15;差異 +9,879/-1 ticker 疑為 6/27 LC→HAPN alias+history stitch 只作用於本地）。使用者裁決原文要點:(1) **Audit/Reconcile First**——PG-only/local-only 按 (ticker,interval,datetime) 分解,驗 alias 假說,查 market_sync_meta/coverage/status 依賴;(2) **No Bulk Copy Unless Audit Proves Need**——差異可由 alias 解釋則禁止 bulk copy,真缺口才做小範圍 deterministic backfill;**不把已經更乾淨的本地資料用 PG 覆蓋回去**;(3) **Ingest Cutover 是主體**——collect_ibkr_prices.py 退場,direct-local writer 走 S-A1 模式（python -m src…、gateway lock、market_write_lock、idempotent upsert、sanitized output）,cutover=pause→final reconcile→flip→resume,無雙寫期;(4) **Retire Mirror/Fallback 納入 P0-C**——incremental_update(prices)/market_sync_meta['prices']/_local_refresh(("prices",))/manual-update-bootstrap 殘餘+query_prices PG fallback 收 honest-empty;(5) **PG drop = Non-goal**——PG prices 留作 archive/rollback basis,真 drop 是 batch-3,複用 N9 dump/restore/drop gate 另行批准（EXCLUDED_TABLES 屆時有意識地翻）。第一版 plan = docs-only plan+audit,不實作。**Batch-2（job_runs+孤兒 news_search_vector_update()+DatabaseBackend 死方法+migrate_to_supabase 退役碼）soak 判準:本地 job_runs 正常運行至 2026-07-10 + provider/SA/macro health 無 job-history 異常 → 到期按小 slice 插隊,不擋 P0-C 設計。**
- **2026-07-03 (N9 batch-1 LIVE DROP EXECUTED — PG 只剩 prices + 凍結 job_runs)**: 完整 gate 鏈跑通並執行第一個破壞性 PG 操作。時序：quiet window（scheduler/native-host/manual 全停,`ps` 證據）→ preview ×2 byte-identical → reviewer 獨立核對（MCP 聚合逐數字吻合;SA local superset;macro/cal 九表空證明）→ dump + **restore-to-disposable-DB proof（ok:true,row fingerprints 吻合）** → 使用者顯式批准 → drop（單 txn、4 語句、no CASCADE、txn 內 catalog 驗證）→ postcheck ok → app smoke pass（scored news == S-G 基線 8,869）。**最終批准包：fingerprint `fd995f7092ad9535499294506ec328836a44fe71e1370f2746fcef211bd14d21`,archive `data/pg_archive/n9_batch1_20260703T045919/`,dump sha `eb9fd854…`。** Dropped: 21 表（news 371,672/news_scores 503,226/fundamentals 130/iv_history 24/financial_data_cache 24/signals 0/sa_*×6/macro+cal 空表×9）+ view `news_latest_scores` + functions `news_sentiment_summary`+`get_recent_news`。過程中 gate 攔下四層真問題並修復（pgvector/pg_trgm 擴充複製 `c33e2b0`、trigger 函式 DDL+分號+`ON_ERROR_STOP` `33c2d7d`/`30540a0`、`get_recent_news` rowtype 依賴→drop #1 正確 rollback→目標修訂+依賴掃描補 `pg_type` 邊 `c331b55`、兩段式 DDL `d450fd3`）;批准包因 repo-grep 入 fingerprint 而三度換版（正確的 gate 行為）。教訓入檔（scoping §8）:preflight 工具鏈（pg client ≥ server major、真 ripgrep binary）、evidence→drop 期間 repo freeze、psql 一律 `ON_ERROR_STOP=1`。**Rollback levers 對已 drop 域正式失效,恢復=先 restore archive。Batch-2 隊列:job_runs（soak 後）+ 孤兒 `news_search_vector_update()` + DatabaseBackend 死方法 + migrate_to_supabase 退役碼。PG-exit 只剩 P0-C prices reconcile/direct-local cutover。**
- **2026-07-03 (N9 batch-1 offline implementation ready; live drop still gated)**: Implemented the reviewed `PG_EXIT_N9_BATCH1_DROP_PLAN.md` through the offline gate. Runtime hardening now prevents old PG-domain paths before destruction: manual/local refresh requests prices only, retired bootstrap/validate paths fail honestly, local IV reads are honest-empty on miss, `migrate_to_supabase --news/--iv/--fundamentals` is refused, and scheduler `iv_history` fails before provider work/PG sync with an N9 retirement message. Added `scripts/migration/n9_batch1_pg_drop.py` with deterministic evidence preview, targeted `pg_dump`, restore verification, reviewed-fingerprint drop gating, archive sha validation, re-preview before drop, no-`CASCADE` SQL, and post-drop catalog checks. Offline gate: focused suite `324 passed`; repo grep classifier reports `blocker_count=0` (`allowed_count=6054`). **No live PG evidence, dump, restore, or drop has been run by this commit.** Next step is the approval-gated live evidence/dump packet; destructive drop remains a separate explicit user approval.
- **2026-07-03 (N9 batch-1 destructive-drop plan authored; pending review)**: Added `docs/design/PG_EXIT_N9_BATCH1_DROP_PLAN.md` as the reviewable authority for the first destructive PG drop batch. The plan preserves the 2026-07-03 ruling (drop once after a compressed archive, with SA rollback basis replaced by the dump file) but tightens the gate: batch-1 must first remove any remaining runtime fallback to dropped domains, especially PG `iv_history`; must produce reader-free evidence and a deterministic fingerprint; must create a targeted `pg_dump` archive; and must prove that archive by restoring into a disposable database with matching row fingerprints before any live drop is approved. No live drop is authorized by this docs commit.
- **2026-07-03 (S-H2 financial-cache cold-start complete; next = N9 batch-1 evidence/plan)**: S-H2 implemented the 2026-07-03 cold-start ruling for PG `financial_data_cache` (PG evidence: 24 rows / 7 unexpired / 0 paid FD rows). Runtime fallback/promotion was removed from `LocalMarketDatabaseBackend.get_financial_cache()`: local miss now returns honest `None`, and callers refetch SEC/FD into local `financial_cache`. Verification: focused consumer gate passed (`tests/test_sqlite_backend.py` 67 passed; FD cache backend-mode tests 5 passed; fundamentals cache/stored-mode tests 15 passed), and the runtime grep gate found no PG fallback path (`DatabaseBackend.get_financial_cache` appears only in the allowed test assertion). The active P0-B line is now **N9 batch-1 evidence/plan**: include S-H3 empty macro/cal proof, final reader/script grep, compressed `pg_dump` table list, SA rollback-basis replacement citation, and a destructive-drop runbook requiring explicit approval.
- **2026-07-03 (S-H2/S-H3/N9 batch-1 正式裁決：cold-start、evidence-fold、dump once then drop)**: 基於 S-H audit + reviewer 的 MCP 實測（PG `financial_data_cache`=24 列/未過期 7/全 `sec_edgar`/付費 FD 列 0；PG `macro_*`/`cal_*` 九表全空 0 live tuples），使用者正式裁決三點。(1) **S-H2 = cold-start，不 promote**：小 TDD slice——移除 `get_financial_cache()` 的 PG fallback / read-through promotion，保留 local miss → SEC/FD 正常 refetch；「promote 7 列免費可重抓的 SEC 快取」無價值，少一個一次性遷移工具、少留代碼。(2) **S-H3 併入 N9 evidence，不另開實作 slice**：PG macro/cal 全空 → 無 seed/refetch 決策存在；只需 reader grep + 空表證據 + docs，空表列入 N9 batch；本地空 `cal_economic_events`/`cal_earnings_events` = honest-empty / future forward collection，非 migration gap。(3) **N9 batch 1 = dump once, then drop**：先做只含 drop 表的壓縮 pg_dump 離線 archive（破壞性操作前的證據保險，非 runtime 保留、非雙路徑維護），再 drop：PG `news`、`news_scores`、`fundamentals`、`iv_history`、空 `macro_*`/`cal_*`、`signals`（若存在且 reader-free）。**`sa_*` 一併 drop——本裁決明文取代 2026-06-13 SA cutover 的「PG frozen rollback basis / do NOT delete（revisit after cross-machine-migration gate）」規則：rollback basis 由 PG 活表轉為 dump 檔**。`job_runs` 等 soak 後入 batch 2；`prices` 維持獨立大 slice。**修正版排序：S-H2 cold-start → N9 batch-1 evidence/plan（含 S-H3 證據、reader grep、pg_dump/drop 清單、SA 裁決記錄）→ N9 batch-1 live drop（approval gate）→ soak 後 batch 2 → prices migration。** 本地 SQLite 的備份策略（可能對雲端）為獨立未來項，屬 Portability Layer，不綁 N9。
- **2026-07-03 (S-H1 local job-runs cutover live-complete; next = financial-cache / macro-cal follow-ups)**: S-H1 migrated PG `job_runs` into local `profile_state.db` and flipped `use_local_job_runs=true` after the same preview/copy/live gate discipline used by N8a/S-G. The implementation also preserves LOCK #9: `scripts/sa_native_host.py` no longer writes app state directly and records extension job telemetry by best-effort POST to the sidecar-owned endpoint. Live proof: reviewed fingerprint `38cf152141aae4304344baeeb46c6476f9870ff0f86fb793469bed96b0cad447`, `pg_rows=13,652`, local `job_runs=13,652`, status distribution `succeeded=13,371 / failed=257 / running=24`, backup `data/profile_state.db.bak-pre-s-h1-job-runs-20260703T021241462312Z.db`, copy dry-run idempotence `already_applied=true`, live `quick_check=ok`, local `JobRunsLocalStore.run_summary_by_name()` smoke succeeded for scheduler/SA jobs, and focused gate `216 passed` after fixing the SA health regression that distinguished unavailable job history from empty history. PG `job_runs` is now archive-only and an N9 drop candidate after soak + final reader/script grep. Active next for P0-B becomes S-H2 financial-cache strict-local/promotion and S-H3 macro/cal proof; prices migration remains a separate large P0-C slice.
- **2026-07-03 (S-H orphan/app-state audit complete; next = job_runs / financial-cache / macro-cal follow-ups)**: S-H produced `PG_EXIT_S_H_ORPHAN_APP_STATE_AUDIT.md`, a read-only audit over local SQLite state plus static reader grep. Current local profile flags are `use_local_records=true`, `use_local_sa=true`, `use_local_macro=true`, `use_local_market=true`, `news_pg_exit_completed=true`, `use_normalized_news_writes=true`; `use_local_market_strict` remains unset. Local proof: `sa_capture.db` is healthy and populated (`sa_market_news=22,086`, `sa_comment_signals=39,853`), `profile_state.db` owns app-records (`agent_queries=2`, `research_reports=2`, `agent_memories=1`, `scheduler_state=5`), `macro_calendar.db` owns macro/FRED and IPO rows but has empty economic/earnings event tables, and `market_data.db` now has `news_article_scores=491,808` after S-G. Disposition: PG `news`, `news_scores`, `fundamentals`, `iv_history`, and likely `sa_*` are N9 candidates after final reader/script grep; legacy PG `signals` has no runtime SQL reader/writer found and can be retired if present. Not drop-safe yet: `job_runs` is still an active best-effort operational log used by jobs/provider health/SA health/macro health/scheduler continuity, `financial_data_cache` still has PG read-through promotion when market strict mode is unset, and macro/cal needs PG row-count comparison because local economic/earnings tables are empty. Active next for P0-B becomes the S-H follow-up set: S-H1 local job-runs/ops store (`profile_state.db` vs `ops.db` decision), S-H2 financial-cache strict-local/promotion decision, S-H3 macro/cal proof; N9 drops follow only after those seams are closed or explicitly accepted. S-H1 now has a reviewable implementation plan at `PG_EXIT_S_H1_JOB_RUNS_LOCAL_PLAN.md` recommending `profile_state.db` for v1 plus a fingerprinted PG→local migration gate.
- **2026-07-03 (S-G scorer cutover live-complete; active next shifts to S-H/N9 audit)**: S-G migrated PG `news_scores` into local SQLite `news_article_scores` and removed the hard-local score degradation from score-dependent news reads. Gate discipline matched the established N7/N8a pattern: live preview was byte-identical across two runs; reviewer independently verified PG/local aggregates; copy dry-run apply + idempotence passed; live apply used reviewed fingerprint `34607859293ae7ee20726448e1b733fe55b2cf9fc720a31f6c97a853dec76ab3`; backup path `data/market_data.db.bak-pre-news-scores-sg-20260703T002733945228Z.db`; live apply inserted/updated `491,808` rows from `503,226` PG rows, with `604` rows skipped because their N7 legacy rows were rejected and `14` skipped because their legacy IDs were missing from the local map; invalid/missing-score invariants were zero; idempotent reapply inserted/updated `0`. Future active score imports use `scripts/scoring/import_news_scores_local.py`; `migrate_to_supabase --scores --archive-scores` is archive-only; PG `news_scores` and the PG-only `query_news_scores` helper are now N9 drop/dead-path candidates after final reader grep. Active next for P0-B becomes **S-H orphan/app-state audit**, followed by N9-safe drops; prices migration stays a separate large P0-C slice.
- **2026-07-03 (C-1 status reconciliation — already landed; active next shifts to S-G)**: 開 C-1 implementation-plan worktree 時先按本 map 對照，結果發現 `get_sa_feed` / `GET /sa/feed` / News 表面 SA source filter / agent bridge wiring 已經在 master 歷史中落地（核心 commit `b3ebd21` backend+route+tool、`a71d317` backend verification follow-ups、`3d717d6` frontend、`61982e3` hardening、`1abf3e9`/`52df08b` snippet cleanup）。這表示 2026-07-02 re-rank 的「C-1 是使用者可見主線」方向沒錯，但 implementation state stale；不應再寫一份新的 C-1 implementation plan。Focused verification: `tests/test_sa_feed.py` 14 passed in the C-1 plan worktree. `SA_EVIDENCE_FEED_C1_SPEC.md` 狀態改為 implemented/reconciled。Active next 因此改成 **P0-B / S-G scorer cutover**；C-1 現在是 C-2 AI Research 的既有 primitive，而非待實作項。
- **2026-07-02 (方向校準：PG-exit 主線確認 on-mission；IV 線收斂定位、付費採樣 gated；下一優先 = PG-exit 收尾核心 + Layer C-1)**: 使用者於 S-C IV survey + proof-packet plan（`IV_PROVIDER_PROOF_PACKET_PLAN.md`）併入後主動發起偏航檢查。結論分四點。(1) **已完成的 PG-exit slices 全部在主線上**：news N8a live-complete、S-B fundamentals refetch/cache（FF `502bca8`）、S-A1 scripts-retirement demonstrator、S-J provider-config authority Phase 0–1 + email-first UA micro-fix（FF `df32661` / `5d25d70`）——皆隸屬 2026-06-15「PG退場是產品目標」決策；S-J 另直接服務 north star 的 zip-and-go 可攜性（config 入 profile 而非 `.env`）並把 DG-0（SEC contact 治理）永久化為 app-managed 欄位。(2) **IV 線收斂定位**：IV 不在本 map 上、無現任 consumer（RL 為 P3 暫停、signals 為 separate future surface），而 PG-exit 對 IV 的要求只有「drop + 能力設計保存」。已完成的 §7 契約 + S-C survey + proof-packet plan 視為**便宜的設計保險，到此為止**；packet Task 1（免費、names-only 成本/授權盤點、停在 sign-off）可執行。**付費 IV 資料 = research subscription，適用本 map 訂閱框架：需 written hypothesis + OOS plan + kill criteria + cost-vs-value 方可批准——目前皆不存在，故 sign-off 站預設 defer**；S-D schema lock / S-E prototype 排入主線之後，除非 hypothesis doc 先行成立、或免費採樣結果實質改變判斷。(3) **下一優先（本次 re-rank；先後同日裁決）**：**C-1 SA evidence feed 先行**（spec 已鎖 `SA_EVIDENCE_FEED_C1_SPEC.md`，直接使用者價值）；**S-G scorer cutover 作為 PG-exit 小件平行/緊接**（小、補 PG-exit 缺口，不被 C-1 擠掉）；**prices migration（2.25M rows）單獨排大 slice**，不塞進小修節奏；S-H app-state/orphan audit、S-I/N9 隨 PG-exit 收尾序推進。使用者正式裁決原文：「IV 線定位為『PG-exit 設計附屬』。已完成 survey/packet plan；只允許執行免費 Task 1 到 sign-off，付費採樣預設 defer，除非另有 written hypothesis + OOS plan + kill criteria + cost-vs-value。下一主線為 C-1 SA evidence feed；S-G scorer 作為 PG-exit 小件平行/緊接；prices migration 單獨排大 slice。同步刷新 priority map TL;DR，避免 post-pivot 路線繼續靠舊表格判斷。」Task 1 因此定位為 IV 線的**收尾封存動作，不是開線**。**首個實際動作 = 本 map TL;DR refresh**（防漂移地基，先於 C-1 / S-G 開工完成）。(4) **流程教訓**：IV 線是「map 外逐步局部合理化」的標準漂移形態——每個 slice 個別 review 皆通過，但無人對照本 map（reviewer 已認領此疏漏）。今後開任何新 multi-slice 線，開線時必須先對照本 map（不在 map 上 → 論證插隊或補記錄後才動工）；slice review 檢查表新增「map 對照」一項。
- **2026-06-19 (agent data-gap fallback recorded)**: The `SNDK` SEC log (`SEC_CONTACT_EMAIL` missing, `Could not find CIK`, repeated `No SEC data found`) clarified a broader gap: the tools exist (SEC EDGAR + Tavily/OpenAI web search), but data primitives do not yet return enough structured absence for a deterministic fallback workflow, and web search is model-selected rather than guaranteed. Added `AGENT_DATA_GAP_FALLBACK_PLAN.md` as the active backlog: DG-0 SEC contact hygiene, DG-1 structured SEC absence, DG-2 ticker/CIK resolver, DG-3 controlled web-evidence fallback, DG-4 provider data-gap telemetry. Priority: set `SEC_CONTACT_EMAIL` immediately; do not interrupt Slice 6/7 closeout unless evidence gaps become the live blocker; fold telemetry into the provider-health work.
- **2026-06-15 (PG runtime retirement + provider-growth storage rule locked)**: PG退場是產品目標，不是可有可無的優化；PG→SQLite mirror 只是 migration bridge，最終 app runtime 應直接讀寫本地 stores，PG 保留為 legacy/archive/import。新增/調整 `DATA_COLLECTION_AND_LOCAL_STORAGE_PLAN.md` §4a：R0 inventory → R1 local job/provider telemetry → R2 provider adapters write local → R3 local app records → R4 disable live PG fallback → R5 retire/move migration scripts (`migrate_market_to_sqlite.py`, `migrate_sa_to_sqlite.py`, `migrate_to_supabase.py`). 同時鎖定 `LOCAL_STORAGE_TOPOLOGY.md` 的 provider-growth rule：這不是把 PG 退場和多 DB 混為一談；資料源增加會提高 SQLite writer contention 風險，所以每個新 provider/collector 必須先判斷 writer shape。in-process、低頻、idempotent 可直接寫 canonical DB；獨立 process、高頻 stream、bursty capture、paid-provider response 應使用 source DB/inbox 或 app-owned writer。若 lock safety rule 不成立，答案不是保留 PG，而是增加本地隔離邊界。
- **2026-06-15 (AI Research context/memory principles locked)**: C-2a AI Research streaming surface and C-2b local thread persistence are not sufficient by themselves; persisted transcript != usable memory. New plan at `docs/design/AI_RESEARCH_CONTEXT_MEMORY_PLAN.md`. Locked principles: (1) persisted thread transcript is complete; never delete/truncate history as a cost optimization, (2) prompt context selection is not memory deletion, (3) no silent truncation — context overflow must be visible/persisted or use an explicitly configured policy, (4) no hidden "last N messages" default; caps must be user-configurable, (5) conversation history helps intent but fresh market/SA/news/filing facts still require tool/evidence reads, (6) future compaction must retain raw transcript/evidence, following P1.4's "summary in prompt + raw on disk" precedent. Immediate C-2c direction: provider-neutral server-side history builder for `/query/stream`, default full-thread context while sessions are small, fail loud on provider context rejection. Longer-term post-baseline pillars recorded in the same plan: low-value news/information filtering, user-visible strategy/skill generation from conversation, and stable tool-interface extensibility. External projects (Hermes Agent, GitNexus, Graphify) are research references only, not adopted architecture.
- **2026-05-31 (ArkScope rename Phase 2 EXECUTED — P3.2)**: Local repo dir `mv MindfulRL-Intraday → ArkScope` (by user) + follow-on completed this session — case-sensitive `MindfulRL`→`ArkScope` across **47** code/config/script/test + pure-identity-doc files (SEC `_DEFAULT_SEC_CONTACT`, FastAPI title/desc, package docstrings, agent/Discord names, test banners, fixed `cd <repo-root>` shell paths); Claude Code private memory copied to the new `…-ArkScope/memory/` project key (16 files, verified identical); root `CLAUDE.md` **deleted** (gitignored → backed up `~/arkscope-CLAUDE.md.backup-20260531`, rebuild later). **Intentionally NOT renamed** (lowercase `mindfulrl`): PostgreSQL DB name/password (`mindfulrl`, `mindfulrl-postgres`, `mindfulrl_pgdata`, password redacted — see `config/.env`), Native Messaging host id `com.mindfulrl.sa_alpha_picks`, Firefox addon id `sa-alpha-picks@mindfulrl.local`, `/tmp/mindfulrl_exec_` prefix, extension JS internals, `docs/design/archive/*`, dated `PAID_SUBSCRIPTION_EVALUATION` report, `UPSTREAM.md`/`rl_trading_primer.md` lineage diagrams. Conversation transcripts NOT migrated (old project dir retained for lookback). Discussion docs (README / this §1 / CURRENT_PROJECT_CONTEXT / LOCAL_FIRST_*_SPEC+AUDIT / DESKTOP_APP_VISION / runbook status) rewritten, not blind-replaced; `ARKSCOPE_RENAME_PHASE2.md` was the authority. **Still open**: a further *product-brand* rename if the workbench positioning picks a name other than ArkScope — do NOT preemptively introduce a 4th name.
- **2026-05-03 (local-first spec landed + docs cleanup queued)**: `docs/design/LOCAL_FIRST_RESEARCH_WORKBENCH_SPEC.md` committed as `b0743da` — 9 locks + 7 open-question decisions; no spec-internal opens. Two tightenings vs the 2026-05-02 entry below: (1) v1 storage = **two-SQLite split (`workbench.db` app state + `sa_cache.db` SA ingest, independent per-file writer locks)** + **transient DuckDB on parquet** (NO persistent `warehouse.duckdb` in v1; `warehouse.duckdb` named as v2 candidate per spec §11.1); (2) **external ingest clients contract (LOCK #9)** — SA native host writes `sa_cache.db` exclusively, never `workbench.db`; future ingest clients each get their own isolated cache DB / inbox file; main app reads SA via `ATTACH DATABASE 'file:...?mode=ro'` (URI form mandatory; storage-isolation tests assert writes to `sa.*` fail with `SQLITE_READONLY`). Migration cut #1 (`ProfileLocator` + `app/001_research_reports.sql` + manifest + lock + cross-machine smoke) does NOT open yet — docs cleanup queued first per `docs/design/DOCS_GOVERNANCE_AUDIT_2026_05.md` to remove pre-pivot entrypoint pollution before code work begins.
- **2026-05-02 (formal product repositioning — local-first financial research agent workbench, Phase C paused)**: This is more than a deployment-target pivot. The project formally transitions from "agent dual-SDK refactor + service expansion (a backend with tools)" to **local-first financial research agent workbench (a product)**. **North star**: the agent reads its own accumulated knowledge substrate across sessions and machines; the user sees and edits everything via a research GUI; zipping the profile directory moves a researcher's work between machines. The product combines Hermes-like agent capabilities (persistent memory, skills, tools, scheduler, replay, compression, subagents) with this codebase's existing financial data layer (news / SA / macro / prices / fundamentals / signals) and a research GUI (ticker workspace, evidence browser, memory/notes, scheduled jobs, reports, source health, agent investigation trace). **Five-layer architecture**: (1) **Workbench UI** — research workspace (NOT admin dashboard; P1.5 minimal admin UI is repositioned to ops/health view only); (2) **Agent Layer** — Hermes-capability stack on existing dual-SDK paths (Phase C will eventually unify but is paused); (3) **Data Layer** — ingestion + caches + FTS5 + future vector + cross-source joins; (4) **Profile Layer** — single local profile directory (SQLite app DB + DuckDB/Parquet OLAP + raw files + reports + memories + skills + exports); (5) **Portability Layer** — zip-and-go + single-writer lock + manual export/import; v2 selective sync. **Hermes is capability reference, NOT product reference**: borrow filesystem-first profile + daemon scheduler (`~/.hermes/cron/jobs.json` + 60s gateway tick) + FTS5 cross-session recall + skills/tools flexibility; differentiation = financial data layer + investment research workflow, not general-purpose agent platform. **Phase C paused** — runner refactor is now second-order to product boundary / profile / sync / GUI IA / agent-to-data API. Spec at `docs/design/PHASE_C_UNIFIED_RUNNER_SPEC.md` preserved as-is (closures all valid). Resume gate (all three): workbench v1 ships + 2 weeks stable single-user use + ≥1 verified cross-machine migration. **Three things being formally re-evaluated**: (i) PostgreSQL-first schema → SQLite (app state) + DuckDB (OLAP) profile-bound storage; (ii) CLI-first workflow → GUI-first workflow (CLI becomes test/dev tool); (iii) Phase C runner refactor immediate priority → paused. **What survives unchanged (foundation, not throwaway)**: P0.1 replay net, P1.4 compression, job_runs persistence, SA ingestion + comment intelligence, macro calendar, tools registry, skills system, episodic memory tools, file attachment system, prompt caching. The agent capability stack is already ~80% of Hermes parity; the gap is filesystem-first profile + daemon scheduler + FTS5, not capability rewrites. **Four locked decisions**: (a) PG NOT optional runtime — v1 SQLite + DuckDB only; PG → archive/import; dual-backend mode rejected (multiplies query / migration / test cost); (b) DuckDB added but strictly OLAP/columnar (news / price / macro / backtest / cross-source) — SQLite owns app state / job_runs / memory / settings / watchlist / notes / FTS5; DuckDB `ATTACH` SQLite for cross-engine joins; (c) packaging deferred — spec only locks deployment-target invariant (local web app or desktop wrapper must read same local profile directory); PyInstaller / launcher / Tauri choice waits for scheduler daemon model + storage profile to settle; (d) audit done before spec — without it spec is a wishlist. **Active sequence**: `docs/design/LOCAL_FIRST_RESEARCH_WORKBENCH_AUDIT.md` (file name explicitly carries the new positioning) → `LOCAL_FIRST_RESEARCH_WORKBENCH_SPEC.md` → read-only UI skeleton against existing PG reads → SQLite/DuckDB migration first cut (kill criterion: zip-and-go works on a second machine). **Six refinement locks** (not re-litigated): (a) sync surface is the main axis, not an appendix — "能不能跨機同步是根本問題"; (b) read-only prototype lists future write actions even if not implemented (else DTOs will be wrong-shape); (c) DTOs bidirectional from day one (`ReportDTO` + `ReportCreateInput` + `ReportUpdateInput`, three together); (d) "first migration cut" = read-only-prototype demand + zip-and-go smoke (NOT "migrate everything"); (e) Phase C resume gate is the three-AND above (NOT "when boundary feels stable"); (f) cross-machine smoke at mid-stream, not end — first SQLite table migrated triggers immediate second-machine test. **Risk**: SQLite has no GIN/tsvector — `agent_memories` (sql/004) and `news_search` (sql/006) need FTS5 redesign + tokenizer choice; Chinese support requires custom tokenizer / jieba pre-tokenize / trigram fallback. Spec must size that explicitly. **Repo identity implication**: `MindfulRL-Intraday` no longer matches the positioning. ArkScope rename Phase 2 (P3.2) becomes higher-priority once workbench v1 ships; README + `CLAUDE.md` content rewrite scheduled for spec phase (flag, not action). **Reference projects (capability or shape, not blueprints)**: Hermes Agent (filesystem + daemon pattern), FinceptTerminal (Qt6+C++ native — too heavy a stack but validates "single-binary research terminal" concept), `daily_stock_analysis` (Python + web workbench + scheduler — closer shape but batch-then-push, not interactive workbench).
- **2026-05-02 (Phase C §8 closures)**: Phase C spec §8 open questions all resolved before commit 1 opens. (1) **`build_runner(provider, config, dal, ...)` is the public entrypoint**; subclasses still exported but labeled tests/advanced-only — business call sites use the factory. (2) **`AgentEvent` imported only from `src/agents/shared/events.py`**; `runner/message_types.py` owns only `ToolCall` / `ToolResult` / `TurnResult`. (3) **`run_stream` emits coarse `AgentEvent` shape only**; raw provider deltas stay internal to per-provider subclass; raw-delta passthrough is a Phase C v2 candidate (matches P0.1 non-goal on token-level capture). (4) **Commit 5 adds `dispatch_subagent_unified` path** alongside legacy `_run_anthropic_subagent` / `_run_openai_subagent`; both paths coexist through commit 6 (so flag-ON / flag-OFF dispatch can be compared identically by regression tests); legacy is deleted at commit 7. (5) **Flat `tests/test_runner_*.py`** layout (no nested `tests/runner/` dir) matching repo convention. Spec status flipped to "ready for commit 1". Code OPENS once these closures are reviewed — but not before.
- **2026-05-02 (Phase C spec follow-up)**: Spec review pass 1 closed three High findings + two Medium + one Low in `docs/design/PHASE_C_UNIFIED_RUNNER_SPEC.md`. (1) **OpenAI raw Responses API moved out of v1**: reviewer flagged that switching from `openai-agents` SDK to raw `openai.responses.create()` is a behaviour migration (event/history/retry/tool-loop semantics), not a refactor — replay net only partially covers SDK drift. v1 now keeps `OpenAIRunner` as an SDK-`Runner.run()` wrapper that transforms output to the unified shape; SDK stays installed through commit 7. Raw API migration relocated to a new §9 "Phase C v2" placeholder — separate spec, separate cycle, parked unless a concrete trigger emerges. (2) **Commit count drift**: prose said "7 commits", table had 0-7 = 8 entries. Clarified: 7 implementation commits (1-7) + spec landing as commit 0. (3) **Compaction asymmetry**: matrix row 11 falsely claimed both providers wire compaction via `ContextManager` — actually only Anthropic does; OpenAI uses `OpenAIResponsesCompactionSession` (SDK-driven server-side). Row split into 11a (Anthropic client-side via `ContextManager`/`ContextCompressor`) + 11b (OpenAI server-side via SDK) — Phase C preserves the asymmetry, does NOT try to unify. (4) Registry count 49 → "registered built-in tool table" (current count is 55, but spec shouldn't hard-code; tests own count). (5) "call existing builders unchanged" → "consume `shared/server_tools.py` directly" (the old `_build_*_tools_list` shims get deleted in commit 7, so the unified runner must not depend on them). (6) `AgentEvent` path corrected from `shared/scratchpad.py` → `src/agents/shared/events.py` (verified). Spec stays in spec phase — code does NOT open until reviewer confirms these closures.
- **2026-05-02 (Phase C spec, SUPERSEDED)**: **Superseded by the review-pass-1 entry above on the same date.** Original entry described the spec landing with: 7-commit chain, 19-row parity matrix, final commit removing `openai-agents` SDK, runner using raw `openai` Responses API directly, OpenAI both providers wiring compaction via `ContextManager`. Reviewer caught three High findings on these claims (raw API in v1 is behaviour migration not refactor; commit count drift 7 vs 8; compaction asymmetry — only Anthropic uses ContextManager) plus two Medium + one Low. See review-pass-1 entry above for the corrected shape (v1 keeps SDK + wraps `Runner.run()`, v1 deletes old `*/agent.py` + `*/tools.py` but `requirements.txt` unchanged, raw API moved to a parked §9 "Phase C v2" placeholder, matrix split into 11a/11b for compaction asymmetry, hard-coded tool count dropped, builder shims replaced with direct `shared/server_tools.py` consumption, `AgentEvent` path corrected to `shared/events.py`). This entry is preserved for historical traceability of what the spec originally locked vs what review-pass-1 corrected.
- **2026-05-02 (spike done)**: P1.5 OSS spike closed. Decision doc at `docs/design/P1_5_S3_OSS_SPIKE_DECISION.md`. **Custom minimal (FastAPI + Jinja2) chosen** — sqladmin / FastAPI Admin rejected on SQLAlchemy-gap kill criterion (repo is psycopg2-direct in 8 source files, ORM migration is a multi-day refactor that nets negative for a tool we'd then strip CRUD off); Superset rejected on deployment overhead + BI-shape mismatch (use case is ops monitoring, not data exploration); RQ Dashboard / Prefect UI rejected as out-of-scope (we have an in-process scheduler, no queue). Custom minimal passes the four non-negotiables: no ORM, single-process, reuses existing pydantic-typed JSON read paths (`/jobs/status`, `/jobs/history`, `/sa/market-news/health`, `/macro/health`, `/reports`), read-only default. v1 estimated 300-500 LoC under `src/api/admin/` (Jinja2 templates + thin route module). HTMX is a v1.1 additive choice, not v1. Integration is NOT next — P1.5 stops at the decision doc, Phase C spec begins immediately. When integration cycle opens, the decision doc is the input.
- **2026-05-02**: Next-step decision after P0.1 full-v1 + P1.4 land. Selected sequence: (1) P1.5 strict 1-day OSS spike — decision doc ONLY (sqladmin / FastAPI Admin / Superset / minimal-custom); first kill criterion is the SQLAlchemy gap (repo is FastAPI + psycopg, not ORM-first); explicit non-goals: no UI integration, no styling, no "while we're at it" features. (2) Phase C spec — no code yet, locks v1 boundaries + feature parity matrix vs current dual-SDK paths + replay-gate use during cutover + flag-rollout ramp + commit chain (will mirror P1.4's 5-commit shape). Phase C code opens only after spec review. Rationale: Phase C is the highest-leverage P2 item but has the highest blast radius — replay safety net is exactly what was built for it, but rushing into the refactor without a written spec wastes the safety net. P1.5 spike between is a bounded, interruptible context-switch that delivers a real artifact (decision doc) before re-entering the main thrust. Kept deferred: P0.4 anti-bot fallback / detail backfill / p95 monitoring (waiting for telemetry to indicate chronic problem); P2.2 Knowledge Graph (SA + News data not yet mature); P2.4 Key Markets (opportunistic only, must not preempt main thrust).
- **2026-05-01**: P0.1 Replay Harness FULL v1 landed in 3 commits + 5 review-fix passes (`14567db` commit 1 OpenAI capture + canonicalization + shared `server_tools.py` for hosted tools → `8829780`/`682ac49`/`0c63f49`/`c3353ea` close High/Medium/Low + AST-guard test → `229d033` commit 2 fixture set + 3 schema fields + attachment classifier + OpenAI install-order fix → `d0a3f21`/`0a46512` round-1/round-2 review findings → `a8eaf60` round-3 wording cleanup → `4e30e71` commit 3 parametrised pytest gate + unified resolver via new `shared/bridge_tools.py` + (type, block_kind) attachment pair gate + subagent-recursion role-prefixed errors → `172b5a1` Low fix availability diff is resolver-aware). Full-v1 spec at `docs/design/P0_1_FULL_V1_SPEC.md` — status "3/3 commits landed". Phase C now has the safety net; resolver contract proves pinning is REQUIRED-RESOLUTION (NOT skip-list) end-to-end. 98 replay-suite tests + 294 cross-suite regression. CI wiring stays orthogonal — `tests/test_replay_fixtures.py` is the parametrised gate, runs whenever pytest does. Post-commit-3 ad-hoc `scripts/replay_run.py subagent_turn.json` produces no misleading "Tools no longer registered: delegate_to_subagent" warning.
- **2026-04-30**: P1.4 Compression Phase B 5/5 landed in 5 commits + 5 review-fix passes (`99f90c9` spec → `05960fc` overflow_store → `15f4259` Layers 0-3 → `08bbbb0` ContextCompressor wiring → `eaf13c0` observability + /overflow CLI → `1ed4d77` Layer 5 + 6 → `95ff3cb` final follow-up). All five spec guarantees held end-to-end (library-not-runner, subagent path unchanged, Layer 0 retrieval-on-demand, no fabricated reasoning summaries, per-layer toggles). Default OFF maintained: master `compaction.enabled=true` ships the always-cheap layers, but `compaction_layer_5_enabled=false` keeps the LLM-call layer dormant until explicitly opted in or armed for one turn via `/compact`. Layer 4 (provider-native compaction) is wired as opt-in adapter; Layer 5 caller defaults to `claude-sonnet-4-6` streaming with 3-failure circuit breaker; Layer 6 anchor recovery is hard-capped at 1 KB. `get_overflow_payload` retrieval helper retained CLI-only — gated agent exposure deferred to v1.1 to avoid an unbounded prompt-injection vector. Three review-driven design changes made it through (none structural): (1) commit-4 `/overflow show` was bypassing the OverflowStore integrity gate — fixed in `b3d44e2`; (2) commit-5 `/compact` was silently no-op'ing under the planned default config because the CLI only built `AnthropicSummaryCaller` when `layer_5_enabled=True` — fixed in `95ff3cb`; (3) commit-5 `apply_layer_5` was conflating boundary-noop with caller-failure → three rapid `/compact` attempts on a short history would burn the circuit breaker with zero actual LLM faults — fixed by switching to `Layer5Outcome` literal (`success` / `noop` / `failed`) in `95ff3cb`. 308 tests across compressor + adjacent layers (≈230 net new for P1.4). Unblocks Phase C (Unified Runner) — replay capture wired into Anthropic path already covers compaction-triggered turns.
- **2026-04-28**: P1.4 Compression Phase B framing locked. Decision: compaction is a **steady-state operating layer**, not a defensive cap. Today's path with `server_compaction=false` means large tool results dump straight into the prompt; P1.4 turns that into "summary in-prompt + raw on disk (retrievable)". Borrowed three patterns from `~/PycharmProjects/AI_Agent_Researcher` (marker-based summary, recent boundary, circuit breaker) but rejected drop-in code (their `ClientCompactionSession` is OpenAI-Responses-only; we need dual-provider) and rejected OpenAI SDK Session injection (proven painful in their codebase). Provider-native compaction (Anthropic `compact-2026-01-12`, OpenAI `responses.compact()`) kept as Layer 4 *optional* adapters, not the default path. Five guarantees in the spec stop scope creep: (1) compressor is a library not a runner — future multi-round work reuses without rewriting, (2) subagent path unchanged, (3) Layer 0 turns "always-dump" into "retrieve-on-demand" — net positive for heavy-tool queries, (4) no fabricated reasoning summaries (drop or verbatim, never invent), (5) per-layer toggles + master `/compaction off`. `get_overflow_payload` retrieval helper kept CLI-only in v1 to avoid creating an unbridled prompt-injection vector; v1.1 candidate is gated agent exposure with size_cap + session_id binding + audit log. Multi-round runner explicitly deferred (Phase C / P2.x). Spec at `docs/design/P1_4_SPEC.md`; 5-commit plan locked.
- **2026-04-27**: P1.3 SA Digest / Reading Workflow v1 landed in 3 commits + 4 follow-ups (`ce5f780` spec → `1108310` 5 review findings → `55c02bd` tool+tests → `70065c5` 4 commit-1 follow-ups → `a19441e` registry+bridges → `68a60a5` dispatch regression+stale docstrings → `fd9fb80` envelope assertion → commit-3 skill prompts+acceptance tests+status flip). Locked-in shape: `get_sa_digest(ticker, days=14, max_articles=5, max_news=5, max_comments=8, min_comment_score=4.0)` returns deterministic evidence pack with `recent_articles[]`, `high_discussion_news[]`, `high_value_comments.{ticker_mentions[], candidate_mentions[]}`, `data_quality`, `source_notes[]`. Selection rules in SQL (layered CTE: per-article cap of 3 applied BEFORE per-kind `rn_per_kind` to avoid underfill); `needs_verification=true` rows pass through as flagged claims; `min_comment_score` clamped to `[0.0, 10.0]`; ticker uppercased reaching SQL params; `keyword_buckets` shape preserved as `Dict[str, List[str]]`. Skill prompt updates: `earnings_prep` mandates `days=30` call (added to `data_sources.required`), `full_analysis` recommends `days=14` (added to `data_sources.optional`). 22 unit tests + 2 skill body tests + 1 dispatch regression test + 12-file count-assertion sync. Bridge counts after wiring: registry 55, news category 8, Anthropic + OpenAI bridges 56 each. The reframe-then-narrow-then-test pattern proved valuable: review at every commit caught the SQL underfill case, the `keyword_buckets` shape drift, and the wrapper-removed-still-passes envelope assertion gap before they shipped.
- **2026-04-27 (revision)**: P1.3 reframed from "SA Latest Articles metadata v1" → "SA Digest / Reading Workflow v1". Diagnosis: existing SA data (Alpha Picks articles, Market News, Stage 1 comment signals) is already in DB but the agent doesn't surface it in decision flows; the bottleneck is **usage**, not coverage. v1 ships a deterministic evidence-pack tool `get_sa_digest(ticker, days=14, ...)` that composes the three existing tables with locked selection rules (≤3 comments per article, `min_comment_score=4.0` default, `needs_verification` rows kept, ticker-vs-candidate mention split preserved). **No internal LLM call** in the tool — main agent produces Chinese summary in its own context, keeping the test surface deterministic and avoiding nested LLM cost/retry/timeout/prompt-injection (gpt-5.5 review during spec). Skill prompt updates target `earnings_prep` (must call, days=30) and `full_analysis` (recommended, days=14); `portfolio_scan` deferred until SA-coverage rate is observed. Out of scope for v1: new SA data sources (transcripts / StockTalks / author tracking), server-side scrapers (kept out forever — extension is the only ingress), market-wide digest, internal LLM summary (v1.1 candidate: `summary_mode="llm"`). Spec at `docs/design/P1_3_SPEC.md`.
- **2026-04-27**: P1.2 Free calendar / macro layer landed in 6 commits + 2 follow-ups (`e8fc1db` schema/DAL → `f03cf86`/`6536652`/`959c589` FRED → `d3ec85a`/`5b376aa`/`35e8d4f` Finnhub + rename `src/p1_2`→`src/macro_calendar` → `2edea5e`/`3bde0ec`/`6cca46a` jobs+API → `b8454a2`/`a5b24d3` health → `2478e74` read API+tools → `7eeffb0`/`95a644d` review follow-ups). Locked-in shape: 6 jobs gated on `macro_calendar.enabled`, 4 read routes (`/macro/economic-calendar`, `/macro/earnings-calendar`, `/macro/ipo-calendar`, `/macro/series/{series_id}`), 2 agent tools (`get_economic_calendar`, `get_macro_value`), `/macro/health[?strict]`. `as_of` ISO contract: timestamps honoured at second precision, date-only inputs map to end-of-day UTC for `to_date`/`as_of` and start-of-day UTC for `from_date` (split via `_parse_iso_datetime_start` / `_parse_iso_datetime_end`). FRED ALFRED replay pinned to `output_type=1` (full revisions) / `=4` (initial release only) so future default changes can't silently shift semantics. Earnings calendar uses per-symbol Finnhub queries because `/calendar/earnings` ranges miss watchlist symbols on free tier. `importance` filter accepts CSV (e.g. `'high,medium'`) — earlier enum-only schema would have rejected the documented form.
- **2026-04-26 (revision)**: P1.1 follow-up `e739e79` closes two review findings: (1) `/signals/factor-rank` was shadowed by `/{ticker}` because Starlette matches in declaration order; static route now declared above the dynamic one with an inline guard comment + TestClient regression so direct-call tests can no longer hide the misorder; (2) `contribution = impact * weight` double-counted because `SignalFactor.impact` is already the weighted score_delta added to `composite_score` (synthesizer `_process_*` helpers). Changed to `contribution = impact` so per-factor contributions sum to `composite_score` — the explainability contract. New invariant test asserts the additive property. `synthesize_signal`'s flat factors dict left as-is for backward compatibility (display weighting, not additive decomposition).
- **2026-04-26**: P1.1 Multi-factor signals exposure landed (commit `550c4da`). Framing locked as "recommendation breakdown, NOT price prediction" — `get_signal_factors` tool exposes raw per-factor impact/weight/contribution + `data_quality`, `/signals/factor-rank` does cross-sectional ranking with single global anchor + shared news_df + dedup. Real factor names (`SECTOR_MOMENTUM` / `EVENT_CHAIN` / `SENTIMENT_ANOMALY` / `VOLUME_SPIKE` / `EXTREME_SENTIMENT`) — explicitly NOT renamed to value/quality/momentum/low-vol since those factors don't exist here. `volume_spike` URL param maps to output factor_type `VOLUME_SPIKE`, not synthesizer input bucket `volume_anomaly`. Signal validation (forward-return decile analysis) deferred to a future "signal validation" cycle; entry/exit logic deferred to a strategy/backtest layer. SA comments deliberately not wired in — Stage 1 has no direction.
- **2026-04-25 (revision)**: P0.4 follow-up `b8dedea` closes three review findings: (1) extension job_names canonicalised to `sa_alpha_picks_refresh` / `sa_market_news_refresh` so /jobs/status actually merges extension runs (was using slugified names that missed the static catalog); (2) health now prefers `extension_last_success_at` from `job_runs` over `MAX(fetched_at)` because upsert dedup only bumps `updated_at` on conflict — fetched_at could go stale on a healthy pipeline; (3) `recordExtensionJobAsync` now Promise-wrapped with 2s timeout and awaited in `enqueueSaSyncJob.finally`, so MV3 service-worker teardown can no longer drop a recording.
- **2026-04-25**: P0.4 SA Market News observability slice landed (commits `d923177` health telemetry, `78e56d2` extension → job_runs). Anti-bot fallback, detail backfill job, and p95 latency monitoring all deferred — wait for telemetry to point at a real chronic problem before automating around it. Severity ladder uses three layers (`freshness` / `feed_health` / `detail_health`) plus market-hours upgrade rule for items_24h==0 → critical only during regular session. Strict-mode 503 supports healthcheck use; default returns 200 + structured payload so dashboards / agents / curl can inspect any layer.
- **2026-04-25**: P0.1 Replay Harness, P0.2 S2 jobs persist, P0.3 SA Comment Stage 1 all delivered same day. P0.4 SA Market News reliability is the next mainline target. `get_sa_comment_consensus` deliberately deferred — without sentiment direction it would represent discussion-density only and risks LLM over-reading it as bullish/bearish consensus.
- **2026-04-25**: Archived 7 design docs absorbed into newer authoritative docs (DAILY_STOCK_ANALYSIS notes → MAJOR_REFACTORING_PLAN; FINRL/RL_PIPELINE_DESIGN/TRAINING_PIPELINE_ARCHITECTURE → RL_COLLAPSE_FINDINGS; PHASE12_DATABASE_DEPLOYMENT/IBKR_NEWS_COLLECTION_IMPROVEMENTS → historical). Conservative archive, no deletes — see `docs/design/archive/README.md`.
- **2026-04-25 (revision)**: API route examples corrected to match existing convention (no `/api` prefix). `/api/market-news/health` → `/sa/market-news/health`; `/api/signals/factor-rank` → `/signals/factor-rank`. Existing routers: `/sa`, `/jobs`, `/news`, `/options`, `/prices`, `/reports`, `/signals`, `/scan`, plus root-level `/status`, `/query`, `/analysis/run`, `/fundamentals/{ticker}`, `/sec/{ticker}` (see `src/api/routes/`).
- **2026-04-25 (revision)**: P0.1 Replay Harness criteria split into minimal-spike vs full-v1 — first version is trace schema + 1-2 fixtures + diff tool, expand only after schema stabilises. Avoids over-spec'ing day 1.
- **2026-04-25**: Open-source-first principle codified for any "build vs use" decision (S3 dashboard, future queue, future graph backend).
- **2026-04-25**: This priority map created. Replay Harness escalated to lead-of-P0 (was implicit Phase 0). Multi-factor signals exposure escalated to P1 lead position because it ships existing latent value. Subscription strategy explicitly split into operational vs research.
- **2026-04-25**: RL line paused. See `RL_COLLAPSE_FINDINGS.md` §11 and `memory/project_rl_paused.md`. OOS validation harness explicitly not scheduled.



---

## 11. Open Questions

1. **Replay Harness scope**: Capture token-level streaming or only final response? (Token-level enables post-hoc partial-output analysis but doubles capture size.)
2. **S2 schema separation**: Single `job_runs` table vs `job_definitions` + `job_runs`? (Separation enables config-driven job catalog; merge enables faster build.)
3. **S3 OSS candidates to spike**: Top 3? My current pick is FastAPI Admin + sqladmin + RQ Dashboard, but Prefect UI has stronger job-history features. Need user decision before spike.
4. **SA comment rule-set versioning**: Inline `rule_set_version` string + git-tracked rule file, or DB-stored rule definitions for hot-reload?
5. **FRED API key**: ✅ resolved 2026-04-26. Free key is required (no usable anonymous quota); registered and in `config/.env`. See `P1_2_PROVIDER_DISCOVERY.md` §6 for smoke results.
6. **Subscription evidence threshold**: How many days of OOS validation before approving a research subscription? (Suggestion: 30 trading days minimum, with predefined success metric.)

---

## 12. Reference

- `PHASE_A_KNOWLEDGE_GRAPH_SKETCH.md` — Knowledge Graph v2 candidate design (extracted from retired `MAJOR_REFACTORING_PLAN.md`)
- `SA_COMMENT_INTELLIGENCE_PLAN.md` — comment extraction design
- `SA_EXTENSION_ROADMAP.md` — SA extension feature roadmap
- `MULTI_FACTOR_SIGNAL_DETECTION.md` — signal factor architecture
- `RL_COLLAPSE_FINDINGS.md` — RL pause rationale
- rename migration checklist — see git history + memory `project_rename_arkscope.md` (the executed runbook `ARKSCOPE_RENAME_PHASE2.md` was removed 2026-06-07, recoverable via git)
- `AGENT_EVOLUTION_TRACKER.md` — agent state + history (this map references it for current snapshot)
