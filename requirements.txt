openai
# Shared numerical stack pinned while resolving the Coverage v2 dependency set
numpy==1.26.4
pandas==2.3.1
# Coverage v2 calendar solution
exchange-calendars==4.13.2
pyluach==2.3.0
toolz==1.1.0
tzdata==2025.2
korean-lunar-calendar==0.4.0
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0
scipy
requests

# DAL + Tools layer
pydantic>=2.0
pyyaml

# HTTP API
fastapi>=0.110.0
uvicorn[standard]
httpx

# Agent SDKs (optional - install for POST /query endpoint)
openai-agents>=0.22.0
anthropic>=1.2.0
claude-agent-sdk==0.2.105  # Claude-subscription Research driver (Slice 7B); PINNED — the bundled CLI it ships is load-bearing (negative-test gates re-run on any bump)


# Web Search (Phase 10)
playwright>=1.40.0
