# Trusted Lifecycle Automation Stage 5 Repair Evidence

This packet supersedes the original Stage 5 packet for migration and cutover
authority. The original packet remains intact as rejected evidence; it proved
deterministic restraint but did not prove that a positive transition path was
reachable.

The repair closes four reproduced blockers. A first-discovery `LC -> HAPN`
declaration now emits HAPN without pre-persisting it as an alias. Only a
complete, explicitly dated Form 25/25-NSE chain can emit terminal delisting.
All nine fact types validate their closed value shapes at the fact boundary,
and `transaction_structure` remains a mapping with an honest `terms_status`
instead of silently becoming an empty prefill. A transition-approval failure
now preserves the accepted assessment and proposals while moving the case to
visible, typed, daily bounded revalidation.

The repaired shadow report retains the four historical restraint cases and
adds a positive first-discovery capability replay. It starts with only `LC` in
the alias set, extracts regulator-declared `HAPN`, queries synthetic IBKR shapes
for exactly `LC` and `HAPN`, leaves the alias set unchanged, and reaches one
eligible `LC -> HAPN` transition preview. Filing prose and IBKR replies remain
synthetic source shapes, not captured provider bytes. No fresh SEC, IBKR,
news, model, or general-search call was made.

The browser packet contains English and Traditional Chinese at `1440x900` and
`390x844`. Each entry captures activity, evidence, automation state, facts and
review, and transition state. The final replay displays blockers only from the
run whose status is shown; superseded source and market blockers remain in the
fixture but are not presented as current facts. All 20 screenshots passed
geometry and nonblank-pixel checks and were visually inspected. The harness
observed zero writes, external requests, console errors, page errors, render
acknowledgements, horizontal overflow, or incoherent control overlap.

Final repaired offline admission at product/test authority `7cb479a8` is five
backend additions passing, focused backend `66P`, collection `4416` twice with
byte-identical normalized node streams, and two isolated full backend runs of
`4404P / 12S / 0F`. Frontend admission is `105 files / 1229P`, focused
`4 files / 67P`, with typecheck, visible-literal scanner, and production build
passing. Runtime inventory remains `187` routes, `17` lifecycle routes, and
registry/Anthropic/OpenAI tool counts `50/51/51`.

The live-migration runbook and manifest are unexecuted templates. Real SEC
source-byte validation for the four benchmarks and a read-only IBKR shape
canary are explicitly separate and unauthorized. Production preflight, backup,
restore, migration, merge, push, provider calls, and app cutover remain hard
stops.

`SHA256SUMS` is the final packet manifest and intentionally excludes itself.
